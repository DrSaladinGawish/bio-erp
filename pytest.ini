[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: pure unit tests, no external systems
    integration: requires live PostgreSQL/Redis (deselect with -m "not integration")
    slow: long-running tests (deselect with -m "not slow")
    e2e: end-to-end flows through the full app
