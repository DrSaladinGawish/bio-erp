"""
Inventory Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.627365
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .inventory_models import *
from .inventory_schemas import *

class CycleCountsService:
    """CRUD service for CycleCounts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: CycleCountsCreate, created_by: Optional[UUID] = None) -> CycleCountsResponse:
        """Create a new CycleCounts record."""
        db_obj = CycleCounts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return CycleCountsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[CycleCountsResponse]:
        """Get CycleCounts by primary key."""
        db_obj = self.db.query(CycleCounts).filter(CycleCounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CycleCounts not found")
        return CycleCountsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> CycleCountsListResponse:
        """List CycleCounts records with pagination and filtering."""
        query = self.db.query(CycleCounts)
        if company_id and hasattr(CycleCounts, "company_id"):
            query = query.filter(CycleCounts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(CycleCounts, key) and value is not None:
                    query = query.filter(getattr(CycleCounts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(CycleCounts, order_by, CycleCounts.created_at)))
        else:
            query = query.order_by(getattr(CycleCounts, order_by, CycleCounts.created_at))
        items = query.offset(skip).limit(limit).all()
        return CycleCountsListResponse(
            items=[CycleCountsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: CycleCountsUpdate,
               updated_by: Optional[UUID] = None) -> CycleCountsResponse:
        """Update an existing CycleCounts record."""
        db_obj = self.db.query(CycleCounts).filter(CycleCounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CycleCounts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return CycleCountsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a CycleCounts record. Soft delete by default."""
        db_obj = self.db.query(CycleCounts).filter(CycleCounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CycleCounts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[CycleCountsResponse]:
        """Search CycleCounts by name/code fields."""
        query = self.db.query(CycleCounts)
        if company_id and hasattr(CycleCounts, "company_id"):
            query = query.filter(CycleCounts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(CycleCounts, f)]
        if search_fields:
            conditions = [getattr(CycleCounts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [CycleCountsResponse.model_validate(i) for i in items]

class StockBalancesService:
    """CRUD service for StockBalances."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: StockBalancesCreate, created_by: Optional[UUID] = None) -> StockBalancesResponse:
        """Create a new StockBalances record."""
        db_obj = StockBalances(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return StockBalancesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[StockBalancesResponse]:
        """Get StockBalances by primary key."""
        db_obj = self.db.query(StockBalances).filter(StockBalances.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="StockBalances not found")
        return StockBalancesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> StockBalancesListResponse:
        """List StockBalances records with pagination and filtering."""
        query = self.db.query(StockBalances)
        if company_id and hasattr(StockBalances, "company_id"):
            query = query.filter(StockBalances.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(StockBalances, key) and value is not None:
                    query = query.filter(getattr(StockBalances, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(StockBalances, order_by, StockBalances.created_at)))
        else:
            query = query.order_by(getattr(StockBalances, order_by, StockBalances.created_at))
        items = query.offset(skip).limit(limit).all()
        return StockBalancesListResponse(
            items=[StockBalancesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: StockBalancesUpdate,
               updated_by: Optional[UUID] = None) -> StockBalancesResponse:
        """Update an existing StockBalances record."""
        db_obj = self.db.query(StockBalances).filter(StockBalances.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="StockBalances not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return StockBalancesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a StockBalances record. Soft delete by default."""
        db_obj = self.db.query(StockBalances).filter(StockBalances.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="StockBalances not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[StockBalancesResponse]:
        """Search StockBalances by name/code fields."""
        query = self.db.query(StockBalances)
        if company_id and hasattr(StockBalances, "company_id"):
            query = query.filter(StockBalances.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(StockBalances, f)]
        if search_fields:
            conditions = [getattr(StockBalances, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [StockBalancesResponse.model_validate(i) for i in items]

class StockMovementsService:
    """CRUD service for StockMovements."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: StockMovementsCreate, created_by: Optional[UUID] = None) -> StockMovementsResponse:
        """Create a new StockMovements record."""
        db_obj = StockMovements(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return StockMovementsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[StockMovementsResponse]:
        """Get StockMovements by primary key."""
        db_obj = self.db.query(StockMovements).filter(StockMovements.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="StockMovements not found")
        return StockMovementsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> StockMovementsListResponse:
        """List StockMovements records with pagination and filtering."""
        query = self.db.query(StockMovements)
        if company_id and hasattr(StockMovements, "company_id"):
            query = query.filter(StockMovements.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(StockMovements, key) and value is not None:
                    query = query.filter(getattr(StockMovements, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(StockMovements, order_by, StockMovements.created_at)))
        else:
            query = query.order_by(getattr(StockMovements, order_by, StockMovements.created_at))
        items = query.offset(skip).limit(limit).all()
        return StockMovementsListResponse(
            items=[StockMovementsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: StockMovementsUpdate,
               updated_by: Optional[UUID] = None) -> StockMovementsResponse:
        """Update an existing StockMovements record."""
        db_obj = self.db.query(StockMovements).filter(StockMovements.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="StockMovements not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return StockMovementsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a StockMovements record. Soft delete by default."""
        db_obj = self.db.query(StockMovements).filter(StockMovements.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="StockMovements not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[StockMovementsResponse]:
        """Search StockMovements by name/code fields."""
        query = self.db.query(StockMovements)
        if company_id and hasattr(StockMovements, "company_id"):
            query = query.filter(StockMovements.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(StockMovements, f)]
        if search_fields:
            conditions = [getattr(StockMovements, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [StockMovementsResponse.model_validate(i) for i in items]

class WarehouseLocationsService:
    """CRUD service for WarehouseLocations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WarehouseLocationsCreate, created_by: Optional[UUID] = None) -> WarehouseLocationsResponse:
        """Create a new WarehouseLocations record."""
        db_obj = WarehouseLocations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WarehouseLocationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WarehouseLocationsResponse]:
        """Get WarehouseLocations by primary key."""
        db_obj = self.db.query(WarehouseLocations).filter(WarehouseLocations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WarehouseLocations not found")
        return WarehouseLocationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WarehouseLocationsListResponse:
        """List WarehouseLocations records with pagination and filtering."""
        query = self.db.query(WarehouseLocations)
        if company_id and hasattr(WarehouseLocations, "company_id"):
            query = query.filter(WarehouseLocations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WarehouseLocations, key) and value is not None:
                    query = query.filter(getattr(WarehouseLocations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WarehouseLocations, order_by, WarehouseLocations.created_at)))
        else:
            query = query.order_by(getattr(WarehouseLocations, order_by, WarehouseLocations.created_at))
        items = query.offset(skip).limit(limit).all()
        return WarehouseLocationsListResponse(
            items=[WarehouseLocationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WarehouseLocationsUpdate,
               updated_by: Optional[UUID] = None) -> WarehouseLocationsResponse:
        """Update an existing WarehouseLocations record."""
        db_obj = self.db.query(WarehouseLocations).filter(WarehouseLocations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WarehouseLocations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WarehouseLocationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WarehouseLocations record. Soft delete by default."""
        db_obj = self.db.query(WarehouseLocations).filter(WarehouseLocations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WarehouseLocations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WarehouseLocationsResponse]:
        """Search WarehouseLocations by name/code fields."""
        query = self.db.query(WarehouseLocations)
        if company_id and hasattr(WarehouseLocations, "company_id"):
            query = query.filter(WarehouseLocations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WarehouseLocations, f)]
        if search_fields:
            conditions = [getattr(WarehouseLocations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WarehouseLocationsResponse.model_validate(i) for i in items]

class WarehousesService:
    """CRUD service for Warehouses."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WarehousesCreate, created_by: Optional[UUID] = None) -> WarehousesResponse:
        """Create a new Warehouses record."""
        db_obj = Warehouses(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WarehousesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WarehousesResponse]:
        """Get Warehouses by primary key."""
        db_obj = self.db.query(Warehouses).filter(Warehouses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Warehouses not found")
        return WarehousesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WarehousesListResponse:
        """List Warehouses records with pagination and filtering."""
        query = self.db.query(Warehouses)
        if company_id and hasattr(Warehouses, "company_id"):
            query = query.filter(Warehouses.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Warehouses, key) and value is not None:
                    query = query.filter(getattr(Warehouses, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Warehouses, order_by, Warehouses.created_at)))
        else:
            query = query.order_by(getattr(Warehouses, order_by, Warehouses.created_at))
        items = query.offset(skip).limit(limit).all()
        return WarehousesListResponse(
            items=[WarehousesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WarehousesUpdate,
               updated_by: Optional[UUID] = None) -> WarehousesResponse:
        """Update an existing Warehouses record."""
        db_obj = self.db.query(Warehouses).filter(Warehouses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Warehouses not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WarehousesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Warehouses record. Soft delete by default."""
        db_obj = self.db.query(Warehouses).filter(Warehouses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Warehouses not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WarehousesResponse]:
        """Search Warehouses by name/code fields."""
        query = self.db.query(Warehouses)
        if company_id and hasattr(Warehouses, "company_id"):
            query = query.filter(Warehouses.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Warehouses, f)]
        if search_fields:
            conditions = [getattr(Warehouses, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WarehousesResponse.model_validate(i) for i in items]
