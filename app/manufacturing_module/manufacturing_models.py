"""
Manufacturing Module — SQLAlchemy ORM Models
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.648564
One model class per table with full relationship support.
"""
from __future__ import annotations

from decimal import Decimal
from datetime import datetime, date, time
from typing import List, Optional
from uuid import UUID
import uuid

from sqlalchemy import (
    Column, Integer, BigInteger, SmallInteger,
    Numeric, Float, Boolean,
    String, Text, Unicode, UnicodeText,
    DateTime, Date, Time, DateTime, LargeBinary,
    ForeignKey, ForeignKeyConstraint, UniqueConstraint,
    Index, CheckConstraint, PrimaryKeyConstraint,
    TIMESTAMP, JSON, Enum, VARBINARY
)
from sqlalchemy.dialects.mssql import UNIQUEIDENTIFIER, MONEY, SMALLMONEY, NVARCHAR, NTEXT, IMAGE
from sqlalchemy.orm import relationship, backref, mapped_column, Mapped
from sqlalchemy.sql import func

from app.core.database_mssql import Base

# Module metadata
__module_code__ = "manufacturing-ebuild"
__tables__ = 6

class BomLines(Base):
    """BomLines — 9 columns, 3 FKs."""
    __tablename__ = "bom_lines"

    # FK: bom_id -> boms.id
    # FK: component_id -> items.id
    # FK: uom_id -> unit_of_measures.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    bom_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    component_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    quantity: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    uom_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    is_phantom: Mapped[bool] = mapped_column(Boolean)
    scrap_pct: Mapped[Decimal] = mapped_column(Numeric(6, 4))
    sequence: Mapped[int] = mapped_column(Integer)
    notes: Mapped[str] = mapped_column(Text)

    # bom = relationship("Boms", back_populates="bom_liness")
    # component = relationship("Items", back_populates="bom_liness")
    # uom = relationship("UnitOfMeasures", back_populates="bom_liness")

class Boms(Base):
    """Boms — 9 columns, 3 FKs."""
    __tablename__ = "boms"

    # FK: company_id -> companies.id
    # FK: item_id -> items.id
    # FK: uom_id -> unit_of_measures.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    item_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    version: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    quantity: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    uom_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="bomss")
    # item = relationship("Items", back_populates="bomss")
    # uom = relationship("UnitOfMeasures", back_populates="bomss")

class RoutingOperations(Base):
    """RoutingOperations — 8 columns, 2 FKs."""
    __tablename__ = "routing_operations"

    # FK: routing_id -> routings.id
    # FK: work_centre_id -> work_centres.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    routing_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    sequence: Mapped[int] = mapped_column(Integer, nullable=False)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    work_centre_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    setup_time_min: Mapped[Decimal] = mapped_column(Numeric(10, 2))
    run_time_min: Mapped[Decimal] = mapped_column(Numeric(10, 2), nullable=False)
    teardown_time_min: Mapped[Decimal] = mapped_column(Numeric(10, 2))

    # routing = relationship("Routings", back_populates="routing_operationss")
    # work_centre = relationship("WorkCentres", back_populates="routing_operationss")

class Routings(Base):
    """Routings — 6 columns, 2 FKs."""
    __tablename__ = "routings"

    # FK: company_id -> companies.id
    # FK: item_id -> items.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    item_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="routingss")
    # item = relationship("Items", back_populates="routingss")

class WorkCentres(Base):
    """WorkCentres — 9 columns, 1 FKs."""
    __tablename__ = "work_centres"

    # FK: company_id -> companies.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    code: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    capacity_per_hour: Mapped[Decimal] = mapped_column(Numeric(10, 4))
    cost_per_hour: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    overhead_pct: Mapped[Decimal] = mapped_column(Numeric(6, 4))
    is_active: Mapped[bool] = mapped_column(Boolean)
    metadata_: Mapped[str] = mapped_column("metadata", Text)

    # company = relationship("Companies", back_populates="work_centress")

class WorkOrders(Base):
    """WorkOrders — 18 columns, 5 FKs."""
    __tablename__ = "work_orders"

    # FK: bom_id -> boms.id
    # FK: company_id -> companies.id
    # FK: item_id -> items.id
    # FK: routing_id -> routings.id
    # FK: warehouse_id -> warehouses.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    number: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    bom_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    item_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    planned_qty: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    produced_qty: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    scrapped_qty: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    planned_start: Mapped[date] = mapped_column(Date)
    planned_end: Mapped[date] = mapped_column(Date)
    actual_start: Mapped[datetime] = mapped_column(DateTime)
    actual_end: Mapped[datetime] = mapped_column(DateTime)
    routing_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    warehouse_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    notes: Mapped[str] = mapped_column(Text)
    status: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # bom = relationship("Boms", back_populates="work_orderss")
    # company = relationship("Companies", back_populates="work_orderss")
    # item = relationship("Items", back_populates="work_orderss")
    # routing = relationship("Routings", back_populates="work_orderss")
    # warehouse = relationship("Warehouses", back_populates="work_orderss")
