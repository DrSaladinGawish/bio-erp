"""
Manufacturing Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.657925
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .manufacturing_models import *
from .manufacturing_schemas import *

class BomLinesService:
    """CRUD service for BomLines."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BomLinesCreate, created_by: Optional[UUID] = None) -> BomLinesResponse:
        """Create a new BomLines record."""
        db_obj = BomLines(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BomLinesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BomLinesResponse]:
        """Get BomLines by primary key."""
        db_obj = self.db.query(BomLines).filter(BomLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BomLines not found")
        return BomLinesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BomLinesListResponse:
        """List BomLines records with pagination and filtering."""
        query = self.db.query(BomLines)
        if company_id and hasattr(BomLines, "company_id"):
            query = query.filter(BomLines.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BomLines, key) and value is not None:
                    query = query.filter(getattr(BomLines, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BomLines, order_by, BomLines.created_at)))
        else:
            query = query.order_by(getattr(BomLines, order_by, BomLines.created_at))
        items = query.offset(skip).limit(limit).all()
        return BomLinesListResponse(
            items=[BomLinesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BomLinesUpdate,
               updated_by: Optional[UUID] = None) -> BomLinesResponse:
        """Update an existing BomLines record."""
        db_obj = self.db.query(BomLines).filter(BomLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BomLines not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BomLinesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BomLines record. Soft delete by default."""
        db_obj = self.db.query(BomLines).filter(BomLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BomLines not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BomLinesResponse]:
        """Search BomLines by name/code fields."""
        query = self.db.query(BomLines)
        if company_id and hasattr(BomLines, "company_id"):
            query = query.filter(BomLines.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BomLines, f)]
        if search_fields:
            conditions = [getattr(BomLines, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BomLinesResponse.model_validate(i) for i in items]

class BomsService:
    """CRUD service for Boms."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BomsCreate, created_by: Optional[UUID] = None) -> BomsResponse:
        """Create a new Boms record."""
        db_obj = Boms(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BomsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BomsResponse]:
        """Get Boms by primary key."""
        db_obj = self.db.query(Boms).filter(Boms.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Boms not found")
        return BomsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BomsListResponse:
        """List Boms records with pagination and filtering."""
        query = self.db.query(Boms)
        if company_id and hasattr(Boms, "company_id"):
            query = query.filter(Boms.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Boms, key) and value is not None:
                    query = query.filter(getattr(Boms, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Boms, order_by, Boms.created_at)))
        else:
            query = query.order_by(getattr(Boms, order_by, Boms.created_at))
        items = query.offset(skip).limit(limit).all()
        return BomsListResponse(
            items=[BomsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BomsUpdate,
               updated_by: Optional[UUID] = None) -> BomsResponse:
        """Update an existing Boms record."""
        db_obj = self.db.query(Boms).filter(Boms.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Boms not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BomsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Boms record. Soft delete by default."""
        db_obj = self.db.query(Boms).filter(Boms.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Boms not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BomsResponse]:
        """Search Boms by name/code fields."""
        query = self.db.query(Boms)
        if company_id and hasattr(Boms, "company_id"):
            query = query.filter(Boms.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Boms, f)]
        if search_fields:
            conditions = [getattr(Boms, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BomsResponse.model_validate(i) for i in items]

class RoutingOperationsService:
    """CRUD service for RoutingOperations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RoutingOperationsCreate, created_by: Optional[UUID] = None) -> RoutingOperationsResponse:
        """Create a new RoutingOperations record."""
        db_obj = RoutingOperations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RoutingOperationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RoutingOperationsResponse]:
        """Get RoutingOperations by primary key."""
        db_obj = self.db.query(RoutingOperations).filter(RoutingOperations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RoutingOperations not found")
        return RoutingOperationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RoutingOperationsListResponse:
        """List RoutingOperations records with pagination and filtering."""
        query = self.db.query(RoutingOperations)
        if company_id and hasattr(RoutingOperations, "company_id"):
            query = query.filter(RoutingOperations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RoutingOperations, key) and value is not None:
                    query = query.filter(getattr(RoutingOperations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RoutingOperations, order_by, RoutingOperations.created_at)))
        else:
            query = query.order_by(getattr(RoutingOperations, order_by, RoutingOperations.created_at))
        items = query.offset(skip).limit(limit).all()
        return RoutingOperationsListResponse(
            items=[RoutingOperationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RoutingOperationsUpdate,
               updated_by: Optional[UUID] = None) -> RoutingOperationsResponse:
        """Update an existing RoutingOperations record."""
        db_obj = self.db.query(RoutingOperations).filter(RoutingOperations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RoutingOperations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RoutingOperationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RoutingOperations record. Soft delete by default."""
        db_obj = self.db.query(RoutingOperations).filter(RoutingOperations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RoutingOperations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RoutingOperationsResponse]:
        """Search RoutingOperations by name/code fields."""
        query = self.db.query(RoutingOperations)
        if company_id and hasattr(RoutingOperations, "company_id"):
            query = query.filter(RoutingOperations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RoutingOperations, f)]
        if search_fields:
            conditions = [getattr(RoutingOperations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RoutingOperationsResponse.model_validate(i) for i in items]

class RoutingsService:
    """CRUD service for Routings."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RoutingsCreate, created_by: Optional[UUID] = None) -> RoutingsResponse:
        """Create a new Routings record."""
        db_obj = Routings(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RoutingsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RoutingsResponse]:
        """Get Routings by primary key."""
        db_obj = self.db.query(Routings).filter(Routings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Routings not found")
        return RoutingsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RoutingsListResponse:
        """List Routings records with pagination and filtering."""
        query = self.db.query(Routings)
        if company_id and hasattr(Routings, "company_id"):
            query = query.filter(Routings.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Routings, key) and value is not None:
                    query = query.filter(getattr(Routings, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Routings, order_by, Routings.created_at)))
        else:
            query = query.order_by(getattr(Routings, order_by, Routings.created_at))
        items = query.offset(skip).limit(limit).all()
        return RoutingsListResponse(
            items=[RoutingsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RoutingsUpdate,
               updated_by: Optional[UUID] = None) -> RoutingsResponse:
        """Update an existing Routings record."""
        db_obj = self.db.query(Routings).filter(Routings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Routings not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RoutingsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Routings record. Soft delete by default."""
        db_obj = self.db.query(Routings).filter(Routings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Routings not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RoutingsResponse]:
        """Search Routings by name/code fields."""
        query = self.db.query(Routings)
        if company_id and hasattr(Routings, "company_id"):
            query = query.filter(Routings.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Routings, f)]
        if search_fields:
            conditions = [getattr(Routings, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RoutingsResponse.model_validate(i) for i in items]

class WorkCentresService:
    """CRUD service for WorkCentres."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WorkCentresCreate, created_by: Optional[UUID] = None) -> WorkCentresResponse:
        """Create a new WorkCentres record."""
        db_obj = WorkCentres(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WorkCentresResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WorkCentresResponse]:
        """Get WorkCentres by primary key."""
        db_obj = self.db.query(WorkCentres).filter(WorkCentres.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WorkCentres not found")
        return WorkCentresResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WorkCentresListResponse:
        """List WorkCentres records with pagination and filtering."""
        query = self.db.query(WorkCentres)
        if company_id and hasattr(WorkCentres, "company_id"):
            query = query.filter(WorkCentres.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WorkCentres, key) and value is not None:
                    query = query.filter(getattr(WorkCentres, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WorkCentres, order_by, WorkCentres.created_at)))
        else:
            query = query.order_by(getattr(WorkCentres, order_by, WorkCentres.created_at))
        items = query.offset(skip).limit(limit).all()
        return WorkCentresListResponse(
            items=[WorkCentresResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WorkCentresUpdate,
               updated_by: Optional[UUID] = None) -> WorkCentresResponse:
        """Update an existing WorkCentres record."""
        db_obj = self.db.query(WorkCentres).filter(WorkCentres.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WorkCentres not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WorkCentresResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WorkCentres record. Soft delete by default."""
        db_obj = self.db.query(WorkCentres).filter(WorkCentres.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WorkCentres not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WorkCentresResponse]:
        """Search WorkCentres by name/code fields."""
        query = self.db.query(WorkCentres)
        if company_id and hasattr(WorkCentres, "company_id"):
            query = query.filter(WorkCentres.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WorkCentres, f)]
        if search_fields:
            conditions = [getattr(WorkCentres, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WorkCentresResponse.model_validate(i) for i in items]

class WorkOrdersService:
    """CRUD service for WorkOrders."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WorkOrdersCreate, created_by: Optional[UUID] = None) -> WorkOrdersResponse:
        """Create a new WorkOrders record."""
        db_obj = WorkOrders(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WorkOrdersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WorkOrdersResponse]:
        """Get WorkOrders by primary key."""
        db_obj = self.db.query(WorkOrders).filter(WorkOrders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WorkOrders not found")
        return WorkOrdersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WorkOrdersListResponse:
        """List WorkOrders records with pagination and filtering."""
        query = self.db.query(WorkOrders)
        if company_id and hasattr(WorkOrders, "company_id"):
            query = query.filter(WorkOrders.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WorkOrders, key) and value is not None:
                    query = query.filter(getattr(WorkOrders, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WorkOrders, order_by, WorkOrders.created_at)))
        else:
            query = query.order_by(getattr(WorkOrders, order_by, WorkOrders.created_at))
        items = query.offset(skip).limit(limit).all()
        return WorkOrdersListResponse(
            items=[WorkOrdersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WorkOrdersUpdate,
               updated_by: Optional[UUID] = None) -> WorkOrdersResponse:
        """Update an existing WorkOrders record."""
        db_obj = self.db.query(WorkOrders).filter(WorkOrders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WorkOrders not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WorkOrdersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WorkOrders record. Soft delete by default."""
        db_obj = self.db.query(WorkOrders).filter(WorkOrders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WorkOrders not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WorkOrdersResponse]:
        """Search WorkOrders by name/code fields."""
        query = self.db.query(WorkOrders)
        if company_id and hasattr(WorkOrders, "company_id"):
            query = query.filter(WorkOrders.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WorkOrders, f)]
        if search_fields:
            conditions = [getattr(WorkOrders, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WorkOrdersResponse.model_validate(i) for i in items]
