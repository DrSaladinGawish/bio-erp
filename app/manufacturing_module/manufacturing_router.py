"""
Manufacturing Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.658141
Mounted at /api/v1/mfg
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .manufacturing_schemas import *
from .manufacturing_services import *

router = APIRouter(prefix="/mfg", tags=["Manufacturing"])

@router.get("/bom_liness", response_model=BomLinesListResponse)
def list_bom_liness(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BomLines records."""
    service = BomLinesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bom_liness/{id}", response_model=BomLinesResponse)
def get_bom_lines(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BomLines by ID."""
    service = BomLinesService(db)
    return service.get(id)

@router.post("/bom_liness", response_model=BomLinesResponse, status_code=status.HTTP_201_CREATED)
def create_bom_lines(
    data: BomLinesCreate,
    db: Session = Depends(get_db)
):
    """Create a new BomLines."""
    service = BomLinesService(db)
    return service.create(data)

@router.put("/bom_liness/{id}", response_model=BomLinesResponse)
def update_bom_lines(
    id: UUID,
    data: BomLinesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BomLines."""
    service = BomLinesService(db)
    return service.update(id, data)

@router.delete("/bom_liness/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bom_lines(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BomLines (soft delete by default)."""
    service = BomLinesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bom_liness/search", response_model=List[BomLinesResponse])
def search_bom_liness(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BomLines by name/code/description."""
    service = BomLinesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bomss", response_model=BomsListResponse)
def list_bomss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Boms records."""
    service = BomsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bomss/{id}", response_model=BomsResponse)
def get_boms(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Boms by ID."""
    service = BomsService(db)
    return service.get(id)

@router.post("/bomss", response_model=BomsResponse, status_code=status.HTTP_201_CREATED)
def create_boms(
    data: BomsCreate,
    db: Session = Depends(get_db)
):
    """Create a new Boms."""
    service = BomsService(db)
    return service.create(data)

@router.put("/bomss/{id}", response_model=BomsResponse)
def update_boms(
    id: UUID,
    data: BomsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Boms."""
    service = BomsService(db)
    return service.update(id, data)

@router.delete("/bomss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_boms(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Boms (soft delete by default)."""
    service = BomsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bomss/search", response_model=List[BomsResponse])
def search_bomss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Boms by name/code/description."""
    service = BomsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/routing_operationss", response_model=RoutingOperationsListResponse)
def list_routing_operationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RoutingOperations records."""
    service = RoutingOperationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/routing_operationss/{id}", response_model=RoutingOperationsResponse)
def get_routing_operations(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RoutingOperations by ID."""
    service = RoutingOperationsService(db)
    return service.get(id)

@router.post("/routing_operationss", response_model=RoutingOperationsResponse, status_code=status.HTTP_201_CREATED)
def create_routing_operations(
    data: RoutingOperationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new RoutingOperations."""
    service = RoutingOperationsService(db)
    return service.create(data)

@router.put("/routing_operationss/{id}", response_model=RoutingOperationsResponse)
def update_routing_operations(
    id: UUID,
    data: RoutingOperationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RoutingOperations."""
    service = RoutingOperationsService(db)
    return service.update(id, data)

@router.delete("/routing_operationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_routing_operations(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RoutingOperations (soft delete by default)."""
    service = RoutingOperationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/routing_operationss/search", response_model=List[RoutingOperationsResponse])
def search_routing_operationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RoutingOperations by name/code/description."""
    service = RoutingOperationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/routingss", response_model=RoutingsListResponse)
def list_routingss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Routings records."""
    service = RoutingsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/routingss/{id}", response_model=RoutingsResponse)
def get_routings(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Routings by ID."""
    service = RoutingsService(db)
    return service.get(id)

@router.post("/routingss", response_model=RoutingsResponse, status_code=status.HTTP_201_CREATED)
def create_routings(
    data: RoutingsCreate,
    db: Session = Depends(get_db)
):
    """Create a new Routings."""
    service = RoutingsService(db)
    return service.create(data)

@router.put("/routingss/{id}", response_model=RoutingsResponse)
def update_routings(
    id: UUID,
    data: RoutingsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Routings."""
    service = RoutingsService(db)
    return service.update(id, data)

@router.delete("/routingss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_routings(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Routings (soft delete by default)."""
    service = RoutingsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/routingss/search", response_model=List[RoutingsResponse])
def search_routingss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Routings by name/code/description."""
    service = RoutingsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/work_centress", response_model=WorkCentresListResponse)
def list_work_centress(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WorkCentres records."""
    service = WorkCentresService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/work_centress/{id}", response_model=WorkCentresResponse)
def get_work_centres(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WorkCentres by ID."""
    service = WorkCentresService(db)
    return service.get(id)

@router.post("/work_centress", response_model=WorkCentresResponse, status_code=status.HTTP_201_CREATED)
def create_work_centres(
    data: WorkCentresCreate,
    db: Session = Depends(get_db)
):
    """Create a new WorkCentres."""
    service = WorkCentresService(db)
    return service.create(data)

@router.put("/work_centress/{id}", response_model=WorkCentresResponse)
def update_work_centres(
    id: UUID,
    data: WorkCentresUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WorkCentres."""
    service = WorkCentresService(db)
    return service.update(id, data)

@router.delete("/work_centress/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_work_centres(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WorkCentres (soft delete by default)."""
    service = WorkCentresService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/work_centress/search", response_model=List[WorkCentresResponse])
def search_work_centress(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WorkCentres by name/code/description."""
    service = WorkCentresService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/work_orderss", response_model=WorkOrdersListResponse)
def list_work_orderss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WorkOrders records."""
    service = WorkOrdersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/work_orderss/{id}", response_model=WorkOrdersResponse)
def get_work_orders(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WorkOrders by ID."""
    service = WorkOrdersService(db)
    return service.get(id)

@router.post("/work_orderss", response_model=WorkOrdersResponse, status_code=status.HTTP_201_CREATED)
def create_work_orders(
    data: WorkOrdersCreate,
    db: Session = Depends(get_db)
):
    """Create a new WorkOrders."""
    service = WorkOrdersService(db)
    return service.create(data)

@router.put("/work_orderss/{id}", response_model=WorkOrdersResponse)
def update_work_orders(
    id: UUID,
    data: WorkOrdersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WorkOrders."""
    service = WorkOrdersService(db)
    return service.update(id, data)

@router.delete("/work_orderss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_work_orders(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WorkOrders (soft delete by default)."""
    service = WorkOrdersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/work_orderss/search", response_model=List[WorkOrdersResponse])
def search_work_orderss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WorkOrders by name/code/description."""
    service = WorkOrdersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_manufacturing_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get Manufacturing dashboard summary."""
    return {
        "module": "manufacturing",
        "company_id": str(company_id),
        "tables": 6,
        "metrics": {}
    }
