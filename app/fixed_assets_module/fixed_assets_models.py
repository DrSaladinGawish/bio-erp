"""
Fixed Assets Module — SQLAlchemy ORM Models
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.504406
One model class per table with full relationship support.
"""
from __future__ import annotations

from decimal import Decimal
from datetime import datetime, date, time
from typing import List, Optional
from uuid import UUID
import uuid

from sqlalchemy import (
    Column, Integer, BigInteger, SmallInteger,
    Numeric, Float, Boolean,
    String, Text, Unicode, UnicodeText,
    DateTime, Date, Time, DateTime, LargeBinary,
    ForeignKey, ForeignKeyConstraint, UniqueConstraint,
    Index, CheckConstraint, PrimaryKeyConstraint,
    TIMESTAMP, JSON, Enum, VARBINARY
)
from sqlalchemy.dialects.mssql import UNIQUEIDENTIFIER, MONEY, SMALLMONEY, NVARCHAR, NTEXT, IMAGE
from sqlalchemy.orm import relationship, backref, mapped_column, Mapped
from sqlalchemy.sql import func

from app.core.database_mssql import Base

# Module metadata
__module_code__ = "fixed_assets-ebuild"
__tables__ = 5

class AssetCategories(Base):
    """AssetCategories — 12 columns, 4 FKs."""
    __tablename__ = "asset_categories"

    # FK: accum_dep_account_id -> accounts.id
    # FK: asset_account_id -> accounts.id
    # FK: company_id -> companies.id
    # FK: dep_expense_account_id -> accounts.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    depreciation_method: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    useful_life_years: Mapped[Decimal] = mapped_column(Numeric(6, 2))
    salvage_value_pct: Mapped[Decimal] = mapped_column(Numeric(6, 4))
    asset_account_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    accum_dep_account_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    dep_expense_account_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    is_active: Mapped[bool] = mapped_column(Boolean)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # accum_dep_account = relationship("Accounts", back_populates="asset_categoriess")
    # asset_account = relationship("Accounts", back_populates="asset_categoriess")
    # company = relationship("Companies", back_populates="asset_categoriess")
    # dep_expense_account = relationship("Accounts", back_populates="asset_categoriess")

class AssetDepreciationLines(Base):
    """AssetDepreciationLines — 7 columns, 2 FKs."""
    __tablename__ = "asset_depreciation_lines"

    # FK: asset_id -> fixed_assets.id
    # FK: run_id -> asset_depreciation_runs.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    run_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    asset_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    dep_amount: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    opening_nbv: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    closing_nbv: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    metadata_: Mapped[str] = mapped_column("metadata", Text)

    # asset = relationship("FixedAssets", back_populates="asset_depreciation_liness")
    # run = relationship("AssetDepreciationRuns", back_populates="asset_depreciation_liness")

class AssetDepreciationRuns(Base):
    """AssetDepreciationRuns — 8 columns, 3 FKs."""
    __tablename__ = "asset_depreciation_runs"

    # FK: company_id -> companies.id
    # FK: journal_entry_id -> journal_entries.id
    # FK: period_id -> fiscal_periods.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    period_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    run_date: Mapped[date] = mapped_column(Date, nullable=False)
    status: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    journal_entry_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    total_depreciation: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="asset_depreciation_runss")
    # journal_entry = relationship("JournalEntries", back_populates="asset_depreciation_runss")
    # period = relationship("FiscalPeriods", back_populates="asset_depreciation_runss")

class AssetMaintenance(Base):
    """AssetMaintenance — 10 columns, 2 FKs."""
    __tablename__ = "asset_maintenance"

    # FK: asset_id -> fixed_assets.id
    # FK: vendor_id -> business_partners.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    asset_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    maintenance_date: Mapped[date] = mapped_column(Date, nullable=False)
    maintenance_type: Mapped[str] = mapped_column(NVARCHAR(255))
    description: Mapped[str] = mapped_column(Text)
    cost: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    vendor_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    next_due_date: Mapped[date] = mapped_column(Date)
    notes: Mapped[str] = mapped_column(Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # asset = relationship("FixedAssets", back_populates="asset_maintenances")
    # vendor = relationship("BusinessPartners", back_populates="asset_maintenances")

class FixedAssets(Base):
    """FixedAssets — 23 columns, 3 FKs."""
    __tablename__ = "fixed_assets"

    # FK: category_id -> asset_categories.id
    # FK: company_id -> companies.id
    # FK: po_id -> purchase_orders.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    asset_number: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    category_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    purchase_date: Mapped[date] = mapped_column(Date, nullable=False)
    in_service_date: Mapped[date] = mapped_column(Date)
    purchase_cost: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    salvage_value: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    depreciation_method: Mapped[str] = mapped_column(NVARCHAR(255))
    useful_life_years: Mapped[Decimal] = mapped_column(Numeric(6, 2))
    accumulated_dep: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    net_book_value: Mapped[Decimal] = mapped_column(Numeric(19, 4))
    location: Mapped[str] = mapped_column(NVARCHAR(255))
    serial_number: Mapped[str] = mapped_column(NVARCHAR(255))
    po_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    insurance_value: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    insured_until: Mapped[date] = mapped_column(Date)
    disposal_date: Mapped[date] = mapped_column(Date)
    disposal_amount: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    status: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # category = relationship("AssetCategories", back_populates="fixed_assetss")
    # company = relationship("Companies", back_populates="fixed_assetss")
    # po = relationship("PurchaseOrders", back_populates="fixed_assetss")
