"""
New P0 Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.727481
Mounted at /api/v1/p0
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .new_p0_schemas import *
from .new_p0_services import *

router = APIRouter(prefix="/p0", tags=["New P0"])

@router.get("/ai_anomaliess", response_model=AiAnomaliesListResponse)
def list_ai_anomaliess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiAnomalies records."""
    service = AiAnomaliesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_anomaliess/{id}", response_model=AiAnomaliesResponse)
def get_ai_anomalies(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiAnomalies by ID."""
    service = AiAnomaliesService(db)
    return service.get(id)

@router.post("/ai_anomaliess", response_model=AiAnomaliesResponse, status_code=status.HTTP_201_CREATED)
def create_ai_anomalies(
    data: AiAnomaliesCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiAnomalies."""
    service = AiAnomaliesService(db)
    return service.create(data)

@router.put("/ai_anomaliess/{id}", response_model=AiAnomaliesResponse)
def update_ai_anomalies(
    id: UUID,
    data: AiAnomaliesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiAnomalies."""
    service = AiAnomaliesService(db)
    return service.update(id, data)

@router.delete("/ai_anomaliess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_anomalies(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiAnomalies (soft delete by default)."""
    service = AiAnomaliesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_anomaliess/search", response_model=List[AiAnomaliesResponse])
def search_ai_anomaliess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiAnomalies by name/code/description."""
    service = AiAnomaliesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_conversationss", response_model=AiConversationsListResponse)
def list_ai_conversationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiConversations records."""
    service = AiConversationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_conversationss/{id}", response_model=AiConversationsResponse)
def get_ai_conversations(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiConversations by ID."""
    service = AiConversationsService(db)
    return service.get(id)

@router.post("/ai_conversationss", response_model=AiConversationsResponse, status_code=status.HTTP_201_CREATED)
def create_ai_conversations(
    data: AiConversationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiConversations."""
    service = AiConversationsService(db)
    return service.create(data)

@router.put("/ai_conversationss/{id}", response_model=AiConversationsResponse)
def update_ai_conversations(
    id: UUID,
    data: AiConversationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiConversations."""
    service = AiConversationsService(db)
    return service.update(id, data)

@router.delete("/ai_conversationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_conversations(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiConversations (soft delete by default)."""
    service = AiConversationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_conversationss/search", response_model=List[AiConversationsResponse])
def search_ai_conversationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiConversations by name/code/description."""
    service = AiConversationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_embeddingss", response_model=AiEmbeddingsListResponse)
def list_ai_embeddingss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiEmbeddings records."""
    service = AiEmbeddingsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_embeddingss/{id}", response_model=AiEmbeddingsResponse)
def get_ai_embeddings(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiEmbeddings by ID."""
    service = AiEmbeddingsService(db)
    return service.get(id)

@router.post("/ai_embeddingss", response_model=AiEmbeddingsResponse, status_code=status.HTTP_201_CREATED)
def create_ai_embeddings(
    data: AiEmbeddingsCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiEmbeddings."""
    service = AiEmbeddingsService(db)
    return service.create(data)

@router.put("/ai_embeddingss/{id}", response_model=AiEmbeddingsResponse)
def update_ai_embeddings(
    id: UUID,
    data: AiEmbeddingsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiEmbeddings."""
    service = AiEmbeddingsService(db)
    return service.update(id, data)

@router.delete("/ai_embeddingss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_embeddings(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiEmbeddings (soft delete by default)."""
    service = AiEmbeddingsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_embeddingss/search", response_model=List[AiEmbeddingsResponse])
def search_ai_embeddingss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiEmbeddings by name/code/description."""
    service = AiEmbeddingsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_feedbacks", response_model=AiFeedbackListResponse)
def list_ai_feedbacks(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiFeedback records."""
    service = AiFeedbackService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_feedbacks/{id}", response_model=AiFeedbackResponse)
def get_ai_feedback(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiFeedback by ID."""
    service = AiFeedbackService(db)
    return service.get(id)

@router.post("/ai_feedbacks", response_model=AiFeedbackResponse, status_code=status.HTTP_201_CREATED)
def create_ai_feedback(
    data: AiFeedbackCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiFeedback."""
    service = AiFeedbackService(db)
    return service.create(data)

@router.put("/ai_feedbacks/{id}", response_model=AiFeedbackResponse)
def update_ai_feedback(
    id: UUID,
    data: AiFeedbackUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiFeedback."""
    service = AiFeedbackService(db)
    return service.update(id, data)

@router.delete("/ai_feedbacks/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_feedback(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiFeedback (soft delete by default)."""
    service = AiFeedbackService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_feedbacks/search", response_model=List[AiFeedbackResponse])
def search_ai_feedbacks(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiFeedback by name/code/description."""
    service = AiFeedbackService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_modelss", response_model=AiModelsListResponse)
def list_ai_modelss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiModels records."""
    service = AiModelsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_modelss/{id}", response_model=AiModelsResponse)
def get_ai_models(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiModels by ID."""
    service = AiModelsService(db)
    return service.get(id)

@router.post("/ai_modelss", response_model=AiModelsResponse, status_code=status.HTTP_201_CREATED)
def create_ai_models(
    data: AiModelsCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiModels."""
    service = AiModelsService(db)
    return service.create(data)

@router.put("/ai_modelss/{id}", response_model=AiModelsResponse)
def update_ai_models(
    id: UUID,
    data: AiModelsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiModels."""
    service = AiModelsService(db)
    return service.update(id, data)

@router.delete("/ai_modelss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_models(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiModels (soft delete by default)."""
    service = AiModelsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_modelss/search", response_model=List[AiModelsResponse])
def search_ai_modelss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiModels by name/code/description."""
    service = AiModelsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_predictionss", response_model=AiPredictionsListResponse)
def list_ai_predictionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiPredictions records."""
    service = AiPredictionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_predictionss/{id}", response_model=AiPredictionsResponse)
def get_ai_predictions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiPredictions by ID."""
    service = AiPredictionsService(db)
    return service.get(id)

@router.post("/ai_predictionss", response_model=AiPredictionsResponse, status_code=status.HTTP_201_CREATED)
def create_ai_predictions(
    data: AiPredictionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiPredictions."""
    service = AiPredictionsService(db)
    return service.create(data)

@router.put("/ai_predictionss/{id}", response_model=AiPredictionsResponse)
def update_ai_predictions(
    id: UUID,
    data: AiPredictionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiPredictions."""
    service = AiPredictionsService(db)
    return service.update(id, data)

@router.delete("/ai_predictionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_predictions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiPredictions (soft delete by default)."""
    service = AiPredictionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_predictionss/search", response_model=List[AiPredictionsResponse])
def search_ai_predictionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiPredictions by name/code/description."""
    service = AiPredictionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_promptss", response_model=AiPromptsListResponse)
def list_ai_promptss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiPrompts records."""
    service = AiPromptsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_promptss/{id}", response_model=AiPromptsResponse)
def get_ai_prompts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiPrompts by ID."""
    service = AiPromptsService(db)
    return service.get(id)

@router.post("/ai_promptss", response_model=AiPromptsResponse, status_code=status.HTTP_201_CREATED)
def create_ai_prompts(
    data: AiPromptsCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiPrompts."""
    service = AiPromptsService(db)
    return service.create(data)

@router.put("/ai_promptss/{id}", response_model=AiPromptsResponse)
def update_ai_prompts(
    id: UUID,
    data: AiPromptsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiPrompts."""
    service = AiPromptsService(db)
    return service.update(id, data)

@router.delete("/ai_promptss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_prompts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiPrompts (soft delete by default)."""
    service = AiPromptsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_promptss/search", response_model=List[AiPromptsResponse])
def search_ai_promptss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiPrompts by name/code/description."""
    service = AiPromptsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_recon_training_datas", response_model=AiReconTrainingDataListResponse)
def list_ai_recon_training_datas(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiReconTrainingData records."""
    service = AiReconTrainingDataService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_recon_training_datas/{id}", response_model=AiReconTrainingDataResponse)
def get_ai_recon_training_data(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiReconTrainingData by ID."""
    service = AiReconTrainingDataService(db)
    return service.get(id)

@router.post("/ai_recon_training_datas", response_model=AiReconTrainingDataResponse, status_code=status.HTTP_201_CREATED)
def create_ai_recon_training_data(
    data: AiReconTrainingDataCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiReconTrainingData."""
    service = AiReconTrainingDataService(db)
    return service.create(data)

@router.put("/ai_recon_training_datas/{id}", response_model=AiReconTrainingDataResponse)
def update_ai_recon_training_data(
    id: UUID,
    data: AiReconTrainingDataUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiReconTrainingData."""
    service = AiReconTrainingDataService(db)
    return service.update(id, data)

@router.delete("/ai_recon_training_datas/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_recon_training_data(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiReconTrainingData (soft delete by default)."""
    service = AiReconTrainingDataService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_recon_training_datas/search", response_model=List[AiReconTrainingDataResponse])
def search_ai_recon_training_datas(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiReconTrainingData by name/code/description."""
    service = AiReconTrainingDataService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/ai_training_datas", response_model=AiTrainingDataListResponse)
def list_ai_training_datas(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AiTrainingData records."""
    service = AiTrainingDataService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/ai_training_datas/{id}", response_model=AiTrainingDataResponse)
def get_ai_training_data(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AiTrainingData by ID."""
    service = AiTrainingDataService(db)
    return service.get(id)

@router.post("/ai_training_datas", response_model=AiTrainingDataResponse, status_code=status.HTTP_201_CREATED)
def create_ai_training_data(
    data: AiTrainingDataCreate,
    db: Session = Depends(get_db)
):
    """Create a new AiTrainingData."""
    service = AiTrainingDataService(db)
    return service.create(data)

@router.put("/ai_training_datas/{id}", response_model=AiTrainingDataResponse)
def update_ai_training_data(
    id: UUID,
    data: AiTrainingDataUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AiTrainingData."""
    service = AiTrainingDataService(db)
    return service.update(id, data)

@router.delete("/ai_training_datas/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_ai_training_data(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AiTrainingData (soft delete by default)."""
    service = AiTrainingDataService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/ai_training_datas/search", response_model=List[AiTrainingDataResponse])
def search_ai_training_datas(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AiTrainingData by name/code/description."""
    service = AiTrainingDataService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/carbon_activitiess", response_model=CarbonActivitiesListResponse)
def list_carbon_activitiess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all CarbonActivities records."""
    service = CarbonActivitiesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/carbon_activitiess/{id}", response_model=CarbonActivitiesResponse)
def get_carbon_activities(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get CarbonActivities by ID."""
    service = CarbonActivitiesService(db)
    return service.get(id)

@router.post("/carbon_activitiess", response_model=CarbonActivitiesResponse, status_code=status.HTTP_201_CREATED)
def create_carbon_activities(
    data: CarbonActivitiesCreate,
    db: Session = Depends(get_db)
):
    """Create a new CarbonActivities."""
    service = CarbonActivitiesService(db)
    return service.create(data)

@router.put("/carbon_activitiess/{id}", response_model=CarbonActivitiesResponse)
def update_carbon_activities(
    id: UUID,
    data: CarbonActivitiesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing CarbonActivities."""
    service = CarbonActivitiesService(db)
    return service.update(id, data)

@router.delete("/carbon_activitiess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_carbon_activities(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a CarbonActivities (soft delete by default)."""
    service = CarbonActivitiesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/carbon_activitiess/search", response_model=List[CarbonActivitiesResponse])
def search_carbon_activitiess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search CarbonActivities by name/code/description."""
    service = CarbonActivitiesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/carbon_emissionss", response_model=CarbonEmissionsListResponse)
def list_carbon_emissionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all CarbonEmissions records."""
    service = CarbonEmissionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/carbon_emissionss/{id}", response_model=CarbonEmissionsResponse)
def get_carbon_emissions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get CarbonEmissions by ID."""
    service = CarbonEmissionsService(db)
    return service.get(id)

@router.post("/carbon_emissionss", response_model=CarbonEmissionsResponse, status_code=status.HTTP_201_CREATED)
def create_carbon_emissions(
    data: CarbonEmissionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new CarbonEmissions."""
    service = CarbonEmissionsService(db)
    return service.create(data)

@router.put("/carbon_emissionss/{id}", response_model=CarbonEmissionsResponse)
def update_carbon_emissions(
    id: UUID,
    data: CarbonEmissionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing CarbonEmissions."""
    service = CarbonEmissionsService(db)
    return service.update(id, data)

@router.delete("/carbon_emissionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_carbon_emissions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a CarbonEmissions (soft delete by default)."""
    service = CarbonEmissionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/carbon_emissionss/search", response_model=List[CarbonEmissionsResponse])
def search_carbon_emissionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search CarbonEmissions by name/code/description."""
    service = CarbonEmissionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/carbon_factorss", response_model=CarbonFactorsListResponse)
def list_carbon_factorss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all CarbonFactors records."""
    service = CarbonFactorsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/carbon_factorss/{id}", response_model=CarbonFactorsResponse)
def get_carbon_factors(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get CarbonFactors by ID."""
    service = CarbonFactorsService(db)
    return service.get(id)

@router.post("/carbon_factorss", response_model=CarbonFactorsResponse, status_code=status.HTTP_201_CREATED)
def create_carbon_factors(
    data: CarbonFactorsCreate,
    db: Session = Depends(get_db)
):
    """Create a new CarbonFactors."""
    service = CarbonFactorsService(db)
    return service.create(data)

@router.put("/carbon_factorss/{id}", response_model=CarbonFactorsResponse)
def update_carbon_factors(
    id: UUID,
    data: CarbonFactorsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing CarbonFactors."""
    service = CarbonFactorsService(db)
    return service.update(id, data)

@router.delete("/carbon_factorss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_carbon_factors(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a CarbonFactors (soft delete by default)."""
    service = CarbonFactorsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/carbon_factorss/search", response_model=List[CarbonFactorsResponse])
def search_carbon_factorss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search CarbonFactors by name/code/description."""
    service = CarbonFactorsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/carbon_reductionss", response_model=CarbonReductionsListResponse)
def list_carbon_reductionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all CarbonReductions records."""
    service = CarbonReductionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/carbon_reductionss/{id}", response_model=CarbonReductionsResponse)
def get_carbon_reductions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get CarbonReductions by ID."""
    service = CarbonReductionsService(db)
    return service.get(id)

@router.post("/carbon_reductionss", response_model=CarbonReductionsResponse, status_code=status.HTTP_201_CREATED)
def create_carbon_reductions(
    data: CarbonReductionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new CarbonReductions."""
    service = CarbonReductionsService(db)
    return service.create(data)

@router.put("/carbon_reductionss/{id}", response_model=CarbonReductionsResponse)
def update_carbon_reductions(
    id: UUID,
    data: CarbonReductionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing CarbonReductions."""
    service = CarbonReductionsService(db)
    return service.update(id, data)

@router.delete("/carbon_reductionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_carbon_reductions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a CarbonReductions (soft delete by default)."""
    service = CarbonReductionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/carbon_reductionss/search", response_model=List[CarbonReductionsResponse])
def search_carbon_reductionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search CarbonReductions by name/code/description."""
    service = CarbonReductionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/carbon_scenarioss", response_model=CarbonScenariosListResponse)
def list_carbon_scenarioss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all CarbonScenarios records."""
    service = CarbonScenariosService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/carbon_scenarioss/{id}", response_model=CarbonScenariosResponse)
def get_carbon_scenarios(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get CarbonScenarios by ID."""
    service = CarbonScenariosService(db)
    return service.get(id)

@router.post("/carbon_scenarioss", response_model=CarbonScenariosResponse, status_code=status.HTTP_201_CREATED)
def create_carbon_scenarios(
    data: CarbonScenariosCreate,
    db: Session = Depends(get_db)
):
    """Create a new CarbonScenarios."""
    service = CarbonScenariosService(db)
    return service.create(data)

@router.put("/carbon_scenarioss/{id}", response_model=CarbonScenariosResponse)
def update_carbon_scenarios(
    id: UUID,
    data: CarbonScenariosUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing CarbonScenarios."""
    service = CarbonScenariosService(db)
    return service.update(id, data)

@router.delete("/carbon_scenarioss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_carbon_scenarios(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a CarbonScenarios (soft delete by default)."""
    service = CarbonScenariosService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/carbon_scenarioss/search", response_model=List[CarbonScenariosResponse])
def search_carbon_scenarioss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search CarbonScenarios by name/code/description."""
    service = CarbonScenariosService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/compliance_alertss", response_model=ComplianceAlertsListResponse)
def list_compliance_alertss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ComplianceAlerts records."""
    service = ComplianceAlertsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/compliance_alertss/{id}", response_model=ComplianceAlertsResponse)
def get_compliance_alerts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ComplianceAlerts by ID."""
    service = ComplianceAlertsService(db)
    return service.get(id)

@router.post("/compliance_alertss", response_model=ComplianceAlertsResponse, status_code=status.HTTP_201_CREATED)
def create_compliance_alerts(
    data: ComplianceAlertsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ComplianceAlerts."""
    service = ComplianceAlertsService(db)
    return service.create(data)

@router.put("/compliance_alertss/{id}", response_model=ComplianceAlertsResponse)
def update_compliance_alerts(
    id: UUID,
    data: ComplianceAlertsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ComplianceAlerts."""
    service = ComplianceAlertsService(db)
    return service.update(id, data)

@router.delete("/compliance_alertss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_compliance_alerts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ComplianceAlerts (soft delete by default)."""
    service = ComplianceAlertsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/compliance_alertss/search", response_model=List[ComplianceAlertsResponse])
def search_compliance_alertss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ComplianceAlerts by name/code/description."""
    service = ComplianceAlertsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/compliance_assessmentss", response_model=ComplianceAssessmentsListResponse)
def list_compliance_assessmentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ComplianceAssessments records."""
    service = ComplianceAssessmentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/compliance_assessmentss/{id}", response_model=ComplianceAssessmentsResponse)
def get_compliance_assessments(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ComplianceAssessments by ID."""
    service = ComplianceAssessmentsService(db)
    return service.get(id)

@router.post("/compliance_assessmentss", response_model=ComplianceAssessmentsResponse, status_code=status.HTTP_201_CREATED)
def create_compliance_assessments(
    data: ComplianceAssessmentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ComplianceAssessments."""
    service = ComplianceAssessmentsService(db)
    return service.create(data)

@router.put("/compliance_assessmentss/{id}", response_model=ComplianceAssessmentsResponse)
def update_compliance_assessments(
    id: UUID,
    data: ComplianceAssessmentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ComplianceAssessments."""
    service = ComplianceAssessmentsService(db)
    return service.update(id, data)

@router.delete("/compliance_assessmentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_compliance_assessments(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ComplianceAssessments (soft delete by default)."""
    service = ComplianceAssessmentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/compliance_assessmentss/search", response_model=List[ComplianceAssessmentsResponse])
def search_compliance_assessmentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ComplianceAssessments by name/code/description."""
    service = ComplianceAssessmentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/compliance_documentss", response_model=ComplianceDocumentsListResponse)
def list_compliance_documentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ComplianceDocuments records."""
    service = ComplianceDocumentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/compliance_documentss/{id}", response_model=ComplianceDocumentsResponse)
def get_compliance_documents(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ComplianceDocuments by ID."""
    service = ComplianceDocumentsService(db)
    return service.get(id)

@router.post("/compliance_documentss", response_model=ComplianceDocumentsResponse, status_code=status.HTTP_201_CREATED)
def create_compliance_documents(
    data: ComplianceDocumentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ComplianceDocuments."""
    service = ComplianceDocumentsService(db)
    return service.create(data)

@router.put("/compliance_documentss/{id}", response_model=ComplianceDocumentsResponse)
def update_compliance_documents(
    id: UUID,
    data: ComplianceDocumentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ComplianceDocuments."""
    service = ComplianceDocumentsService(db)
    return service.update(id, data)

@router.delete("/compliance_documentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_compliance_documents(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ComplianceDocuments (soft delete by default)."""
    service = ComplianceDocumentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/compliance_documentss/search", response_model=List[ComplianceDocumentsResponse])
def search_compliance_documentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ComplianceDocuments by name/code/description."""
    service = ComplianceDocumentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/compliance_ruless", response_model=ComplianceRulesListResponse)
def list_compliance_ruless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ComplianceRules records."""
    service = ComplianceRulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/compliance_ruless/{id}", response_model=ComplianceRulesResponse)
def get_compliance_rules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ComplianceRules by ID."""
    service = ComplianceRulesService(db)
    return service.get(id)

@router.post("/compliance_ruless", response_model=ComplianceRulesResponse, status_code=status.HTTP_201_CREATED)
def create_compliance_rules(
    data: ComplianceRulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ComplianceRules."""
    service = ComplianceRulesService(db)
    return service.create(data)

@router.put("/compliance_ruless/{id}", response_model=ComplianceRulesResponse)
def update_compliance_rules(
    id: UUID,
    data: ComplianceRulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ComplianceRules."""
    service = ComplianceRulesService(db)
    return service.update(id, data)

@router.delete("/compliance_ruless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_compliance_rules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ComplianceRules (soft delete by default)."""
    service = ComplianceRulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/compliance_ruless/search", response_model=List[ComplianceRulesResponse])
def search_compliance_ruless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ComplianceRules by name/code/description."""
    service = ComplianceRulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/esg_audit_trails", response_model=EsgAuditTrailListResponse)
def list_esg_audit_trails(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EsgAuditTrail records."""
    service = EsgAuditTrailService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/esg_audit_trails/{id}", response_model=EsgAuditTrailResponse)
def get_esg_audit_trail(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EsgAuditTrail by ID."""
    service = EsgAuditTrailService(db)
    return service.get(id)

@router.post("/esg_audit_trails", response_model=EsgAuditTrailResponse, status_code=status.HTTP_201_CREATED)
def create_esg_audit_trail(
    data: EsgAuditTrailCreate,
    db: Session = Depends(get_db)
):
    """Create a new EsgAuditTrail."""
    service = EsgAuditTrailService(db)
    return service.create(data)

@router.put("/esg_audit_trails/{id}", response_model=EsgAuditTrailResponse)
def update_esg_audit_trail(
    id: UUID,
    data: EsgAuditTrailUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EsgAuditTrail."""
    service = EsgAuditTrailService(db)
    return service.update(id, data)

@router.delete("/esg_audit_trails/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_esg_audit_trail(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EsgAuditTrail (soft delete by default)."""
    service = EsgAuditTrailService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/esg_audit_trails/search", response_model=List[EsgAuditTrailResponse])
def search_esg_audit_trails(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EsgAuditTrail by name/code/description."""
    service = EsgAuditTrailService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/esg_frameworkss", response_model=EsgFrameworksListResponse)
def list_esg_frameworkss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EsgFrameworks records."""
    service = EsgFrameworksService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/esg_frameworkss/{id}", response_model=EsgFrameworksResponse)
def get_esg_frameworks(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EsgFrameworks by ID."""
    service = EsgFrameworksService(db)
    return service.get(id)

@router.post("/esg_frameworkss", response_model=EsgFrameworksResponse, status_code=status.HTTP_201_CREATED)
def create_esg_frameworks(
    data: EsgFrameworksCreate,
    db: Session = Depends(get_db)
):
    """Create a new EsgFrameworks."""
    service = EsgFrameworksService(db)
    return service.create(data)

@router.put("/esg_frameworkss/{id}", response_model=EsgFrameworksResponse)
def update_esg_frameworks(
    id: UUID,
    data: EsgFrameworksUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EsgFrameworks."""
    service = EsgFrameworksService(db)
    return service.update(id, data)

@router.delete("/esg_frameworkss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_esg_frameworks(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EsgFrameworks (soft delete by default)."""
    service = EsgFrameworksService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/esg_frameworkss/search", response_model=List[EsgFrameworksResponse])
def search_esg_frameworkss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EsgFrameworks by name/code/description."""
    service = EsgFrameworksService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/esg_kpiss", response_model=EsgKpisListResponse)
def list_esg_kpiss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EsgKpis records."""
    service = EsgKpisService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/esg_kpiss/{id}", response_model=EsgKpisResponse)
def get_esg_kpis(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EsgKpis by ID."""
    service = EsgKpisService(db)
    return service.get(id)

@router.post("/esg_kpiss", response_model=EsgKpisResponse, status_code=status.HTTP_201_CREATED)
def create_esg_kpis(
    data: EsgKpisCreate,
    db: Session = Depends(get_db)
):
    """Create a new EsgKpis."""
    service = EsgKpisService(db)
    return service.create(data)

@router.put("/esg_kpiss/{id}", response_model=EsgKpisResponse)
def update_esg_kpis(
    id: UUID,
    data: EsgKpisUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EsgKpis."""
    service = EsgKpisService(db)
    return service.update(id, data)

@router.delete("/esg_kpiss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_esg_kpis(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EsgKpis (soft delete by default)."""
    service = EsgKpisService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/esg_kpiss/search", response_model=List[EsgKpisResponse])
def search_esg_kpiss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EsgKpis by name/code/description."""
    service = EsgKpisService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/esg_materialitys", response_model=EsgMaterialityListResponse)
def list_esg_materialitys(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EsgMateriality records."""
    service = EsgMaterialityService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/esg_materialitys/{id}", response_model=EsgMaterialityResponse)
def get_esg_materiality(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EsgMateriality by ID."""
    service = EsgMaterialityService(db)
    return service.get(id)

@router.post("/esg_materialitys", response_model=EsgMaterialityResponse, status_code=status.HTTP_201_CREATED)
def create_esg_materiality(
    data: EsgMaterialityCreate,
    db: Session = Depends(get_db)
):
    """Create a new EsgMateriality."""
    service = EsgMaterialityService(db)
    return service.create(data)

@router.put("/esg_materialitys/{id}", response_model=EsgMaterialityResponse)
def update_esg_materiality(
    id: UUID,
    data: EsgMaterialityUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EsgMateriality."""
    service = EsgMaterialityService(db)
    return service.update(id, data)

@router.delete("/esg_materialitys/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_esg_materiality(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EsgMateriality (soft delete by default)."""
    service = EsgMaterialityService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/esg_materialitys/search", response_model=List[EsgMaterialityResponse])
def search_esg_materialitys(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EsgMateriality by name/code/description."""
    service = EsgMaterialityService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/esg_reportss", response_model=EsgReportsListResponse)
def list_esg_reportss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EsgReports records."""
    service = EsgReportsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/esg_reportss/{id}", response_model=EsgReportsResponse)
def get_esg_reports(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EsgReports by ID."""
    service = EsgReportsService(db)
    return service.get(id)

@router.post("/esg_reportss", response_model=EsgReportsResponse, status_code=status.HTTP_201_CREATED)
def create_esg_reports(
    data: EsgReportsCreate,
    db: Session = Depends(get_db)
):
    """Create a new EsgReports."""
    service = EsgReportsService(db)
    return service.create(data)

@router.put("/esg_reportss/{id}", response_model=EsgReportsResponse)
def update_esg_reports(
    id: UUID,
    data: EsgReportsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EsgReports."""
    service = EsgReportsService(db)
    return service.update(id, data)

@router.delete("/esg_reportss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_esg_reports(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EsgReports (soft delete by default)."""
    service = EsgReportsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/esg_reportss/search", response_model=List[EsgReportsResponse])
def search_esg_reportss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EsgReports by name/code/description."""
    service = EsgReportsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/esg_targetss", response_model=EsgTargetsListResponse)
def list_esg_targetss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EsgTargets records."""
    service = EsgTargetsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/esg_targetss/{id}", response_model=EsgTargetsResponse)
def get_esg_targets(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EsgTargets by ID."""
    service = EsgTargetsService(db)
    return service.get(id)

@router.post("/esg_targetss", response_model=EsgTargetsResponse, status_code=status.HTTP_201_CREATED)
def create_esg_targets(
    data: EsgTargetsCreate,
    db: Session = Depends(get_db)
):
    """Create a new EsgTargets."""
    service = EsgTargetsService(db)
    return service.create(data)

@router.put("/esg_targetss/{id}", response_model=EsgTargetsResponse)
def update_esg_targets(
    id: UUID,
    data: EsgTargetsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EsgTargets."""
    service = EsgTargetsService(db)
    return service.update(id, data)

@router.delete("/esg_targetss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_esg_targets(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EsgTargets (soft delete by default)."""
    service = EsgTargetsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/esg_targetss/search", response_model=List[EsgTargetsResponse])
def search_esg_targetss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EsgTargets by name/code/description."""
    service = EsgTargetsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_new_p0_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get New P0 dashboard summary."""
    return {
        "module": "new_p0",
        "company_id": str(company_id),
        "tables": 24,
        "metrics": {}
    }
