"""
New P0 Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.726125
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .new_p0_models import *
from .new_p0_schemas import *

class AiAnomaliesService:
    """CRUD service for AiAnomalies."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiAnomaliesCreate, created_by: Optional[UUID] = None) -> AiAnomaliesResponse:
        """Create a new AiAnomalies record."""
        db_obj = AiAnomalies(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiAnomaliesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiAnomaliesResponse]:
        """Get AiAnomalies by primary key."""
        db_obj = self.db.query(AiAnomalies).filter(AiAnomalies.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiAnomalies not found")
        return AiAnomaliesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiAnomaliesListResponse:
        """List AiAnomalies records with pagination and filtering."""
        query = self.db.query(AiAnomalies)
        if company_id and hasattr(AiAnomalies, "company_id"):
            query = query.filter(AiAnomalies.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiAnomalies, key) and value is not None:
                    query = query.filter(getattr(AiAnomalies, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiAnomalies, order_by, AiAnomalies.created_at)))
        else:
            query = query.order_by(getattr(AiAnomalies, order_by, AiAnomalies.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiAnomaliesListResponse(
            items=[AiAnomaliesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiAnomaliesUpdate,
               updated_by: Optional[UUID] = None) -> AiAnomaliesResponse:
        """Update an existing AiAnomalies record."""
        db_obj = self.db.query(AiAnomalies).filter(AiAnomalies.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiAnomalies not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiAnomaliesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiAnomalies record. Soft delete by default."""
        db_obj = self.db.query(AiAnomalies).filter(AiAnomalies.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiAnomalies not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiAnomaliesResponse]:
        """Search AiAnomalies by name/code fields."""
        query = self.db.query(AiAnomalies)
        if company_id and hasattr(AiAnomalies, "company_id"):
            query = query.filter(AiAnomalies.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiAnomalies, f)]
        if search_fields:
            conditions = [getattr(AiAnomalies, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiAnomaliesResponse.model_validate(i) for i in items]

class AiConversationsService:
    """CRUD service for AiConversations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiConversationsCreate, created_by: Optional[UUID] = None) -> AiConversationsResponse:
        """Create a new AiConversations record."""
        db_obj = AiConversations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiConversationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiConversationsResponse]:
        """Get AiConversations by primary key."""
        db_obj = self.db.query(AiConversations).filter(AiConversations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiConversations not found")
        return AiConversationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiConversationsListResponse:
        """List AiConversations records with pagination and filtering."""
        query = self.db.query(AiConversations)
        if company_id and hasattr(AiConversations, "company_id"):
            query = query.filter(AiConversations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiConversations, key) and value is not None:
                    query = query.filter(getattr(AiConversations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiConversations, order_by, AiConversations.created_at)))
        else:
            query = query.order_by(getattr(AiConversations, order_by, AiConversations.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiConversationsListResponse(
            items=[AiConversationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiConversationsUpdate,
               updated_by: Optional[UUID] = None) -> AiConversationsResponse:
        """Update an existing AiConversations record."""
        db_obj = self.db.query(AiConversations).filter(AiConversations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiConversations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiConversationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiConversations record. Soft delete by default."""
        db_obj = self.db.query(AiConversations).filter(AiConversations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiConversations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiConversationsResponse]:
        """Search AiConversations by name/code fields."""
        query = self.db.query(AiConversations)
        if company_id and hasattr(AiConversations, "company_id"):
            query = query.filter(AiConversations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiConversations, f)]
        if search_fields:
            conditions = [getattr(AiConversations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiConversationsResponse.model_validate(i) for i in items]

class AiEmbeddingsService:
    """CRUD service for AiEmbeddings."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiEmbeddingsCreate, created_by: Optional[UUID] = None) -> AiEmbeddingsResponse:
        """Create a new AiEmbeddings record."""
        db_obj = AiEmbeddings(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiEmbeddingsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiEmbeddingsResponse]:
        """Get AiEmbeddings by primary key."""
        db_obj = self.db.query(AiEmbeddings).filter(AiEmbeddings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiEmbeddings not found")
        return AiEmbeddingsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiEmbeddingsListResponse:
        """List AiEmbeddings records with pagination and filtering."""
        query = self.db.query(AiEmbeddings)
        if company_id and hasattr(AiEmbeddings, "company_id"):
            query = query.filter(AiEmbeddings.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiEmbeddings, key) and value is not None:
                    query = query.filter(getattr(AiEmbeddings, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiEmbeddings, order_by, AiEmbeddings.created_at)))
        else:
            query = query.order_by(getattr(AiEmbeddings, order_by, AiEmbeddings.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiEmbeddingsListResponse(
            items=[AiEmbeddingsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiEmbeddingsUpdate,
               updated_by: Optional[UUID] = None) -> AiEmbeddingsResponse:
        """Update an existing AiEmbeddings record."""
        db_obj = self.db.query(AiEmbeddings).filter(AiEmbeddings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiEmbeddings not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiEmbeddingsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiEmbeddings record. Soft delete by default."""
        db_obj = self.db.query(AiEmbeddings).filter(AiEmbeddings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiEmbeddings not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiEmbeddingsResponse]:
        """Search AiEmbeddings by name/code fields."""
        query = self.db.query(AiEmbeddings)
        if company_id and hasattr(AiEmbeddings, "company_id"):
            query = query.filter(AiEmbeddings.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiEmbeddings, f)]
        if search_fields:
            conditions = [getattr(AiEmbeddings, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiEmbeddingsResponse.model_validate(i) for i in items]

class AiFeedbackService:
    """CRUD service for AiFeedback."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiFeedbackCreate, created_by: Optional[UUID] = None) -> AiFeedbackResponse:
        """Create a new AiFeedback record."""
        db_obj = AiFeedback(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiFeedbackResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiFeedbackResponse]:
        """Get AiFeedback by primary key."""
        db_obj = self.db.query(AiFeedback).filter(AiFeedback.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiFeedback not found")
        return AiFeedbackResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiFeedbackListResponse:
        """List AiFeedback records with pagination and filtering."""
        query = self.db.query(AiFeedback)
        if company_id and hasattr(AiFeedback, "company_id"):
            query = query.filter(AiFeedback.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiFeedback, key) and value is not None:
                    query = query.filter(getattr(AiFeedback, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiFeedback, order_by, AiFeedback.created_at)))
        else:
            query = query.order_by(getattr(AiFeedback, order_by, AiFeedback.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiFeedbackListResponse(
            items=[AiFeedbackResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiFeedbackUpdate,
               updated_by: Optional[UUID] = None) -> AiFeedbackResponse:
        """Update an existing AiFeedback record."""
        db_obj = self.db.query(AiFeedback).filter(AiFeedback.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiFeedback not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiFeedbackResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiFeedback record. Soft delete by default."""
        db_obj = self.db.query(AiFeedback).filter(AiFeedback.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiFeedback not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiFeedbackResponse]:
        """Search AiFeedback by name/code fields."""
        query = self.db.query(AiFeedback)
        if company_id and hasattr(AiFeedback, "company_id"):
            query = query.filter(AiFeedback.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiFeedback, f)]
        if search_fields:
            conditions = [getattr(AiFeedback, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiFeedbackResponse.model_validate(i) for i in items]

class AiModelsService:
    """CRUD service for AiModels."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiModelsCreate, created_by: Optional[UUID] = None) -> AiModelsResponse:
        """Create a new AiModels record."""
        db_obj = AiModels(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiModelsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiModelsResponse]:
        """Get AiModels by primary key."""
        db_obj = self.db.query(AiModels).filter(AiModels.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiModels not found")
        return AiModelsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiModelsListResponse:
        """List AiModels records with pagination and filtering."""
        query = self.db.query(AiModels)
        if company_id and hasattr(AiModels, "company_id"):
            query = query.filter(AiModels.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiModels, key) and value is not None:
                    query = query.filter(getattr(AiModels, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiModels, order_by, AiModels.created_at)))
        else:
            query = query.order_by(getattr(AiModels, order_by, AiModels.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiModelsListResponse(
            items=[AiModelsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiModelsUpdate,
               updated_by: Optional[UUID] = None) -> AiModelsResponse:
        """Update an existing AiModels record."""
        db_obj = self.db.query(AiModels).filter(AiModels.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiModels not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiModelsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiModels record. Soft delete by default."""
        db_obj = self.db.query(AiModels).filter(AiModels.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiModels not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiModelsResponse]:
        """Search AiModels by name/code fields."""
        query = self.db.query(AiModels)
        if company_id and hasattr(AiModels, "company_id"):
            query = query.filter(AiModels.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiModels, f)]
        if search_fields:
            conditions = [getattr(AiModels, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiModelsResponse.model_validate(i) for i in items]

class AiPredictionsService:
    """CRUD service for AiPredictions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiPredictionsCreate, created_by: Optional[UUID] = None) -> AiPredictionsResponse:
        """Create a new AiPredictions record."""
        db_obj = AiPredictions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiPredictionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiPredictionsResponse]:
        """Get AiPredictions by primary key."""
        db_obj = self.db.query(AiPredictions).filter(AiPredictions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiPredictions not found")
        return AiPredictionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiPredictionsListResponse:
        """List AiPredictions records with pagination and filtering."""
        query = self.db.query(AiPredictions)
        if company_id and hasattr(AiPredictions, "company_id"):
            query = query.filter(AiPredictions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiPredictions, key) and value is not None:
                    query = query.filter(getattr(AiPredictions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiPredictions, order_by, AiPredictions.created_at)))
        else:
            query = query.order_by(getattr(AiPredictions, order_by, AiPredictions.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiPredictionsListResponse(
            items=[AiPredictionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiPredictionsUpdate,
               updated_by: Optional[UUID] = None) -> AiPredictionsResponse:
        """Update an existing AiPredictions record."""
        db_obj = self.db.query(AiPredictions).filter(AiPredictions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiPredictions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiPredictionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiPredictions record. Soft delete by default."""
        db_obj = self.db.query(AiPredictions).filter(AiPredictions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiPredictions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiPredictionsResponse]:
        """Search AiPredictions by name/code fields."""
        query = self.db.query(AiPredictions)
        if company_id and hasattr(AiPredictions, "company_id"):
            query = query.filter(AiPredictions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiPredictions, f)]
        if search_fields:
            conditions = [getattr(AiPredictions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiPredictionsResponse.model_validate(i) for i in items]

class AiPromptsService:
    """CRUD service for AiPrompts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiPromptsCreate, created_by: Optional[UUID] = None) -> AiPromptsResponse:
        """Create a new AiPrompts record."""
        db_obj = AiPrompts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiPromptsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiPromptsResponse]:
        """Get AiPrompts by primary key."""
        db_obj = self.db.query(AiPrompts).filter(AiPrompts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiPrompts not found")
        return AiPromptsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiPromptsListResponse:
        """List AiPrompts records with pagination and filtering."""
        query = self.db.query(AiPrompts)
        if company_id and hasattr(AiPrompts, "company_id"):
            query = query.filter(AiPrompts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiPrompts, key) and value is not None:
                    query = query.filter(getattr(AiPrompts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiPrompts, order_by, AiPrompts.created_at)))
        else:
            query = query.order_by(getattr(AiPrompts, order_by, AiPrompts.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiPromptsListResponse(
            items=[AiPromptsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiPromptsUpdate,
               updated_by: Optional[UUID] = None) -> AiPromptsResponse:
        """Update an existing AiPrompts record."""
        db_obj = self.db.query(AiPrompts).filter(AiPrompts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiPrompts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiPromptsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiPrompts record. Soft delete by default."""
        db_obj = self.db.query(AiPrompts).filter(AiPrompts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiPrompts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiPromptsResponse]:
        """Search AiPrompts by name/code fields."""
        query = self.db.query(AiPrompts)
        if company_id and hasattr(AiPrompts, "company_id"):
            query = query.filter(AiPrompts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiPrompts, f)]
        if search_fields:
            conditions = [getattr(AiPrompts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiPromptsResponse.model_validate(i) for i in items]

class AiReconTrainingDataService:
    """CRUD service for AiReconTrainingData."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiReconTrainingDataCreate, created_by: Optional[UUID] = None) -> AiReconTrainingDataResponse:
        """Create a new AiReconTrainingData record."""
        db_obj = AiReconTrainingData(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiReconTrainingDataResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiReconTrainingDataResponse]:
        """Get AiReconTrainingData by primary key."""
        db_obj = self.db.query(AiReconTrainingData).filter(AiReconTrainingData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiReconTrainingData not found")
        return AiReconTrainingDataResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiReconTrainingDataListResponse:
        """List AiReconTrainingData records with pagination and filtering."""
        query = self.db.query(AiReconTrainingData)
        if company_id and hasattr(AiReconTrainingData, "company_id"):
            query = query.filter(AiReconTrainingData.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiReconTrainingData, key) and value is not None:
                    query = query.filter(getattr(AiReconTrainingData, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiReconTrainingData, order_by, AiReconTrainingData.created_at)))
        else:
            query = query.order_by(getattr(AiReconTrainingData, order_by, AiReconTrainingData.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiReconTrainingDataListResponse(
            items=[AiReconTrainingDataResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiReconTrainingDataUpdate,
               updated_by: Optional[UUID] = None) -> AiReconTrainingDataResponse:
        """Update an existing AiReconTrainingData record."""
        db_obj = self.db.query(AiReconTrainingData).filter(AiReconTrainingData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiReconTrainingData not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiReconTrainingDataResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiReconTrainingData record. Soft delete by default."""
        db_obj = self.db.query(AiReconTrainingData).filter(AiReconTrainingData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiReconTrainingData not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiReconTrainingDataResponse]:
        """Search AiReconTrainingData by name/code fields."""
        query = self.db.query(AiReconTrainingData)
        if company_id and hasattr(AiReconTrainingData, "company_id"):
            query = query.filter(AiReconTrainingData.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiReconTrainingData, f)]
        if search_fields:
            conditions = [getattr(AiReconTrainingData, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiReconTrainingDataResponse.model_validate(i) for i in items]

class AiTrainingDataService:
    """CRUD service for AiTrainingData."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AiTrainingDataCreate, created_by: Optional[UUID] = None) -> AiTrainingDataResponse:
        """Create a new AiTrainingData record."""
        db_obj = AiTrainingData(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AiTrainingDataResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AiTrainingDataResponse]:
        """Get AiTrainingData by primary key."""
        db_obj = self.db.query(AiTrainingData).filter(AiTrainingData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiTrainingData not found")
        return AiTrainingDataResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AiTrainingDataListResponse:
        """List AiTrainingData records with pagination and filtering."""
        query = self.db.query(AiTrainingData)
        if company_id and hasattr(AiTrainingData, "company_id"):
            query = query.filter(AiTrainingData.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(AiTrainingData, key) and value is not None:
                    query = query.filter(getattr(AiTrainingData, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(AiTrainingData, order_by, AiTrainingData.created_at)))
        else:
            query = query.order_by(getattr(AiTrainingData, order_by, AiTrainingData.created_at))
        items = query.offset(skip).limit(limit).all()
        return AiTrainingDataListResponse(
            items=[AiTrainingDataResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AiTrainingDataUpdate,
               updated_by: Optional[UUID] = None) -> AiTrainingDataResponse:
        """Update an existing AiTrainingData record."""
        db_obj = self.db.query(AiTrainingData).filter(AiTrainingData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiTrainingData not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AiTrainingDataResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a AiTrainingData record. Soft delete by default."""
        db_obj = self.db.query(AiTrainingData).filter(AiTrainingData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="AiTrainingData not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AiTrainingDataResponse]:
        """Search AiTrainingData by name/code fields."""
        query = self.db.query(AiTrainingData)
        if company_id and hasattr(AiTrainingData, "company_id"):
            query = query.filter(AiTrainingData.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(AiTrainingData, f)]
        if search_fields:
            conditions = [getattr(AiTrainingData, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AiTrainingDataResponse.model_validate(i) for i in items]

class CarbonActivitiesService:
    """CRUD service for CarbonActivities."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: CarbonActivitiesCreate, created_by: Optional[UUID] = None) -> CarbonActivitiesResponse:
        """Create a new CarbonActivities record."""
        db_obj = CarbonActivities(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonActivitiesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[CarbonActivitiesResponse]:
        """Get CarbonActivities by primary key."""
        db_obj = self.db.query(CarbonActivities).filter(CarbonActivities.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonActivities not found")
        return CarbonActivitiesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> CarbonActivitiesListResponse:
        """List CarbonActivities records with pagination and filtering."""
        query = self.db.query(CarbonActivities)
        if company_id and hasattr(CarbonActivities, "company_id"):
            query = query.filter(CarbonActivities.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(CarbonActivities, key) and value is not None:
                    query = query.filter(getattr(CarbonActivities, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(CarbonActivities, order_by, CarbonActivities.created_at)))
        else:
            query = query.order_by(getattr(CarbonActivities, order_by, CarbonActivities.created_at))
        items = query.offset(skip).limit(limit).all()
        return CarbonActivitiesListResponse(
            items=[CarbonActivitiesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: CarbonActivitiesUpdate,
               updated_by: Optional[UUID] = None) -> CarbonActivitiesResponse:
        """Update an existing CarbonActivities record."""
        db_obj = self.db.query(CarbonActivities).filter(CarbonActivities.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonActivities not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonActivitiesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a CarbonActivities record. Soft delete by default."""
        db_obj = self.db.query(CarbonActivities).filter(CarbonActivities.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonActivities not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[CarbonActivitiesResponse]:
        """Search CarbonActivities by name/code fields."""
        query = self.db.query(CarbonActivities)
        if company_id and hasattr(CarbonActivities, "company_id"):
            query = query.filter(CarbonActivities.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(CarbonActivities, f)]
        if search_fields:
            conditions = [getattr(CarbonActivities, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [CarbonActivitiesResponse.model_validate(i) for i in items]

class CarbonEmissionsService:
    """CRUD service for CarbonEmissions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: CarbonEmissionsCreate, created_by: Optional[UUID] = None) -> CarbonEmissionsResponse:
        """Create a new CarbonEmissions record."""
        db_obj = CarbonEmissions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonEmissionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[CarbonEmissionsResponse]:
        """Get CarbonEmissions by primary key."""
        db_obj = self.db.query(CarbonEmissions).filter(CarbonEmissions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonEmissions not found")
        return CarbonEmissionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> CarbonEmissionsListResponse:
        """List CarbonEmissions records with pagination and filtering."""
        query = self.db.query(CarbonEmissions)
        if company_id and hasattr(CarbonEmissions, "company_id"):
            query = query.filter(CarbonEmissions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(CarbonEmissions, key) and value is not None:
                    query = query.filter(getattr(CarbonEmissions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(CarbonEmissions, order_by, CarbonEmissions.created_at)))
        else:
            query = query.order_by(getattr(CarbonEmissions, order_by, CarbonEmissions.created_at))
        items = query.offset(skip).limit(limit).all()
        return CarbonEmissionsListResponse(
            items=[CarbonEmissionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: CarbonEmissionsUpdate,
               updated_by: Optional[UUID] = None) -> CarbonEmissionsResponse:
        """Update an existing CarbonEmissions record."""
        db_obj = self.db.query(CarbonEmissions).filter(CarbonEmissions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonEmissions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonEmissionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a CarbonEmissions record. Soft delete by default."""
        db_obj = self.db.query(CarbonEmissions).filter(CarbonEmissions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonEmissions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[CarbonEmissionsResponse]:
        """Search CarbonEmissions by name/code fields."""
        query = self.db.query(CarbonEmissions)
        if company_id and hasattr(CarbonEmissions, "company_id"):
            query = query.filter(CarbonEmissions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(CarbonEmissions, f)]
        if search_fields:
            conditions = [getattr(CarbonEmissions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [CarbonEmissionsResponse.model_validate(i) for i in items]

class CarbonFactorsService:
    """CRUD service for CarbonFactors."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: CarbonFactorsCreate, created_by: Optional[UUID] = None) -> CarbonFactorsResponse:
        """Create a new CarbonFactors record."""
        db_obj = CarbonFactors(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonFactorsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[CarbonFactorsResponse]:
        """Get CarbonFactors by primary key."""
        db_obj = self.db.query(CarbonFactors).filter(CarbonFactors.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonFactors not found")
        return CarbonFactorsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> CarbonFactorsListResponse:
        """List CarbonFactors records with pagination and filtering."""
        query = self.db.query(CarbonFactors)
        if company_id and hasattr(CarbonFactors, "company_id"):
            query = query.filter(CarbonFactors.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(CarbonFactors, key) and value is not None:
                    query = query.filter(getattr(CarbonFactors, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(CarbonFactors, order_by, CarbonFactors.created_at)))
        else:
            query = query.order_by(getattr(CarbonFactors, order_by, CarbonFactors.created_at))
        items = query.offset(skip).limit(limit).all()
        return CarbonFactorsListResponse(
            items=[CarbonFactorsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: CarbonFactorsUpdate,
               updated_by: Optional[UUID] = None) -> CarbonFactorsResponse:
        """Update an existing CarbonFactors record."""
        db_obj = self.db.query(CarbonFactors).filter(CarbonFactors.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonFactors not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonFactorsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a CarbonFactors record. Soft delete by default."""
        db_obj = self.db.query(CarbonFactors).filter(CarbonFactors.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonFactors not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[CarbonFactorsResponse]:
        """Search CarbonFactors by name/code fields."""
        query = self.db.query(CarbonFactors)
        if company_id and hasattr(CarbonFactors, "company_id"):
            query = query.filter(CarbonFactors.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(CarbonFactors, f)]
        if search_fields:
            conditions = [getattr(CarbonFactors, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [CarbonFactorsResponse.model_validate(i) for i in items]

class CarbonReductionsService:
    """CRUD service for CarbonReductions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: CarbonReductionsCreate, created_by: Optional[UUID] = None) -> CarbonReductionsResponse:
        """Create a new CarbonReductions record."""
        db_obj = CarbonReductions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonReductionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[CarbonReductionsResponse]:
        """Get CarbonReductions by primary key."""
        db_obj = self.db.query(CarbonReductions).filter(CarbonReductions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonReductions not found")
        return CarbonReductionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> CarbonReductionsListResponse:
        """List CarbonReductions records with pagination and filtering."""
        query = self.db.query(CarbonReductions)
        if company_id and hasattr(CarbonReductions, "company_id"):
            query = query.filter(CarbonReductions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(CarbonReductions, key) and value is not None:
                    query = query.filter(getattr(CarbonReductions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(CarbonReductions, order_by, CarbonReductions.created_at)))
        else:
            query = query.order_by(getattr(CarbonReductions, order_by, CarbonReductions.created_at))
        items = query.offset(skip).limit(limit).all()
        return CarbonReductionsListResponse(
            items=[CarbonReductionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: CarbonReductionsUpdate,
               updated_by: Optional[UUID] = None) -> CarbonReductionsResponse:
        """Update an existing CarbonReductions record."""
        db_obj = self.db.query(CarbonReductions).filter(CarbonReductions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonReductions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonReductionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a CarbonReductions record. Soft delete by default."""
        db_obj = self.db.query(CarbonReductions).filter(CarbonReductions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonReductions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[CarbonReductionsResponse]:
        """Search CarbonReductions by name/code fields."""
        query = self.db.query(CarbonReductions)
        if company_id and hasattr(CarbonReductions, "company_id"):
            query = query.filter(CarbonReductions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(CarbonReductions, f)]
        if search_fields:
            conditions = [getattr(CarbonReductions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [CarbonReductionsResponse.model_validate(i) for i in items]

class CarbonScenariosService:
    """CRUD service for CarbonScenarios."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: CarbonScenariosCreate, created_by: Optional[UUID] = None) -> CarbonScenariosResponse:
        """Create a new CarbonScenarios record."""
        db_obj = CarbonScenarios(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonScenariosResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[CarbonScenariosResponse]:
        """Get CarbonScenarios by primary key."""
        db_obj = self.db.query(CarbonScenarios).filter(CarbonScenarios.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonScenarios not found")
        return CarbonScenariosResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> CarbonScenariosListResponse:
        """List CarbonScenarios records with pagination and filtering."""
        query = self.db.query(CarbonScenarios)
        if company_id and hasattr(CarbonScenarios, "company_id"):
            query = query.filter(CarbonScenarios.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(CarbonScenarios, key) and value is not None:
                    query = query.filter(getattr(CarbonScenarios, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(CarbonScenarios, order_by, CarbonScenarios.created_at)))
        else:
            query = query.order_by(getattr(CarbonScenarios, order_by, CarbonScenarios.created_at))
        items = query.offset(skip).limit(limit).all()
        return CarbonScenariosListResponse(
            items=[CarbonScenariosResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: CarbonScenariosUpdate,
               updated_by: Optional[UUID] = None) -> CarbonScenariosResponse:
        """Update an existing CarbonScenarios record."""
        db_obj = self.db.query(CarbonScenarios).filter(CarbonScenarios.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonScenarios not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return CarbonScenariosResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a CarbonScenarios record. Soft delete by default."""
        db_obj = self.db.query(CarbonScenarios).filter(CarbonScenarios.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="CarbonScenarios not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[CarbonScenariosResponse]:
        """Search CarbonScenarios by name/code fields."""
        query = self.db.query(CarbonScenarios)
        if company_id and hasattr(CarbonScenarios, "company_id"):
            query = query.filter(CarbonScenarios.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(CarbonScenarios, f)]
        if search_fields:
            conditions = [getattr(CarbonScenarios, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [CarbonScenariosResponse.model_validate(i) for i in items]

class ComplianceAlertsService:
    """CRUD service for ComplianceAlerts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ComplianceAlertsCreate, created_by: Optional[UUID] = None) -> ComplianceAlertsResponse:
        """Create a new ComplianceAlerts record."""
        db_obj = ComplianceAlerts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceAlertsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ComplianceAlertsResponse]:
        """Get ComplianceAlerts by primary key."""
        db_obj = self.db.query(ComplianceAlerts).filter(ComplianceAlerts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceAlerts not found")
        return ComplianceAlertsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ComplianceAlertsListResponse:
        """List ComplianceAlerts records with pagination and filtering."""
        query = self.db.query(ComplianceAlerts)
        if company_id and hasattr(ComplianceAlerts, "company_id"):
            query = query.filter(ComplianceAlerts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ComplianceAlerts, key) and value is not None:
                    query = query.filter(getattr(ComplianceAlerts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ComplianceAlerts, order_by, ComplianceAlerts.created_at)))
        else:
            query = query.order_by(getattr(ComplianceAlerts, order_by, ComplianceAlerts.created_at))
        items = query.offset(skip).limit(limit).all()
        return ComplianceAlertsListResponse(
            items=[ComplianceAlertsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ComplianceAlertsUpdate,
               updated_by: Optional[UUID] = None) -> ComplianceAlertsResponse:
        """Update an existing ComplianceAlerts record."""
        db_obj = self.db.query(ComplianceAlerts).filter(ComplianceAlerts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceAlerts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceAlertsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ComplianceAlerts record. Soft delete by default."""
        db_obj = self.db.query(ComplianceAlerts).filter(ComplianceAlerts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceAlerts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ComplianceAlertsResponse]:
        """Search ComplianceAlerts by name/code fields."""
        query = self.db.query(ComplianceAlerts)
        if company_id and hasattr(ComplianceAlerts, "company_id"):
            query = query.filter(ComplianceAlerts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ComplianceAlerts, f)]
        if search_fields:
            conditions = [getattr(ComplianceAlerts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ComplianceAlertsResponse.model_validate(i) for i in items]

class ComplianceAssessmentsService:
    """CRUD service for ComplianceAssessments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ComplianceAssessmentsCreate, created_by: Optional[UUID] = None) -> ComplianceAssessmentsResponse:
        """Create a new ComplianceAssessments record."""
        db_obj = ComplianceAssessments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceAssessmentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ComplianceAssessmentsResponse]:
        """Get ComplianceAssessments by primary key."""
        db_obj = self.db.query(ComplianceAssessments).filter(ComplianceAssessments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceAssessments not found")
        return ComplianceAssessmentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ComplianceAssessmentsListResponse:
        """List ComplianceAssessments records with pagination and filtering."""
        query = self.db.query(ComplianceAssessments)
        if company_id and hasattr(ComplianceAssessments, "company_id"):
            query = query.filter(ComplianceAssessments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ComplianceAssessments, key) and value is not None:
                    query = query.filter(getattr(ComplianceAssessments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ComplianceAssessments, order_by, ComplianceAssessments.created_at)))
        else:
            query = query.order_by(getattr(ComplianceAssessments, order_by, ComplianceAssessments.created_at))
        items = query.offset(skip).limit(limit).all()
        return ComplianceAssessmentsListResponse(
            items=[ComplianceAssessmentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ComplianceAssessmentsUpdate,
               updated_by: Optional[UUID] = None) -> ComplianceAssessmentsResponse:
        """Update an existing ComplianceAssessments record."""
        db_obj = self.db.query(ComplianceAssessments).filter(ComplianceAssessments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceAssessments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceAssessmentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ComplianceAssessments record. Soft delete by default."""
        db_obj = self.db.query(ComplianceAssessments).filter(ComplianceAssessments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceAssessments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ComplianceAssessmentsResponse]:
        """Search ComplianceAssessments by name/code fields."""
        query = self.db.query(ComplianceAssessments)
        if company_id and hasattr(ComplianceAssessments, "company_id"):
            query = query.filter(ComplianceAssessments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ComplianceAssessments, f)]
        if search_fields:
            conditions = [getattr(ComplianceAssessments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ComplianceAssessmentsResponse.model_validate(i) for i in items]

class ComplianceDocumentsService:
    """CRUD service for ComplianceDocuments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ComplianceDocumentsCreate, created_by: Optional[UUID] = None) -> ComplianceDocumentsResponse:
        """Create a new ComplianceDocuments record."""
        db_obj = ComplianceDocuments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceDocumentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ComplianceDocumentsResponse]:
        """Get ComplianceDocuments by primary key."""
        db_obj = self.db.query(ComplianceDocuments).filter(ComplianceDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceDocuments not found")
        return ComplianceDocumentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ComplianceDocumentsListResponse:
        """List ComplianceDocuments records with pagination and filtering."""
        query = self.db.query(ComplianceDocuments)
        if company_id and hasattr(ComplianceDocuments, "company_id"):
            query = query.filter(ComplianceDocuments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ComplianceDocuments, key) and value is not None:
                    query = query.filter(getattr(ComplianceDocuments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ComplianceDocuments, order_by, ComplianceDocuments.created_at)))
        else:
            query = query.order_by(getattr(ComplianceDocuments, order_by, ComplianceDocuments.created_at))
        items = query.offset(skip).limit(limit).all()
        return ComplianceDocumentsListResponse(
            items=[ComplianceDocumentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ComplianceDocumentsUpdate,
               updated_by: Optional[UUID] = None) -> ComplianceDocumentsResponse:
        """Update an existing ComplianceDocuments record."""
        db_obj = self.db.query(ComplianceDocuments).filter(ComplianceDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceDocuments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceDocumentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ComplianceDocuments record. Soft delete by default."""
        db_obj = self.db.query(ComplianceDocuments).filter(ComplianceDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceDocuments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ComplianceDocumentsResponse]:
        """Search ComplianceDocuments by name/code fields."""
        query = self.db.query(ComplianceDocuments)
        if company_id and hasattr(ComplianceDocuments, "company_id"):
            query = query.filter(ComplianceDocuments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ComplianceDocuments, f)]
        if search_fields:
            conditions = [getattr(ComplianceDocuments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ComplianceDocumentsResponse.model_validate(i) for i in items]

class ComplianceRulesService:
    """CRUD service for ComplianceRules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ComplianceRulesCreate, created_by: Optional[UUID] = None) -> ComplianceRulesResponse:
        """Create a new ComplianceRules record."""
        db_obj = ComplianceRules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceRulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ComplianceRulesResponse]:
        """Get ComplianceRules by primary key."""
        db_obj = self.db.query(ComplianceRules).filter(ComplianceRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceRules not found")
        return ComplianceRulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ComplianceRulesListResponse:
        """List ComplianceRules records with pagination and filtering."""
        query = self.db.query(ComplianceRules)
        if company_id and hasattr(ComplianceRules, "company_id"):
            query = query.filter(ComplianceRules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ComplianceRules, key) and value is not None:
                    query = query.filter(getattr(ComplianceRules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ComplianceRules, order_by, ComplianceRules.created_at)))
        else:
            query = query.order_by(getattr(ComplianceRules, order_by, ComplianceRules.created_at))
        items = query.offset(skip).limit(limit).all()
        return ComplianceRulesListResponse(
            items=[ComplianceRulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ComplianceRulesUpdate,
               updated_by: Optional[UUID] = None) -> ComplianceRulesResponse:
        """Update an existing ComplianceRules record."""
        db_obj = self.db.query(ComplianceRules).filter(ComplianceRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceRules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ComplianceRulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ComplianceRules record. Soft delete by default."""
        db_obj = self.db.query(ComplianceRules).filter(ComplianceRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ComplianceRules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ComplianceRulesResponse]:
        """Search ComplianceRules by name/code fields."""
        query = self.db.query(ComplianceRules)
        if company_id and hasattr(ComplianceRules, "company_id"):
            query = query.filter(ComplianceRules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ComplianceRules, f)]
        if search_fields:
            conditions = [getattr(ComplianceRules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ComplianceRulesResponse.model_validate(i) for i in items]

class EsgAuditTrailService:
    """CRUD service for EsgAuditTrail."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EsgAuditTrailCreate, created_by: Optional[UUID] = None) -> EsgAuditTrailResponse:
        """Create a new EsgAuditTrail record."""
        db_obj = EsgAuditTrail(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgAuditTrailResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EsgAuditTrailResponse]:
        """Get EsgAuditTrail by primary key."""
        db_obj = self.db.query(EsgAuditTrail).filter(EsgAuditTrail.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgAuditTrail not found")
        return EsgAuditTrailResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EsgAuditTrailListResponse:
        """List EsgAuditTrail records with pagination and filtering."""
        query = self.db.query(EsgAuditTrail)
        if company_id and hasattr(EsgAuditTrail, "company_id"):
            query = query.filter(EsgAuditTrail.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EsgAuditTrail, key) and value is not None:
                    query = query.filter(getattr(EsgAuditTrail, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EsgAuditTrail, order_by, EsgAuditTrail.created_at)))
        else:
            query = query.order_by(getattr(EsgAuditTrail, order_by, EsgAuditTrail.created_at))
        items = query.offset(skip).limit(limit).all()
        return EsgAuditTrailListResponse(
            items=[EsgAuditTrailResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EsgAuditTrailUpdate,
               updated_by: Optional[UUID] = None) -> EsgAuditTrailResponse:
        """Update an existing EsgAuditTrail record."""
        db_obj = self.db.query(EsgAuditTrail).filter(EsgAuditTrail.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgAuditTrail not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgAuditTrailResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EsgAuditTrail record. Soft delete by default."""
        db_obj = self.db.query(EsgAuditTrail).filter(EsgAuditTrail.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgAuditTrail not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EsgAuditTrailResponse]:
        """Search EsgAuditTrail by name/code fields."""
        query = self.db.query(EsgAuditTrail)
        if company_id and hasattr(EsgAuditTrail, "company_id"):
            query = query.filter(EsgAuditTrail.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EsgAuditTrail, f)]
        if search_fields:
            conditions = [getattr(EsgAuditTrail, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EsgAuditTrailResponse.model_validate(i) for i in items]

class EsgFrameworksService:
    """CRUD service for EsgFrameworks."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EsgFrameworksCreate, created_by: Optional[UUID] = None) -> EsgFrameworksResponse:
        """Create a new EsgFrameworks record."""
        db_obj = EsgFrameworks(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgFrameworksResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EsgFrameworksResponse]:
        """Get EsgFrameworks by primary key."""
        db_obj = self.db.query(EsgFrameworks).filter(EsgFrameworks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgFrameworks not found")
        return EsgFrameworksResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EsgFrameworksListResponse:
        """List EsgFrameworks records with pagination and filtering."""
        query = self.db.query(EsgFrameworks)
        if company_id and hasattr(EsgFrameworks, "company_id"):
            query = query.filter(EsgFrameworks.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EsgFrameworks, key) and value is not None:
                    query = query.filter(getattr(EsgFrameworks, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EsgFrameworks, order_by, EsgFrameworks.created_at)))
        else:
            query = query.order_by(getattr(EsgFrameworks, order_by, EsgFrameworks.created_at))
        items = query.offset(skip).limit(limit).all()
        return EsgFrameworksListResponse(
            items=[EsgFrameworksResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EsgFrameworksUpdate,
               updated_by: Optional[UUID] = None) -> EsgFrameworksResponse:
        """Update an existing EsgFrameworks record."""
        db_obj = self.db.query(EsgFrameworks).filter(EsgFrameworks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgFrameworks not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgFrameworksResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EsgFrameworks record. Soft delete by default."""
        db_obj = self.db.query(EsgFrameworks).filter(EsgFrameworks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgFrameworks not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EsgFrameworksResponse]:
        """Search EsgFrameworks by name/code fields."""
        query = self.db.query(EsgFrameworks)
        if company_id and hasattr(EsgFrameworks, "company_id"):
            query = query.filter(EsgFrameworks.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EsgFrameworks, f)]
        if search_fields:
            conditions = [getattr(EsgFrameworks, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EsgFrameworksResponse.model_validate(i) for i in items]

class EsgKpisService:
    """CRUD service for EsgKpis."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EsgKpisCreate, created_by: Optional[UUID] = None) -> EsgKpisResponse:
        """Create a new EsgKpis record."""
        db_obj = EsgKpis(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgKpisResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EsgKpisResponse]:
        """Get EsgKpis by primary key."""
        db_obj = self.db.query(EsgKpis).filter(EsgKpis.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgKpis not found")
        return EsgKpisResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EsgKpisListResponse:
        """List EsgKpis records with pagination and filtering."""
        query = self.db.query(EsgKpis)
        if company_id and hasattr(EsgKpis, "company_id"):
            query = query.filter(EsgKpis.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EsgKpis, key) and value is not None:
                    query = query.filter(getattr(EsgKpis, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EsgKpis, order_by, EsgKpis.created_at)))
        else:
            query = query.order_by(getattr(EsgKpis, order_by, EsgKpis.created_at))
        items = query.offset(skip).limit(limit).all()
        return EsgKpisListResponse(
            items=[EsgKpisResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EsgKpisUpdate,
               updated_by: Optional[UUID] = None) -> EsgKpisResponse:
        """Update an existing EsgKpis record."""
        db_obj = self.db.query(EsgKpis).filter(EsgKpis.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgKpis not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgKpisResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EsgKpis record. Soft delete by default."""
        db_obj = self.db.query(EsgKpis).filter(EsgKpis.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgKpis not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EsgKpisResponse]:
        """Search EsgKpis by name/code fields."""
        query = self.db.query(EsgKpis)
        if company_id and hasattr(EsgKpis, "company_id"):
            query = query.filter(EsgKpis.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EsgKpis, f)]
        if search_fields:
            conditions = [getattr(EsgKpis, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EsgKpisResponse.model_validate(i) for i in items]

class EsgMaterialityService:
    """CRUD service for EsgMateriality."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EsgMaterialityCreate, created_by: Optional[UUID] = None) -> EsgMaterialityResponse:
        """Create a new EsgMateriality record."""
        db_obj = EsgMateriality(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgMaterialityResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EsgMaterialityResponse]:
        """Get EsgMateriality by primary key."""
        db_obj = self.db.query(EsgMateriality).filter(EsgMateriality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgMateriality not found")
        return EsgMaterialityResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EsgMaterialityListResponse:
        """List EsgMateriality records with pagination and filtering."""
        query = self.db.query(EsgMateriality)
        if company_id and hasattr(EsgMateriality, "company_id"):
            query = query.filter(EsgMateriality.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EsgMateriality, key) and value is not None:
                    query = query.filter(getattr(EsgMateriality, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EsgMateriality, order_by, EsgMateriality.created_at)))
        else:
            query = query.order_by(getattr(EsgMateriality, order_by, EsgMateriality.created_at))
        items = query.offset(skip).limit(limit).all()
        return EsgMaterialityListResponse(
            items=[EsgMaterialityResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EsgMaterialityUpdate,
               updated_by: Optional[UUID] = None) -> EsgMaterialityResponse:
        """Update an existing EsgMateriality record."""
        db_obj = self.db.query(EsgMateriality).filter(EsgMateriality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgMateriality not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgMaterialityResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EsgMateriality record. Soft delete by default."""
        db_obj = self.db.query(EsgMateriality).filter(EsgMateriality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgMateriality not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EsgMaterialityResponse]:
        """Search EsgMateriality by name/code fields."""
        query = self.db.query(EsgMateriality)
        if company_id and hasattr(EsgMateriality, "company_id"):
            query = query.filter(EsgMateriality.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EsgMateriality, f)]
        if search_fields:
            conditions = [getattr(EsgMateriality, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EsgMaterialityResponse.model_validate(i) for i in items]

class EsgReportsService:
    """CRUD service for EsgReports."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EsgReportsCreate, created_by: Optional[UUID] = None) -> EsgReportsResponse:
        """Create a new EsgReports record."""
        db_obj = EsgReports(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgReportsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EsgReportsResponse]:
        """Get EsgReports by primary key."""
        db_obj = self.db.query(EsgReports).filter(EsgReports.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgReports not found")
        return EsgReportsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EsgReportsListResponse:
        """List EsgReports records with pagination and filtering."""
        query = self.db.query(EsgReports)
        if company_id and hasattr(EsgReports, "company_id"):
            query = query.filter(EsgReports.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EsgReports, key) and value is not None:
                    query = query.filter(getattr(EsgReports, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EsgReports, order_by, EsgReports.created_at)))
        else:
            query = query.order_by(getattr(EsgReports, order_by, EsgReports.created_at))
        items = query.offset(skip).limit(limit).all()
        return EsgReportsListResponse(
            items=[EsgReportsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EsgReportsUpdate,
               updated_by: Optional[UUID] = None) -> EsgReportsResponse:
        """Update an existing EsgReports record."""
        db_obj = self.db.query(EsgReports).filter(EsgReports.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgReports not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgReportsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EsgReports record. Soft delete by default."""
        db_obj = self.db.query(EsgReports).filter(EsgReports.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgReports not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EsgReportsResponse]:
        """Search EsgReports by name/code fields."""
        query = self.db.query(EsgReports)
        if company_id and hasattr(EsgReports, "company_id"):
            query = query.filter(EsgReports.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EsgReports, f)]
        if search_fields:
            conditions = [getattr(EsgReports, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EsgReportsResponse.model_validate(i) for i in items]

class EsgTargetsService:
    """CRUD service for EsgTargets."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EsgTargetsCreate, created_by: Optional[UUID] = None) -> EsgTargetsResponse:
        """Create a new EsgTargets record."""
        db_obj = EsgTargets(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgTargetsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EsgTargetsResponse]:
        """Get EsgTargets by primary key."""
        db_obj = self.db.query(EsgTargets).filter(EsgTargets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgTargets not found")
        return EsgTargetsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EsgTargetsListResponse:
        """List EsgTargets records with pagination and filtering."""
        query = self.db.query(EsgTargets)
        if company_id and hasattr(EsgTargets, "company_id"):
            query = query.filter(EsgTargets.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EsgTargets, key) and value is not None:
                    query = query.filter(getattr(EsgTargets, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EsgTargets, order_by, EsgTargets.created_at)))
        else:
            query = query.order_by(getattr(EsgTargets, order_by, EsgTargets.created_at))
        items = query.offset(skip).limit(limit).all()
        return EsgTargetsListResponse(
            items=[EsgTargetsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EsgTargetsUpdate,
               updated_by: Optional[UUID] = None) -> EsgTargetsResponse:
        """Update an existing EsgTargets record."""
        db_obj = self.db.query(EsgTargets).filter(EsgTargets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgTargets not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EsgTargetsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EsgTargets record. Soft delete by default."""
        db_obj = self.db.query(EsgTargets).filter(EsgTargets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EsgTargets not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EsgTargetsResponse]:
        """Search EsgTargets by name/code fields."""
        query = self.db.query(EsgTargets)
        if company_id and hasattr(EsgTargets, "company_id"):
            query = query.filter(EsgTargets.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EsgTargets, f)]
        if search_fields:
            conditions = [getattr(EsgTargets, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EsgTargetsResponse.model_validate(i) for i in items]
