"""
Sales Module — SQLAlchemy ORM Models
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:41.056160
One model class per table with full relationship support.
"""
from __future__ import annotations

from decimal import Decimal
from datetime import datetime, date, time
from typing import List, Optional
from uuid import UUID
import uuid

from sqlalchemy import (
    Column, Integer, BigInteger, SmallInteger,
    Numeric, Float, Boolean,
    String, Text, Unicode, UnicodeText,
    DateTime, Date, Time, DateTime, LargeBinary,
    ForeignKey, ForeignKeyConstraint, UniqueConstraint,
    Index, CheckConstraint, PrimaryKeyConstraint,
    TIMESTAMP, JSON, Enum, VARBINARY
)
from sqlalchemy.dialects.mssql import UNIQUEIDENTIFIER, MONEY, SMALLMONEY, NVARCHAR, NTEXT, IMAGE
from sqlalchemy.orm import relationship, backref, mapped_column, Mapped
from sqlalchemy.sql import func

from app.core.database_mssql import Base

# Module metadata
__module_code__ = "sales-ebuild"
__tables__ = 3

class SalesOrderLines(Base):
    """SalesOrderLines — 12 columns, 4 FKs."""
    __tablename__ = "sales_order_lines"

    # FK: item_id -> items.id
    # FK: so_id -> sales_orders.id
    # FK: tax_code_id -> tax_codes.id
    # FK: uom_id -> unit_of_measures.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    so_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    item_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    description: Mapped[str] = mapped_column(Text)
    quantity: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    delivered_qty: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    invoiced_qty: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    uom_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    unit_price: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    discount_pct: Mapped[Decimal] = mapped_column(Numeric(6, 4))
    tax_code_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    line_total: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)

    # item = relationship("Items", back_populates="sales_order_liness")
    # so = relationship("SalesOrders", back_populates="sales_order_liness")
    # tax_code = relationship("TaxCodes", back_populates="sales_order_liness")
    # uom = relationship("UnitOfMeasures", back_populates="sales_order_liness")

class SalesOrders(Base):
    """SalesOrders — 19 columns, 5 FKs."""
    __tablename__ = "sales_orders"

    # FK: company_id -> companies.id
    # FK: customer_id -> business_partners.id
    # FK: price_list_id -> price_lists.id
    # FK: salesperson_id -> users.id
    # FK: shipping_address_id -> partner_addresses.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    number: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    customer_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    order_date: Mapped[date] = mapped_column(Date, nullable=False)
    delivery_date: Mapped[date] = mapped_column(Date)
    price_list_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    currency: Mapped[str] = mapped_column(NVARCHAR(3), nullable=False)
    fx_rate: Mapped[Decimal] = mapped_column(Numeric(18, 6))
    subtotal: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    discount_amount: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    tax_amount: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    total_amount: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    shipping_address_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    salesperson_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    notes: Mapped[str] = mapped_column(Text)
    status: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="sales_orderss")
    # customer = relationship("BusinessPartners", back_populates="sales_orderss")
    # price_list = relationship("PriceLists", back_populates="sales_orderss")
    # salesperson = relationship("Users", back_populates="sales_orderss")
    # shipping_address = relationship("PartnerAddresses", back_populates="sales_orderss")

class Shipments(Base):
    """Shipments — 24 columns, 1 FKs."""
    __tablename__ = "shipments"

    # FK: company_id -> companies.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    shipment_number: Mapped[str] = mapped_column(NVARCHAR(64), nullable=False)
    transport_mode: Mapped[str] = mapped_column(NVARCHAR(16), nullable=False)
    carrier_name: Mapped[str] = mapped_column(NVARCHAR(255))
    carrier_reference: Mapped[str] = mapped_column(NVARCHAR(64))
    vessel_name: Mapped[str] = mapped_column(NVARCHAR(255))
    voyage_number: Mapped[str] = mapped_column(NVARCHAR(32))
    port_of_loading: Mapped[str] = mapped_column(NVARCHAR(255))
    port_of_discharge: Mapped[str] = mapped_column(NVARCHAR(255))
    final_destination: Mapped[str] = mapped_column(NVARCHAR(255))
    etd: Mapped[date] = mapped_column(Date)
    atd: Mapped[date] = mapped_column(Date)
    eta: Mapped[date] = mapped_column(Date)
    ata: Mapped[date] = mapped_column(Date)
    container_type: Mapped[str] = mapped_column(NVARCHAR(32))
    container_count: Mapped[int] = mapped_column(Integer)
    gross_weight_kg: Mapped[Decimal] = mapped_column(Numeric(12, 2))
    volume_cbm: Mapped[Decimal] = mapped_column(Numeric(12, 2))
    bill_of_lading_number: Mapped[str] = mapped_column(NVARCHAR(64))
    status: Mapped[str] = mapped_column(NVARCHAR(16))
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime)
    updated_at: Mapped[datetime] = mapped_column(DateTime)

    # company = relationship("Companies", back_populates="shipmentss")
