"""
Sales Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:41.066555
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .sales_models import *
from .sales_schemas import *

class SalesOrderLinesService:
    """CRUD service for SalesOrderLines."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SalesOrderLinesCreate, created_by: Optional[UUID] = None) -> SalesOrderLinesResponse:
        """Create a new SalesOrderLines record."""
        db_obj = SalesOrderLines(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SalesOrderLinesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SalesOrderLinesResponse]:
        """Get SalesOrderLines by primary key."""
        db_obj = self.db.query(SalesOrderLines).filter(SalesOrderLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalesOrderLines not found")
        return SalesOrderLinesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SalesOrderLinesListResponse:
        """List SalesOrderLines records with pagination and filtering."""
        query = self.db.query(SalesOrderLines)
        if company_id and hasattr(SalesOrderLines, "company_id"):
            query = query.filter(SalesOrderLines.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SalesOrderLines, key) and value is not None:
                    query = query.filter(getattr(SalesOrderLines, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SalesOrderLines, order_by, SalesOrderLines.created_at)))
        else:
            query = query.order_by(getattr(SalesOrderLines, order_by, SalesOrderLines.created_at))
        items = query.offset(skip).limit(limit).all()
        return SalesOrderLinesListResponse(
            items=[SalesOrderLinesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SalesOrderLinesUpdate,
               updated_by: Optional[UUID] = None) -> SalesOrderLinesResponse:
        """Update an existing SalesOrderLines record."""
        db_obj = self.db.query(SalesOrderLines).filter(SalesOrderLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalesOrderLines not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SalesOrderLinesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SalesOrderLines record. Soft delete by default."""
        db_obj = self.db.query(SalesOrderLines).filter(SalesOrderLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalesOrderLines not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SalesOrderLinesResponse]:
        """Search SalesOrderLines by name/code fields."""
        query = self.db.query(SalesOrderLines)
        if company_id and hasattr(SalesOrderLines, "company_id"):
            query = query.filter(SalesOrderLines.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SalesOrderLines, f)]
        if search_fields:
            conditions = [getattr(SalesOrderLines, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SalesOrderLinesResponse.model_validate(i) for i in items]

class SalesOrdersService:
    """CRUD service for SalesOrders."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SalesOrdersCreate, created_by: Optional[UUID] = None) -> SalesOrdersResponse:
        """Create a new SalesOrders record."""
        db_obj = SalesOrders(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SalesOrdersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SalesOrdersResponse]:
        """Get SalesOrders by primary key."""
        db_obj = self.db.query(SalesOrders).filter(SalesOrders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalesOrders not found")
        return SalesOrdersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SalesOrdersListResponse:
        """List SalesOrders records with pagination and filtering."""
        query = self.db.query(SalesOrders)
        if company_id and hasattr(SalesOrders, "company_id"):
            query = query.filter(SalesOrders.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SalesOrders, key) and value is not None:
                    query = query.filter(getattr(SalesOrders, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SalesOrders, order_by, SalesOrders.created_at)))
        else:
            query = query.order_by(getattr(SalesOrders, order_by, SalesOrders.created_at))
        items = query.offset(skip).limit(limit).all()
        return SalesOrdersListResponse(
            items=[SalesOrdersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SalesOrdersUpdate,
               updated_by: Optional[UUID] = None) -> SalesOrdersResponse:
        """Update an existing SalesOrders record."""
        db_obj = self.db.query(SalesOrders).filter(SalesOrders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalesOrders not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SalesOrdersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SalesOrders record. Soft delete by default."""
        db_obj = self.db.query(SalesOrders).filter(SalesOrders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalesOrders not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SalesOrdersResponse]:
        """Search SalesOrders by name/code fields."""
        query = self.db.query(SalesOrders)
        if company_id and hasattr(SalesOrders, "company_id"):
            query = query.filter(SalesOrders.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SalesOrders, f)]
        if search_fields:
            conditions = [getattr(SalesOrders, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SalesOrdersResponse.model_validate(i) for i in items]

class ShipmentsService:
    """CRUD service for Shipments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ShipmentsCreate, created_by: Optional[UUID] = None) -> ShipmentsResponse:
        """Create a new Shipments record."""
        db_obj = Shipments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ShipmentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ShipmentsResponse]:
        """Get Shipments by primary key."""
        db_obj = self.db.query(Shipments).filter(Shipments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Shipments not found")
        return ShipmentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ShipmentsListResponse:
        """List Shipments records with pagination and filtering."""
        query = self.db.query(Shipments)
        if company_id and hasattr(Shipments, "company_id"):
            query = query.filter(Shipments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Shipments, key) and value is not None:
                    query = query.filter(getattr(Shipments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Shipments, order_by, Shipments.created_at)))
        else:
            query = query.order_by(getattr(Shipments, order_by, Shipments.created_at))
        items = query.offset(skip).limit(limit).all()
        return ShipmentsListResponse(
            items=[ShipmentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ShipmentsUpdate,
               updated_by: Optional[UUID] = None) -> ShipmentsResponse:
        """Update an existing Shipments record."""
        db_obj = self.db.query(Shipments).filter(Shipments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Shipments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ShipmentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Shipments record. Soft delete by default."""
        db_obj = self.db.query(Shipments).filter(Shipments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Shipments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ShipmentsResponse]:
        """Search Shipments by name/code fields."""
        query = self.db.query(Shipments)
        if company_id and hasattr(Shipments, "company_id"):
            query = query.filter(Shipments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Shipments, f)]
        if search_fields:
            conditions = [getattr(Shipments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ShipmentsResponse.model_validate(i) for i in items]
