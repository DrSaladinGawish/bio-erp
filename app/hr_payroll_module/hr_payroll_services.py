"""
Hr Payroll Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.553046
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .hr_payroll_models import *
from .hr_payroll_schemas import *

class AttendanceService:
    """CRUD service for Attendance."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: AttendanceCreate, created_by: Optional[UUID] = None) -> AttendanceResponse:
        """Create a new Attendance record."""
        db_obj = Attendance(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return AttendanceResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[AttendanceResponse]:
        """Get Attendance by primary key."""
        db_obj = self.db.query(Attendance).filter(Attendance.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Attendance not found")
        return AttendanceResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> AttendanceListResponse:
        """List Attendance records with pagination and filtering."""
        query = self.db.query(Attendance)
        if company_id and hasattr(Attendance, "company_id"):
            query = query.filter(Attendance.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Attendance, key) and value is not None:
                    query = query.filter(getattr(Attendance, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Attendance, order_by, Attendance.created_at)))
        else:
            query = query.order_by(getattr(Attendance, order_by, Attendance.created_at))
        items = query.offset(skip).limit(limit).all()
        return AttendanceListResponse(
            items=[AttendanceResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: AttendanceUpdate,
               updated_by: Optional[UUID] = None) -> AttendanceResponse:
        """Update an existing Attendance record."""
        db_obj = self.db.query(Attendance).filter(Attendance.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Attendance not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return AttendanceResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Attendance record. Soft delete by default."""
        db_obj = self.db.query(Attendance).filter(Attendance.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Attendance not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[AttendanceResponse]:
        """Search Attendance by name/code fields."""
        query = self.db.query(Attendance)
        if company_id and hasattr(Attendance, "company_id"):
            query = query.filter(Attendance.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Attendance, f)]
        if search_fields:
            conditions = [getattr(Attendance, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [AttendanceResponse.model_validate(i) for i in items]

class EmployeeContractsService:
    """CRUD service for EmployeeContracts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EmployeeContractsCreate, created_by: Optional[UUID] = None) -> EmployeeContractsResponse:
        """Create a new EmployeeContracts record."""
        db_obj = EmployeeContracts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EmployeeContractsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EmployeeContractsResponse]:
        """Get EmployeeContracts by primary key."""
        db_obj = self.db.query(EmployeeContracts).filter(EmployeeContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EmployeeContracts not found")
        return EmployeeContractsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EmployeeContractsListResponse:
        """List EmployeeContracts records with pagination and filtering."""
        query = self.db.query(EmployeeContracts)
        if company_id and hasattr(EmployeeContracts, "company_id"):
            query = query.filter(EmployeeContracts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EmployeeContracts, key) and value is not None:
                    query = query.filter(getattr(EmployeeContracts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EmployeeContracts, order_by, EmployeeContracts.created_at)))
        else:
            query = query.order_by(getattr(EmployeeContracts, order_by, EmployeeContracts.created_at))
        items = query.offset(skip).limit(limit).all()
        return EmployeeContractsListResponse(
            items=[EmployeeContractsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EmployeeContractsUpdate,
               updated_by: Optional[UUID] = None) -> EmployeeContractsResponse:
        """Update an existing EmployeeContracts record."""
        db_obj = self.db.query(EmployeeContracts).filter(EmployeeContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EmployeeContracts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EmployeeContractsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EmployeeContracts record. Soft delete by default."""
        db_obj = self.db.query(EmployeeContracts).filter(EmployeeContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EmployeeContracts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EmployeeContractsResponse]:
        """Search EmployeeContracts by name/code fields."""
        query = self.db.query(EmployeeContracts)
        if company_id and hasattr(EmployeeContracts, "company_id"):
            query = query.filter(EmployeeContracts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EmployeeContracts, f)]
        if search_fields:
            conditions = [getattr(EmployeeContracts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EmployeeContractsResponse.model_validate(i) for i in items]

class EmployeesService:
    """CRUD service for Employees."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EmployeesCreate, created_by: Optional[UUID] = None) -> EmployeesResponse:
        """Create a new Employees record."""
        db_obj = Employees(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EmployeesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EmployeesResponse]:
        """Get Employees by primary key."""
        db_obj = self.db.query(Employees).filter(Employees.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Employees not found")
        return EmployeesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EmployeesListResponse:
        """List Employees records with pagination and filtering."""
        query = self.db.query(Employees)
        if company_id and hasattr(Employees, "company_id"):
            query = query.filter(Employees.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Employees, key) and value is not None:
                    query = query.filter(getattr(Employees, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Employees, order_by, Employees.created_at)))
        else:
            query = query.order_by(getattr(Employees, order_by, Employees.created_at))
        items = query.offset(skip).limit(limit).all()
        return EmployeesListResponse(
            items=[EmployeesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EmployeesUpdate,
               updated_by: Optional[UUID] = None) -> EmployeesResponse:
        """Update an existing Employees record."""
        db_obj = self.db.query(Employees).filter(Employees.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Employees not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EmployeesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Employees record. Soft delete by default."""
        db_obj = self.db.query(Employees).filter(Employees.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Employees not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EmployeesResponse]:
        """Search Employees by name/code fields."""
        query = self.db.query(Employees)
        if company_id and hasattr(Employees, "company_id"):
            query = query.filter(Employees.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Employees, f)]
        if search_fields:
            conditions = [getattr(Employees, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EmployeesResponse.model_validate(i) for i in items]

class LeaveRequestsService:
    """CRUD service for LeaveRequests."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: LeaveRequestsCreate, created_by: Optional[UUID] = None) -> LeaveRequestsResponse:
        """Create a new LeaveRequests record."""
        db_obj = LeaveRequests(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return LeaveRequestsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[LeaveRequestsResponse]:
        """Get LeaveRequests by primary key."""
        db_obj = self.db.query(LeaveRequests).filter(LeaveRequests.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LeaveRequests not found")
        return LeaveRequestsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> LeaveRequestsListResponse:
        """List LeaveRequests records with pagination and filtering."""
        query = self.db.query(LeaveRequests)
        if company_id and hasattr(LeaveRequests, "company_id"):
            query = query.filter(LeaveRequests.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(LeaveRequests, key) and value is not None:
                    query = query.filter(getattr(LeaveRequests, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(LeaveRequests, order_by, LeaveRequests.created_at)))
        else:
            query = query.order_by(getattr(LeaveRequests, order_by, LeaveRequests.created_at))
        items = query.offset(skip).limit(limit).all()
        return LeaveRequestsListResponse(
            items=[LeaveRequestsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: LeaveRequestsUpdate,
               updated_by: Optional[UUID] = None) -> LeaveRequestsResponse:
        """Update an existing LeaveRequests record."""
        db_obj = self.db.query(LeaveRequests).filter(LeaveRequests.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LeaveRequests not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return LeaveRequestsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a LeaveRequests record. Soft delete by default."""
        db_obj = self.db.query(LeaveRequests).filter(LeaveRequests.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LeaveRequests not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[LeaveRequestsResponse]:
        """Search LeaveRequests by name/code fields."""
        query = self.db.query(LeaveRequests)
        if company_id and hasattr(LeaveRequests, "company_id"):
            query = query.filter(LeaveRequests.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(LeaveRequests, f)]
        if search_fields:
            conditions = [getattr(LeaveRequests, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [LeaveRequestsResponse.model_validate(i) for i in items]

class LeaveTypesService:
    """CRUD service for LeaveTypes."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: LeaveTypesCreate, created_by: Optional[UUID] = None) -> LeaveTypesResponse:
        """Create a new LeaveTypes record."""
        db_obj = LeaveTypes(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return LeaveTypesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[LeaveTypesResponse]:
        """Get LeaveTypes by primary key."""
        db_obj = self.db.query(LeaveTypes).filter(LeaveTypes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LeaveTypes not found")
        return LeaveTypesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> LeaveTypesListResponse:
        """List LeaveTypes records with pagination and filtering."""
        query = self.db.query(LeaveTypes)
        if company_id and hasattr(LeaveTypes, "company_id"):
            query = query.filter(LeaveTypes.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(LeaveTypes, key) and value is not None:
                    query = query.filter(getattr(LeaveTypes, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(LeaveTypes, order_by, LeaveTypes.created_at)))
        else:
            query = query.order_by(getattr(LeaveTypes, order_by, LeaveTypes.created_at))
        items = query.offset(skip).limit(limit).all()
        return LeaveTypesListResponse(
            items=[LeaveTypesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: LeaveTypesUpdate,
               updated_by: Optional[UUID] = None) -> LeaveTypesResponse:
        """Update an existing LeaveTypes record."""
        db_obj = self.db.query(LeaveTypes).filter(LeaveTypes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LeaveTypes not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return LeaveTypesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a LeaveTypes record. Soft delete by default."""
        db_obj = self.db.query(LeaveTypes).filter(LeaveTypes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LeaveTypes not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[LeaveTypesResponse]:
        """Search LeaveTypes by name/code fields."""
        query = self.db.query(LeaveTypes)
        if company_id and hasattr(LeaveTypes, "company_id"):
            query = query.filter(LeaveTypes.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(LeaveTypes, f)]
        if search_fields:
            conditions = [getattr(LeaveTypes, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [LeaveTypesResponse.model_validate(i) for i in items]

class PayrollRunsService:
    """CRUD service for PayrollRuns."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PayrollRunsCreate, created_by: Optional[UUID] = None) -> PayrollRunsResponse:
        """Create a new PayrollRuns record."""
        db_obj = PayrollRuns(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PayrollRunsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PayrollRunsResponse]:
        """Get PayrollRuns by primary key."""
        db_obj = self.db.query(PayrollRuns).filter(PayrollRuns.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PayrollRuns not found")
        return PayrollRunsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PayrollRunsListResponse:
        """List PayrollRuns records with pagination and filtering."""
        query = self.db.query(PayrollRuns)
        if company_id and hasattr(PayrollRuns, "company_id"):
            query = query.filter(PayrollRuns.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PayrollRuns, key) and value is not None:
                    query = query.filter(getattr(PayrollRuns, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PayrollRuns, order_by, PayrollRuns.created_at)))
        else:
            query = query.order_by(getattr(PayrollRuns, order_by, PayrollRuns.created_at))
        items = query.offset(skip).limit(limit).all()
        return PayrollRunsListResponse(
            items=[PayrollRunsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PayrollRunsUpdate,
               updated_by: Optional[UUID] = None) -> PayrollRunsResponse:
        """Update an existing PayrollRuns record."""
        db_obj = self.db.query(PayrollRuns).filter(PayrollRuns.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PayrollRuns not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PayrollRunsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PayrollRuns record. Soft delete by default."""
        db_obj = self.db.query(PayrollRuns).filter(PayrollRuns.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PayrollRuns not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PayrollRunsResponse]:
        """Search PayrollRuns by name/code fields."""
        query = self.db.query(PayrollRuns)
        if company_id and hasattr(PayrollRuns, "company_id"):
            query = query.filter(PayrollRuns.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PayrollRuns, f)]
        if search_fields:
            conditions = [getattr(PayrollRuns, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PayrollRunsResponse.model_validate(i) for i in items]

class PayslipsService:
    """CRUD service for Payslips."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PayslipsCreate, created_by: Optional[UUID] = None) -> PayslipsResponse:
        """Create a new Payslips record."""
        db_obj = Payslips(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PayslipsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PayslipsResponse]:
        """Get Payslips by primary key."""
        db_obj = self.db.query(Payslips).filter(Payslips.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Payslips not found")
        return PayslipsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PayslipsListResponse:
        """List Payslips records with pagination and filtering."""
        query = self.db.query(Payslips)
        if company_id and hasattr(Payslips, "company_id"):
            query = query.filter(Payslips.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Payslips, key) and value is not None:
                    query = query.filter(getattr(Payslips, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Payslips, order_by, Payslips.created_at)))
        else:
            query = query.order_by(getattr(Payslips, order_by, Payslips.created_at))
        items = query.offset(skip).limit(limit).all()
        return PayslipsListResponse(
            items=[PayslipsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PayslipsUpdate,
               updated_by: Optional[UUID] = None) -> PayslipsResponse:
        """Update an existing Payslips record."""
        db_obj = self.db.query(Payslips).filter(Payslips.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Payslips not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PayslipsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Payslips record. Soft delete by default."""
        db_obj = self.db.query(Payslips).filter(Payslips.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Payslips not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PayslipsResponse]:
        """Search Payslips by name/code fields."""
        query = self.db.query(Payslips)
        if company_id and hasattr(Payslips, "company_id"):
            query = query.filter(Payslips.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Payslips, f)]
        if search_fields:
            conditions = [getattr(Payslips, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PayslipsResponse.model_validate(i) for i in items]

class SalaryStructuresService:
    """CRUD service for SalaryStructures."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SalaryStructuresCreate, created_by: Optional[UUID] = None) -> SalaryStructuresResponse:
        """Create a new SalaryStructures record."""
        db_obj = SalaryStructures(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SalaryStructuresResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SalaryStructuresResponse]:
        """Get SalaryStructures by primary key."""
        db_obj = self.db.query(SalaryStructures).filter(SalaryStructures.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalaryStructures not found")
        return SalaryStructuresResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SalaryStructuresListResponse:
        """List SalaryStructures records with pagination and filtering."""
        query = self.db.query(SalaryStructures)
        if company_id and hasattr(SalaryStructures, "company_id"):
            query = query.filter(SalaryStructures.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SalaryStructures, key) and value is not None:
                    query = query.filter(getattr(SalaryStructures, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SalaryStructures, order_by, SalaryStructures.created_at)))
        else:
            query = query.order_by(getattr(SalaryStructures, order_by, SalaryStructures.created_at))
        items = query.offset(skip).limit(limit).all()
        return SalaryStructuresListResponse(
            items=[SalaryStructuresResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SalaryStructuresUpdate,
               updated_by: Optional[UUID] = None) -> SalaryStructuresResponse:
        """Update an existing SalaryStructures record."""
        db_obj = self.db.query(SalaryStructures).filter(SalaryStructures.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalaryStructures not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SalaryStructuresResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SalaryStructures record. Soft delete by default."""
        db_obj = self.db.query(SalaryStructures).filter(SalaryStructures.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SalaryStructures not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SalaryStructuresResponse]:
        """Search SalaryStructures by name/code fields."""
        query = self.db.query(SalaryStructures)
        if company_id and hasattr(SalaryStructures, "company_id"):
            query = query.filter(SalaryStructures.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SalaryStructures, f)]
        if search_fields:
            conditions = [getattr(SalaryStructures, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SalaryStructuresResponse.model_validate(i) for i in items]

class TimesheetsService:
    """CRUD service for Timesheets."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TimesheetsCreate, created_by: Optional[UUID] = None) -> TimesheetsResponse:
        """Create a new Timesheets record."""
        db_obj = Timesheets(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TimesheetsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TimesheetsResponse]:
        """Get Timesheets by primary key."""
        db_obj = self.db.query(Timesheets).filter(Timesheets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Timesheets not found")
        return TimesheetsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TimesheetsListResponse:
        """List Timesheets records with pagination and filtering."""
        query = self.db.query(Timesheets)
        if company_id and hasattr(Timesheets, "company_id"):
            query = query.filter(Timesheets.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Timesheets, key) and value is not None:
                    query = query.filter(getattr(Timesheets, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Timesheets, order_by, Timesheets.created_at)))
        else:
            query = query.order_by(getattr(Timesheets, order_by, Timesheets.created_at))
        items = query.offset(skip).limit(limit).all()
        return TimesheetsListResponse(
            items=[TimesheetsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TimesheetsUpdate,
               updated_by: Optional[UUID] = None) -> TimesheetsResponse:
        """Update an existing Timesheets record."""
        db_obj = self.db.query(Timesheets).filter(Timesheets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Timesheets not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TimesheetsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Timesheets record. Soft delete by default."""
        db_obj = self.db.query(Timesheets).filter(Timesheets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Timesheets not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TimesheetsResponse]:
        """Search Timesheets by name/code fields."""
        query = self.db.query(Timesheets)
        if company_id and hasattr(Timesheets, "company_id"):
            query = query.filter(Timesheets.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Timesheets, f)]
        if search_fields:
            conditions = [getattr(Timesheets, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TimesheetsResponse.model_validate(i) for i in items]
