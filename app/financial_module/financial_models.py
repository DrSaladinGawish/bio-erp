"""
Financial Module — SQLAlchemy ORM Models
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.484924
One model class per table with full relationship support.
"""
from __future__ import annotations

from decimal import Decimal
from datetime import datetime, date, time
from typing import List, Optional
from uuid import UUID
import uuid

from sqlalchemy import (
    Column, Integer, BigInteger, SmallInteger,
    Numeric, Float, Boolean,
    String, Text, Unicode, UnicodeText,
    DateTime, Date, Time, DateTime, LargeBinary,
    ForeignKey, ForeignKeyConstraint, UniqueConstraint,
    Index, CheckConstraint, PrimaryKeyConstraint,
    TIMESTAMP, JSON, Enum, VARBINARY
)
from sqlalchemy.dialects.mssql import UNIQUEIDENTIFIER, MONEY, SMALLMONEY, NVARCHAR, NTEXT, IMAGE
from sqlalchemy.orm import relationship, backref, mapped_column, Mapped
from sqlalchemy.sql import func

from app.core.database_mssql import Base

# Module metadata
__module_code__ = "financial-ebuild"
__tables__ = 6

class Accounts(Base):
    """Accounts — 16 columns, 2 FKs."""
    __tablename__ = "accounts"

    # FK: company_id -> companies.id
    # FK: parent_id -> accounts.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    code: Mapped[str] = mapped_column(NVARCHAR(100), nullable=False)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    name_ar: Mapped[str] = mapped_column(NVARCHAR(255))
    account_type: Mapped[str] = mapped_column(NVARCHAR(50), nullable=False)
    normal_balance: Mapped[str] = mapped_column(NVARCHAR(20), nullable=False)
    parent_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    is_control: Mapped[bool] = mapped_column(Boolean)
    is_bank_account: Mapped[bool] = mapped_column(Boolean)
    is_active: Mapped[bool] = mapped_column(Boolean)
    currency: Mapped[str] = mapped_column(NVARCHAR(3))
    sort_code: Mapped[str] = mapped_column(NVARCHAR(100))
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="accountss")
    # parent = relationship("Accounts", back_populates="accountss")

class Currencies(Base):
    """Currencies — 6 columns, 0 FKs."""
    __tablename__ = "currencies"

    code: Mapped[str] = mapped_column(NVARCHAR(3), primary_key=True, nullable=False)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    name_ar: Mapped[str] = mapped_column(NVARCHAR(255))
    symbol: Mapped[str] = mapped_column(NVARCHAR(10))
    decimal_places: Mapped[int] = mapped_column(Integer, nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean)


class ExchangeRates(Base):
    """ExchangeRates — 8 columns, 3 FKs."""
    __tablename__ = "exchange_rates"

    # FK: company_id -> companies.id
    # FK: from_currency -> currencies.code
    # FK: to_currency -> currencies.code

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    from_currency: Mapped[str] = mapped_column(NVARCHAR(3), nullable=False)
    to_currency: Mapped[str] = mapped_column(NVARCHAR(3), nullable=False)
    rate: Mapped[Decimal] = mapped_column(Numeric(18, 6), nullable=False)
    effective_date: Mapped[date] = mapped_column(Date, nullable=False)
    source: Mapped[str] = mapped_column(NVARCHAR(50))
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="exchange_ratess")
    # from_currency = relationship("Currencies", back_populates="exchange_ratess")
    # to_currency = relationship("Currencies", back_populates="exchange_ratess")

class FiscalPeriods(Base):
    """FiscalPeriods — 14 columns, 2 FKs."""
    __tablename__ = "fiscal_periods"

    # FK: closed_by -> users.id
    # FK: company_id -> companies.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    fiscal_year: Mapped[int] = mapped_column(Integer, nullable=False)
    period_number: Mapped[int] = mapped_column(Integer, nullable=False)
    name: Mapped[str] = mapped_column(NVARCHAR(255), nullable=False)
    start_date: Mapped[date] = mapped_column(Date, nullable=False)
    end_date: Mapped[date] = mapped_column(Date, nullable=False)
    is_closed: Mapped[bool] = mapped_column(Boolean, nullable=False)
    is_locked: Mapped[bool] = mapped_column(Boolean, nullable=False)
    is_year_end: Mapped[bool] = mapped_column(Boolean, nullable=False)
    closed_by: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    closed_at: Mapped[datetime] = mapped_column(DateTime)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # closed_by = relationship("Users", back_populates="fiscal_periodss")
    # company = relationship("Companies", back_populates="fiscal_periodss")

class JournalEntries(Base):
    """JournalEntries — 22 columns, 5 FKs."""
    __tablename__ = "journal_entries"

    # FK: company_id -> companies.id
    # FK: period_id -> fiscal_periods.id
    # FK: posted_by -> users.id
    # FK: reversed_by -> users.id
    # FK: reversal_of_id -> journal_entries.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    company_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    period_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    journal_number: Mapped[str] = mapped_column(NVARCHAR(100), nullable=False)
    entry_date: Mapped[date] = mapped_column(Date, nullable=False)
    reference: Mapped[str] = mapped_column(Text)
    description: Mapped[str] = mapped_column(Text)
    description_ar: Mapped[str] = mapped_column(Text)
    source: Mapped[str] = mapped_column(NVARCHAR(100))
    source_doc_type: Mapped[str] = mapped_column(NVARCHAR(50))
    source_doc_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    total_debit: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    total_credit: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    is_balanced: Mapped[bool] = mapped_column(Boolean)
    status: Mapped[str] = mapped_column(NVARCHAR(50), nullable=False)
    posted_by: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    posted_at: Mapped[datetime] = mapped_column(DateTime)
    reversed_by: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    reversed_at: Mapped[datetime] = mapped_column(DateTime)
    reversal_of_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    metadata_: Mapped[str] = mapped_column("metadata", Text)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False)

    # company = relationship("Companies", back_populates="journal_entriess")
    # period = relationship("FiscalPeriods", back_populates="journal_entriess")
    # posted_by = relationship("Users", back_populates="journal_entriess")
    # reversed_by = relationship("Users", back_populates="journal_entriess")
    # reversal_of = relationship("JournalEntries", back_populates="journal_entriess")

class JournalLines(Base):
    """JournalLines — 15 columns, 2 FKs."""
    __tablename__ = "journal_lines"

    # FK: account_id -> accounts.id
    # FK: journal_entry_id -> journal_entries.id

    id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, primary_key=True, nullable=False)
    journal_entry_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    account_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, nullable=False, default=uuid.uuid4)
    line_number: Mapped[int] = mapped_column(Integer, nullable=False)
    description: Mapped[str] = mapped_column(Text)
    debit: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    credit: Mapped[Decimal] = mapped_column(Numeric(18, 4), nullable=False)
    currency: Mapped[str] = mapped_column(NVARCHAR(3), nullable=False)
    fx_rate: Mapped[Decimal] = mapped_column(Numeric(18, 6))
    base_debit: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    base_credit: Mapped[Decimal] = mapped_column(Numeric(18, 4))
    cost_centre_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    project_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    partner_id: Mapped[UUID] = mapped_column(UNIQUEIDENTIFIER, default=uuid.uuid4)
    metadata_: Mapped[str] = mapped_column("metadata", Text)

    # account = relationship("Accounts", back_populates="journal_liness")
    # journal_entry = relationship("JournalEntries", back_populates="journal_liness")
