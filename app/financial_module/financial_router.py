"""
Financial Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.497609
Mounted at /api/v1/fin
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .financial_schemas import *
from .financial_services import *

router = APIRouter(prefix="/fin", tags=["Financial"])

@router.get("/accountss", response_model=AccountsListResponse)
def list_accountss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Accounts records."""
    service = AccountsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/accountss/{id}", response_model=AccountsResponse)
def get_accounts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Accounts by ID."""
    service = AccountsService(db)
    return service.get(id)

@router.post("/accountss", response_model=AccountsResponse, status_code=status.HTTP_201_CREATED)
def create_accounts(
    data: AccountsCreate,
    db: Session = Depends(get_db)
):
    """Create a new Accounts."""
    service = AccountsService(db)
    return service.create(data)

@router.put("/accountss/{id}", response_model=AccountsResponse)
def update_accounts(
    id: UUID,
    data: AccountsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Accounts."""
    service = AccountsService(db)
    return service.update(id, data)

@router.delete("/accountss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_accounts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Accounts (soft delete by default)."""
    service = AccountsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/accountss/search", response_model=List[AccountsResponse])
def search_accountss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Accounts by name/code/description."""
    service = AccountsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/currenciess", response_model=CurrenciesListResponse)
def list_currenciess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Currencies records."""
    service = CurrenciesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/currenciess/{code}", response_model=CurrenciesResponse)
def get_currencies(
    code: UUID,
    db: Session = Depends(get_db)
):
    """Get Currencies by ID."""
    service = CurrenciesService(db)
    return service.get(code)

@router.post("/currenciess", response_model=CurrenciesResponse, status_code=status.HTTP_201_CREATED)
def create_currencies(
    data: CurrenciesCreate,
    db: Session = Depends(get_db)
):
    """Create a new Currencies."""
    service = CurrenciesService(db)
    return service.create(data)

@router.put("/currenciess/{code}", response_model=CurrenciesResponse)
def update_currencies(
    code: UUID,
    data: CurrenciesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Currencies."""
    service = CurrenciesService(db)
    return service.update(code, data)

@router.delete("/currenciess/{code}", status_code=status.HTTP_204_NO_CONTENT)
def delete_currencies(
    code: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Currencies (soft delete by default)."""
    service = CurrenciesService(db)
    service.delete(code, hard_delete=hard_delete)
    return None

@router.get("/currenciess/search", response_model=List[CurrenciesResponse])
def search_currenciess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Currencies by name/code/description."""
    service = CurrenciesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/exchange_ratess", response_model=ExchangeRatesListResponse)
def list_exchange_ratess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ExchangeRates records."""
    service = ExchangeRatesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/exchange_ratess/{id}", response_model=ExchangeRatesResponse)
def get_exchange_rates(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ExchangeRates by ID."""
    service = ExchangeRatesService(db)
    return service.get(id)

@router.post("/exchange_ratess", response_model=ExchangeRatesResponse, status_code=status.HTTP_201_CREATED)
def create_exchange_rates(
    data: ExchangeRatesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ExchangeRates."""
    service = ExchangeRatesService(db)
    return service.create(data)

@router.put("/exchange_ratess/{id}", response_model=ExchangeRatesResponse)
def update_exchange_rates(
    id: UUID,
    data: ExchangeRatesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ExchangeRates."""
    service = ExchangeRatesService(db)
    return service.update(id, data)

@router.delete("/exchange_ratess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_exchange_rates(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ExchangeRates (soft delete by default)."""
    service = ExchangeRatesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/exchange_ratess/search", response_model=List[ExchangeRatesResponse])
def search_exchange_ratess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ExchangeRates by name/code/description."""
    service = ExchangeRatesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/fiscal_periodss", response_model=FiscalPeriodsListResponse)
def list_fiscal_periodss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all FiscalPeriods records."""
    service = FiscalPeriodsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/fiscal_periodss/{id}", response_model=FiscalPeriodsResponse)
def get_fiscal_periods(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get FiscalPeriods by ID."""
    service = FiscalPeriodsService(db)
    return service.get(id)

@router.post("/fiscal_periodss", response_model=FiscalPeriodsResponse, status_code=status.HTTP_201_CREATED)
def create_fiscal_periods(
    data: FiscalPeriodsCreate,
    db: Session = Depends(get_db)
):
    """Create a new FiscalPeriods."""
    service = FiscalPeriodsService(db)
    return service.create(data)

@router.put("/fiscal_periodss/{id}", response_model=FiscalPeriodsResponse)
def update_fiscal_periods(
    id: UUID,
    data: FiscalPeriodsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing FiscalPeriods."""
    service = FiscalPeriodsService(db)
    return service.update(id, data)

@router.delete("/fiscal_periodss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_fiscal_periods(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a FiscalPeriods (soft delete by default)."""
    service = FiscalPeriodsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/fiscal_periodss/search", response_model=List[FiscalPeriodsResponse])
def search_fiscal_periodss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search FiscalPeriods by name/code/description."""
    service = FiscalPeriodsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/journal_entriess", response_model=JournalEntriesListResponse)
def list_journal_entriess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all JournalEntries records."""
    service = JournalEntriesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/journal_entriess/{id}", response_model=JournalEntriesResponse)
def get_journal_entries(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get JournalEntries by ID."""
    service = JournalEntriesService(db)
    return service.get(id)

@router.post("/journal_entriess", response_model=JournalEntriesResponse, status_code=status.HTTP_201_CREATED)
def create_journal_entries(
    data: JournalEntriesCreate,
    db: Session = Depends(get_db)
):
    """Create a new JournalEntries."""
    service = JournalEntriesService(db)
    return service.create(data)

@router.put("/journal_entriess/{id}", response_model=JournalEntriesResponse)
def update_journal_entries(
    id: UUID,
    data: JournalEntriesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing JournalEntries."""
    service = JournalEntriesService(db)
    return service.update(id, data)

@router.delete("/journal_entriess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_journal_entries(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a JournalEntries (soft delete by default)."""
    service = JournalEntriesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/journal_entriess/search", response_model=List[JournalEntriesResponse])
def search_journal_entriess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search JournalEntries by name/code/description."""
    service = JournalEntriesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/journal_liness", response_model=JournalLinesListResponse)
def list_journal_liness(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all JournalLines records."""
    service = JournalLinesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/journal_liness/{id}", response_model=JournalLinesResponse)
def get_journal_lines(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get JournalLines by ID."""
    service = JournalLinesService(db)
    return service.get(id)

@router.post("/journal_liness", response_model=JournalLinesResponse, status_code=status.HTTP_201_CREATED)
def create_journal_lines(
    data: JournalLinesCreate,
    db: Session = Depends(get_db)
):
    """Create a new JournalLines."""
    service = JournalLinesService(db)
    return service.create(data)

@router.put("/journal_liness/{id}", response_model=JournalLinesResponse)
def update_journal_lines(
    id: UUID,
    data: JournalLinesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing JournalLines."""
    service = JournalLinesService(db)
    return service.update(id, data)

@router.delete("/journal_liness/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_journal_lines(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a JournalLines (soft delete by default)."""
    service = JournalLinesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/journal_liness/search", response_model=List[JournalLinesResponse])
def search_journal_liness(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search JournalLines by name/code/description."""
    service = JournalLinesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_financial_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get Financial dashboard summary."""
    return {
        "module": "financial",
        "company_id": str(company_id),
        "tables": 6,
        "metrics": {}
    }
