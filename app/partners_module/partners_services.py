"""
Partners Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.976767
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .partners_models import *
from .partners_schemas import *

class BusinessPartnersService:
    """CRUD service for BusinessPartners."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BusinessPartnersCreate, created_by: Optional[UUID] = None) -> BusinessPartnersResponse:
        """Create a new BusinessPartners record."""
        db_obj = BusinessPartners(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BusinessPartnersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BusinessPartnersResponse]:
        """Get BusinessPartners by primary key."""
        db_obj = self.db.query(BusinessPartners).filter(BusinessPartners.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BusinessPartners not found")
        return BusinessPartnersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BusinessPartnersListResponse:
        """List BusinessPartners records with pagination and filtering."""
        query = self.db.query(BusinessPartners)
        if company_id and hasattr(BusinessPartners, "company_id"):
            query = query.filter(BusinessPartners.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BusinessPartners, key) and value is not None:
                    query = query.filter(getattr(BusinessPartners, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BusinessPartners, order_by, BusinessPartners.created_at)))
        else:
            query = query.order_by(getattr(BusinessPartners, order_by, BusinessPartners.created_at))
        items = query.offset(skip).limit(limit).all()
        return BusinessPartnersListResponse(
            items=[BusinessPartnersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BusinessPartnersUpdate,
               updated_by: Optional[UUID] = None) -> BusinessPartnersResponse:
        """Update an existing BusinessPartners record."""
        db_obj = self.db.query(BusinessPartners).filter(BusinessPartners.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BusinessPartners not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BusinessPartnersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BusinessPartners record. Soft delete by default."""
        db_obj = self.db.query(BusinessPartners).filter(BusinessPartners.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BusinessPartners not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BusinessPartnersResponse]:
        """Search BusinessPartners by name/code fields."""
        query = self.db.query(BusinessPartners)
        if company_id and hasattr(BusinessPartners, "company_id"):
            query = query.filter(BusinessPartners.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BusinessPartners, f)]
        if search_fields:
            conditions = [getattr(BusinessPartners, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BusinessPartnersResponse.model_validate(i) for i in items]

class PartnerAddressesService:
    """CRUD service for PartnerAddresses."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PartnerAddressesCreate, created_by: Optional[UUID] = None) -> PartnerAddressesResponse:
        """Create a new PartnerAddresses record."""
        db_obj = PartnerAddresses(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PartnerAddressesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PartnerAddressesResponse]:
        """Get PartnerAddresses by primary key."""
        db_obj = self.db.query(PartnerAddresses).filter(PartnerAddresses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerAddresses not found")
        return PartnerAddressesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PartnerAddressesListResponse:
        """List PartnerAddresses records with pagination and filtering."""
        query = self.db.query(PartnerAddresses)
        if company_id and hasattr(PartnerAddresses, "company_id"):
            query = query.filter(PartnerAddresses.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PartnerAddresses, key) and value is not None:
                    query = query.filter(getattr(PartnerAddresses, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PartnerAddresses, order_by, PartnerAddresses.created_at)))
        else:
            query = query.order_by(getattr(PartnerAddresses, order_by, PartnerAddresses.created_at))
        items = query.offset(skip).limit(limit).all()
        return PartnerAddressesListResponse(
            items=[PartnerAddressesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PartnerAddressesUpdate,
               updated_by: Optional[UUID] = None) -> PartnerAddressesResponse:
        """Update an existing PartnerAddresses record."""
        db_obj = self.db.query(PartnerAddresses).filter(PartnerAddresses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerAddresses not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PartnerAddressesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PartnerAddresses record. Soft delete by default."""
        db_obj = self.db.query(PartnerAddresses).filter(PartnerAddresses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerAddresses not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PartnerAddressesResponse]:
        """Search PartnerAddresses by name/code fields."""
        query = self.db.query(PartnerAddresses)
        if company_id and hasattr(PartnerAddresses, "company_id"):
            query = query.filter(PartnerAddresses.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PartnerAddresses, f)]
        if search_fields:
            conditions = [getattr(PartnerAddresses, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PartnerAddressesResponse.model_validate(i) for i in items]

class PartnerBankAccountsService:
    """CRUD service for PartnerBankAccounts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PartnerBankAccountsCreate, created_by: Optional[UUID] = None) -> PartnerBankAccountsResponse:
        """Create a new PartnerBankAccounts record."""
        db_obj = PartnerBankAccounts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PartnerBankAccountsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PartnerBankAccountsResponse]:
        """Get PartnerBankAccounts by primary key."""
        db_obj = self.db.query(PartnerBankAccounts).filter(PartnerBankAccounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerBankAccounts not found")
        return PartnerBankAccountsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PartnerBankAccountsListResponse:
        """List PartnerBankAccounts records with pagination and filtering."""
        query = self.db.query(PartnerBankAccounts)
        if company_id and hasattr(PartnerBankAccounts, "company_id"):
            query = query.filter(PartnerBankAccounts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PartnerBankAccounts, key) and value is not None:
                    query = query.filter(getattr(PartnerBankAccounts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PartnerBankAccounts, order_by, PartnerBankAccounts.created_at)))
        else:
            query = query.order_by(getattr(PartnerBankAccounts, order_by, PartnerBankAccounts.created_at))
        items = query.offset(skip).limit(limit).all()
        return PartnerBankAccountsListResponse(
            items=[PartnerBankAccountsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PartnerBankAccountsUpdate,
               updated_by: Optional[UUID] = None) -> PartnerBankAccountsResponse:
        """Update an existing PartnerBankAccounts record."""
        db_obj = self.db.query(PartnerBankAccounts).filter(PartnerBankAccounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerBankAccounts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PartnerBankAccountsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PartnerBankAccounts record. Soft delete by default."""
        db_obj = self.db.query(PartnerBankAccounts).filter(PartnerBankAccounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerBankAccounts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PartnerBankAccountsResponse]:
        """Search PartnerBankAccounts by name/code fields."""
        query = self.db.query(PartnerBankAccounts)
        if company_id and hasattr(PartnerBankAccounts, "company_id"):
            query = query.filter(PartnerBankAccounts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PartnerBankAccounts, f)]
        if search_fields:
            conditions = [getattr(PartnerBankAccounts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PartnerBankAccountsResponse.model_validate(i) for i in items]

class PartnerContactsService:
    """CRUD service for PartnerContacts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PartnerContactsCreate, created_by: Optional[UUID] = None) -> PartnerContactsResponse:
        """Create a new PartnerContacts record."""
        db_obj = PartnerContacts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PartnerContactsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PartnerContactsResponse]:
        """Get PartnerContacts by primary key."""
        db_obj = self.db.query(PartnerContacts).filter(PartnerContacts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerContacts not found")
        return PartnerContactsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PartnerContactsListResponse:
        """List PartnerContacts records with pagination and filtering."""
        query = self.db.query(PartnerContacts)
        if company_id and hasattr(PartnerContacts, "company_id"):
            query = query.filter(PartnerContacts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PartnerContacts, key) and value is not None:
                    query = query.filter(getattr(PartnerContacts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PartnerContacts, order_by, PartnerContacts.created_at)))
        else:
            query = query.order_by(getattr(PartnerContacts, order_by, PartnerContacts.created_at))
        items = query.offset(skip).limit(limit).all()
        return PartnerContactsListResponse(
            items=[PartnerContactsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PartnerContactsUpdate,
               updated_by: Optional[UUID] = None) -> PartnerContactsResponse:
        """Update an existing PartnerContacts record."""
        db_obj = self.db.query(PartnerContacts).filter(PartnerContacts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerContacts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PartnerContactsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PartnerContacts record. Soft delete by default."""
        db_obj = self.db.query(PartnerContacts).filter(PartnerContacts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PartnerContacts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PartnerContactsResponse]:
        """Search PartnerContacts by name/code fields."""
        query = self.db.query(PartnerContacts)
        if company_id and hasattr(PartnerContacts, "company_id"):
            query = query.filter(PartnerContacts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PartnerContacts, f)]
        if search_fields:
            conditions = [getattr(PartnerContacts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PartnerContactsResponse.model_validate(i) for i in items]
