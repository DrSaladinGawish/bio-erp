{
  "form_name": "vendor",
  "title": "Vendor",
  "validations": {
    "VendorName": { "required": true },
    "TaxID": { "regex": "^\\d{9}$", "message": "Tax ID must be 9 digits" }
  },
  "autosave": { "enabled": true, "interval_seconds": 30 },
  "audit": { "enabled": true }
}
