"""
Items Module — FastAPI Sub-App
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.643747
Mounted at /api/v1/ite in Bio-ERP main.py
"""
from __future__ import annotations

from fastapi import FastAPI

from .items_router import router as items_router

def create_items_app() -> FastAPI:
    """Factory function to create the items sub-application."""
    app = FastAPI(
        title="Items Module",
        description="ERP Items module — 6 tables",
        version="3.0.0",
        docs_url="/docs",
        redoc_url="/redoc",
        openapi_url="/openapi.json"
    )
    app.include_router(items_router)

    @app.get("/health")
    async def health_check():
        return {
            "status": "healthy",
            "module": "items",
            "version": "3.0.0",
            "tables": 6,
            "database": "SQL Server 2022"
        }

    @app.get("/")
    async def root():
        return {
            "module": "items",
            "version": "3.0.0",
            "endpoints": "/docs"
        }

    return app
