"""
Banking Treasury Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.391425
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .banking_treasury_models import *
from .banking_treasury_schemas import *

class BankAccountBalancesService:
    """CRUD service for BankAccountBalances."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BankAccountBalancesCreate, created_by: Optional[UUID] = None) -> BankAccountBalancesResponse:
        """Create a new BankAccountBalances record."""
        db_obj = BankAccountBalances(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BankAccountBalancesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BankAccountBalancesResponse]:
        """Get BankAccountBalances by primary key."""
        db_obj = self.db.query(BankAccountBalances).filter(BankAccountBalances.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccountBalances not found")
        return BankAccountBalancesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BankAccountBalancesListResponse:
        """List BankAccountBalances records with pagination and filtering."""
        query = self.db.query(BankAccountBalances)
        if company_id and hasattr(BankAccountBalances, "company_id"):
            query = query.filter(BankAccountBalances.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BankAccountBalances, key) and value is not None:
                    query = query.filter(getattr(BankAccountBalances, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BankAccountBalances, order_by, BankAccountBalances.created_at)))
        else:
            query = query.order_by(getattr(BankAccountBalances, order_by, BankAccountBalances.created_at))
        items = query.offset(skip).limit(limit).all()
        return BankAccountBalancesListResponse(
            items=[BankAccountBalancesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BankAccountBalancesUpdate,
               updated_by: Optional[UUID] = None) -> BankAccountBalancesResponse:
        """Update an existing BankAccountBalances record."""
        db_obj = self.db.query(BankAccountBalances).filter(BankAccountBalances.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccountBalances not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BankAccountBalancesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BankAccountBalances record. Soft delete by default."""
        db_obj = self.db.query(BankAccountBalances).filter(BankAccountBalances.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccountBalances not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BankAccountBalancesResponse]:
        """Search BankAccountBalances by name/code fields."""
        query = self.db.query(BankAccountBalances)
        if company_id and hasattr(BankAccountBalances, "company_id"):
            query = query.filter(BankAccountBalances.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BankAccountBalances, f)]
        if search_fields:
            conditions = [getattr(BankAccountBalances, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BankAccountBalancesResponse.model_validate(i) for i in items]

class BankAccountsService:
    """CRUD service for BankAccounts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BankAccountsCreate, created_by: Optional[UUID] = None) -> BankAccountsResponse:
        """Create a new BankAccounts record."""
        db_obj = BankAccounts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BankAccountsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BankAccountsResponse]:
        """Get BankAccounts by primary key."""
        db_obj = self.db.query(BankAccounts).filter(BankAccounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccounts not found")
        return BankAccountsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BankAccountsListResponse:
        """List BankAccounts records with pagination and filtering."""
        query = self.db.query(BankAccounts)
        if company_id and hasattr(BankAccounts, "company_id"):
            query = query.filter(BankAccounts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BankAccounts, key) and value is not None:
                    query = query.filter(getattr(BankAccounts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BankAccounts, order_by, BankAccounts.created_at)))
        else:
            query = query.order_by(getattr(BankAccounts, order_by, BankAccounts.created_at))
        items = query.offset(skip).limit(limit).all()
        return BankAccountsListResponse(
            items=[BankAccountsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BankAccountsUpdate,
               updated_by: Optional[UUID] = None) -> BankAccountsResponse:
        """Update an existing BankAccounts record."""
        db_obj = self.db.query(BankAccounts).filter(BankAccounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccounts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BankAccountsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BankAccounts record. Soft delete by default."""
        db_obj = self.db.query(BankAccounts).filter(BankAccounts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccounts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BankAccountsResponse]:
        """Search BankAccounts by name/code fields."""
        query = self.db.query(BankAccounts)
        if company_id and hasattr(BankAccounts, "company_id"):
            query = query.filter(BankAccounts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BankAccounts, f)]
        if search_fields:
            conditions = [getattr(BankAccounts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BankAccountsResponse.model_validate(i) for i in items]

class BankAccountsGlService:
    """CRUD service for BankAccountsGl."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BankAccountsGlCreate, created_by: Optional[UUID] = None) -> BankAccountsGlResponse:
        """Create a new BankAccountsGl record."""
        db_obj = BankAccountsGl(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BankAccountsGlResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BankAccountsGlResponse]:
        """Get BankAccountsGl by primary key."""
        db_obj = self.db.query(BankAccountsGl).filter(BankAccountsGl.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccountsGl not found")
        return BankAccountsGlResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BankAccountsGlListResponse:
        """List BankAccountsGl records with pagination and filtering."""
        query = self.db.query(BankAccountsGl)
        if company_id and hasattr(BankAccountsGl, "company_id"):
            query = query.filter(BankAccountsGl.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BankAccountsGl, key) and value is not None:
                    query = query.filter(getattr(BankAccountsGl, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BankAccountsGl, order_by, BankAccountsGl.created_at)))
        else:
            query = query.order_by(getattr(BankAccountsGl, order_by, BankAccountsGl.created_at))
        items = query.offset(skip).limit(limit).all()
        return BankAccountsGlListResponse(
            items=[BankAccountsGlResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BankAccountsGlUpdate,
               updated_by: Optional[UUID] = None) -> BankAccountsGlResponse:
        """Update an existing BankAccountsGl record."""
        db_obj = self.db.query(BankAccountsGl).filter(BankAccountsGl.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccountsGl not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BankAccountsGlResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BankAccountsGl record. Soft delete by default."""
        db_obj = self.db.query(BankAccountsGl).filter(BankAccountsGl.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankAccountsGl not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BankAccountsGlResponse]:
        """Search BankAccountsGl by name/code fields."""
        query = self.db.query(BankAccountsGl)
        if company_id and hasattr(BankAccountsGl, "company_id"):
            query = query.filter(BankAccountsGl.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BankAccountsGl, f)]
        if search_fields:
            conditions = [getattr(BankAccountsGl, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BankAccountsGlResponse.model_validate(i) for i in items]

class BankGuaranteesService:
    """CRUD service for BankGuarantees."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BankGuaranteesCreate, created_by: Optional[UUID] = None) -> BankGuaranteesResponse:
        """Create a new BankGuarantees record."""
        db_obj = BankGuarantees(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BankGuaranteesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BankGuaranteesResponse]:
        """Get BankGuarantees by primary key."""
        db_obj = self.db.query(BankGuarantees).filter(BankGuarantees.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankGuarantees not found")
        return BankGuaranteesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BankGuaranteesListResponse:
        """List BankGuarantees records with pagination and filtering."""
        query = self.db.query(BankGuarantees)
        if company_id and hasattr(BankGuarantees, "company_id"):
            query = query.filter(BankGuarantees.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BankGuarantees, key) and value is not None:
                    query = query.filter(getattr(BankGuarantees, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BankGuarantees, order_by, BankGuarantees.created_at)))
        else:
            query = query.order_by(getattr(BankGuarantees, order_by, BankGuarantees.created_at))
        items = query.offset(skip).limit(limit).all()
        return BankGuaranteesListResponse(
            items=[BankGuaranteesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BankGuaranteesUpdate,
               updated_by: Optional[UUID] = None) -> BankGuaranteesResponse:
        """Update an existing BankGuarantees record."""
        db_obj = self.db.query(BankGuarantees).filter(BankGuarantees.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankGuarantees not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BankGuaranteesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BankGuarantees record. Soft delete by default."""
        db_obj = self.db.query(BankGuarantees).filter(BankGuarantees.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankGuarantees not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BankGuaranteesResponse]:
        """Search BankGuarantees by name/code fields."""
        query = self.db.query(BankGuarantees)
        if company_id and hasattr(BankGuarantees, "company_id"):
            query = query.filter(BankGuarantees.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BankGuarantees, f)]
        if search_fields:
            conditions = [getattr(BankGuarantees, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BankGuaranteesResponse.model_validate(i) for i in items]

class BankTransactionsService:
    """CRUD service for BankTransactions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BankTransactionsCreate, created_by: Optional[UUID] = None) -> BankTransactionsResponse:
        """Create a new BankTransactions record."""
        db_obj = BankTransactions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BankTransactionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BankTransactionsResponse]:
        """Get BankTransactions by primary key."""
        db_obj = self.db.query(BankTransactions).filter(BankTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankTransactions not found")
        return BankTransactionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BankTransactionsListResponse:
        """List BankTransactions records with pagination and filtering."""
        query = self.db.query(BankTransactions)
        if company_id and hasattr(BankTransactions, "company_id"):
            query = query.filter(BankTransactions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BankTransactions, key) and value is not None:
                    query = query.filter(getattr(BankTransactions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BankTransactions, order_by, BankTransactions.created_at)))
        else:
            query = query.order_by(getattr(BankTransactions, order_by, BankTransactions.created_at))
        items = query.offset(skip).limit(limit).all()
        return BankTransactionsListResponse(
            items=[BankTransactionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BankTransactionsUpdate,
               updated_by: Optional[UUID] = None) -> BankTransactionsResponse:
        """Update an existing BankTransactions record."""
        db_obj = self.db.query(BankTransactions).filter(BankTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankTransactions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BankTransactionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BankTransactions record. Soft delete by default."""
        db_obj = self.db.query(BankTransactions).filter(BankTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BankTransactions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BankTransactionsResponse]:
        """Search BankTransactions by name/code fields."""
        query = self.db.query(BankTransactions)
        if company_id and hasattr(BankTransactions, "company_id"):
            query = query.filter(BankTransactions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BankTransactions, f)]
        if search_fields:
            conditions = [getattr(BankTransactions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BankTransactionsResponse.model_validate(i) for i in items]

class BanksService:
    """CRUD service for Banks."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BanksCreate, created_by: Optional[UUID] = None) -> BanksResponse:
        """Create a new Banks record."""
        db_obj = Banks(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BanksResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BanksResponse]:
        """Get Banks by primary key."""
        db_obj = self.db.query(Banks).filter(Banks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Banks not found")
        return BanksResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BanksListResponse:
        """List Banks records with pagination and filtering."""
        query = self.db.query(Banks)
        if company_id and hasattr(Banks, "company_id"):
            query = query.filter(Banks.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Banks, key) and value is not None:
                    query = query.filter(getattr(Banks, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Banks, order_by, Banks.created_at)))
        else:
            query = query.order_by(getattr(Banks, order_by, Banks.created_at))
        items = query.offset(skip).limit(limit).all()
        return BanksListResponse(
            items=[BanksResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BanksUpdate,
               updated_by: Optional[UUID] = None) -> BanksResponse:
        """Update an existing Banks record."""
        db_obj = self.db.query(Banks).filter(Banks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Banks not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BanksResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Banks record. Soft delete by default."""
        db_obj = self.db.query(Banks).filter(Banks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Banks not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BanksResponse]:
        """Search Banks by name/code fields."""
        query = self.db.query(Banks)
        if company_id and hasattr(Banks, "company_id"):
            query = query.filter(Banks.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Banks, f)]
        if search_fields:
            conditions = [getattr(Banks, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BanksResponse.model_validate(i) for i in items]

class LcAmendmentsService:
    """CRUD service for LcAmendments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: LcAmendmentsCreate, created_by: Optional[UUID] = None) -> LcAmendmentsResponse:
        """Create a new LcAmendments record."""
        db_obj = LcAmendments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return LcAmendmentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[LcAmendmentsResponse]:
        """Get LcAmendments by primary key."""
        db_obj = self.db.query(LcAmendments).filter(LcAmendments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LcAmendments not found")
        return LcAmendmentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> LcAmendmentsListResponse:
        """List LcAmendments records with pagination and filtering."""
        query = self.db.query(LcAmendments)
        if company_id and hasattr(LcAmendments, "company_id"):
            query = query.filter(LcAmendments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(LcAmendments, key) and value is not None:
                    query = query.filter(getattr(LcAmendments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(LcAmendments, order_by, LcAmendments.created_at)))
        else:
            query = query.order_by(getattr(LcAmendments, order_by, LcAmendments.created_at))
        items = query.offset(skip).limit(limit).all()
        return LcAmendmentsListResponse(
            items=[LcAmendmentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: LcAmendmentsUpdate,
               updated_by: Optional[UUID] = None) -> LcAmendmentsResponse:
        """Update an existing LcAmendments record."""
        db_obj = self.db.query(LcAmendments).filter(LcAmendments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LcAmendments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return LcAmendmentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a LcAmendments record. Soft delete by default."""
        db_obj = self.db.query(LcAmendments).filter(LcAmendments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LcAmendments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[LcAmendmentsResponse]:
        """Search LcAmendments by name/code fields."""
        query = self.db.query(LcAmendments)
        if company_id and hasattr(LcAmendments, "company_id"):
            query = query.filter(LcAmendments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(LcAmendments, f)]
        if search_fields:
            conditions = [getattr(LcAmendments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [LcAmendmentsResponse.model_validate(i) for i in items]

class LetterOfCreditService:
    """CRUD service for LetterOfCredit."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: LetterOfCreditCreate, created_by: Optional[UUID] = None) -> LetterOfCreditResponse:
        """Create a new LetterOfCredit record."""
        db_obj = LetterOfCredit(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return LetterOfCreditResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[LetterOfCreditResponse]:
        """Get LetterOfCredit by primary key."""
        db_obj = self.db.query(LetterOfCredit).filter(LetterOfCredit.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LetterOfCredit not found")
        return LetterOfCreditResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> LetterOfCreditListResponse:
        """List LetterOfCredit records with pagination and filtering."""
        query = self.db.query(LetterOfCredit)
        if company_id and hasattr(LetterOfCredit, "company_id"):
            query = query.filter(LetterOfCredit.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(LetterOfCredit, key) and value is not None:
                    query = query.filter(getattr(LetterOfCredit, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(LetterOfCredit, order_by, LetterOfCredit.created_at)))
        else:
            query = query.order_by(getattr(LetterOfCredit, order_by, LetterOfCredit.created_at))
        items = query.offset(skip).limit(limit).all()
        return LetterOfCreditListResponse(
            items=[LetterOfCreditResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: LetterOfCreditUpdate,
               updated_by: Optional[UUID] = None) -> LetterOfCreditResponse:
        """Update an existing LetterOfCredit record."""
        db_obj = self.db.query(LetterOfCredit).filter(LetterOfCredit.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LetterOfCredit not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return LetterOfCreditResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a LetterOfCredit record. Soft delete by default."""
        db_obj = self.db.query(LetterOfCredit).filter(LetterOfCredit.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="LetterOfCredit not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[LetterOfCreditResponse]:
        """Search LetterOfCredit by name/code fields."""
        query = self.db.query(LetterOfCredit)
        if company_id and hasattr(LetterOfCredit, "company_id"):
            query = query.filter(LetterOfCredit.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(LetterOfCredit, f)]
        if search_fields:
            conditions = [getattr(LetterOfCredit, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [LetterOfCreditResponse.model_validate(i) for i in items]

class PaymentApprovalsService:
    """CRUD service for PaymentApprovals."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PaymentApprovalsCreate, created_by: Optional[UUID] = None) -> PaymentApprovalsResponse:
        """Create a new PaymentApprovals record."""
        db_obj = PaymentApprovals(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentApprovalsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PaymentApprovalsResponse]:
        """Get PaymentApprovals by primary key."""
        db_obj = self.db.query(PaymentApprovals).filter(PaymentApprovals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentApprovals not found")
        return PaymentApprovalsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PaymentApprovalsListResponse:
        """List PaymentApprovals records with pagination and filtering."""
        query = self.db.query(PaymentApprovals)
        if company_id and hasattr(PaymentApprovals, "company_id"):
            query = query.filter(PaymentApprovals.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PaymentApprovals, key) and value is not None:
                    query = query.filter(getattr(PaymentApprovals, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PaymentApprovals, order_by, PaymentApprovals.created_at)))
        else:
            query = query.order_by(getattr(PaymentApprovals, order_by, PaymentApprovals.created_at))
        items = query.offset(skip).limit(limit).all()
        return PaymentApprovalsListResponse(
            items=[PaymentApprovalsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PaymentApprovalsUpdate,
               updated_by: Optional[UUID] = None) -> PaymentApprovalsResponse:
        """Update an existing PaymentApprovals record."""
        db_obj = self.db.query(PaymentApprovals).filter(PaymentApprovals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentApprovals not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentApprovalsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PaymentApprovals record. Soft delete by default."""
        db_obj = self.db.query(PaymentApprovals).filter(PaymentApprovals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentApprovals not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PaymentApprovalsResponse]:
        """Search PaymentApprovals by name/code fields."""
        query = self.db.query(PaymentApprovals)
        if company_id and hasattr(PaymentApprovals, "company_id"):
            query = query.filter(PaymentApprovals.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PaymentApprovals, f)]
        if search_fields:
            conditions = [getattr(PaymentApprovals, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PaymentApprovalsResponse.model_validate(i) for i in items]

class PaymentBatchItemsService:
    """CRUD service for PaymentBatchItems."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PaymentBatchItemsCreate, created_by: Optional[UUID] = None) -> PaymentBatchItemsResponse:
        """Create a new PaymentBatchItems record."""
        db_obj = PaymentBatchItems(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentBatchItemsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PaymentBatchItemsResponse]:
        """Get PaymentBatchItems by primary key."""
        db_obj = self.db.query(PaymentBatchItems).filter(PaymentBatchItems.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentBatchItems not found")
        return PaymentBatchItemsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PaymentBatchItemsListResponse:
        """List PaymentBatchItems records with pagination and filtering."""
        query = self.db.query(PaymentBatchItems)
        if company_id and hasattr(PaymentBatchItems, "company_id"):
            query = query.filter(PaymentBatchItems.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PaymentBatchItems, key) and value is not None:
                    query = query.filter(getattr(PaymentBatchItems, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PaymentBatchItems, order_by, PaymentBatchItems.created_at)))
        else:
            query = query.order_by(getattr(PaymentBatchItems, order_by, PaymentBatchItems.created_at))
        items = query.offset(skip).limit(limit).all()
        return PaymentBatchItemsListResponse(
            items=[PaymentBatchItemsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PaymentBatchItemsUpdate,
               updated_by: Optional[UUID] = None) -> PaymentBatchItemsResponse:
        """Update an existing PaymentBatchItems record."""
        db_obj = self.db.query(PaymentBatchItems).filter(PaymentBatchItems.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentBatchItems not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentBatchItemsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PaymentBatchItems record. Soft delete by default."""
        db_obj = self.db.query(PaymentBatchItems).filter(PaymentBatchItems.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentBatchItems not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PaymentBatchItemsResponse]:
        """Search PaymentBatchItems by name/code fields."""
        query = self.db.query(PaymentBatchItems)
        if company_id and hasattr(PaymentBatchItems, "company_id"):
            query = query.filter(PaymentBatchItems.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PaymentBatchItems, f)]
        if search_fields:
            conditions = [getattr(PaymentBatchItems, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PaymentBatchItemsResponse.model_validate(i) for i in items]

class PaymentBatchesService:
    """CRUD service for PaymentBatches."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PaymentBatchesCreate, created_by: Optional[UUID] = None) -> PaymentBatchesResponse:
        """Create a new PaymentBatches record."""
        db_obj = PaymentBatches(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentBatchesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PaymentBatchesResponse]:
        """Get PaymentBatches by primary key."""
        db_obj = self.db.query(PaymentBatches).filter(PaymentBatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentBatches not found")
        return PaymentBatchesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PaymentBatchesListResponse:
        """List PaymentBatches records with pagination and filtering."""
        query = self.db.query(PaymentBatches)
        if company_id and hasattr(PaymentBatches, "company_id"):
            query = query.filter(PaymentBatches.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PaymentBatches, key) and value is not None:
                    query = query.filter(getattr(PaymentBatches, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PaymentBatches, order_by, PaymentBatches.created_at)))
        else:
            query = query.order_by(getattr(PaymentBatches, order_by, PaymentBatches.created_at))
        items = query.offset(skip).limit(limit).all()
        return PaymentBatchesListResponse(
            items=[PaymentBatchesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PaymentBatchesUpdate,
               updated_by: Optional[UUID] = None) -> PaymentBatchesResponse:
        """Update an existing PaymentBatches record."""
        db_obj = self.db.query(PaymentBatches).filter(PaymentBatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentBatches not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentBatchesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PaymentBatches record. Soft delete by default."""
        db_obj = self.db.query(PaymentBatches).filter(PaymentBatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentBatches not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PaymentBatchesResponse]:
        """Search PaymentBatches by name/code fields."""
        query = self.db.query(PaymentBatches)
        if company_id and hasattr(PaymentBatches, "company_id"):
            query = query.filter(PaymentBatches.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PaymentBatches, f)]
        if search_fields:
            conditions = [getattr(PaymentBatches, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PaymentBatchesResponse.model_validate(i) for i in items]

class ReconciliationBatchesService:
    """CRUD service for ReconciliationBatches."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ReconciliationBatchesCreate, created_by: Optional[UUID] = None) -> ReconciliationBatchesResponse:
        """Create a new ReconciliationBatches record."""
        db_obj = ReconciliationBatches(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ReconciliationBatchesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ReconciliationBatchesResponse]:
        """Get ReconciliationBatches by primary key."""
        db_obj = self.db.query(ReconciliationBatches).filter(ReconciliationBatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationBatches not found")
        return ReconciliationBatchesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ReconciliationBatchesListResponse:
        """List ReconciliationBatches records with pagination and filtering."""
        query = self.db.query(ReconciliationBatches)
        if company_id and hasattr(ReconciliationBatches, "company_id"):
            query = query.filter(ReconciliationBatches.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ReconciliationBatches, key) and value is not None:
                    query = query.filter(getattr(ReconciliationBatches, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ReconciliationBatches, order_by, ReconciliationBatches.created_at)))
        else:
            query = query.order_by(getattr(ReconciliationBatches, order_by, ReconciliationBatches.created_at))
        items = query.offset(skip).limit(limit).all()
        return ReconciliationBatchesListResponse(
            items=[ReconciliationBatchesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ReconciliationBatchesUpdate,
               updated_by: Optional[UUID] = None) -> ReconciliationBatchesResponse:
        """Update an existing ReconciliationBatches record."""
        db_obj = self.db.query(ReconciliationBatches).filter(ReconciliationBatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationBatches not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ReconciliationBatchesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ReconciliationBatches record. Soft delete by default."""
        db_obj = self.db.query(ReconciliationBatches).filter(ReconciliationBatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationBatches not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ReconciliationBatchesResponse]:
        """Search ReconciliationBatches by name/code fields."""
        query = self.db.query(ReconciliationBatches)
        if company_id and hasattr(ReconciliationBatches, "company_id"):
            query = query.filter(ReconciliationBatches.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ReconciliationBatches, f)]
        if search_fields:
            conditions = [getattr(ReconciliationBatches, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ReconciliationBatchesResponse.model_validate(i) for i in items]

class ReconciliationMatchesService:
    """CRUD service for ReconciliationMatches."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ReconciliationMatchesCreate, created_by: Optional[UUID] = None) -> ReconciliationMatchesResponse:
        """Create a new ReconciliationMatches record."""
        db_obj = ReconciliationMatches(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ReconciliationMatchesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ReconciliationMatchesResponse]:
        """Get ReconciliationMatches by primary key."""
        db_obj = self.db.query(ReconciliationMatches).filter(ReconciliationMatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationMatches not found")
        return ReconciliationMatchesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ReconciliationMatchesListResponse:
        """List ReconciliationMatches records with pagination and filtering."""
        query = self.db.query(ReconciliationMatches)
        if company_id and hasattr(ReconciliationMatches, "company_id"):
            query = query.filter(ReconciliationMatches.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ReconciliationMatches, key) and value is not None:
                    query = query.filter(getattr(ReconciliationMatches, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ReconciliationMatches, order_by, ReconciliationMatches.created_at)))
        else:
            query = query.order_by(getattr(ReconciliationMatches, order_by, ReconciliationMatches.created_at))
        items = query.offset(skip).limit(limit).all()
        return ReconciliationMatchesListResponse(
            items=[ReconciliationMatchesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ReconciliationMatchesUpdate,
               updated_by: Optional[UUID] = None) -> ReconciliationMatchesResponse:
        """Update an existing ReconciliationMatches record."""
        db_obj = self.db.query(ReconciliationMatches).filter(ReconciliationMatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationMatches not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ReconciliationMatchesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ReconciliationMatches record. Soft delete by default."""
        db_obj = self.db.query(ReconciliationMatches).filter(ReconciliationMatches.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationMatches not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ReconciliationMatchesResponse]:
        """Search ReconciliationMatches by name/code fields."""
        query = self.db.query(ReconciliationMatches)
        if company_id and hasattr(ReconciliationMatches, "company_id"):
            query = query.filter(ReconciliationMatches.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ReconciliationMatches, f)]
        if search_fields:
            conditions = [getattr(ReconciliationMatches, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ReconciliationMatchesResponse.model_validate(i) for i in items]

class ReconciliationRulesService:
    """CRUD service for ReconciliationRules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ReconciliationRulesCreate, created_by: Optional[UUID] = None) -> ReconciliationRulesResponse:
        """Create a new ReconciliationRules record."""
        db_obj = ReconciliationRules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ReconciliationRulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ReconciliationRulesResponse]:
        """Get ReconciliationRules by primary key."""
        db_obj = self.db.query(ReconciliationRules).filter(ReconciliationRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationRules not found")
        return ReconciliationRulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ReconciliationRulesListResponse:
        """List ReconciliationRules records with pagination and filtering."""
        query = self.db.query(ReconciliationRules)
        if company_id and hasattr(ReconciliationRules, "company_id"):
            query = query.filter(ReconciliationRules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ReconciliationRules, key) and value is not None:
                    query = query.filter(getattr(ReconciliationRules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ReconciliationRules, order_by, ReconciliationRules.created_at)))
        else:
            query = query.order_by(getattr(ReconciliationRules, order_by, ReconciliationRules.created_at))
        items = query.offset(skip).limit(limit).all()
        return ReconciliationRulesListResponse(
            items=[ReconciliationRulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ReconciliationRulesUpdate,
               updated_by: Optional[UUID] = None) -> ReconciliationRulesResponse:
        """Update an existing ReconciliationRules record."""
        db_obj = self.db.query(ReconciliationRules).filter(ReconciliationRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationRules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ReconciliationRulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ReconciliationRules record. Soft delete by default."""
        db_obj = self.db.query(ReconciliationRules).filter(ReconciliationRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ReconciliationRules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ReconciliationRulesResponse]:
        """Search ReconciliationRules by name/code fields."""
        query = self.db.query(ReconciliationRules)
        if company_id and hasattr(ReconciliationRules, "company_id"):
            query = query.filter(ReconciliationRules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ReconciliationRules, f)]
        if search_fields:
            conditions = [getattr(ReconciliationRules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ReconciliationRulesResponse.model_validate(i) for i in items]

class TaxCodesService:
    """CRUD service for TaxCodes."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TaxCodesCreate, created_by: Optional[UUID] = None) -> TaxCodesResponse:
        """Create a new TaxCodes record."""
        db_obj = TaxCodes(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TaxCodesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TaxCodesResponse]:
        """Get TaxCodes by primary key."""
        db_obj = self.db.query(TaxCodes).filter(TaxCodes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TaxCodes not found")
        return TaxCodesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TaxCodesListResponse:
        """List TaxCodes records with pagination and filtering."""
        query = self.db.query(TaxCodes)
        if company_id and hasattr(TaxCodes, "company_id"):
            query = query.filter(TaxCodes.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TaxCodes, key) and value is not None:
                    query = query.filter(getattr(TaxCodes, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TaxCodes, order_by, TaxCodes.created_at)))
        else:
            query = query.order_by(getattr(TaxCodes, order_by, TaxCodes.created_at))
        items = query.offset(skip).limit(limit).all()
        return TaxCodesListResponse(
            items=[TaxCodesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TaxCodesUpdate,
               updated_by: Optional[UUID] = None) -> TaxCodesResponse:
        """Update an existing TaxCodes record."""
        db_obj = self.db.query(TaxCodes).filter(TaxCodes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TaxCodes not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TaxCodesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TaxCodes record. Soft delete by default."""
        db_obj = self.db.query(TaxCodes).filter(TaxCodes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TaxCodes not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TaxCodesResponse]:
        """Search TaxCodes by name/code fields."""
        query = self.db.query(TaxCodes)
        if company_id and hasattr(TaxCodes, "company_id"):
            query = query.filter(TaxCodes.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TaxCodes, f)]
        if search_fields:
            conditions = [getattr(TaxCodes, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TaxCodesResponse.model_validate(i) for i in items]

class TradeCyclesService:
    """CRUD service for TradeCycles."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TradeCyclesCreate, created_by: Optional[UUID] = None) -> TradeCyclesResponse:
        """Create a new TradeCycles record."""
        db_obj = TradeCycles(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TradeCyclesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TradeCyclesResponse]:
        """Get TradeCycles by primary key."""
        db_obj = self.db.query(TradeCycles).filter(TradeCycles.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TradeCycles not found")
        return TradeCyclesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TradeCyclesListResponse:
        """List TradeCycles records with pagination and filtering."""
        query = self.db.query(TradeCycles)
        if company_id and hasattr(TradeCycles, "company_id"):
            query = query.filter(TradeCycles.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TradeCycles, key) and value is not None:
                    query = query.filter(getattr(TradeCycles, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TradeCycles, order_by, TradeCycles.created_at)))
        else:
            query = query.order_by(getattr(TradeCycles, order_by, TradeCycles.created_at))
        items = query.offset(skip).limit(limit).all()
        return TradeCyclesListResponse(
            items=[TradeCyclesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TradeCyclesUpdate,
               updated_by: Optional[UUID] = None) -> TradeCyclesResponse:
        """Update an existing TradeCycles record."""
        db_obj = self.db.query(TradeCycles).filter(TradeCycles.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TradeCycles not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TradeCyclesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TradeCycles record. Soft delete by default."""
        db_obj = self.db.query(TradeCycles).filter(TradeCycles.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TradeCycles not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TradeCyclesResponse]:
        """Search TradeCycles by name/code fields."""
        query = self.db.query(TradeCycles)
        if company_id and hasattr(TradeCycles, "company_id"):
            query = query.filter(TradeCycles.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TradeCycles, f)]
        if search_fields:
            conditions = [getattr(TradeCycles, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TradeCyclesResponse.model_validate(i) for i in items]

class TradeTransactionsService:
    """CRUD service for TradeTransactions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TradeTransactionsCreate, created_by: Optional[UUID] = None) -> TradeTransactionsResponse:
        """Create a new TradeTransactions record."""
        db_obj = TradeTransactions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TradeTransactionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TradeTransactionsResponse]:
        """Get TradeTransactions by primary key."""
        db_obj = self.db.query(TradeTransactions).filter(TradeTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TradeTransactions not found")
        return TradeTransactionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TradeTransactionsListResponse:
        """List TradeTransactions records with pagination and filtering."""
        query = self.db.query(TradeTransactions)
        if company_id and hasattr(TradeTransactions, "company_id"):
            query = query.filter(TradeTransactions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TradeTransactions, key) and value is not None:
                    query = query.filter(getattr(TradeTransactions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TradeTransactions, order_by, TradeTransactions.created_at)))
        else:
            query = query.order_by(getattr(TradeTransactions, order_by, TradeTransactions.created_at))
        items = query.offset(skip).limit(limit).all()
        return TradeTransactionsListResponse(
            items=[TradeTransactionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TradeTransactionsUpdate,
               updated_by: Optional[UUID] = None) -> TradeTransactionsResponse:
        """Update an existing TradeTransactions record."""
        db_obj = self.db.query(TradeTransactions).filter(TradeTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TradeTransactions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TradeTransactionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TradeTransactions record. Soft delete by default."""
        db_obj = self.db.query(TradeTransactions).filter(TradeTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TradeTransactions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TradeTransactionsResponse]:
        """Search TradeTransactions by name/code fields."""
        query = self.db.query(TradeTransactions)
        if company_id and hasattr(TradeTransactions, "company_id"):
            query = query.filter(TradeTransactions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TradeTransactions, f)]
        if search_fields:
            conditions = [getattr(TradeTransactions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TradeTransactionsResponse.model_validate(i) for i in items]

class TransactionTypeCategoriesService:
    """CRUD service for TransactionTypeCategories."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TransactionTypeCategoriesCreate, created_by: Optional[UUID] = None) -> TransactionTypeCategoriesResponse:
        """Create a new TransactionTypeCategories record."""
        db_obj = TransactionTypeCategories(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TransactionTypeCategoriesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TransactionTypeCategoriesResponse]:
        """Get TransactionTypeCategories by primary key."""
        db_obj = self.db.query(TransactionTypeCategories).filter(TransactionTypeCategories.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TransactionTypeCategories not found")
        return TransactionTypeCategoriesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TransactionTypeCategoriesListResponse:
        """List TransactionTypeCategories records with pagination and filtering."""
        query = self.db.query(TransactionTypeCategories)
        if company_id and hasattr(TransactionTypeCategories, "company_id"):
            query = query.filter(TransactionTypeCategories.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TransactionTypeCategories, key) and value is not None:
                    query = query.filter(getattr(TransactionTypeCategories, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TransactionTypeCategories, order_by, TransactionTypeCategories.created_at)))
        else:
            query = query.order_by(getattr(TransactionTypeCategories, order_by, TransactionTypeCategories.created_at))
        items = query.offset(skip).limit(limit).all()
        return TransactionTypeCategoriesListResponse(
            items=[TransactionTypeCategoriesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TransactionTypeCategoriesUpdate,
               updated_by: Optional[UUID] = None) -> TransactionTypeCategoriesResponse:
        """Update an existing TransactionTypeCategories record."""
        db_obj = self.db.query(TransactionTypeCategories).filter(TransactionTypeCategories.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TransactionTypeCategories not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TransactionTypeCategoriesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TransactionTypeCategories record. Soft delete by default."""
        db_obj = self.db.query(TransactionTypeCategories).filter(TransactionTypeCategories.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TransactionTypeCategories not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TransactionTypeCategoriesResponse]:
        """Search TransactionTypeCategories by name/code fields."""
        query = self.db.query(TransactionTypeCategories)
        if company_id and hasattr(TransactionTypeCategories, "company_id"):
            query = query.filter(TransactionTypeCategories.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TransactionTypeCategories, f)]
        if search_fields:
            conditions = [getattr(TransactionTypeCategories, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TransactionTypeCategoriesResponse.model_validate(i) for i in items]

class TransactionTypesService:
    """CRUD service for TransactionTypes."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TransactionTypesCreate, created_by: Optional[UUID] = None) -> TransactionTypesResponse:
        """Create a new TransactionTypes record."""
        db_obj = TransactionTypes(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TransactionTypesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TransactionTypesResponse]:
        """Get TransactionTypes by primary key."""
        db_obj = self.db.query(TransactionTypes).filter(TransactionTypes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TransactionTypes not found")
        return TransactionTypesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TransactionTypesListResponse:
        """List TransactionTypes records with pagination and filtering."""
        query = self.db.query(TransactionTypes)
        if company_id and hasattr(TransactionTypes, "company_id"):
            query = query.filter(TransactionTypes.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TransactionTypes, key) and value is not None:
                    query = query.filter(getattr(TransactionTypes, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TransactionTypes, order_by, TransactionTypes.created_at)))
        else:
            query = query.order_by(getattr(TransactionTypes, order_by, TransactionTypes.created_at))
        items = query.offset(skip).limit(limit).all()
        return TransactionTypesListResponse(
            items=[TransactionTypesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TransactionTypesUpdate,
               updated_by: Optional[UUID] = None) -> TransactionTypesResponse:
        """Update an existing TransactionTypes record."""
        db_obj = self.db.query(TransactionTypes).filter(TransactionTypes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TransactionTypes not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TransactionTypesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TransactionTypes record. Soft delete by default."""
        db_obj = self.db.query(TransactionTypes).filter(TransactionTypes.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TransactionTypes not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TransactionTypesResponse]:
        """Search TransactionTypes by name/code fields."""
        query = self.db.query(TransactionTypes)
        if company_id and hasattr(TransactionTypes, "company_id"):
            query = query.filter(TransactionTypes.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TransactionTypes, f)]
        if search_fields:
            conditions = [getattr(TransactionTypes, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TransactionTypesResponse.model_validate(i) for i in items]

class TreasuryCashPoolMembersService:
    """CRUD service for TreasuryCashPoolMembers."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TreasuryCashPoolMembersCreate, created_by: Optional[UUID] = None) -> TreasuryCashPoolMembersResponse:
        """Create a new TreasuryCashPoolMembers record."""
        db_obj = TreasuryCashPoolMembers(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryCashPoolMembersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TreasuryCashPoolMembersResponse]:
        """Get TreasuryCashPoolMembers by primary key."""
        db_obj = self.db.query(TreasuryCashPoolMembers).filter(TreasuryCashPoolMembers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryCashPoolMembers not found")
        return TreasuryCashPoolMembersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TreasuryCashPoolMembersListResponse:
        """List TreasuryCashPoolMembers records with pagination and filtering."""
        query = self.db.query(TreasuryCashPoolMembers)
        if company_id and hasattr(TreasuryCashPoolMembers, "company_id"):
            query = query.filter(TreasuryCashPoolMembers.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TreasuryCashPoolMembers, key) and value is not None:
                    query = query.filter(getattr(TreasuryCashPoolMembers, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TreasuryCashPoolMembers, order_by, TreasuryCashPoolMembers.created_at)))
        else:
            query = query.order_by(getattr(TreasuryCashPoolMembers, order_by, TreasuryCashPoolMembers.created_at))
        items = query.offset(skip).limit(limit).all()
        return TreasuryCashPoolMembersListResponse(
            items=[TreasuryCashPoolMembersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TreasuryCashPoolMembersUpdate,
               updated_by: Optional[UUID] = None) -> TreasuryCashPoolMembersResponse:
        """Update an existing TreasuryCashPoolMembers record."""
        db_obj = self.db.query(TreasuryCashPoolMembers).filter(TreasuryCashPoolMembers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryCashPoolMembers not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryCashPoolMembersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TreasuryCashPoolMembers record. Soft delete by default."""
        db_obj = self.db.query(TreasuryCashPoolMembers).filter(TreasuryCashPoolMembers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryCashPoolMembers not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TreasuryCashPoolMembersResponse]:
        """Search TreasuryCashPoolMembers by name/code fields."""
        query = self.db.query(TreasuryCashPoolMembers)
        if company_id and hasattr(TreasuryCashPoolMembers, "company_id"):
            query = query.filter(TreasuryCashPoolMembers.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TreasuryCashPoolMembers, f)]
        if search_fields:
            conditions = [getattr(TreasuryCashPoolMembers, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TreasuryCashPoolMembersResponse.model_validate(i) for i in items]

class TreasuryCashPoolsService:
    """CRUD service for TreasuryCashPools."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TreasuryCashPoolsCreate, created_by: Optional[UUID] = None) -> TreasuryCashPoolsResponse:
        """Create a new TreasuryCashPools record."""
        db_obj = TreasuryCashPools(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryCashPoolsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TreasuryCashPoolsResponse]:
        """Get TreasuryCashPools by primary key."""
        db_obj = self.db.query(TreasuryCashPools).filter(TreasuryCashPools.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryCashPools not found")
        return TreasuryCashPoolsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TreasuryCashPoolsListResponse:
        """List TreasuryCashPools records with pagination and filtering."""
        query = self.db.query(TreasuryCashPools)
        if company_id and hasattr(TreasuryCashPools, "company_id"):
            query = query.filter(TreasuryCashPools.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TreasuryCashPools, key) and value is not None:
                    query = query.filter(getattr(TreasuryCashPools, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TreasuryCashPools, order_by, TreasuryCashPools.created_at)))
        else:
            query = query.order_by(getattr(TreasuryCashPools, order_by, TreasuryCashPools.created_at))
        items = query.offset(skip).limit(limit).all()
        return TreasuryCashPoolsListResponse(
            items=[TreasuryCashPoolsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TreasuryCashPoolsUpdate,
               updated_by: Optional[UUID] = None) -> TreasuryCashPoolsResponse:
        """Update an existing TreasuryCashPools record."""
        db_obj = self.db.query(TreasuryCashPools).filter(TreasuryCashPools.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryCashPools not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryCashPoolsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TreasuryCashPools record. Soft delete by default."""
        db_obj = self.db.query(TreasuryCashPools).filter(TreasuryCashPools.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryCashPools not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TreasuryCashPoolsResponse]:
        """Search TreasuryCashPools by name/code fields."""
        query = self.db.query(TreasuryCashPools)
        if company_id and hasattr(TreasuryCashPools, "company_id"):
            query = query.filter(TreasuryCashPools.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TreasuryCashPools, f)]
        if search_fields:
            conditions = [getattr(TreasuryCashPools, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TreasuryCashPoolsResponse.model_validate(i) for i in items]

class TreasuryFxExposuresService:
    """CRUD service for TreasuryFxExposures."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TreasuryFxExposuresCreate, created_by: Optional[UUID] = None) -> TreasuryFxExposuresResponse:
        """Create a new TreasuryFxExposures record."""
        db_obj = TreasuryFxExposures(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryFxExposuresResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TreasuryFxExposuresResponse]:
        """Get TreasuryFxExposures by primary key."""
        db_obj = self.db.query(TreasuryFxExposures).filter(TreasuryFxExposures.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryFxExposures not found")
        return TreasuryFxExposuresResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TreasuryFxExposuresListResponse:
        """List TreasuryFxExposures records with pagination and filtering."""
        query = self.db.query(TreasuryFxExposures)
        if company_id and hasattr(TreasuryFxExposures, "company_id"):
            query = query.filter(TreasuryFxExposures.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TreasuryFxExposures, key) and value is not None:
                    query = query.filter(getattr(TreasuryFxExposures, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TreasuryFxExposures, order_by, TreasuryFxExposures.created_at)))
        else:
            query = query.order_by(getattr(TreasuryFxExposures, order_by, TreasuryFxExposures.created_at))
        items = query.offset(skip).limit(limit).all()
        return TreasuryFxExposuresListResponse(
            items=[TreasuryFxExposuresResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TreasuryFxExposuresUpdate,
               updated_by: Optional[UUID] = None) -> TreasuryFxExposuresResponse:
        """Update an existing TreasuryFxExposures record."""
        db_obj = self.db.query(TreasuryFxExposures).filter(TreasuryFxExposures.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryFxExposures not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryFxExposuresResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TreasuryFxExposures record. Soft delete by default."""
        db_obj = self.db.query(TreasuryFxExposures).filter(TreasuryFxExposures.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryFxExposures not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TreasuryFxExposuresResponse]:
        """Search TreasuryFxExposures by name/code fields."""
        query = self.db.query(TreasuryFxExposures)
        if company_id and hasattr(TreasuryFxExposures, "company_id"):
            query = query.filter(TreasuryFxExposures.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TreasuryFxExposures, f)]
        if search_fields:
            conditions = [getattr(TreasuryFxExposures, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TreasuryFxExposuresResponse.model_validate(i) for i in items]

class TreasuryHedgeInstrumentsService:
    """CRUD service for TreasuryHedgeInstruments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TreasuryHedgeInstrumentsCreate, created_by: Optional[UUID] = None) -> TreasuryHedgeInstrumentsResponse:
        """Create a new TreasuryHedgeInstruments record."""
        db_obj = TreasuryHedgeInstruments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryHedgeInstrumentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TreasuryHedgeInstrumentsResponse]:
        """Get TreasuryHedgeInstruments by primary key."""
        db_obj = self.db.query(TreasuryHedgeInstruments).filter(TreasuryHedgeInstruments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryHedgeInstruments not found")
        return TreasuryHedgeInstrumentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TreasuryHedgeInstrumentsListResponse:
        """List TreasuryHedgeInstruments records with pagination and filtering."""
        query = self.db.query(TreasuryHedgeInstruments)
        if company_id and hasattr(TreasuryHedgeInstruments, "company_id"):
            query = query.filter(TreasuryHedgeInstruments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TreasuryHedgeInstruments, key) and value is not None:
                    query = query.filter(getattr(TreasuryHedgeInstruments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TreasuryHedgeInstruments, order_by, TreasuryHedgeInstruments.created_at)))
        else:
            query = query.order_by(getattr(TreasuryHedgeInstruments, order_by, TreasuryHedgeInstruments.created_at))
        items = query.offset(skip).limit(limit).all()
        return TreasuryHedgeInstrumentsListResponse(
            items=[TreasuryHedgeInstrumentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TreasuryHedgeInstrumentsUpdate,
               updated_by: Optional[UUID] = None) -> TreasuryHedgeInstrumentsResponse:
        """Update an existing TreasuryHedgeInstruments record."""
        db_obj = self.db.query(TreasuryHedgeInstruments).filter(TreasuryHedgeInstruments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryHedgeInstruments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryHedgeInstrumentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TreasuryHedgeInstruments record. Soft delete by default."""
        db_obj = self.db.query(TreasuryHedgeInstruments).filter(TreasuryHedgeInstruments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryHedgeInstruments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TreasuryHedgeInstrumentsResponse]:
        """Search TreasuryHedgeInstruments by name/code fields."""
        query = self.db.query(TreasuryHedgeInstruments)
        if company_id and hasattr(TreasuryHedgeInstruments, "company_id"):
            query = query.filter(TreasuryHedgeInstruments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TreasuryHedgeInstruments, f)]
        if search_fields:
            conditions = [getattr(TreasuryHedgeInstruments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TreasuryHedgeInstrumentsResponse.model_validate(i) for i in items]

class TreasuryLiquidityForecastsService:
    """CRUD service for TreasuryLiquidityForecasts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TreasuryLiquidityForecastsCreate, created_by: Optional[UUID] = None) -> TreasuryLiquidityForecastsResponse:
        """Create a new TreasuryLiquidityForecasts record."""
        db_obj = TreasuryLiquidityForecasts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryLiquidityForecastsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TreasuryLiquidityForecastsResponse]:
        """Get TreasuryLiquidityForecasts by primary key."""
        db_obj = self.db.query(TreasuryLiquidityForecasts).filter(TreasuryLiquidityForecasts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryLiquidityForecasts not found")
        return TreasuryLiquidityForecastsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TreasuryLiquidityForecastsListResponse:
        """List TreasuryLiquidityForecasts records with pagination and filtering."""
        query = self.db.query(TreasuryLiquidityForecasts)
        if company_id and hasattr(TreasuryLiquidityForecasts, "company_id"):
            query = query.filter(TreasuryLiquidityForecasts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TreasuryLiquidityForecasts, key) and value is not None:
                    query = query.filter(getattr(TreasuryLiquidityForecasts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TreasuryLiquidityForecasts, order_by, TreasuryLiquidityForecasts.created_at)))
        else:
            query = query.order_by(getattr(TreasuryLiquidityForecasts, order_by, TreasuryLiquidityForecasts.created_at))
        items = query.offset(skip).limit(limit).all()
        return TreasuryLiquidityForecastsListResponse(
            items=[TreasuryLiquidityForecastsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TreasuryLiquidityForecastsUpdate,
               updated_by: Optional[UUID] = None) -> TreasuryLiquidityForecastsResponse:
        """Update an existing TreasuryLiquidityForecasts record."""
        db_obj = self.db.query(TreasuryLiquidityForecasts).filter(TreasuryLiquidityForecasts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryLiquidityForecasts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasuryLiquidityForecastsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TreasuryLiquidityForecasts record. Soft delete by default."""
        db_obj = self.db.query(TreasuryLiquidityForecasts).filter(TreasuryLiquidityForecasts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasuryLiquidityForecasts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TreasuryLiquidityForecastsResponse]:
        """Search TreasuryLiquidityForecasts by name/code fields."""
        query = self.db.query(TreasuryLiquidityForecasts)
        if company_id and hasattr(TreasuryLiquidityForecasts, "company_id"):
            query = query.filter(TreasuryLiquidityForecasts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TreasuryLiquidityForecasts, f)]
        if search_fields:
            conditions = [getattr(TreasuryLiquidityForecasts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TreasuryLiquidityForecastsResponse.model_validate(i) for i in items]

class TreasurySweepTransactionsService:
    """CRUD service for TreasurySweepTransactions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: TreasurySweepTransactionsCreate, created_by: Optional[UUID] = None) -> TreasurySweepTransactionsResponse:
        """Create a new TreasurySweepTransactions record."""
        db_obj = TreasurySweepTransactions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasurySweepTransactionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[TreasurySweepTransactionsResponse]:
        """Get TreasurySweepTransactions by primary key."""
        db_obj = self.db.query(TreasurySweepTransactions).filter(TreasurySweepTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasurySweepTransactions not found")
        return TreasurySweepTransactionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> TreasurySweepTransactionsListResponse:
        """List TreasurySweepTransactions records with pagination and filtering."""
        query = self.db.query(TreasurySweepTransactions)
        if company_id and hasattr(TreasurySweepTransactions, "company_id"):
            query = query.filter(TreasurySweepTransactions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(TreasurySweepTransactions, key) and value is not None:
                    query = query.filter(getattr(TreasurySweepTransactions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(TreasurySweepTransactions, order_by, TreasurySweepTransactions.created_at)))
        else:
            query = query.order_by(getattr(TreasurySweepTransactions, order_by, TreasurySweepTransactions.created_at))
        items = query.offset(skip).limit(limit).all()
        return TreasurySweepTransactionsListResponse(
            items=[TreasurySweepTransactionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: TreasurySweepTransactionsUpdate,
               updated_by: Optional[UUID] = None) -> TreasurySweepTransactionsResponse:
        """Update an existing TreasurySweepTransactions record."""
        db_obj = self.db.query(TreasurySweepTransactions).filter(TreasurySweepTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasurySweepTransactions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return TreasurySweepTransactionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a TreasurySweepTransactions record. Soft delete by default."""
        db_obj = self.db.query(TreasurySweepTransactions).filter(TreasurySweepTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="TreasurySweepTransactions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[TreasurySweepTransactionsResponse]:
        """Search TreasurySweepTransactions by name/code fields."""
        query = self.db.query(TreasurySweepTransactions)
        if company_id and hasattr(TreasurySweepTransactions, "company_id"):
            query = query.filter(TreasurySweepTransactions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(TreasurySweepTransactions, f)]
        if search_fields:
            conditions = [getattr(TreasurySweepTransactions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [TreasurySweepTransactionsResponse.model_validate(i) for i in items]
