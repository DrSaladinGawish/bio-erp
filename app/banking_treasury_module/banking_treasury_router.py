"""
Banking Treasury Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.392354
Mounted at /api/v1/bnk
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .banking_treasury_schemas import *
from .banking_treasury_services import *

router = APIRouter(prefix="/bnk", tags=["Banking Treasury"])

@router.get("/bank_account_balancess", response_model=BankAccountBalancesListResponse)
def list_bank_account_balancess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BankAccountBalances records."""
    service = BankAccountBalancesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bank_account_balancess/{id}", response_model=BankAccountBalancesResponse)
def get_bank_account_balances(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BankAccountBalances by ID."""
    service = BankAccountBalancesService(db)
    return service.get(id)

@router.post("/bank_account_balancess", response_model=BankAccountBalancesResponse, status_code=status.HTTP_201_CREATED)
def create_bank_account_balances(
    data: BankAccountBalancesCreate,
    db: Session = Depends(get_db)
):
    """Create a new BankAccountBalances."""
    service = BankAccountBalancesService(db)
    return service.create(data)

@router.put("/bank_account_balancess/{id}", response_model=BankAccountBalancesResponse)
def update_bank_account_balances(
    id: UUID,
    data: BankAccountBalancesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BankAccountBalances."""
    service = BankAccountBalancesService(db)
    return service.update(id, data)

@router.delete("/bank_account_balancess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bank_account_balances(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BankAccountBalances (soft delete by default)."""
    service = BankAccountBalancesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bank_account_balancess/search", response_model=List[BankAccountBalancesResponse])
def search_bank_account_balancess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BankAccountBalances by name/code/description."""
    service = BankAccountBalancesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bank_accountss", response_model=BankAccountsListResponse)
def list_bank_accountss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BankAccounts records."""
    service = BankAccountsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bank_accountss/{id}", response_model=BankAccountsResponse)
def get_bank_accounts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BankAccounts by ID."""
    service = BankAccountsService(db)
    return service.get(id)

@router.post("/bank_accountss", response_model=BankAccountsResponse, status_code=status.HTTP_201_CREATED)
def create_bank_accounts(
    data: BankAccountsCreate,
    db: Session = Depends(get_db)
):
    """Create a new BankAccounts."""
    service = BankAccountsService(db)
    return service.create(data)

@router.put("/bank_accountss/{id}", response_model=BankAccountsResponse)
def update_bank_accounts(
    id: UUID,
    data: BankAccountsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BankAccounts."""
    service = BankAccountsService(db)
    return service.update(id, data)

@router.delete("/bank_accountss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bank_accounts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BankAccounts (soft delete by default)."""
    service = BankAccountsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bank_accountss/search", response_model=List[BankAccountsResponse])
def search_bank_accountss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BankAccounts by name/code/description."""
    service = BankAccountsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bank_accounts_gls", response_model=BankAccountsGlListResponse)
def list_bank_accounts_gls(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BankAccountsGl records."""
    service = BankAccountsGlService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bank_accounts_gls/{id}", response_model=BankAccountsGlResponse)
def get_bank_accounts_gl(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BankAccountsGl by ID."""
    service = BankAccountsGlService(db)
    return service.get(id)

@router.post("/bank_accounts_gls", response_model=BankAccountsGlResponse, status_code=status.HTTP_201_CREATED)
def create_bank_accounts_gl(
    data: BankAccountsGlCreate,
    db: Session = Depends(get_db)
):
    """Create a new BankAccountsGl."""
    service = BankAccountsGlService(db)
    return service.create(data)

@router.put("/bank_accounts_gls/{id}", response_model=BankAccountsGlResponse)
def update_bank_accounts_gl(
    id: UUID,
    data: BankAccountsGlUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BankAccountsGl."""
    service = BankAccountsGlService(db)
    return service.update(id, data)

@router.delete("/bank_accounts_gls/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bank_accounts_gl(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BankAccountsGl (soft delete by default)."""
    service = BankAccountsGlService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bank_accounts_gls/search", response_model=List[BankAccountsGlResponse])
def search_bank_accounts_gls(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BankAccountsGl by name/code/description."""
    service = BankAccountsGlService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bank_guaranteess", response_model=BankGuaranteesListResponse)
def list_bank_guaranteess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BankGuarantees records."""
    service = BankGuaranteesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bank_guaranteess/{id}", response_model=BankGuaranteesResponse)
def get_bank_guarantees(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BankGuarantees by ID."""
    service = BankGuaranteesService(db)
    return service.get(id)

@router.post("/bank_guaranteess", response_model=BankGuaranteesResponse, status_code=status.HTTP_201_CREATED)
def create_bank_guarantees(
    data: BankGuaranteesCreate,
    db: Session = Depends(get_db)
):
    """Create a new BankGuarantees."""
    service = BankGuaranteesService(db)
    return service.create(data)

@router.put("/bank_guaranteess/{id}", response_model=BankGuaranteesResponse)
def update_bank_guarantees(
    id: UUID,
    data: BankGuaranteesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BankGuarantees."""
    service = BankGuaranteesService(db)
    return service.update(id, data)

@router.delete("/bank_guaranteess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bank_guarantees(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BankGuarantees (soft delete by default)."""
    service = BankGuaranteesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bank_guaranteess/search", response_model=List[BankGuaranteesResponse])
def search_bank_guaranteess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BankGuarantees by name/code/description."""
    service = BankGuaranteesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bank_transactionss", response_model=BankTransactionsListResponse)
def list_bank_transactionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BankTransactions records."""
    service = BankTransactionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bank_transactionss/{id}", response_model=BankTransactionsResponse)
def get_bank_transactions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BankTransactions by ID."""
    service = BankTransactionsService(db)
    return service.get(id)

@router.post("/bank_transactionss", response_model=BankTransactionsResponse, status_code=status.HTTP_201_CREATED)
def create_bank_transactions(
    data: BankTransactionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new BankTransactions."""
    service = BankTransactionsService(db)
    return service.create(data)

@router.put("/bank_transactionss/{id}", response_model=BankTransactionsResponse)
def update_bank_transactions(
    id: UUID,
    data: BankTransactionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BankTransactions."""
    service = BankTransactionsService(db)
    return service.update(id, data)

@router.delete("/bank_transactionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bank_transactions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BankTransactions (soft delete by default)."""
    service = BankTransactionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bank_transactionss/search", response_model=List[BankTransactionsResponse])
def search_bank_transactionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BankTransactions by name/code/description."""
    service = BankTransactionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bankss", response_model=BanksListResponse)
def list_bankss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Banks records."""
    service = BanksService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bankss/{id}", response_model=BanksResponse)
def get_banks(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Banks by ID."""
    service = BanksService(db)
    return service.get(id)

@router.post("/bankss", response_model=BanksResponse, status_code=status.HTTP_201_CREATED)
def create_banks(
    data: BanksCreate,
    db: Session = Depends(get_db)
):
    """Create a new Banks."""
    service = BanksService(db)
    return service.create(data)

@router.put("/bankss/{id}", response_model=BanksResponse)
def update_banks(
    id: UUID,
    data: BanksUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Banks."""
    service = BanksService(db)
    return service.update(id, data)

@router.delete("/bankss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_banks(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Banks (soft delete by default)."""
    service = BanksService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bankss/search", response_model=List[BanksResponse])
def search_bankss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Banks by name/code/description."""
    service = BanksService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/lc_amendmentss", response_model=LcAmendmentsListResponse)
def list_lc_amendmentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all LcAmendments records."""
    service = LcAmendmentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/lc_amendmentss/{id}", response_model=LcAmendmentsResponse)
def get_lc_amendments(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get LcAmendments by ID."""
    service = LcAmendmentsService(db)
    return service.get(id)

@router.post("/lc_amendmentss", response_model=LcAmendmentsResponse, status_code=status.HTTP_201_CREATED)
def create_lc_amendments(
    data: LcAmendmentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new LcAmendments."""
    service = LcAmendmentsService(db)
    return service.create(data)

@router.put("/lc_amendmentss/{id}", response_model=LcAmendmentsResponse)
def update_lc_amendments(
    id: UUID,
    data: LcAmendmentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing LcAmendments."""
    service = LcAmendmentsService(db)
    return service.update(id, data)

@router.delete("/lc_amendmentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_lc_amendments(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a LcAmendments (soft delete by default)."""
    service = LcAmendmentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/lc_amendmentss/search", response_model=List[LcAmendmentsResponse])
def search_lc_amendmentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search LcAmendments by name/code/description."""
    service = LcAmendmentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/letter_of_credits", response_model=LetterOfCreditListResponse)
def list_letter_of_credits(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all LetterOfCredit records."""
    service = LetterOfCreditService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/letter_of_credits/{id}", response_model=LetterOfCreditResponse)
def get_letter_of_credit(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get LetterOfCredit by ID."""
    service = LetterOfCreditService(db)
    return service.get(id)

@router.post("/letter_of_credits", response_model=LetterOfCreditResponse, status_code=status.HTTP_201_CREATED)
def create_letter_of_credit(
    data: LetterOfCreditCreate,
    db: Session = Depends(get_db)
):
    """Create a new LetterOfCredit."""
    service = LetterOfCreditService(db)
    return service.create(data)

@router.put("/letter_of_credits/{id}", response_model=LetterOfCreditResponse)
def update_letter_of_credit(
    id: UUID,
    data: LetterOfCreditUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing LetterOfCredit."""
    service = LetterOfCreditService(db)
    return service.update(id, data)

@router.delete("/letter_of_credits/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_letter_of_credit(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a LetterOfCredit (soft delete by default)."""
    service = LetterOfCreditService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/letter_of_credits/search", response_model=List[LetterOfCreditResponse])
def search_letter_of_credits(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search LetterOfCredit by name/code/description."""
    service = LetterOfCreditService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/payment_approvalss", response_model=PaymentApprovalsListResponse)
def list_payment_approvalss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PaymentApprovals records."""
    service = PaymentApprovalsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/payment_approvalss/{id}", response_model=PaymentApprovalsResponse)
def get_payment_approvals(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PaymentApprovals by ID."""
    service = PaymentApprovalsService(db)
    return service.get(id)

@router.post("/payment_approvalss", response_model=PaymentApprovalsResponse, status_code=status.HTTP_201_CREATED)
def create_payment_approvals(
    data: PaymentApprovalsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PaymentApprovals."""
    service = PaymentApprovalsService(db)
    return service.create(data)

@router.put("/payment_approvalss/{id}", response_model=PaymentApprovalsResponse)
def update_payment_approvals(
    id: UUID,
    data: PaymentApprovalsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PaymentApprovals."""
    service = PaymentApprovalsService(db)
    return service.update(id, data)

@router.delete("/payment_approvalss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_payment_approvals(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PaymentApprovals (soft delete by default)."""
    service = PaymentApprovalsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/payment_approvalss/search", response_model=List[PaymentApprovalsResponse])
def search_payment_approvalss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PaymentApprovals by name/code/description."""
    service = PaymentApprovalsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/payment_batch_itemss", response_model=PaymentBatchItemsListResponse)
def list_payment_batch_itemss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PaymentBatchItems records."""
    service = PaymentBatchItemsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/payment_batch_itemss/{id}", response_model=PaymentBatchItemsResponse)
def get_payment_batch_items(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PaymentBatchItems by ID."""
    service = PaymentBatchItemsService(db)
    return service.get(id)

@router.post("/payment_batch_itemss", response_model=PaymentBatchItemsResponse, status_code=status.HTTP_201_CREATED)
def create_payment_batch_items(
    data: PaymentBatchItemsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PaymentBatchItems."""
    service = PaymentBatchItemsService(db)
    return service.create(data)

@router.put("/payment_batch_itemss/{id}", response_model=PaymentBatchItemsResponse)
def update_payment_batch_items(
    id: UUID,
    data: PaymentBatchItemsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PaymentBatchItems."""
    service = PaymentBatchItemsService(db)
    return service.update(id, data)

@router.delete("/payment_batch_itemss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_payment_batch_items(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PaymentBatchItems (soft delete by default)."""
    service = PaymentBatchItemsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/payment_batch_itemss/search", response_model=List[PaymentBatchItemsResponse])
def search_payment_batch_itemss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PaymentBatchItems by name/code/description."""
    service = PaymentBatchItemsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/payment_batchess", response_model=PaymentBatchesListResponse)
def list_payment_batchess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PaymentBatches records."""
    service = PaymentBatchesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/payment_batchess/{id}", response_model=PaymentBatchesResponse)
def get_payment_batches(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PaymentBatches by ID."""
    service = PaymentBatchesService(db)
    return service.get(id)

@router.post("/payment_batchess", response_model=PaymentBatchesResponse, status_code=status.HTTP_201_CREATED)
def create_payment_batches(
    data: PaymentBatchesCreate,
    db: Session = Depends(get_db)
):
    """Create a new PaymentBatches."""
    service = PaymentBatchesService(db)
    return service.create(data)

@router.put("/payment_batchess/{id}", response_model=PaymentBatchesResponse)
def update_payment_batches(
    id: UUID,
    data: PaymentBatchesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PaymentBatches."""
    service = PaymentBatchesService(db)
    return service.update(id, data)

@router.delete("/payment_batchess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_payment_batches(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PaymentBatches (soft delete by default)."""
    service = PaymentBatchesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/payment_batchess/search", response_model=List[PaymentBatchesResponse])
def search_payment_batchess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PaymentBatches by name/code/description."""
    service = PaymentBatchesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/reconciliation_batchess", response_model=ReconciliationBatchesListResponse)
def list_reconciliation_batchess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ReconciliationBatches records."""
    service = ReconciliationBatchesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/reconciliation_batchess/{id}", response_model=ReconciliationBatchesResponse)
def get_reconciliation_batches(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ReconciliationBatches by ID."""
    service = ReconciliationBatchesService(db)
    return service.get(id)

@router.post("/reconciliation_batchess", response_model=ReconciliationBatchesResponse, status_code=status.HTTP_201_CREATED)
def create_reconciliation_batches(
    data: ReconciliationBatchesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ReconciliationBatches."""
    service = ReconciliationBatchesService(db)
    return service.create(data)

@router.put("/reconciliation_batchess/{id}", response_model=ReconciliationBatchesResponse)
def update_reconciliation_batches(
    id: UUID,
    data: ReconciliationBatchesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ReconciliationBatches."""
    service = ReconciliationBatchesService(db)
    return service.update(id, data)

@router.delete("/reconciliation_batchess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_reconciliation_batches(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ReconciliationBatches (soft delete by default)."""
    service = ReconciliationBatchesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/reconciliation_batchess/search", response_model=List[ReconciliationBatchesResponse])
def search_reconciliation_batchess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ReconciliationBatches by name/code/description."""
    service = ReconciliationBatchesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/reconciliation_matchess", response_model=ReconciliationMatchesListResponse)
def list_reconciliation_matchess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ReconciliationMatches records."""
    service = ReconciliationMatchesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/reconciliation_matchess/{id}", response_model=ReconciliationMatchesResponse)
def get_reconciliation_matches(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ReconciliationMatches by ID."""
    service = ReconciliationMatchesService(db)
    return service.get(id)

@router.post("/reconciliation_matchess", response_model=ReconciliationMatchesResponse, status_code=status.HTTP_201_CREATED)
def create_reconciliation_matches(
    data: ReconciliationMatchesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ReconciliationMatches."""
    service = ReconciliationMatchesService(db)
    return service.create(data)

@router.put("/reconciliation_matchess/{id}", response_model=ReconciliationMatchesResponse)
def update_reconciliation_matches(
    id: UUID,
    data: ReconciliationMatchesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ReconciliationMatches."""
    service = ReconciliationMatchesService(db)
    return service.update(id, data)

@router.delete("/reconciliation_matchess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_reconciliation_matches(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ReconciliationMatches (soft delete by default)."""
    service = ReconciliationMatchesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/reconciliation_matchess/search", response_model=List[ReconciliationMatchesResponse])
def search_reconciliation_matchess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ReconciliationMatches by name/code/description."""
    service = ReconciliationMatchesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/reconciliation_ruless", response_model=ReconciliationRulesListResponse)
def list_reconciliation_ruless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ReconciliationRules records."""
    service = ReconciliationRulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/reconciliation_ruless/{id}", response_model=ReconciliationRulesResponse)
def get_reconciliation_rules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ReconciliationRules by ID."""
    service = ReconciliationRulesService(db)
    return service.get(id)

@router.post("/reconciliation_ruless", response_model=ReconciliationRulesResponse, status_code=status.HTTP_201_CREATED)
def create_reconciliation_rules(
    data: ReconciliationRulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ReconciliationRules."""
    service = ReconciliationRulesService(db)
    return service.create(data)

@router.put("/reconciliation_ruless/{id}", response_model=ReconciliationRulesResponse)
def update_reconciliation_rules(
    id: UUID,
    data: ReconciliationRulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ReconciliationRules."""
    service = ReconciliationRulesService(db)
    return service.update(id, data)

@router.delete("/reconciliation_ruless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_reconciliation_rules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ReconciliationRules (soft delete by default)."""
    service = ReconciliationRulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/reconciliation_ruless/search", response_model=List[ReconciliationRulesResponse])
def search_reconciliation_ruless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ReconciliationRules by name/code/description."""
    service = ReconciliationRulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/tax_codess", response_model=TaxCodesListResponse)
def list_tax_codess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TaxCodes records."""
    service = TaxCodesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/tax_codess/{id}", response_model=TaxCodesResponse)
def get_tax_codes(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TaxCodes by ID."""
    service = TaxCodesService(db)
    return service.get(id)

@router.post("/tax_codess", response_model=TaxCodesResponse, status_code=status.HTTP_201_CREATED)
def create_tax_codes(
    data: TaxCodesCreate,
    db: Session = Depends(get_db)
):
    """Create a new TaxCodes."""
    service = TaxCodesService(db)
    return service.create(data)

@router.put("/tax_codess/{id}", response_model=TaxCodesResponse)
def update_tax_codes(
    id: UUID,
    data: TaxCodesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TaxCodes."""
    service = TaxCodesService(db)
    return service.update(id, data)

@router.delete("/tax_codess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_tax_codes(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TaxCodes (soft delete by default)."""
    service = TaxCodesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/tax_codess/search", response_model=List[TaxCodesResponse])
def search_tax_codess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TaxCodes by name/code/description."""
    service = TaxCodesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/trade_cycless", response_model=TradeCyclesListResponse)
def list_trade_cycless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TradeCycles records."""
    service = TradeCyclesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/trade_cycless/{id}", response_model=TradeCyclesResponse)
def get_trade_cycles(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TradeCycles by ID."""
    service = TradeCyclesService(db)
    return service.get(id)

@router.post("/trade_cycless", response_model=TradeCyclesResponse, status_code=status.HTTP_201_CREATED)
def create_trade_cycles(
    data: TradeCyclesCreate,
    db: Session = Depends(get_db)
):
    """Create a new TradeCycles."""
    service = TradeCyclesService(db)
    return service.create(data)

@router.put("/trade_cycless/{id}", response_model=TradeCyclesResponse)
def update_trade_cycles(
    id: UUID,
    data: TradeCyclesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TradeCycles."""
    service = TradeCyclesService(db)
    return service.update(id, data)

@router.delete("/trade_cycless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_trade_cycles(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TradeCycles (soft delete by default)."""
    service = TradeCyclesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/trade_cycless/search", response_model=List[TradeCyclesResponse])
def search_trade_cycless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TradeCycles by name/code/description."""
    service = TradeCyclesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/trade_transactionss", response_model=TradeTransactionsListResponse)
def list_trade_transactionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TradeTransactions records."""
    service = TradeTransactionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/trade_transactionss/{id}", response_model=TradeTransactionsResponse)
def get_trade_transactions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TradeTransactions by ID."""
    service = TradeTransactionsService(db)
    return service.get(id)

@router.post("/trade_transactionss", response_model=TradeTransactionsResponse, status_code=status.HTTP_201_CREATED)
def create_trade_transactions(
    data: TradeTransactionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new TradeTransactions."""
    service = TradeTransactionsService(db)
    return service.create(data)

@router.put("/trade_transactionss/{id}", response_model=TradeTransactionsResponse)
def update_trade_transactions(
    id: UUID,
    data: TradeTransactionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TradeTransactions."""
    service = TradeTransactionsService(db)
    return service.update(id, data)

@router.delete("/trade_transactionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_trade_transactions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TradeTransactions (soft delete by default)."""
    service = TradeTransactionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/trade_transactionss/search", response_model=List[TradeTransactionsResponse])
def search_trade_transactionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TradeTransactions by name/code/description."""
    service = TradeTransactionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/transaction_type_categoriess", response_model=TransactionTypeCategoriesListResponse)
def list_transaction_type_categoriess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TransactionTypeCategories records."""
    service = TransactionTypeCategoriesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/transaction_type_categoriess/{id}", response_model=TransactionTypeCategoriesResponse)
def get_transaction_type_categories(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TransactionTypeCategories by ID."""
    service = TransactionTypeCategoriesService(db)
    return service.get(id)

@router.post("/transaction_type_categoriess", response_model=TransactionTypeCategoriesResponse, status_code=status.HTTP_201_CREATED)
def create_transaction_type_categories(
    data: TransactionTypeCategoriesCreate,
    db: Session = Depends(get_db)
):
    """Create a new TransactionTypeCategories."""
    service = TransactionTypeCategoriesService(db)
    return service.create(data)

@router.put("/transaction_type_categoriess/{id}", response_model=TransactionTypeCategoriesResponse)
def update_transaction_type_categories(
    id: UUID,
    data: TransactionTypeCategoriesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TransactionTypeCategories."""
    service = TransactionTypeCategoriesService(db)
    return service.update(id, data)

@router.delete("/transaction_type_categoriess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_transaction_type_categories(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TransactionTypeCategories (soft delete by default)."""
    service = TransactionTypeCategoriesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/transaction_type_categoriess/search", response_model=List[TransactionTypeCategoriesResponse])
def search_transaction_type_categoriess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TransactionTypeCategories by name/code/description."""
    service = TransactionTypeCategoriesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/transaction_typess", response_model=TransactionTypesListResponse)
def list_transaction_typess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TransactionTypes records."""
    service = TransactionTypesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/transaction_typess/{id}", response_model=TransactionTypesResponse)
def get_transaction_types(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TransactionTypes by ID."""
    service = TransactionTypesService(db)
    return service.get(id)

@router.post("/transaction_typess", response_model=TransactionTypesResponse, status_code=status.HTTP_201_CREATED)
def create_transaction_types(
    data: TransactionTypesCreate,
    db: Session = Depends(get_db)
):
    """Create a new TransactionTypes."""
    service = TransactionTypesService(db)
    return service.create(data)

@router.put("/transaction_typess/{id}", response_model=TransactionTypesResponse)
def update_transaction_types(
    id: UUID,
    data: TransactionTypesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TransactionTypes."""
    service = TransactionTypesService(db)
    return service.update(id, data)

@router.delete("/transaction_typess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_transaction_types(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TransactionTypes (soft delete by default)."""
    service = TransactionTypesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/transaction_typess/search", response_model=List[TransactionTypesResponse])
def search_transaction_typess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TransactionTypes by name/code/description."""
    service = TransactionTypesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/treasury_cash_pool_memberss", response_model=TreasuryCashPoolMembersListResponse)
def list_treasury_cash_pool_memberss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TreasuryCashPoolMembers records."""
    service = TreasuryCashPoolMembersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/treasury_cash_pool_memberss/{id}", response_model=TreasuryCashPoolMembersResponse)
def get_treasury_cash_pool_members(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TreasuryCashPoolMembers by ID."""
    service = TreasuryCashPoolMembersService(db)
    return service.get(id)

@router.post("/treasury_cash_pool_memberss", response_model=TreasuryCashPoolMembersResponse, status_code=status.HTTP_201_CREATED)
def create_treasury_cash_pool_members(
    data: TreasuryCashPoolMembersCreate,
    db: Session = Depends(get_db)
):
    """Create a new TreasuryCashPoolMembers."""
    service = TreasuryCashPoolMembersService(db)
    return service.create(data)

@router.put("/treasury_cash_pool_memberss/{id}", response_model=TreasuryCashPoolMembersResponse)
def update_treasury_cash_pool_members(
    id: UUID,
    data: TreasuryCashPoolMembersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TreasuryCashPoolMembers."""
    service = TreasuryCashPoolMembersService(db)
    return service.update(id, data)

@router.delete("/treasury_cash_pool_memberss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_treasury_cash_pool_members(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TreasuryCashPoolMembers (soft delete by default)."""
    service = TreasuryCashPoolMembersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/treasury_cash_pool_memberss/search", response_model=List[TreasuryCashPoolMembersResponse])
def search_treasury_cash_pool_memberss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TreasuryCashPoolMembers by name/code/description."""
    service = TreasuryCashPoolMembersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/treasury_cash_poolss", response_model=TreasuryCashPoolsListResponse)
def list_treasury_cash_poolss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TreasuryCashPools records."""
    service = TreasuryCashPoolsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/treasury_cash_poolss/{id}", response_model=TreasuryCashPoolsResponse)
def get_treasury_cash_pools(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TreasuryCashPools by ID."""
    service = TreasuryCashPoolsService(db)
    return service.get(id)

@router.post("/treasury_cash_poolss", response_model=TreasuryCashPoolsResponse, status_code=status.HTTP_201_CREATED)
def create_treasury_cash_pools(
    data: TreasuryCashPoolsCreate,
    db: Session = Depends(get_db)
):
    """Create a new TreasuryCashPools."""
    service = TreasuryCashPoolsService(db)
    return service.create(data)

@router.put("/treasury_cash_poolss/{id}", response_model=TreasuryCashPoolsResponse)
def update_treasury_cash_pools(
    id: UUID,
    data: TreasuryCashPoolsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TreasuryCashPools."""
    service = TreasuryCashPoolsService(db)
    return service.update(id, data)

@router.delete("/treasury_cash_poolss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_treasury_cash_pools(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TreasuryCashPools (soft delete by default)."""
    service = TreasuryCashPoolsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/treasury_cash_poolss/search", response_model=List[TreasuryCashPoolsResponse])
def search_treasury_cash_poolss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TreasuryCashPools by name/code/description."""
    service = TreasuryCashPoolsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/treasury_fx_exposuress", response_model=TreasuryFxExposuresListResponse)
def list_treasury_fx_exposuress(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TreasuryFxExposures records."""
    service = TreasuryFxExposuresService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/treasury_fx_exposuress/{id}", response_model=TreasuryFxExposuresResponse)
def get_treasury_fx_exposures(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TreasuryFxExposures by ID."""
    service = TreasuryFxExposuresService(db)
    return service.get(id)

@router.post("/treasury_fx_exposuress", response_model=TreasuryFxExposuresResponse, status_code=status.HTTP_201_CREATED)
def create_treasury_fx_exposures(
    data: TreasuryFxExposuresCreate,
    db: Session = Depends(get_db)
):
    """Create a new TreasuryFxExposures."""
    service = TreasuryFxExposuresService(db)
    return service.create(data)

@router.put("/treasury_fx_exposuress/{id}", response_model=TreasuryFxExposuresResponse)
def update_treasury_fx_exposures(
    id: UUID,
    data: TreasuryFxExposuresUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TreasuryFxExposures."""
    service = TreasuryFxExposuresService(db)
    return service.update(id, data)

@router.delete("/treasury_fx_exposuress/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_treasury_fx_exposures(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TreasuryFxExposures (soft delete by default)."""
    service = TreasuryFxExposuresService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/treasury_fx_exposuress/search", response_model=List[TreasuryFxExposuresResponse])
def search_treasury_fx_exposuress(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TreasuryFxExposures by name/code/description."""
    service = TreasuryFxExposuresService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/treasury_hedge_instrumentss", response_model=TreasuryHedgeInstrumentsListResponse)
def list_treasury_hedge_instrumentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TreasuryHedgeInstruments records."""
    service = TreasuryHedgeInstrumentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/treasury_hedge_instrumentss/{id}", response_model=TreasuryHedgeInstrumentsResponse)
def get_treasury_hedge_instruments(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TreasuryHedgeInstruments by ID."""
    service = TreasuryHedgeInstrumentsService(db)
    return service.get(id)

@router.post("/treasury_hedge_instrumentss", response_model=TreasuryHedgeInstrumentsResponse, status_code=status.HTTP_201_CREATED)
def create_treasury_hedge_instruments(
    data: TreasuryHedgeInstrumentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new TreasuryHedgeInstruments."""
    service = TreasuryHedgeInstrumentsService(db)
    return service.create(data)

@router.put("/treasury_hedge_instrumentss/{id}", response_model=TreasuryHedgeInstrumentsResponse)
def update_treasury_hedge_instruments(
    id: UUID,
    data: TreasuryHedgeInstrumentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TreasuryHedgeInstruments."""
    service = TreasuryHedgeInstrumentsService(db)
    return service.update(id, data)

@router.delete("/treasury_hedge_instrumentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_treasury_hedge_instruments(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TreasuryHedgeInstruments (soft delete by default)."""
    service = TreasuryHedgeInstrumentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/treasury_hedge_instrumentss/search", response_model=List[TreasuryHedgeInstrumentsResponse])
def search_treasury_hedge_instrumentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TreasuryHedgeInstruments by name/code/description."""
    service = TreasuryHedgeInstrumentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/treasury_liquidity_forecastss", response_model=TreasuryLiquidityForecastsListResponse)
def list_treasury_liquidity_forecastss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TreasuryLiquidityForecasts records."""
    service = TreasuryLiquidityForecastsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/treasury_liquidity_forecastss/{id}", response_model=TreasuryLiquidityForecastsResponse)
def get_treasury_liquidity_forecasts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TreasuryLiquidityForecasts by ID."""
    service = TreasuryLiquidityForecastsService(db)
    return service.get(id)

@router.post("/treasury_liquidity_forecastss", response_model=TreasuryLiquidityForecastsResponse, status_code=status.HTTP_201_CREATED)
def create_treasury_liquidity_forecasts(
    data: TreasuryLiquidityForecastsCreate,
    db: Session = Depends(get_db)
):
    """Create a new TreasuryLiquidityForecasts."""
    service = TreasuryLiquidityForecastsService(db)
    return service.create(data)

@router.put("/treasury_liquidity_forecastss/{id}", response_model=TreasuryLiquidityForecastsResponse)
def update_treasury_liquidity_forecasts(
    id: UUID,
    data: TreasuryLiquidityForecastsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TreasuryLiquidityForecasts."""
    service = TreasuryLiquidityForecastsService(db)
    return service.update(id, data)

@router.delete("/treasury_liquidity_forecastss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_treasury_liquidity_forecasts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TreasuryLiquidityForecasts (soft delete by default)."""
    service = TreasuryLiquidityForecastsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/treasury_liquidity_forecastss/search", response_model=List[TreasuryLiquidityForecastsResponse])
def search_treasury_liquidity_forecastss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TreasuryLiquidityForecasts by name/code/description."""
    service = TreasuryLiquidityForecastsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/treasury_sweep_transactionss", response_model=TreasurySweepTransactionsListResponse)
def list_treasury_sweep_transactionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all TreasurySweepTransactions records."""
    service = TreasurySweepTransactionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/treasury_sweep_transactionss/{id}", response_model=TreasurySweepTransactionsResponse)
def get_treasury_sweep_transactions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get TreasurySweepTransactions by ID."""
    service = TreasurySweepTransactionsService(db)
    return service.get(id)

@router.post("/treasury_sweep_transactionss", response_model=TreasurySweepTransactionsResponse, status_code=status.HTTP_201_CREATED)
def create_treasury_sweep_transactions(
    data: TreasurySweepTransactionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new TreasurySweepTransactions."""
    service = TreasurySweepTransactionsService(db)
    return service.create(data)

@router.put("/treasury_sweep_transactionss/{id}", response_model=TreasurySweepTransactionsResponse)
def update_treasury_sweep_transactions(
    id: UUID,
    data: TreasurySweepTransactionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing TreasurySweepTransactions."""
    service = TreasurySweepTransactionsService(db)
    return service.update(id, data)

@router.delete("/treasury_sweep_transactionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_treasury_sweep_transactions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a TreasurySweepTransactions (soft delete by default)."""
    service = TreasurySweepTransactionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/treasury_sweep_transactionss/search", response_model=List[TreasurySweepTransactionsResponse])
def search_treasury_sweep_transactionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search TreasurySweepTransactions by name/code/description."""
    service = TreasurySweepTransactionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_banking_treasury_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get Banking Treasury dashboard summary."""
    return {
        "module": "banking_treasury",
        "company_id": str(company_id),
        "tables": 25,
        "metrics": {}
    }
