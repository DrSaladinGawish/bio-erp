"""
Ap Ar Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.322056
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .ap_ar_models import *
from .ap_ar_schemas import *

class ExpenseClaimLinesService:
    """CRUD service for ExpenseClaimLines."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ExpenseClaimLinesCreate, created_by: Optional[UUID] = None) -> ExpenseClaimLinesResponse:
        """Create a new ExpenseClaimLines record."""
        db_obj = ExpenseClaimLines(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ExpenseClaimLinesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ExpenseClaimLinesResponse]:
        """Get ExpenseClaimLines by primary key."""
        db_obj = self.db.query(ExpenseClaimLines).filter(ExpenseClaimLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ExpenseClaimLines not found")
        return ExpenseClaimLinesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ExpenseClaimLinesListResponse:
        """List ExpenseClaimLines records with pagination and filtering."""
        query = self.db.query(ExpenseClaimLines)
        if company_id and hasattr(ExpenseClaimLines, "company_id"):
            query = query.filter(ExpenseClaimLines.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ExpenseClaimLines, key) and value is not None:
                    query = query.filter(getattr(ExpenseClaimLines, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ExpenseClaimLines, order_by, ExpenseClaimLines.created_at)))
        else:
            query = query.order_by(getattr(ExpenseClaimLines, order_by, ExpenseClaimLines.created_at))
        items = query.offset(skip).limit(limit).all()
        return ExpenseClaimLinesListResponse(
            items=[ExpenseClaimLinesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ExpenseClaimLinesUpdate,
               updated_by: Optional[UUID] = None) -> ExpenseClaimLinesResponse:
        """Update an existing ExpenseClaimLines record."""
        db_obj = self.db.query(ExpenseClaimLines).filter(ExpenseClaimLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ExpenseClaimLines not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ExpenseClaimLinesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ExpenseClaimLines record. Soft delete by default."""
        db_obj = self.db.query(ExpenseClaimLines).filter(ExpenseClaimLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ExpenseClaimLines not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ExpenseClaimLinesResponse]:
        """Search ExpenseClaimLines by name/code fields."""
        query = self.db.query(ExpenseClaimLines)
        if company_id and hasattr(ExpenseClaimLines, "company_id"):
            query = query.filter(ExpenseClaimLines.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ExpenseClaimLines, f)]
        if search_fields:
            conditions = [getattr(ExpenseClaimLines, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ExpenseClaimLinesResponse.model_validate(i) for i in items]

class ExpenseClaimsService:
    """CRUD service for ExpenseClaims."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ExpenseClaimsCreate, created_by: Optional[UUID] = None) -> ExpenseClaimsResponse:
        """Create a new ExpenseClaims record."""
        db_obj = ExpenseClaims(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ExpenseClaimsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ExpenseClaimsResponse]:
        """Get ExpenseClaims by primary key."""
        db_obj = self.db.query(ExpenseClaims).filter(ExpenseClaims.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ExpenseClaims not found")
        return ExpenseClaimsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ExpenseClaimsListResponse:
        """List ExpenseClaims records with pagination and filtering."""
        query = self.db.query(ExpenseClaims)
        if company_id and hasattr(ExpenseClaims, "company_id"):
            query = query.filter(ExpenseClaims.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ExpenseClaims, key) and value is not None:
                    query = query.filter(getattr(ExpenseClaims, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ExpenseClaims, order_by, ExpenseClaims.created_at)))
        else:
            query = query.order_by(getattr(ExpenseClaims, order_by, ExpenseClaims.created_at))
        items = query.offset(skip).limit(limit).all()
        return ExpenseClaimsListResponse(
            items=[ExpenseClaimsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ExpenseClaimsUpdate,
               updated_by: Optional[UUID] = None) -> ExpenseClaimsResponse:
        """Update an existing ExpenseClaims record."""
        db_obj = self.db.query(ExpenseClaims).filter(ExpenseClaims.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ExpenseClaims not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ExpenseClaimsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ExpenseClaims record. Soft delete by default."""
        db_obj = self.db.query(ExpenseClaims).filter(ExpenseClaims.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ExpenseClaims not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ExpenseClaimsResponse]:
        """Search ExpenseClaims by name/code fields."""
        query = self.db.query(ExpenseClaims)
        if company_id and hasattr(ExpenseClaims, "company_id"):
            query = query.filter(ExpenseClaims.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ExpenseClaims, f)]
        if search_fields:
            conditions = [getattr(ExpenseClaims, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ExpenseClaimsResponse.model_validate(i) for i in items]

class InvoiceLinesService:
    """CRUD service for InvoiceLines."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: InvoiceLinesCreate, created_by: Optional[UUID] = None) -> InvoiceLinesResponse:
        """Create a new InvoiceLines record."""
        db_obj = InvoiceLines(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return InvoiceLinesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[InvoiceLinesResponse]:
        """Get InvoiceLines by primary key."""
        db_obj = self.db.query(InvoiceLines).filter(InvoiceLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="InvoiceLines not found")
        return InvoiceLinesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> InvoiceLinesListResponse:
        """List InvoiceLines records with pagination and filtering."""
        query = self.db.query(InvoiceLines)
        if company_id and hasattr(InvoiceLines, "company_id"):
            query = query.filter(InvoiceLines.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(InvoiceLines, key) and value is not None:
                    query = query.filter(getattr(InvoiceLines, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(InvoiceLines, order_by, InvoiceLines.created_at)))
        else:
            query = query.order_by(getattr(InvoiceLines, order_by, InvoiceLines.created_at))
        items = query.offset(skip).limit(limit).all()
        return InvoiceLinesListResponse(
            items=[InvoiceLinesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: InvoiceLinesUpdate,
               updated_by: Optional[UUID] = None) -> InvoiceLinesResponse:
        """Update an existing InvoiceLines record."""
        db_obj = self.db.query(InvoiceLines).filter(InvoiceLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="InvoiceLines not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return InvoiceLinesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a InvoiceLines record. Soft delete by default."""
        db_obj = self.db.query(InvoiceLines).filter(InvoiceLines.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="InvoiceLines not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[InvoiceLinesResponse]:
        """Search InvoiceLines by name/code fields."""
        query = self.db.query(InvoiceLines)
        if company_id and hasattr(InvoiceLines, "company_id"):
            query = query.filter(InvoiceLines.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(InvoiceLines, f)]
        if search_fields:
            conditions = [getattr(InvoiceLines, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [InvoiceLinesResponse.model_validate(i) for i in items]

class InvoicesService:
    """CRUD service for Invoices."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: InvoicesCreate, created_by: Optional[UUID] = None) -> InvoicesResponse:
        """Create a new Invoices record."""
        db_obj = Invoices(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return InvoicesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[InvoicesResponse]:
        """Get Invoices by primary key."""
        db_obj = self.db.query(Invoices).filter(Invoices.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Invoices not found")
        return InvoicesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> InvoicesListResponse:
        """List Invoices records with pagination and filtering."""
        query = self.db.query(Invoices)
        if company_id and hasattr(Invoices, "company_id"):
            query = query.filter(Invoices.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Invoices, key) and value is not None:
                    query = query.filter(getattr(Invoices, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Invoices, order_by, Invoices.created_at)))
        else:
            query = query.order_by(getattr(Invoices, order_by, Invoices.created_at))
        items = query.offset(skip).limit(limit).all()
        return InvoicesListResponse(
            items=[InvoicesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: InvoicesUpdate,
               updated_by: Optional[UUID] = None) -> InvoicesResponse:
        """Update an existing Invoices record."""
        db_obj = self.db.query(Invoices).filter(Invoices.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Invoices not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return InvoicesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Invoices record. Soft delete by default."""
        db_obj = self.db.query(Invoices).filter(Invoices.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Invoices not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[InvoicesResponse]:
        """Search Invoices by name/code fields."""
        query = self.db.query(Invoices)
        if company_id and hasattr(Invoices, "company_id"):
            query = query.filter(Invoices.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Invoices, f)]
        if search_fields:
            conditions = [getattr(Invoices, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [InvoicesResponse.model_validate(i) for i in items]

class PaymentAllocationsService:
    """CRUD service for PaymentAllocations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PaymentAllocationsCreate, created_by: Optional[UUID] = None) -> PaymentAllocationsResponse:
        """Create a new PaymentAllocations record."""
        db_obj = PaymentAllocations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentAllocationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PaymentAllocationsResponse]:
        """Get PaymentAllocations by primary key."""
        db_obj = self.db.query(PaymentAllocations).filter(PaymentAllocations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentAllocations not found")
        return PaymentAllocationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PaymentAllocationsListResponse:
        """List PaymentAllocations records with pagination and filtering."""
        query = self.db.query(PaymentAllocations)
        if company_id and hasattr(PaymentAllocations, "company_id"):
            query = query.filter(PaymentAllocations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PaymentAllocations, key) and value is not None:
                    query = query.filter(getattr(PaymentAllocations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PaymentAllocations, order_by, PaymentAllocations.created_at)))
        else:
            query = query.order_by(getattr(PaymentAllocations, order_by, PaymentAllocations.created_at))
        items = query.offset(skip).limit(limit).all()
        return PaymentAllocationsListResponse(
            items=[PaymentAllocationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PaymentAllocationsUpdate,
               updated_by: Optional[UUID] = None) -> PaymentAllocationsResponse:
        """Update an existing PaymentAllocations record."""
        db_obj = self.db.query(PaymentAllocations).filter(PaymentAllocations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentAllocations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentAllocationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PaymentAllocations record. Soft delete by default."""
        db_obj = self.db.query(PaymentAllocations).filter(PaymentAllocations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PaymentAllocations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PaymentAllocationsResponse]:
        """Search PaymentAllocations by name/code fields."""
        query = self.db.query(PaymentAllocations)
        if company_id and hasattr(PaymentAllocations, "company_id"):
            query = query.filter(PaymentAllocations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PaymentAllocations, f)]
        if search_fields:
            conditions = [getattr(PaymentAllocations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PaymentAllocationsResponse.model_validate(i) for i in items]

class PaymentsService:
    """CRUD service for Payments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PaymentsCreate, created_by: Optional[UUID] = None) -> PaymentsResponse:
        """Create a new Payments record."""
        db_obj = Payments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PaymentsResponse]:
        """Get Payments by primary key."""
        db_obj = self.db.query(Payments).filter(Payments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Payments not found")
        return PaymentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PaymentsListResponse:
        """List Payments records with pagination and filtering."""
        query = self.db.query(Payments)
        if company_id and hasattr(Payments, "company_id"):
            query = query.filter(Payments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(Payments, key) and value is not None:
                    query = query.filter(getattr(Payments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(Payments, order_by, Payments.created_at)))
        else:
            query = query.order_by(getattr(Payments, order_by, Payments.created_at))
        items = query.offset(skip).limit(limit).all()
        return PaymentsListResponse(
            items=[PaymentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PaymentsUpdate,
               updated_by: Optional[UUID] = None) -> PaymentsResponse:
        """Update an existing Payments record."""
        db_obj = self.db.query(Payments).filter(Payments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Payments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PaymentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a Payments record. Soft delete by default."""
        db_obj = self.db.query(Payments).filter(Payments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Payments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PaymentsResponse]:
        """Search Payments by name/code fields."""
        query = self.db.query(Payments)
        if company_id and hasattr(Payments, "company_id"):
            query = query.filter(Payments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(Payments, f)]
        if search_fields:
            conditions = [getattr(Payments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PaymentsResponse.model_validate(i) for i in items]
