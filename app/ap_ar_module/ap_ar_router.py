"""
Ap Ar Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.322500
Mounted at /api/v1/apa
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .ap_ar_schemas import *
from .ap_ar_services import *

router = APIRouter(prefix="/apa", tags=["Ap Ar"])

@router.get("/expense_claim_liness", response_model=ExpenseClaimLinesListResponse)
def list_expense_claim_liness(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ExpenseClaimLines records."""
    service = ExpenseClaimLinesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/expense_claim_liness/{id}", response_model=ExpenseClaimLinesResponse)
def get_expense_claim_lines(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ExpenseClaimLines by ID."""
    service = ExpenseClaimLinesService(db)
    return service.get(id)

@router.post("/expense_claim_liness", response_model=ExpenseClaimLinesResponse, status_code=status.HTTP_201_CREATED)
def create_expense_claim_lines(
    data: ExpenseClaimLinesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ExpenseClaimLines."""
    service = ExpenseClaimLinesService(db)
    return service.create(data)

@router.put("/expense_claim_liness/{id}", response_model=ExpenseClaimLinesResponse)
def update_expense_claim_lines(
    id: UUID,
    data: ExpenseClaimLinesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ExpenseClaimLines."""
    service = ExpenseClaimLinesService(db)
    return service.update(id, data)

@router.delete("/expense_claim_liness/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_expense_claim_lines(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ExpenseClaimLines (soft delete by default)."""
    service = ExpenseClaimLinesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/expense_claim_liness/search", response_model=List[ExpenseClaimLinesResponse])
def search_expense_claim_liness(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ExpenseClaimLines by name/code/description."""
    service = ExpenseClaimLinesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/expense_claimss", response_model=ExpenseClaimsListResponse)
def list_expense_claimss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ExpenseClaims records."""
    service = ExpenseClaimsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/expense_claimss/{id}", response_model=ExpenseClaimsResponse)
def get_expense_claims(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ExpenseClaims by ID."""
    service = ExpenseClaimsService(db)
    return service.get(id)

@router.post("/expense_claimss", response_model=ExpenseClaimsResponse, status_code=status.HTTP_201_CREATED)
def create_expense_claims(
    data: ExpenseClaimsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ExpenseClaims."""
    service = ExpenseClaimsService(db)
    return service.create(data)

@router.put("/expense_claimss/{id}", response_model=ExpenseClaimsResponse)
def update_expense_claims(
    id: UUID,
    data: ExpenseClaimsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ExpenseClaims."""
    service = ExpenseClaimsService(db)
    return service.update(id, data)

@router.delete("/expense_claimss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_expense_claims(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ExpenseClaims (soft delete by default)."""
    service = ExpenseClaimsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/expense_claimss/search", response_model=List[ExpenseClaimsResponse])
def search_expense_claimss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ExpenseClaims by name/code/description."""
    service = ExpenseClaimsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/invoice_liness", response_model=InvoiceLinesListResponse)
def list_invoice_liness(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all InvoiceLines records."""
    service = InvoiceLinesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/invoice_liness/{id}", response_model=InvoiceLinesResponse)
def get_invoice_lines(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get InvoiceLines by ID."""
    service = InvoiceLinesService(db)
    return service.get(id)

@router.post("/invoice_liness", response_model=InvoiceLinesResponse, status_code=status.HTTP_201_CREATED)
def create_invoice_lines(
    data: InvoiceLinesCreate,
    db: Session = Depends(get_db)
):
    """Create a new InvoiceLines."""
    service = InvoiceLinesService(db)
    return service.create(data)

@router.put("/invoice_liness/{id}", response_model=InvoiceLinesResponse)
def update_invoice_lines(
    id: UUID,
    data: InvoiceLinesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing InvoiceLines."""
    service = InvoiceLinesService(db)
    return service.update(id, data)

@router.delete("/invoice_liness/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_invoice_lines(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a InvoiceLines (soft delete by default)."""
    service = InvoiceLinesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/invoice_liness/search", response_model=List[InvoiceLinesResponse])
def search_invoice_liness(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search InvoiceLines by name/code/description."""
    service = InvoiceLinesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/invoicess", response_model=InvoicesListResponse)
def list_invoicess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Invoices records."""
    service = InvoicesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/invoicess/{id}", response_model=InvoicesResponse)
def get_invoices(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Invoices by ID."""
    service = InvoicesService(db)
    return service.get(id)

@router.post("/invoicess", response_model=InvoicesResponse, status_code=status.HTTP_201_CREATED)
def create_invoices(
    data: InvoicesCreate,
    db: Session = Depends(get_db)
):
    """Create a new Invoices."""
    service = InvoicesService(db)
    return service.create(data)

@router.put("/invoicess/{id}", response_model=InvoicesResponse)
def update_invoices(
    id: UUID,
    data: InvoicesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Invoices."""
    service = InvoicesService(db)
    return service.update(id, data)

@router.delete("/invoicess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_invoices(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Invoices (soft delete by default)."""
    service = InvoicesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/invoicess/search", response_model=List[InvoicesResponse])
def search_invoicess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Invoices by name/code/description."""
    service = InvoicesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/payment_allocationss", response_model=PaymentAllocationsListResponse)
def list_payment_allocationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PaymentAllocations records."""
    service = PaymentAllocationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/payment_allocationss/{id}", response_model=PaymentAllocationsResponse)
def get_payment_allocations(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PaymentAllocations by ID."""
    service = PaymentAllocationsService(db)
    return service.get(id)

@router.post("/payment_allocationss", response_model=PaymentAllocationsResponse, status_code=status.HTTP_201_CREATED)
def create_payment_allocations(
    data: PaymentAllocationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PaymentAllocations."""
    service = PaymentAllocationsService(db)
    return service.create(data)

@router.put("/payment_allocationss/{id}", response_model=PaymentAllocationsResponse)
def update_payment_allocations(
    id: UUID,
    data: PaymentAllocationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PaymentAllocations."""
    service = PaymentAllocationsService(db)
    return service.update(id, data)

@router.delete("/payment_allocationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_payment_allocations(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PaymentAllocations (soft delete by default)."""
    service = PaymentAllocationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/payment_allocationss/search", response_model=List[PaymentAllocationsResponse])
def search_payment_allocationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PaymentAllocations by name/code/description."""
    service = PaymentAllocationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/paymentss", response_model=PaymentsListResponse)
def list_paymentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Payments records."""
    service = PaymentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/paymentss/{id}", response_model=PaymentsResponse)
def get_payments(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Payments by ID."""
    service = PaymentsService(db)
    return service.get(id)

@router.post("/paymentss", response_model=PaymentsResponse, status_code=status.HTTP_201_CREATED)
def create_payments(
    data: PaymentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new Payments."""
    service = PaymentsService(db)
    return service.create(data)

@router.put("/paymentss/{id}", response_model=PaymentsResponse)
def update_payments(
    id: UUID,
    data: PaymentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Payments."""
    service = PaymentsService(db)
    return service.update(id, data)

@router.delete("/paymentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_payments(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Payments (soft delete by default)."""
    service = PaymentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/paymentss/search", response_model=List[PaymentsResponse])
def search_paymentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Payments by name/code/description."""
    service = PaymentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_ap_ar_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get Ap Ar dashboard summary."""
    return {
        "module": "ap_ar",
        "company_id": str(company_id),
        "tables": 6,
        "metrics": {}
    }
