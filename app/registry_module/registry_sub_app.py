"""
Registry Module — FastAPI Sub-App
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:41.044111
Mounted at /api/v1/reg in Bio-ERP main.py
"""
from __future__ import annotations

from fastapi import FastAPI

from .registry_router import router as registry_router

def create_registry_app() -> FastAPI:
    """Factory function to create the registry sub-application."""
    app = FastAPI(
        title="Registry Module",
        description="ERP Registry module — 4 tables",
        version="3.0.0",
        docs_url="/docs",
        redoc_url="/redoc",
        openapi_url="/openapi.json"
    )
    app.include_router(registry_router)

    @app.get("/health")
    async def health_check():
        return {
            "status": "healthy",
            "module": "registry",
            "version": "3.0.0",
            "tables": 4,
            "database": "SQL Server 2022"
        }

    @app.get("/")
    async def root():
        return {
            "module": "registry",
            "version": "3.0.0",
            "endpoints": "/docs"
        }

    return app
