"""
Foundation Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.529195
Mounted at /api/v1/fou
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .foundation_schemas import *
from .foundation_services import *

router = APIRouter(prefix="/fou", tags=["Foundation"])

@router.get("/companiess", response_model=CompaniesListResponse)
def list_companiess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Companies records."""
    service = CompaniesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/companiess/{id}", response_model=CompaniesResponse)
def get_companies(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Companies by ID."""
    service = CompaniesService(db)
    return service.get(id)

@router.post("/companiess", response_model=CompaniesResponse, status_code=status.HTTP_201_CREATED)
def create_companies(
    data: CompaniesCreate,
    db: Session = Depends(get_db)
):
    """Create a new Companies."""
    service = CompaniesService(db)
    return service.create(data)

@router.put("/companiess/{id}", response_model=CompaniesResponse)
def update_companies(
    id: UUID,
    data: CompaniesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Companies."""
    service = CompaniesService(db)
    return service.update(id, data)

@router.delete("/companiess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_companies(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Companies (soft delete by default)."""
    service = CompaniesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/companiess/search", response_model=List[CompaniesResponse])
def search_companiess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Companies by name/code/description."""
    service = CompaniesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/org_unitss", response_model=OrgUnitsListResponse)
def list_org_unitss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all OrgUnits records."""
    service = OrgUnitsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/org_unitss/{id}", response_model=OrgUnitsResponse)
def get_org_units(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get OrgUnits by ID."""
    service = OrgUnitsService(db)
    return service.get(id)

@router.post("/org_unitss", response_model=OrgUnitsResponse, status_code=status.HTTP_201_CREATED)
def create_org_units(
    data: OrgUnitsCreate,
    db: Session = Depends(get_db)
):
    """Create a new OrgUnits."""
    service = OrgUnitsService(db)
    return service.create(data)

@router.put("/org_unitss/{id}", response_model=OrgUnitsResponse)
def update_org_units(
    id: UUID,
    data: OrgUnitsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing OrgUnits."""
    service = OrgUnitsService(db)
    return service.update(id, data)

@router.delete("/org_unitss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_org_units(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a OrgUnits (soft delete by default)."""
    service = OrgUnitsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/org_unitss/search", response_model=List[OrgUnitsResponse])
def search_org_unitss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search OrgUnits by name/code/description."""
    service = OrgUnitsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/roless", response_model=RolesListResponse)
def list_roless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Roles records."""
    service = RolesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/roless/{id}", response_model=RolesResponse)
def get_roles(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Roles by ID."""
    service = RolesService(db)
    return service.get(id)

@router.post("/roless", response_model=RolesResponse, status_code=status.HTTP_201_CREATED)
def create_roles(
    data: RolesCreate,
    db: Session = Depends(get_db)
):
    """Create a new Roles."""
    service = RolesService(db)
    return service.create(data)

@router.put("/roless/{id}", response_model=RolesResponse)
def update_roles(
    id: UUID,
    data: RolesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Roles."""
    service = RolesService(db)
    return service.update(id, data)

@router.delete("/roless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_roles(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Roles (soft delete by default)."""
    service = RolesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/roless/search", response_model=List[RolesResponse])
def search_roless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Roles by name/code/description."""
    service = RolesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/userss", response_model=UsersListResponse)
def list_userss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all Users records."""
    service = UsersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/userss/{id}", response_model=UsersResponse)
def get_users(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get Users by ID."""
    service = UsersService(db)
    return service.get(id)

@router.post("/userss", response_model=UsersResponse, status_code=status.HTTP_201_CREATED)
def create_users(
    data: UsersCreate,
    db: Session = Depends(get_db)
):
    """Create a new Users."""
    service = UsersService(db)
    return service.create(data)

@router.put("/userss/{id}", response_model=UsersResponse)
def update_users(
    id: UUID,
    data: UsersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing Users."""
    service = UsersService(db)
    return service.update(id, data)

@router.delete("/userss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_users(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a Users (soft delete by default)."""
    service = UsersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/userss/search", response_model=List[UsersResponse])
def search_userss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search Users by name/code/description."""
    service = UsersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_foundation_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get Foundation dashboard summary."""
    return {
        "module": "foundation",
        "company_id": str(company_id),
        "tables": 4,
        "metrics": {}
    }
