"""
Integration Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.605656
Mounted at /api/v1/int
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .integration_schemas import *
from .integration_services import *

router = APIRouter(prefix="/int", tags=["Integration"])

@router.get("/audit_logs", response_model=AuditLogListResponse)
def list_audit_logs(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all AuditLog records."""
    service = AuditLogService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/audit_logs/{id}", response_model=AuditLogResponse)
def get_audit_log(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get AuditLog by ID."""
    service = AuditLogService(db)
    return service.get(id)

@router.post("/audit_logs", response_model=AuditLogResponse, status_code=status.HTTP_201_CREATED)
def create_audit_log(
    data: AuditLogCreate,
    db: Session = Depends(get_db)
):
    """Create a new AuditLog."""
    service = AuditLogService(db)
    return service.create(data)

@router.put("/audit_logs/{id}", response_model=AuditLogResponse)
def update_audit_log(
    id: UUID,
    data: AuditLogUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing AuditLog."""
    service = AuditLogService(db)
    return service.update(id, data)

@router.delete("/audit_logs/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_audit_log(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a AuditLog (soft delete by default)."""
    service = AuditLogService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/audit_logs/search", response_model=List[AuditLogResponse])
def search_audit_logs(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search AuditLog by name/code/description."""
    service = AuditLogService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/integration_endpointss", response_model=IntegrationEndpointsListResponse)
def list_integration_endpointss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all IntegrationEndpoints records."""
    service = IntegrationEndpointsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/integration_endpointss/{id}", response_model=IntegrationEndpointsResponse)
def get_integration_endpoints(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get IntegrationEndpoints by ID."""
    service = IntegrationEndpointsService(db)
    return service.get(id)

@router.post("/integration_endpointss", response_model=IntegrationEndpointsResponse, status_code=status.HTTP_201_CREATED)
def create_integration_endpoints(
    data: IntegrationEndpointsCreate,
    db: Session = Depends(get_db)
):
    """Create a new IntegrationEndpoints."""
    service = IntegrationEndpointsService(db)
    return service.create(data)

@router.put("/integration_endpointss/{id}", response_model=IntegrationEndpointsResponse)
def update_integration_endpoints(
    id: UUID,
    data: IntegrationEndpointsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing IntegrationEndpoints."""
    service = IntegrationEndpointsService(db)
    return service.update(id, data)

@router.delete("/integration_endpointss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_integration_endpoints(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a IntegrationEndpoints (soft delete by default)."""
    service = IntegrationEndpointsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/integration_endpointss/search", response_model=List[IntegrationEndpointsResponse])
def search_integration_endpointss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search IntegrationEndpoints by name/code/description."""
    service = IntegrationEndpointsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/integration_logss", response_model=IntegrationLogsListResponse)
def list_integration_logss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all IntegrationLogs records."""
    service = IntegrationLogsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/integration_logss/{id}", response_model=IntegrationLogsResponse)
def get_integration_logs(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get IntegrationLogs by ID."""
    service = IntegrationLogsService(db)
    return service.get(id)

@router.post("/integration_logss", response_model=IntegrationLogsResponse, status_code=status.HTTP_201_CREATED)
def create_integration_logs(
    data: IntegrationLogsCreate,
    db: Session = Depends(get_db)
):
    """Create a new IntegrationLogs."""
    service = IntegrationLogsService(db)
    return service.create(data)

@router.put("/integration_logss/{id}", response_model=IntegrationLogsResponse)
def update_integration_logs(
    id: UUID,
    data: IntegrationLogsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing IntegrationLogs."""
    service = IntegrationLogsService(db)
    return service.update(id, data)

@router.delete("/integration_logss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_integration_logs(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a IntegrationLogs (soft delete by default)."""
    service = IntegrationLogsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/integration_logss/search", response_model=List[IntegrationLogsResponse])
def search_integration_logss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search IntegrationLogs by name/code/description."""
    service = IntegrationLogsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wf_audits", response_model=WfAuditListResponse)
def list_wf_audits(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WfAudit records."""
    service = WfAuditService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wf_audits/{id}", response_model=WfAuditResponse)
def get_wf_audit(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WfAudit by ID."""
    service = WfAuditService(db)
    return service.get(id)

@router.post("/wf_audits", response_model=WfAuditResponse, status_code=status.HTTP_201_CREATED)
def create_wf_audit(
    data: WfAuditCreate,
    db: Session = Depends(get_db)
):
    """Create a new WfAudit."""
    service = WfAuditService(db)
    return service.create(data)

@router.put("/wf_audits/{id}", response_model=WfAuditResponse)
def update_wf_audit(
    id: UUID,
    data: WfAuditUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WfAudit."""
    service = WfAuditService(db)
    return service.update(id, data)

@router.delete("/wf_audits/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wf_audit(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WfAudit (soft delete by default)."""
    service = WfAuditService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wf_audits/search", response_model=List[WfAuditResponse])
def search_wf_audits(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WfAudit by name/code/description."""
    service = WfAuditService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wf_definitionss", response_model=WfDefinitionsListResponse)
def list_wf_definitionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WfDefinitions records."""
    service = WfDefinitionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wf_definitionss/{id}", response_model=WfDefinitionsResponse)
def get_wf_definitions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WfDefinitions by ID."""
    service = WfDefinitionsService(db)
    return service.get(id)

@router.post("/wf_definitionss", response_model=WfDefinitionsResponse, status_code=status.HTTP_201_CREATED)
def create_wf_definitions(
    data: WfDefinitionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new WfDefinitions."""
    service = WfDefinitionsService(db)
    return service.create(data)

@router.put("/wf_definitionss/{id}", response_model=WfDefinitionsResponse)
def update_wf_definitions(
    id: UUID,
    data: WfDefinitionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WfDefinitions."""
    service = WfDefinitionsService(db)
    return service.update(id, data)

@router.delete("/wf_definitionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wf_definitions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WfDefinitions (soft delete by default)."""
    service = WfDefinitionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wf_definitionss/search", response_model=List[WfDefinitionsResponse])
def search_wf_definitionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WfDefinitions by name/code/description."""
    service = WfDefinitionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wf_instancess", response_model=WfInstancesListResponse)
def list_wf_instancess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WfInstances records."""
    service = WfInstancesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wf_instancess/{id}", response_model=WfInstancesResponse)
def get_wf_instances(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WfInstances by ID."""
    service = WfInstancesService(db)
    return service.get(id)

@router.post("/wf_instancess", response_model=WfInstancesResponse, status_code=status.HTTP_201_CREATED)
def create_wf_instances(
    data: WfInstancesCreate,
    db: Session = Depends(get_db)
):
    """Create a new WfInstances."""
    service = WfInstancesService(db)
    return service.create(data)

@router.put("/wf_instancess/{id}", response_model=WfInstancesResponse)
def update_wf_instances(
    id: UUID,
    data: WfInstancesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WfInstances."""
    service = WfInstancesService(db)
    return service.update(id, data)

@router.delete("/wf_instancess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wf_instances(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WfInstances (soft delete by default)."""
    service = WfInstancesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wf_instancess/search", response_model=List[WfInstancesResponse])
def search_wf_instancess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WfInstances by name/code/description."""
    service = WfInstancesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wf_statess", response_model=WfStatesListResponse)
def list_wf_statess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WfStates records."""
    service = WfStatesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wf_statess/{id}", response_model=WfStatesResponse)
def get_wf_states(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WfStates by ID."""
    service = WfStatesService(db)
    return service.get(id)

@router.post("/wf_statess", response_model=WfStatesResponse, status_code=status.HTTP_201_CREATED)
def create_wf_states(
    data: WfStatesCreate,
    db: Session = Depends(get_db)
):
    """Create a new WfStates."""
    service = WfStatesService(db)
    return service.create(data)

@router.put("/wf_statess/{id}", response_model=WfStatesResponse)
def update_wf_states(
    id: UUID,
    data: WfStatesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WfStates."""
    service = WfStatesService(db)
    return service.update(id, data)

@router.delete("/wf_statess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wf_states(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WfStates (soft delete by default)."""
    service = WfStatesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wf_statess/search", response_model=List[WfStatesResponse])
def search_wf_statess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WfStates by name/code/description."""
    service = WfStatesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wf_taskss", response_model=WfTasksListResponse)
def list_wf_taskss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WfTasks records."""
    service = WfTasksService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wf_taskss/{id}", response_model=WfTasksResponse)
def get_wf_tasks(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WfTasks by ID."""
    service = WfTasksService(db)
    return service.get(id)

@router.post("/wf_taskss", response_model=WfTasksResponse, status_code=status.HTTP_201_CREATED)
def create_wf_tasks(
    data: WfTasksCreate,
    db: Session = Depends(get_db)
):
    """Create a new WfTasks."""
    service = WfTasksService(db)
    return service.create(data)

@router.put("/wf_taskss/{id}", response_model=WfTasksResponse)
def update_wf_tasks(
    id: UUID,
    data: WfTasksUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WfTasks."""
    service = WfTasksService(db)
    return service.update(id, data)

@router.delete("/wf_taskss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wf_tasks(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WfTasks (soft delete by default)."""
    service = WfTasksService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wf_taskss/search", response_model=List[WfTasksResponse])
def search_wf_taskss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WfTasks by name/code/description."""
    service = WfTasksService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wf_transitionss", response_model=WfTransitionsListResponse)
def list_wf_transitionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WfTransitions records."""
    service = WfTransitionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wf_transitionss/{id}", response_model=WfTransitionsResponse)
def get_wf_transitions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WfTransitions by ID."""
    service = WfTransitionsService(db)
    return service.get(id)

@router.post("/wf_transitionss", response_model=WfTransitionsResponse, status_code=status.HTTP_201_CREATED)
def create_wf_transitions(
    data: WfTransitionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new WfTransitions."""
    service = WfTransitionsService(db)
    return service.create(data)

@router.put("/wf_transitionss/{id}", response_model=WfTransitionsResponse)
def update_wf_transitions(
    id: UUID,
    data: WfTransitionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WfTransitions."""
    service = WfTransitionsService(db)
    return service.update(id, data)

@router.delete("/wf_transitionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wf_transitions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WfTransitions (soft delete by default)."""
    service = WfTransitionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wf_transitionss/search", response_model=List[WfTransitionsResponse])
def search_wf_transitionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WfTransitions by name/code/description."""
    service = WfTransitionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_integration_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get Integration dashboard summary."""
    return {
        "module": "integration",
        "company_id": str(company_id),
        "tables": 9,
        "metrics": {}
    }
