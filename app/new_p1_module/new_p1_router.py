"""
New P1 Module — FastAPI Router
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.933770
Mounted at /api/v1/p1
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID

from fastapi import APIRouter, Depends, HTTPException, status, Query, Body
from sqlalchemy.orm import Session

from app.core.database_mssql import get_db
from .new_p1_schemas import *
from .new_p1_services import *

router = APIRouter(prefix="/p1", tags=["New P1"])

@router.get("/bi_audit_logs", response_model=BiAuditLogListResponse)
def list_bi_audit_logs(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiAuditLog records."""
    service = BiAuditLogService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_audit_logs/{id}", response_model=BiAuditLogResponse)
def get_bi_audit_log(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiAuditLog by ID."""
    service = BiAuditLogService(db)
    return service.get(id)

@router.post("/bi_audit_logs", response_model=BiAuditLogResponse, status_code=status.HTTP_201_CREATED)
def create_bi_audit_log(
    data: BiAuditLogCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiAuditLog."""
    service = BiAuditLogService(db)
    return service.create(data)

@router.put("/bi_audit_logs/{id}", response_model=BiAuditLogResponse)
def update_bi_audit_log(
    id: UUID,
    data: BiAuditLogUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiAuditLog."""
    service = BiAuditLogService(db)
    return service.update(id, data)

@router.delete("/bi_audit_logs/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_audit_log(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiAuditLog (soft delete by default)."""
    service = BiAuditLogService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_audit_logs/search", response_model=List[BiAuditLogResponse])
def search_bi_audit_logs(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiAuditLog by name/code/description."""
    service = BiAuditLogService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_caches", response_model=BiCacheListResponse)
def list_bi_caches(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiCache records."""
    service = BiCacheService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_caches/{id}", response_model=BiCacheResponse)
def get_bi_cache(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiCache by ID."""
    service = BiCacheService(db)
    return service.get(id)

@router.post("/bi_caches", response_model=BiCacheResponse, status_code=status.HTTP_201_CREATED)
def create_bi_cache(
    data: BiCacheCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiCache."""
    service = BiCacheService(db)
    return service.create(data)

@router.put("/bi_caches/{id}", response_model=BiCacheResponse)
def update_bi_cache(
    id: UUID,
    data: BiCacheUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiCache."""
    service = BiCacheService(db)
    return service.update(id, data)

@router.delete("/bi_caches/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_cache(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiCache (soft delete by default)."""
    service = BiCacheService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_caches/search", response_model=List[BiCacheResponse])
def search_bi_caches(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiCache by name/code/description."""
    service = BiCacheService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_dashboardss", response_model=BiDashboardsListResponse)
def list_bi_dashboardss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiDashboards records."""
    service = BiDashboardsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_dashboardss/{id}", response_model=BiDashboardsResponse)
def get_bi_dashboards(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiDashboards by ID."""
    service = BiDashboardsService(db)
    return service.get(id)

@router.post("/bi_dashboardss", response_model=BiDashboardsResponse, status_code=status.HTTP_201_CREATED)
def create_bi_dashboards(
    data: BiDashboardsCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiDashboards."""
    service = BiDashboardsService(db)
    return service.create(data)

@router.put("/bi_dashboardss/{id}", response_model=BiDashboardsResponse)
def update_bi_dashboards(
    id: UUID,
    data: BiDashboardsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiDashboards."""
    service = BiDashboardsService(db)
    return service.update(id, data)

@router.delete("/bi_dashboardss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_dashboards(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiDashboards (soft delete by default)."""
    service = BiDashboardsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_dashboardss/search", response_model=List[BiDashboardsResponse])
def search_bi_dashboardss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiDashboards by name/code/description."""
    service = BiDashboardsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_data_sourcess", response_model=BiDataSourcesListResponse)
def list_bi_data_sourcess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiDataSources records."""
    service = BiDataSourcesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_data_sourcess/{id}", response_model=BiDataSourcesResponse)
def get_bi_data_sources(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiDataSources by ID."""
    service = BiDataSourcesService(db)
    return service.get(id)

@router.post("/bi_data_sourcess", response_model=BiDataSourcesResponse, status_code=status.HTTP_201_CREATED)
def create_bi_data_sources(
    data: BiDataSourcesCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiDataSources."""
    service = BiDataSourcesService(db)
    return service.create(data)

@router.put("/bi_data_sourcess/{id}", response_model=BiDataSourcesResponse)
def update_bi_data_sources(
    id: UUID,
    data: BiDataSourcesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiDataSources."""
    service = BiDataSourcesService(db)
    return service.update(id, data)

@router.delete("/bi_data_sourcess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_data_sources(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiDataSources (soft delete by default)."""
    service = BiDataSourcesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_data_sourcess/search", response_model=List[BiDataSourcesResponse])
def search_bi_data_sourcess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiDataSources by name/code/description."""
    service = BiDataSourcesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_datasetss", response_model=BiDatasetsListResponse)
def list_bi_datasetss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiDatasets records."""
    service = BiDatasetsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_datasetss/{id}", response_model=BiDatasetsResponse)
def get_bi_datasets(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiDatasets by ID."""
    service = BiDatasetsService(db)
    return service.get(id)

@router.post("/bi_datasetss", response_model=BiDatasetsResponse, status_code=status.HTTP_201_CREATED)
def create_bi_datasets(
    data: BiDatasetsCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiDatasets."""
    service = BiDatasetsService(db)
    return service.create(data)

@router.put("/bi_datasetss/{id}", response_model=BiDatasetsResponse)
def update_bi_datasets(
    id: UUID,
    data: BiDatasetsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiDatasets."""
    service = BiDatasetsService(db)
    return service.update(id, data)

@router.delete("/bi_datasetss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_datasets(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiDatasets (soft delete by default)."""
    service = BiDatasetsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_datasetss/search", response_model=List[BiDatasetsResponse])
def search_bi_datasetss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiDatasets by name/code/description."""
    service = BiDatasetsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_reportss", response_model=BiReportsListResponse)
def list_bi_reportss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiReports records."""
    service = BiReportsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_reportss/{id}", response_model=BiReportsResponse)
def get_bi_reports(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiReports by ID."""
    service = BiReportsService(db)
    return service.get(id)

@router.post("/bi_reportss", response_model=BiReportsResponse, status_code=status.HTTP_201_CREATED)
def create_bi_reports(
    data: BiReportsCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiReports."""
    service = BiReportsService(db)
    return service.create(data)

@router.put("/bi_reportss/{id}", response_model=BiReportsResponse)
def update_bi_reports(
    id: UUID,
    data: BiReportsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiReports."""
    service = BiReportsService(db)
    return service.update(id, data)

@router.delete("/bi_reportss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_reports(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiReports (soft delete by default)."""
    service = BiReportsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_reportss/search", response_model=List[BiReportsResponse])
def search_bi_reportss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiReports by name/code/description."""
    service = BiReportsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_scheduless", response_model=BiSchedulesListResponse)
def list_bi_scheduless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiSchedules records."""
    service = BiSchedulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_scheduless/{id}", response_model=BiSchedulesResponse)
def get_bi_schedules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiSchedules by ID."""
    service = BiSchedulesService(db)
    return service.get(id)

@router.post("/bi_scheduless", response_model=BiSchedulesResponse, status_code=status.HTTP_201_CREATED)
def create_bi_schedules(
    data: BiSchedulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiSchedules."""
    service = BiSchedulesService(db)
    return service.create(data)

@router.put("/bi_scheduless/{id}", response_model=BiSchedulesResponse)
def update_bi_schedules(
    id: UUID,
    data: BiSchedulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiSchedules."""
    service = BiSchedulesService(db)
    return service.update(id, data)

@router.delete("/bi_scheduless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_schedules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiSchedules (soft delete by default)."""
    service = BiSchedulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_scheduless/search", response_model=List[BiSchedulesResponse])
def search_bi_scheduless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiSchedules by name/code/description."""
    service = BiSchedulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/bi_user_favoritess", response_model=BiUserFavoritesListResponse)
def list_bi_user_favoritess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all BiUserFavorites records."""
    service = BiUserFavoritesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/bi_user_favoritess/{id}", response_model=BiUserFavoritesResponse)
def get_bi_user_favorites(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get BiUserFavorites by ID."""
    service = BiUserFavoritesService(db)
    return service.get(id)

@router.post("/bi_user_favoritess", response_model=BiUserFavoritesResponse, status_code=status.HTTP_201_CREATED)
def create_bi_user_favorites(
    data: BiUserFavoritesCreate,
    db: Session = Depends(get_db)
):
    """Create a new BiUserFavorites."""
    service = BiUserFavoritesService(db)
    return service.create(data)

@router.put("/bi_user_favoritess/{id}", response_model=BiUserFavoritesResponse)
def update_bi_user_favorites(
    id: UUID,
    data: BiUserFavoritesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing BiUserFavorites."""
    service = BiUserFavoritesService(db)
    return service.update(id, data)

@router.delete("/bi_user_favoritess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_bi_user_favorites(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a BiUserFavorites (soft delete by default)."""
    service = BiUserFavoritesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/bi_user_favoritess/search", response_model=List[BiUserFavoritesResponse])
def search_bi_user_favoritess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search BiUserFavorites by name/code/description."""
    service = BiUserFavoritesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/clm_amendmentss", response_model=ClmAmendmentsListResponse)
def list_clm_amendmentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ClmAmendments records."""
    service = ClmAmendmentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/clm_amendmentss/{id}", response_model=ClmAmendmentsResponse)
def get_clm_amendments(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ClmAmendments by ID."""
    service = ClmAmendmentsService(db)
    return service.get(id)

@router.post("/clm_amendmentss", response_model=ClmAmendmentsResponse, status_code=status.HTTP_201_CREATED)
def create_clm_amendments(
    data: ClmAmendmentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ClmAmendments."""
    service = ClmAmendmentsService(db)
    return service.create(data)

@router.put("/clm_amendmentss/{id}", response_model=ClmAmendmentsResponse)
def update_clm_amendments(
    id: UUID,
    data: ClmAmendmentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ClmAmendments."""
    service = ClmAmendmentsService(db)
    return service.update(id, data)

@router.delete("/clm_amendmentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_clm_amendments(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ClmAmendments (soft delete by default)."""
    service = ClmAmendmentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/clm_amendmentss/search", response_model=List[ClmAmendmentsResponse])
def search_clm_amendmentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ClmAmendments by name/code/description."""
    service = ClmAmendmentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/clm_clausess", response_model=ClmClausesListResponse)
def list_clm_clausess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ClmClauses records."""
    service = ClmClausesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/clm_clausess/{id}", response_model=ClmClausesResponse)
def get_clm_clauses(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ClmClauses by ID."""
    service = ClmClausesService(db)
    return service.get(id)

@router.post("/clm_clausess", response_model=ClmClausesResponse, status_code=status.HTTP_201_CREATED)
def create_clm_clauses(
    data: ClmClausesCreate,
    db: Session = Depends(get_db)
):
    """Create a new ClmClauses."""
    service = ClmClausesService(db)
    return service.create(data)

@router.put("/clm_clausess/{id}", response_model=ClmClausesResponse)
def update_clm_clauses(
    id: UUID,
    data: ClmClausesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ClmClauses."""
    service = ClmClausesService(db)
    return service.update(id, data)

@router.delete("/clm_clausess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_clm_clauses(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ClmClauses (soft delete by default)."""
    service = ClmClausesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/clm_clausess/search", response_model=List[ClmClausesResponse])
def search_clm_clausess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ClmClauses by name/code/description."""
    service = ClmClausesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/clm_contractss", response_model=ClmContractsListResponse)
def list_clm_contractss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ClmContracts records."""
    service = ClmContractsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/clm_contractss/{id}", response_model=ClmContractsResponse)
def get_clm_contracts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ClmContracts by ID."""
    service = ClmContractsService(db)
    return service.get(id)

@router.post("/clm_contractss", response_model=ClmContractsResponse, status_code=status.HTTP_201_CREATED)
def create_clm_contracts(
    data: ClmContractsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ClmContracts."""
    service = ClmContractsService(db)
    return service.create(data)

@router.put("/clm_contractss/{id}", response_model=ClmContractsResponse)
def update_clm_contracts(
    id: UUID,
    data: ClmContractsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ClmContracts."""
    service = ClmContractsService(db)
    return service.update(id, data)

@router.delete("/clm_contractss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_clm_contracts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ClmContracts (soft delete by default)."""
    service = ClmContractsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/clm_contractss/search", response_model=List[ClmContractsResponse])
def search_clm_contractss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ClmContracts by name/code/description."""
    service = ClmContractsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/clm_renewalss", response_model=ClmRenewalsListResponse)
def list_clm_renewalss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all ClmRenewals records."""
    service = ClmRenewalsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/clm_renewalss/{id}", response_model=ClmRenewalsResponse)
def get_clm_renewals(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get ClmRenewals by ID."""
    service = ClmRenewalsService(db)
    return service.get(id)

@router.post("/clm_renewalss", response_model=ClmRenewalsResponse, status_code=status.HTTP_201_CREATED)
def create_clm_renewals(
    data: ClmRenewalsCreate,
    db: Session = Depends(get_db)
):
    """Create a new ClmRenewals."""
    service = ClmRenewalsService(db)
    return service.create(data)

@router.put("/clm_renewalss/{id}", response_model=ClmRenewalsResponse)
def update_clm_renewals(
    id: UUID,
    data: ClmRenewalsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing ClmRenewals."""
    service = ClmRenewalsService(db)
    return service.update(id, data)

@router.delete("/clm_renewalss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_clm_renewals(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a ClmRenewals (soft delete by default)."""
    service = ClmRenewalsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/clm_renewalss/search", response_model=List[ClmRenewalsResponse])
def search_clm_renewalss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search ClmRenewals by name/code/description."""
    service = ClmRenewalsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/doc_documentss", response_model=DocDocumentsListResponse)
def list_doc_documentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DocDocuments records."""
    service = DocDocumentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/doc_documentss/{id}", response_model=DocDocumentsResponse)
def get_doc_documents(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DocDocuments by ID."""
    service = DocDocumentsService(db)
    return service.get(id)

@router.post("/doc_documentss", response_model=DocDocumentsResponse, status_code=status.HTTP_201_CREATED)
def create_doc_documents(
    data: DocDocumentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new DocDocuments."""
    service = DocDocumentsService(db)
    return service.create(data)

@router.put("/doc_documentss/{id}", response_model=DocDocumentsResponse)
def update_doc_documents(
    id: UUID,
    data: DocDocumentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DocDocuments."""
    service = DocDocumentsService(db)
    return service.update(id, data)

@router.delete("/doc_documentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_doc_documents(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DocDocuments (soft delete by default)."""
    service = DocDocumentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/doc_documentss/search", response_model=List[DocDocumentsResponse])
def search_doc_documentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DocDocuments by name/code/description."""
    service = DocDocumentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/doc_folderss", response_model=DocFoldersListResponse)
def list_doc_folderss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DocFolders records."""
    service = DocFoldersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/doc_folderss/{id}", response_model=DocFoldersResponse)
def get_doc_folders(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DocFolders by ID."""
    service = DocFoldersService(db)
    return service.get(id)

@router.post("/doc_folderss", response_model=DocFoldersResponse, status_code=status.HTTP_201_CREATED)
def create_doc_folders(
    data: DocFoldersCreate,
    db: Session = Depends(get_db)
):
    """Create a new DocFolders."""
    service = DocFoldersService(db)
    return service.create(data)

@router.put("/doc_folderss/{id}", response_model=DocFoldersResponse)
def update_doc_folders(
    id: UUID,
    data: DocFoldersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DocFolders."""
    service = DocFoldersService(db)
    return service.update(id, data)

@router.delete("/doc_folderss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_doc_folders(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DocFolders (soft delete by default)."""
    service = DocFoldersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/doc_folderss/search", response_model=List[DocFoldersResponse])
def search_doc_folderss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DocFolders by name/code/description."""
    service = DocFoldersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/doc_ocr_resultss", response_model=DocOcrResultsListResponse)
def list_doc_ocr_resultss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DocOcrResults records."""
    service = DocOcrResultsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/doc_ocr_resultss/{id}", response_model=DocOcrResultsResponse)
def get_doc_ocr_results(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DocOcrResults by ID."""
    service = DocOcrResultsService(db)
    return service.get(id)

@router.post("/doc_ocr_resultss", response_model=DocOcrResultsResponse, status_code=status.HTTP_201_CREATED)
def create_doc_ocr_results(
    data: DocOcrResultsCreate,
    db: Session = Depends(get_db)
):
    """Create a new DocOcrResults."""
    service = DocOcrResultsService(db)
    return service.create(data)

@router.put("/doc_ocr_resultss/{id}", response_model=DocOcrResultsResponse)
def update_doc_ocr_results(
    id: UUID,
    data: DocOcrResultsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DocOcrResults."""
    service = DocOcrResultsService(db)
    return service.update(id, data)

@router.delete("/doc_ocr_resultss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_doc_ocr_results(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DocOcrResults (soft delete by default)."""
    service = DocOcrResultsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/doc_ocr_resultss/search", response_model=List[DocOcrResultsResponse])
def search_doc_ocr_resultss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DocOcrResults by name/code/description."""
    service = DocOcrResultsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/doc_sharess", response_model=DocSharesListResponse)
def list_doc_sharess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DocShares records."""
    service = DocSharesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/doc_sharess/{id}", response_model=DocSharesResponse)
def get_doc_shares(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DocShares by ID."""
    service = DocSharesService(db)
    return service.get(id)

@router.post("/doc_sharess", response_model=DocSharesResponse, status_code=status.HTTP_201_CREATED)
def create_doc_shares(
    data: DocSharesCreate,
    db: Session = Depends(get_db)
):
    """Create a new DocShares."""
    service = DocSharesService(db)
    return service.create(data)

@router.put("/doc_sharess/{id}", response_model=DocSharesResponse)
def update_doc_shares(
    id: UUID,
    data: DocSharesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DocShares."""
    service = DocSharesService(db)
    return service.update(id, data)

@router.delete("/doc_sharess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_doc_shares(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DocShares (soft delete by default)."""
    service = DocSharesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/doc_sharess/search", response_model=List[DocSharesResponse])
def search_doc_sharess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DocShares by name/code/description."""
    service = DocSharesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/doc_versionss", response_model=DocVersionsListResponse)
def list_doc_versionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DocVersions records."""
    service = DocVersionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/doc_versionss/{id}", response_model=DocVersionsResponse)
def get_doc_versions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DocVersions by ID."""
    service = DocVersionsService(db)
    return service.get(id)

@router.post("/doc_versionss", response_model=DocVersionsResponse, status_code=status.HTTP_201_CREATED)
def create_doc_versions(
    data: DocVersionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new DocVersions."""
    service = DocVersionsService(db)
    return service.create(data)

@router.put("/doc_versionss/{id}", response_model=DocVersionsResponse)
def update_doc_versions(
    id: UUID,
    data: DocVersionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DocVersions."""
    service = DocVersionsService(db)
    return service.update(id, data)

@router.delete("/doc_versionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_doc_versions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DocVersions (soft delete by default)."""
    service = DocVersionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/doc_versionss/search", response_model=List[DocVersionsResponse])
def search_doc_versionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DocVersions by name/code/description."""
    service = DocVersionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_consensuss", response_model=DpConsensusListResponse)
def list_dp_consensuss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpConsensus records."""
    service = DpConsensusService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_consensuss/{id}", response_model=DpConsensusResponse)
def get_dp_consensus(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpConsensus by ID."""
    service = DpConsensusService(db)
    return service.get(id)

@router.post("/dp_consensuss", response_model=DpConsensusResponse, status_code=status.HTTP_201_CREATED)
def create_dp_consensus(
    data: DpConsensusCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpConsensus."""
    service = DpConsensusService(db)
    return service.create(data)

@router.put("/dp_consensuss/{id}", response_model=DpConsensusResponse)
def update_dp_consensus(
    id: UUID,
    data: DpConsensusUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpConsensus."""
    service = DpConsensusService(db)
    return service.update(id, data)

@router.delete("/dp_consensuss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_consensus(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpConsensus (soft delete by default)."""
    service = DpConsensusService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_consensuss/search", response_model=List[DpConsensusResponse])
def search_dp_consensuss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpConsensus by name/code/description."""
    service = DpConsensusService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_forecastss", response_model=DpForecastsListResponse)
def list_dp_forecastss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpForecasts records."""
    service = DpForecastsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_forecastss/{id}", response_model=DpForecastsResponse)
def get_dp_forecasts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpForecasts by ID."""
    service = DpForecastsService(db)
    return service.get(id)

@router.post("/dp_forecastss", response_model=DpForecastsResponse, status_code=status.HTTP_201_CREATED)
def create_dp_forecasts(
    data: DpForecastsCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpForecasts."""
    service = DpForecastsService(db)
    return service.create(data)

@router.put("/dp_forecastss/{id}", response_model=DpForecastsResponse)
def update_dp_forecasts(
    id: UUID,
    data: DpForecastsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpForecasts."""
    service = DpForecastsService(db)
    return service.update(id, data)

@router.delete("/dp_forecastss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_forecasts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpForecasts (soft delete by default)."""
    service = DpForecastsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_forecastss/search", response_model=List[DpForecastsResponse])
def search_dp_forecastss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpForecasts by name/code/description."""
    service = DpForecastsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_historical_datas", response_model=DpHistoricalDataListResponse)
def list_dp_historical_datas(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpHistoricalData records."""
    service = DpHistoricalDataService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_historical_datas/{id}", response_model=DpHistoricalDataResponse)
def get_dp_historical_data(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpHistoricalData by ID."""
    service = DpHistoricalDataService(db)
    return service.get(id)

@router.post("/dp_historical_datas", response_model=DpHistoricalDataResponse, status_code=status.HTTP_201_CREATED)
def create_dp_historical_data(
    data: DpHistoricalDataCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpHistoricalData."""
    service = DpHistoricalDataService(db)
    return service.create(data)

@router.put("/dp_historical_datas/{id}", response_model=DpHistoricalDataResponse)
def update_dp_historical_data(
    id: UUID,
    data: DpHistoricalDataUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpHistoricalData."""
    service = DpHistoricalDataService(db)
    return service.update(id, data)

@router.delete("/dp_historical_datas/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_historical_data(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpHistoricalData (soft delete by default)."""
    service = DpHistoricalDataService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_historical_datas/search", response_model=List[DpHistoricalDataResponse])
def search_dp_historical_datas(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpHistoricalData by name/code/description."""
    service = DpHistoricalDataService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_promotionss", response_model=DpPromotionsListResponse)
def list_dp_promotionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpPromotions records."""
    service = DpPromotionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_promotionss/{id}", response_model=DpPromotionsResponse)
def get_dp_promotions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpPromotions by ID."""
    service = DpPromotionsService(db)
    return service.get(id)

@router.post("/dp_promotionss", response_model=DpPromotionsResponse, status_code=status.HTTP_201_CREATED)
def create_dp_promotions(
    data: DpPromotionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpPromotions."""
    service = DpPromotionsService(db)
    return service.create(data)

@router.put("/dp_promotionss/{id}", response_model=DpPromotionsResponse)
def update_dp_promotions(
    id: UUID,
    data: DpPromotionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpPromotions."""
    service = DpPromotionsService(db)
    return service.update(id, data)

@router.delete("/dp_promotionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_promotions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpPromotions (soft delete by default)."""
    service = DpPromotionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_promotionss/search", response_model=List[DpPromotionsResponse])
def search_dp_promotionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpPromotions by name/code/description."""
    service = DpPromotionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_seasonalitys", response_model=DpSeasonalityListResponse)
def list_dp_seasonalitys(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpSeasonality records."""
    service = DpSeasonalityService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_seasonalitys/{id}", response_model=DpSeasonalityResponse)
def get_dp_seasonality(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpSeasonality by ID."""
    service = DpSeasonalityService(db)
    return service.get(id)

@router.post("/dp_seasonalitys", response_model=DpSeasonalityResponse, status_code=status.HTTP_201_CREATED)
def create_dp_seasonality(
    data: DpSeasonalityCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpSeasonality."""
    service = DpSeasonalityService(db)
    return service.create(data)

@router.put("/dp_seasonalitys/{id}", response_model=DpSeasonalityResponse)
def update_dp_seasonality(
    id: UUID,
    data: DpSeasonalityUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpSeasonality."""
    service = DpSeasonalityService(db)
    return service.update(id, data)

@router.delete("/dp_seasonalitys/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_seasonality(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpSeasonality (soft delete by default)."""
    service = DpSeasonalityService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_seasonalitys/search", response_model=List[DpSeasonalityResponse])
def search_dp_seasonalitys(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpSeasonality by name/code/description."""
    service = DpSeasonalityService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_simulations", response_model=DpSimulationListResponse)
def list_dp_simulations(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpSimulation records."""
    service = DpSimulationService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_simulations/{id}", response_model=DpSimulationResponse)
def get_dp_simulation(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpSimulation by ID."""
    service = DpSimulationService(db)
    return service.get(id)

@router.post("/dp_simulations", response_model=DpSimulationResponse, status_code=status.HTTP_201_CREATED)
def create_dp_simulation(
    data: DpSimulationCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpSimulation."""
    service = DpSimulationService(db)
    return service.create(data)

@router.put("/dp_simulations/{id}", response_model=DpSimulationResponse)
def update_dp_simulation(
    id: UUID,
    data: DpSimulationUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpSimulation."""
    service = DpSimulationService(db)
    return service.update(id, data)

@router.delete("/dp_simulations/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_simulation(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpSimulation (soft delete by default)."""
    service = DpSimulationService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_simulations/search", response_model=List[DpSimulationResponse])
def search_dp_simulations(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpSimulation by name/code/description."""
    service = DpSimulationService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dp_statistical_modelss", response_model=DpStatisticalModelsListResponse)
def list_dp_statistical_modelss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all DpStatisticalModels records."""
    service = DpStatisticalModelsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/dp_statistical_modelss/{id}", response_model=DpStatisticalModelsResponse)
def get_dp_statistical_models(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get DpStatisticalModels by ID."""
    service = DpStatisticalModelsService(db)
    return service.get(id)

@router.post("/dp_statistical_modelss", response_model=DpStatisticalModelsResponse, status_code=status.HTTP_201_CREATED)
def create_dp_statistical_models(
    data: DpStatisticalModelsCreate,
    db: Session = Depends(get_db)
):
    """Create a new DpStatisticalModels."""
    service = DpStatisticalModelsService(db)
    return service.create(data)

@router.put("/dp_statistical_modelss/{id}", response_model=DpStatisticalModelsResponse)
def update_dp_statistical_models(
    id: UUID,
    data: DpStatisticalModelsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing DpStatisticalModels."""
    service = DpStatisticalModelsService(db)
    return service.update(id, data)

@router.delete("/dp_statistical_modelss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_dp_statistical_models(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a DpStatisticalModels (soft delete by default)."""
    service = DpStatisticalModelsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/dp_statistical_modelss/search", response_model=List[DpStatisticalModelsResponse])
def search_dp_statistical_modelss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search DpStatisticalModels by name/code/description."""
    service = DpStatisticalModelsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/edi_mapping_ruless", response_model=EdiMappingRulesListResponse)
def list_edi_mapping_ruless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EdiMappingRules records."""
    service = EdiMappingRulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/edi_mapping_ruless/{id}", response_model=EdiMappingRulesResponse)
def get_edi_mapping_rules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EdiMappingRules by ID."""
    service = EdiMappingRulesService(db)
    return service.get(id)

@router.post("/edi_mapping_ruless", response_model=EdiMappingRulesResponse, status_code=status.HTTP_201_CREATED)
def create_edi_mapping_rules(
    data: EdiMappingRulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new EdiMappingRules."""
    service = EdiMappingRulesService(db)
    return service.create(data)

@router.put("/edi_mapping_ruless/{id}", response_model=EdiMappingRulesResponse)
def update_edi_mapping_rules(
    id: UUID,
    data: EdiMappingRulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EdiMappingRules."""
    service = EdiMappingRulesService(db)
    return service.update(id, data)

@router.delete("/edi_mapping_ruless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_edi_mapping_rules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EdiMappingRules (soft delete by default)."""
    service = EdiMappingRulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/edi_mapping_ruless/search", response_model=List[EdiMappingRulesResponse])
def search_edi_mapping_ruless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EdiMappingRules by name/code/description."""
    service = EdiMappingRulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/edi_partnerss", response_model=EdiPartnersListResponse)
def list_edi_partnerss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EdiPartners records."""
    service = EdiPartnersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/edi_partnerss/{id}", response_model=EdiPartnersResponse)
def get_edi_partners(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EdiPartners by ID."""
    service = EdiPartnersService(db)
    return service.get(id)

@router.post("/edi_partnerss", response_model=EdiPartnersResponse, status_code=status.HTTP_201_CREATED)
def create_edi_partners(
    data: EdiPartnersCreate,
    db: Session = Depends(get_db)
):
    """Create a new EdiPartners."""
    service = EdiPartnersService(db)
    return service.create(data)

@router.put("/edi_partnerss/{id}", response_model=EdiPartnersResponse)
def update_edi_partners(
    id: UUID,
    data: EdiPartnersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EdiPartners."""
    service = EdiPartnersService(db)
    return service.update(id, data)

@router.delete("/edi_partnerss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_edi_partners(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EdiPartners (soft delete by default)."""
    service = EdiPartnersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/edi_partnerss/search", response_model=List[EdiPartnersResponse])
def search_edi_partnerss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EdiPartners by name/code/description."""
    service = EdiPartnersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/edi_transactionss", response_model=EdiTransactionsListResponse)
def list_edi_transactionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EdiTransactions records."""
    service = EdiTransactionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/edi_transactionss/{id}", response_model=EdiTransactionsResponse)
def get_edi_transactions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EdiTransactions by ID."""
    service = EdiTransactionsService(db)
    return service.get(id)

@router.post("/edi_transactionss", response_model=EdiTransactionsResponse, status_code=status.HTTP_201_CREATED)
def create_edi_transactions(
    data: EdiTransactionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new EdiTransactions."""
    service = EdiTransactionsService(db)
    return service.create(data)

@router.put("/edi_transactionss/{id}", response_model=EdiTransactionsResponse)
def update_edi_transactions(
    id: UUID,
    data: EdiTransactionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EdiTransactions."""
    service = EdiTransactionsService(db)
    return service.update(id, data)

@router.delete("/edi_transactionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_edi_transactions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EdiTransactions (soft delete by default)."""
    service = EdiTransactionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/edi_transactionss/search", response_model=List[EdiTransactionsResponse])
def search_edi_transactionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EdiTransactions by name/code/description."""
    service = EdiTransactionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/einvoice_documentss", response_model=EinvoiceDocumentsListResponse)
def list_einvoice_documentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all EinvoiceDocuments records."""
    service = EinvoiceDocumentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/einvoice_documentss/{id}", response_model=EinvoiceDocumentsResponse)
def get_einvoice_documents(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get EinvoiceDocuments by ID."""
    service = EinvoiceDocumentsService(db)
    return service.get(id)

@router.post("/einvoice_documentss", response_model=EinvoiceDocumentsResponse, status_code=status.HTTP_201_CREATED)
def create_einvoice_documents(
    data: EinvoiceDocumentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new EinvoiceDocuments."""
    service = EinvoiceDocumentsService(db)
    return service.create(data)

@router.put("/einvoice_documentss/{id}", response_model=EinvoiceDocumentsResponse)
def update_einvoice_documents(
    id: UUID,
    data: EinvoiceDocumentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing EinvoiceDocuments."""
    service = EinvoiceDocumentsService(db)
    return service.update(id, data)

@router.delete("/einvoice_documentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_einvoice_documents(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a EinvoiceDocuments (soft delete by default)."""
    service = EinvoiceDocumentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/einvoice_documentss/search", response_model=List[EinvoiceDocumentsResponse])
def search_einvoice_documentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search EinvoiceDocuments by name/code/description."""
    service = EinvoiceDocumentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mdm_data_qualitys", response_model=MdmDataQualityListResponse)
def list_mdm_data_qualitys(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MdmDataQuality records."""
    service = MdmDataQualityService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mdm_data_qualitys/{id}", response_model=MdmDataQualityResponse)
def get_mdm_data_quality(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MdmDataQuality by ID."""
    service = MdmDataQualityService(db)
    return service.get(id)

@router.post("/mdm_data_qualitys", response_model=MdmDataQualityResponse, status_code=status.HTTP_201_CREATED)
def create_mdm_data_quality(
    data: MdmDataQualityCreate,
    db: Session = Depends(get_db)
):
    """Create a new MdmDataQuality."""
    service = MdmDataQualityService(db)
    return service.create(data)

@router.put("/mdm_data_qualitys/{id}", response_model=MdmDataQualityResponse)
def update_mdm_data_quality(
    id: UUID,
    data: MdmDataQualityUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MdmDataQuality."""
    service = MdmDataQualityService(db)
    return service.update(id, data)

@router.delete("/mdm_data_qualitys/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mdm_data_quality(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MdmDataQuality (soft delete by default)."""
    service = MdmDataQualityService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mdm_data_qualitys/search", response_model=List[MdmDataQualityResponse])
def search_mdm_data_qualitys(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MdmDataQuality by name/code/description."""
    service = MdmDataQualityService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mdm_data_sourcess", response_model=MdmDataSourcesListResponse)
def list_mdm_data_sourcess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MdmDataSources records."""
    service = MdmDataSourcesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mdm_data_sourcess/{id}", response_model=MdmDataSourcesResponse)
def get_mdm_data_sources(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MdmDataSources by ID."""
    service = MdmDataSourcesService(db)
    return service.get(id)

@router.post("/mdm_data_sourcess", response_model=MdmDataSourcesResponse, status_code=status.HTTP_201_CREATED)
def create_mdm_data_sources(
    data: MdmDataSourcesCreate,
    db: Session = Depends(get_db)
):
    """Create a new MdmDataSources."""
    service = MdmDataSourcesService(db)
    return service.create(data)

@router.put("/mdm_data_sourcess/{id}", response_model=MdmDataSourcesResponse)
def update_mdm_data_sources(
    id: UUID,
    data: MdmDataSourcesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MdmDataSources."""
    service = MdmDataSourcesService(db)
    return service.update(id, data)

@router.delete("/mdm_data_sourcess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mdm_data_sources(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MdmDataSources (soft delete by default)."""
    service = MdmDataSourcesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mdm_data_sourcess/search", response_model=List[MdmDataSourcesResponse])
def search_mdm_data_sourcess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MdmDataSources by name/code/description."""
    service = MdmDataSourcesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mdm_golden_recordss", response_model=MdmGoldenRecordsListResponse)
def list_mdm_golden_recordss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MdmGoldenRecords records."""
    service = MdmGoldenRecordsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mdm_golden_recordss/{id}", response_model=MdmGoldenRecordsResponse)
def get_mdm_golden_records(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MdmGoldenRecords by ID."""
    service = MdmGoldenRecordsService(db)
    return service.get(id)

@router.post("/mdm_golden_recordss", response_model=MdmGoldenRecordsResponse, status_code=status.HTTP_201_CREATED)
def create_mdm_golden_records(
    data: MdmGoldenRecordsCreate,
    db: Session = Depends(get_db)
):
    """Create a new MdmGoldenRecords."""
    service = MdmGoldenRecordsService(db)
    return service.create(data)

@router.put("/mdm_golden_recordss/{id}", response_model=MdmGoldenRecordsResponse)
def update_mdm_golden_records(
    id: UUID,
    data: MdmGoldenRecordsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MdmGoldenRecords."""
    service = MdmGoldenRecordsService(db)
    return service.update(id, data)

@router.delete("/mdm_golden_recordss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mdm_golden_records(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MdmGoldenRecords (soft delete by default)."""
    service = MdmGoldenRecordsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mdm_golden_recordss/search", response_model=List[MdmGoldenRecordsResponse])
def search_mdm_golden_recordss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MdmGoldenRecords by name/code/description."""
    service = MdmGoldenRecordsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mdm_matching_ruless", response_model=MdmMatchingRulesListResponse)
def list_mdm_matching_ruless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MdmMatchingRules records."""
    service = MdmMatchingRulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mdm_matching_ruless/{id}", response_model=MdmMatchingRulesResponse)
def get_mdm_matching_rules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MdmMatchingRules by ID."""
    service = MdmMatchingRulesService(db)
    return service.get(id)

@router.post("/mdm_matching_ruless", response_model=MdmMatchingRulesResponse, status_code=status.HTTP_201_CREATED)
def create_mdm_matching_rules(
    data: MdmMatchingRulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new MdmMatchingRules."""
    service = MdmMatchingRulesService(db)
    return service.create(data)

@router.put("/mdm_matching_ruless/{id}", response_model=MdmMatchingRulesResponse)
def update_mdm_matching_rules(
    id: UUID,
    data: MdmMatchingRulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MdmMatchingRules."""
    service = MdmMatchingRulesService(db)
    return service.update(id, data)

@router.delete("/mdm_matching_ruless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mdm_matching_rules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MdmMatchingRules (soft delete by default)."""
    service = MdmMatchingRulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mdm_matching_ruless/search", response_model=List[MdmMatchingRulesResponse])
def search_mdm_matching_ruless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MdmMatchingRules by name/code/description."""
    service = MdmMatchingRulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mdm_merge_logs", response_model=MdmMergeLogListResponse)
def list_mdm_merge_logs(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MdmMergeLog records."""
    service = MdmMergeLogService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mdm_merge_logs/{id}", response_model=MdmMergeLogResponse)
def get_mdm_merge_log(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MdmMergeLog by ID."""
    service = MdmMergeLogService(db)
    return service.get(id)

@router.post("/mdm_merge_logs", response_model=MdmMergeLogResponse, status_code=status.HTTP_201_CREATED)
def create_mdm_merge_log(
    data: MdmMergeLogCreate,
    db: Session = Depends(get_db)
):
    """Create a new MdmMergeLog."""
    service = MdmMergeLogService(db)
    return service.create(data)

@router.put("/mdm_merge_logs/{id}", response_model=MdmMergeLogResponse)
def update_mdm_merge_log(
    id: UUID,
    data: MdmMergeLogUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MdmMergeLog."""
    service = MdmMergeLogService(db)
    return service.update(id, data)

@router.delete("/mdm_merge_logs/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mdm_merge_log(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MdmMergeLog (soft delete by default)."""
    service = MdmMergeLogService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mdm_merge_logs/search", response_model=List[MdmMergeLogResponse])
def search_mdm_merge_logs(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MdmMergeLog by name/code/description."""
    service = MdmMergeLogService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mobile_appss", response_model=MobileAppsListResponse)
def list_mobile_appss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MobileApps records."""
    service = MobileAppsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mobile_appss/{id}", response_model=MobileAppsResponse)
def get_mobile_apps(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MobileApps by ID."""
    service = MobileAppsService(db)
    return service.get(id)

@router.post("/mobile_appss", response_model=MobileAppsResponse, status_code=status.HTTP_201_CREATED)
def create_mobile_apps(
    data: MobileAppsCreate,
    db: Session = Depends(get_db)
):
    """Create a new MobileApps."""
    service = MobileAppsService(db)
    return service.create(data)

@router.put("/mobile_appss/{id}", response_model=MobileAppsResponse)
def update_mobile_apps(
    id: UUID,
    data: MobileAppsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MobileApps."""
    service = MobileAppsService(db)
    return service.update(id, data)

@router.delete("/mobile_appss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mobile_apps(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MobileApps (soft delete by default)."""
    service = MobileAppsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mobile_appss/search", response_model=List[MobileAppsResponse])
def search_mobile_appss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MobileApps by name/code/description."""
    service = MobileAppsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mobile_device_registrys", response_model=MobileDeviceRegistryListResponse)
def list_mobile_device_registrys(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MobileDeviceRegistry records."""
    service = MobileDeviceRegistryService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mobile_device_registrys/{id}", response_model=MobileDeviceRegistryResponse)
def get_mobile_device_registry(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MobileDeviceRegistry by ID."""
    service = MobileDeviceRegistryService(db)
    return service.get(id)

@router.post("/mobile_device_registrys", response_model=MobileDeviceRegistryResponse, status_code=status.HTTP_201_CREATED)
def create_mobile_device_registry(
    data: MobileDeviceRegistryCreate,
    db: Session = Depends(get_db)
):
    """Create a new MobileDeviceRegistry."""
    service = MobileDeviceRegistryService(db)
    return service.create(data)

@router.put("/mobile_device_registrys/{id}", response_model=MobileDeviceRegistryResponse)
def update_mobile_device_registry(
    id: UUID,
    data: MobileDeviceRegistryUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MobileDeviceRegistry."""
    service = MobileDeviceRegistryService(db)
    return service.update(id, data)

@router.delete("/mobile_device_registrys/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mobile_device_registry(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MobileDeviceRegistry (soft delete by default)."""
    service = MobileDeviceRegistryService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mobile_device_registrys/search", response_model=List[MobileDeviceRegistryResponse])
def search_mobile_device_registrys(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MobileDeviceRegistry by name/code/description."""
    service = MobileDeviceRegistryService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mobile_push_notificationss", response_model=MobilePushNotificationsListResponse)
def list_mobile_push_notificationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MobilePushNotifications records."""
    service = MobilePushNotificationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mobile_push_notificationss/{id}", response_model=MobilePushNotificationsResponse)
def get_mobile_push_notifications(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MobilePushNotifications by ID."""
    service = MobilePushNotificationsService(db)
    return service.get(id)

@router.post("/mobile_push_notificationss", response_model=MobilePushNotificationsResponse, status_code=status.HTTP_201_CREATED)
def create_mobile_push_notifications(
    data: MobilePushNotificationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new MobilePushNotifications."""
    service = MobilePushNotificationsService(db)
    return service.create(data)

@router.put("/mobile_push_notificationss/{id}", response_model=MobilePushNotificationsResponse)
def update_mobile_push_notifications(
    id: UUID,
    data: MobilePushNotificationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MobilePushNotifications."""
    service = MobilePushNotificationsService(db)
    return service.update(id, data)

@router.delete("/mobile_push_notificationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mobile_push_notifications(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MobilePushNotifications (soft delete by default)."""
    service = MobilePushNotificationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mobile_push_notificationss/search", response_model=List[MobilePushNotificationsResponse])
def search_mobile_push_notificationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MobilePushNotifications by name/code/description."""
    service = MobilePushNotificationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/mobile_sync_queuess", response_model=MobileSyncQueuesListResponse)
def list_mobile_sync_queuess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all MobileSyncQueues records."""
    service = MobileSyncQueuesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/mobile_sync_queuess/{id}", response_model=MobileSyncQueuesResponse)
def get_mobile_sync_queues(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get MobileSyncQueues by ID."""
    service = MobileSyncQueuesService(db)
    return service.get(id)

@router.post("/mobile_sync_queuess", response_model=MobileSyncQueuesResponse, status_code=status.HTTP_201_CREATED)
def create_mobile_sync_queues(
    data: MobileSyncQueuesCreate,
    db: Session = Depends(get_db)
):
    """Create a new MobileSyncQueues."""
    service = MobileSyncQueuesService(db)
    return service.create(data)

@router.put("/mobile_sync_queuess/{id}", response_model=MobileSyncQueuesResponse)
def update_mobile_sync_queues(
    id: UUID,
    data: MobileSyncQueuesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing MobileSyncQueues."""
    service = MobileSyncQueuesService(db)
    return service.update(id, data)

@router.delete("/mobile_sync_queuess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_mobile_sync_queues(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a MobileSyncQueues (soft delete by default)."""
    service = MobileSyncQueuesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/mobile_sync_queuess/search", response_model=List[MobileSyncQueuesResponse])
def search_mobile_sync_queuess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search MobileSyncQueues by name/code/description."""
    service = MobileSyncQueuesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/pm_development_planss", response_model=PmDevelopmentPlansListResponse)
def list_pm_development_planss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PmDevelopmentPlans records."""
    service = PmDevelopmentPlansService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/pm_development_planss/{id}", response_model=PmDevelopmentPlansResponse)
def get_pm_development_plans(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PmDevelopmentPlans by ID."""
    service = PmDevelopmentPlansService(db)
    return service.get(id)

@router.post("/pm_development_planss", response_model=PmDevelopmentPlansResponse, status_code=status.HTTP_201_CREATED)
def create_pm_development_plans(
    data: PmDevelopmentPlansCreate,
    db: Session = Depends(get_db)
):
    """Create a new PmDevelopmentPlans."""
    service = PmDevelopmentPlansService(db)
    return service.create(data)

@router.put("/pm_development_planss/{id}", response_model=PmDevelopmentPlansResponse)
def update_pm_development_plans(
    id: UUID,
    data: PmDevelopmentPlansUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PmDevelopmentPlans."""
    service = PmDevelopmentPlansService(db)
    return service.update(id, data)

@router.delete("/pm_development_planss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_pm_development_plans(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PmDevelopmentPlans (soft delete by default)."""
    service = PmDevelopmentPlansService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/pm_development_planss/search", response_model=List[PmDevelopmentPlansResponse])
def search_pm_development_planss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PmDevelopmentPlans by name/code/description."""
    service = PmDevelopmentPlansService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/pm_goalss", response_model=PmGoalsListResponse)
def list_pm_goalss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PmGoals records."""
    service = PmGoalsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/pm_goalss/{id}", response_model=PmGoalsResponse)
def get_pm_goals(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PmGoals by ID."""
    service = PmGoalsService(db)
    return service.get(id)

@router.post("/pm_goalss", response_model=PmGoalsResponse, status_code=status.HTTP_201_CREATED)
def create_pm_goals(
    data: PmGoalsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PmGoals."""
    service = PmGoalsService(db)
    return service.create(data)

@router.put("/pm_goalss/{id}", response_model=PmGoalsResponse)
def update_pm_goals(
    id: UUID,
    data: PmGoalsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PmGoals."""
    service = PmGoalsService(db)
    return service.update(id, data)

@router.delete("/pm_goalss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_pm_goals(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PmGoals (soft delete by default)."""
    service = PmGoalsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/pm_goalss/search", response_model=List[PmGoalsResponse])
def search_pm_goalss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PmGoals by name/code/description."""
    service = PmGoalsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/pm_ratingss", response_model=PmRatingsListResponse)
def list_pm_ratingss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PmRatings records."""
    service = PmRatingsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/pm_ratingss/{id}", response_model=PmRatingsResponse)
def get_pm_ratings(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PmRatings by ID."""
    service = PmRatingsService(db)
    return service.get(id)

@router.post("/pm_ratingss", response_model=PmRatingsResponse, status_code=status.HTTP_201_CREATED)
def create_pm_ratings(
    data: PmRatingsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PmRatings."""
    service = PmRatingsService(db)
    return service.create(data)

@router.put("/pm_ratingss/{id}", response_model=PmRatingsResponse)
def update_pm_ratings(
    id: UUID,
    data: PmRatingsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PmRatings."""
    service = PmRatingsService(db)
    return service.update(id, data)

@router.delete("/pm_ratingss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_pm_ratings(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PmRatings (soft delete by default)."""
    service = PmRatingsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/pm_ratingss/search", response_model=List[PmRatingsResponse])
def search_pm_ratingss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PmRatings by name/code/description."""
    service = PmRatingsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/pm_reviewss", response_model=PmReviewsListResponse)
def list_pm_reviewss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PmReviews records."""
    service = PmReviewsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/pm_reviewss/{id}", response_model=PmReviewsResponse)
def get_pm_reviews(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PmReviews by ID."""
    service = PmReviewsService(db)
    return service.get(id)

@router.post("/pm_reviewss", response_model=PmReviewsResponse, status_code=status.HTTP_201_CREATED)
def create_pm_reviews(
    data: PmReviewsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PmReviews."""
    service = PmReviewsService(db)
    return service.create(data)

@router.put("/pm_reviewss/{id}", response_model=PmReviewsResponse)
def update_pm_reviews(
    id: UUID,
    data: PmReviewsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PmReviews."""
    service = PmReviewsService(db)
    return service.update(id, data)

@router.delete("/pm_reviewss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_pm_reviews(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PmReviews (soft delete by default)."""
    service = PmReviewsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/pm_reviewss/search", response_model=List[PmReviewsResponse])
def search_pm_reviewss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PmReviews by name/code/description."""
    service = PmReviewsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/portal_customerss", response_model=PortalCustomersListResponse)
def list_portal_customerss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PortalCustomers records."""
    service = PortalCustomersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/portal_customerss/{id}", response_model=PortalCustomersResponse)
def get_portal_customers(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PortalCustomers by ID."""
    service = PortalCustomersService(db)
    return service.get(id)

@router.post("/portal_customerss", response_model=PortalCustomersResponse, status_code=status.HTTP_201_CREATED)
def create_portal_customers(
    data: PortalCustomersCreate,
    db: Session = Depends(get_db)
):
    """Create a new PortalCustomers."""
    service = PortalCustomersService(db)
    return service.create(data)

@router.put("/portal_customerss/{id}", response_model=PortalCustomersResponse)
def update_portal_customers(
    id: UUID,
    data: PortalCustomersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PortalCustomers."""
    service = PortalCustomersService(db)
    return service.update(id, data)

@router.delete("/portal_customerss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_portal_customers(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PortalCustomers (soft delete by default)."""
    service = PortalCustomersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/portal_customerss/search", response_model=List[PortalCustomersResponse])
def search_portal_customerss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PortalCustomers by name/code/description."""
    service = PortalCustomersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/portal_knowledge_bases", response_model=PortalKnowledgeBaseListResponse)
def list_portal_knowledge_bases(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PortalKnowledgeBase records."""
    service = PortalKnowledgeBaseService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/portal_knowledge_bases/{id}", response_model=PortalKnowledgeBaseResponse)
def get_portal_knowledge_base(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PortalKnowledgeBase by ID."""
    service = PortalKnowledgeBaseService(db)
    return service.get(id)

@router.post("/portal_knowledge_bases", response_model=PortalKnowledgeBaseResponse, status_code=status.HTTP_201_CREATED)
def create_portal_knowledge_base(
    data: PortalKnowledgeBaseCreate,
    db: Session = Depends(get_db)
):
    """Create a new PortalKnowledgeBase."""
    service = PortalKnowledgeBaseService(db)
    return service.create(data)

@router.put("/portal_knowledge_bases/{id}", response_model=PortalKnowledgeBaseResponse)
def update_portal_knowledge_base(
    id: UUID,
    data: PortalKnowledgeBaseUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PortalKnowledgeBase."""
    service = PortalKnowledgeBaseService(db)
    return service.update(id, data)

@router.delete("/portal_knowledge_bases/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_portal_knowledge_base(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PortalKnowledgeBase (soft delete by default)."""
    service = PortalKnowledgeBaseService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/portal_knowledge_bases/search", response_model=List[PortalKnowledgeBaseResponse])
def search_portal_knowledge_bases(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PortalKnowledgeBase by name/code/description."""
    service = PortalKnowledgeBaseService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/portal_rfqss", response_model=PortalRfqsListResponse)
def list_portal_rfqss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PortalRfqs records."""
    service = PortalRfqsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/portal_rfqss/{id}", response_model=PortalRfqsResponse)
def get_portal_rfqs(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PortalRfqs by ID."""
    service = PortalRfqsService(db)
    return service.get(id)

@router.post("/portal_rfqss", response_model=PortalRfqsResponse, status_code=status.HTTP_201_CREATED)
def create_portal_rfqs(
    data: PortalRfqsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PortalRfqs."""
    service = PortalRfqsService(db)
    return service.create(data)

@router.put("/portal_rfqss/{id}", response_model=PortalRfqsResponse)
def update_portal_rfqs(
    id: UUID,
    data: PortalRfqsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PortalRfqs."""
    service = PortalRfqsService(db)
    return service.update(id, data)

@router.delete("/portal_rfqss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_portal_rfqs(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PortalRfqs (soft delete by default)."""
    service = PortalRfqsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/portal_rfqss/search", response_model=List[PortalRfqsResponse])
def search_portal_rfqss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PortalRfqs by name/code/description."""
    service = PortalRfqsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/portal_ticketss", response_model=PortalTicketsListResponse)
def list_portal_ticketss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PortalTickets records."""
    service = PortalTicketsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/portal_ticketss/{id}", response_model=PortalTicketsResponse)
def get_portal_tickets(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PortalTickets by ID."""
    service = PortalTicketsService(db)
    return service.get(id)

@router.post("/portal_ticketss", response_model=PortalTicketsResponse, status_code=status.HTTP_201_CREATED)
def create_portal_tickets(
    data: PortalTicketsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PortalTickets."""
    service = PortalTicketsService(db)
    return service.create(data)

@router.put("/portal_ticketss/{id}", response_model=PortalTicketsResponse)
def update_portal_tickets(
    id: UUID,
    data: PortalTicketsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PortalTickets."""
    service = PortalTicketsService(db)
    return service.update(id, data)

@router.delete("/portal_ticketss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_portal_tickets(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PortalTickets (soft delete by default)."""
    service = PortalTicketsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/portal_ticketss/search", response_model=List[PortalTicketsResponse])
def search_portal_ticketss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PortalTickets by name/code/description."""
    service = PortalTicketsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/portal_vendor_evaluationss", response_model=PortalVendorEvaluationsListResponse)
def list_portal_vendor_evaluationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PortalVendorEvaluations records."""
    service = PortalVendorEvaluationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/portal_vendor_evaluationss/{id}", response_model=PortalVendorEvaluationsResponse)
def get_portal_vendor_evaluations(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PortalVendorEvaluations by ID."""
    service = PortalVendorEvaluationsService(db)
    return service.get(id)

@router.post("/portal_vendor_evaluationss", response_model=PortalVendorEvaluationsResponse, status_code=status.HTTP_201_CREATED)
def create_portal_vendor_evaluations(
    data: PortalVendorEvaluationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PortalVendorEvaluations."""
    service = PortalVendorEvaluationsService(db)
    return service.create(data)

@router.put("/portal_vendor_evaluationss/{id}", response_model=PortalVendorEvaluationsResponse)
def update_portal_vendor_evaluations(
    id: UUID,
    data: PortalVendorEvaluationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PortalVendorEvaluations."""
    service = PortalVendorEvaluationsService(db)
    return service.update(id, data)

@router.delete("/portal_vendor_evaluationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_portal_vendor_evaluations(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PortalVendorEvaluations (soft delete by default)."""
    service = PortalVendorEvaluationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/portal_vendor_evaluationss/search", response_model=List[PortalVendorEvaluationsResponse])
def search_portal_vendor_evaluationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PortalVendorEvaluations by name/code/description."""
    service = PortalVendorEvaluationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/portal_vendorss", response_model=PortalVendorsListResponse)
def list_portal_vendorss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all PortalVendors records."""
    service = PortalVendorsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/portal_vendorss/{id}", response_model=PortalVendorsResponse)
def get_portal_vendors(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get PortalVendors by ID."""
    service = PortalVendorsService(db)
    return service.get(id)

@router.post("/portal_vendorss", response_model=PortalVendorsResponse, status_code=status.HTTP_201_CREATED)
def create_portal_vendors(
    data: PortalVendorsCreate,
    db: Session = Depends(get_db)
):
    """Create a new PortalVendors."""
    service = PortalVendorsService(db)
    return service.create(data)

@router.put("/portal_vendorss/{id}", response_model=PortalVendorsResponse)
def update_portal_vendors(
    id: UUID,
    data: PortalVendorsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing PortalVendors."""
    service = PortalVendorsService(db)
    return service.update(id, data)

@router.delete("/portal_vendorss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_portal_vendors(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a PortalVendors (soft delete by default)."""
    service = PortalVendorsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/portal_vendorss/search", response_model=List[PortalVendorsResponse])
def search_portal_vendorss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search PortalVendors by name/code/description."""
    service = PortalVendorsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/rec_candidatess", response_model=RecCandidatesListResponse)
def list_rec_candidatess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RecCandidates records."""
    service = RecCandidatesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/rec_candidatess/{id}", response_model=RecCandidatesResponse)
def get_rec_candidates(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RecCandidates by ID."""
    service = RecCandidatesService(db)
    return service.get(id)

@router.post("/rec_candidatess", response_model=RecCandidatesResponse, status_code=status.HTTP_201_CREATED)
def create_rec_candidates(
    data: RecCandidatesCreate,
    db: Session = Depends(get_db)
):
    """Create a new RecCandidates."""
    service = RecCandidatesService(db)
    return service.create(data)

@router.put("/rec_candidatess/{id}", response_model=RecCandidatesResponse)
def update_rec_candidates(
    id: UUID,
    data: RecCandidatesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RecCandidates."""
    service = RecCandidatesService(db)
    return service.update(id, data)

@router.delete("/rec_candidatess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_rec_candidates(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RecCandidates (soft delete by default)."""
    service = RecCandidatesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/rec_candidatess/search", response_model=List[RecCandidatesResponse])
def search_rec_candidatess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RecCandidates by name/code/description."""
    service = RecCandidatesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/rec_interviewss", response_model=RecInterviewsListResponse)
def list_rec_interviewss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RecInterviews records."""
    service = RecInterviewsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/rec_interviewss/{id}", response_model=RecInterviewsResponse)
def get_rec_interviews(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RecInterviews by ID."""
    service = RecInterviewsService(db)
    return service.get(id)

@router.post("/rec_interviewss", response_model=RecInterviewsResponse, status_code=status.HTTP_201_CREATED)
def create_rec_interviews(
    data: RecInterviewsCreate,
    db: Session = Depends(get_db)
):
    """Create a new RecInterviews."""
    service = RecInterviewsService(db)
    return service.create(data)

@router.put("/rec_interviewss/{id}", response_model=RecInterviewsResponse)
def update_rec_interviews(
    id: UUID,
    data: RecInterviewsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RecInterviews."""
    service = RecInterviewsService(db)
    return service.update(id, data)

@router.delete("/rec_interviewss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_rec_interviews(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RecInterviews (soft delete by default)."""
    service = RecInterviewsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/rec_interviewss/search", response_model=List[RecInterviewsResponse])
def search_rec_interviewss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RecInterviews by name/code/description."""
    service = RecInterviewsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/rec_job_requisitionss", response_model=RecJobRequisitionsListResponse)
def list_rec_job_requisitionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RecJobRequisitions records."""
    service = RecJobRequisitionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/rec_job_requisitionss/{id}", response_model=RecJobRequisitionsResponse)
def get_rec_job_requisitions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RecJobRequisitions by ID."""
    service = RecJobRequisitionsService(db)
    return service.get(id)

@router.post("/rec_job_requisitionss", response_model=RecJobRequisitionsResponse, status_code=status.HTTP_201_CREATED)
def create_rec_job_requisitions(
    data: RecJobRequisitionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new RecJobRequisitions."""
    service = RecJobRequisitionsService(db)
    return service.create(data)

@router.put("/rec_job_requisitionss/{id}", response_model=RecJobRequisitionsResponse)
def update_rec_job_requisitions(
    id: UUID,
    data: RecJobRequisitionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RecJobRequisitions."""
    service = RecJobRequisitionsService(db)
    return service.update(id, data)

@router.delete("/rec_job_requisitionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_rec_job_requisitions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RecJobRequisitions (soft delete by default)."""
    service = RecJobRequisitionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/rec_job_requisitionss/search", response_model=List[RecJobRequisitionsResponse])
def search_rec_job_requisitionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RecJobRequisitions by name/code/description."""
    service = RecJobRequisitionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/rec_offerss", response_model=RecOffersListResponse)
def list_rec_offerss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RecOffers records."""
    service = RecOffersService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/rec_offerss/{id}", response_model=RecOffersResponse)
def get_rec_offers(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RecOffers by ID."""
    service = RecOffersService(db)
    return service.get(id)

@router.post("/rec_offerss", response_model=RecOffersResponse, status_code=status.HTTP_201_CREATED)
def create_rec_offers(
    data: RecOffersCreate,
    db: Session = Depends(get_db)
):
    """Create a new RecOffers."""
    service = RecOffersService(db)
    return service.create(data)

@router.put("/rec_offerss/{id}", response_model=RecOffersResponse)
def update_rec_offers(
    id: UUID,
    data: RecOffersUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RecOffers."""
    service = RecOffersService(db)
    return service.update(id, data)

@router.delete("/rec_offerss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_rec_offers(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RecOffers (soft delete by default)."""
    service = RecOffersService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/rec_offerss/search", response_model=List[RecOffersResponse])
def search_rec_offerss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RecOffers by name/code/description."""
    service = RecOffersService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/revrec_adjustmentss", response_model=RevrecAdjustmentsListResponse)
def list_revrec_adjustmentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RevrecAdjustments records."""
    service = RevrecAdjustmentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/revrec_adjustmentss/{id}", response_model=RevrecAdjustmentsResponse)
def get_revrec_adjustments(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RevrecAdjustments by ID."""
    service = RevrecAdjustmentsService(db)
    return service.get(id)

@router.post("/revrec_adjustmentss", response_model=RevrecAdjustmentsResponse, status_code=status.HTTP_201_CREATED)
def create_revrec_adjustments(
    data: RevrecAdjustmentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new RevrecAdjustments."""
    service = RevrecAdjustmentsService(db)
    return service.create(data)

@router.put("/revrec_adjustmentss/{id}", response_model=RevrecAdjustmentsResponse)
def update_revrec_adjustments(
    id: UUID,
    data: RevrecAdjustmentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RevrecAdjustments."""
    service = RevrecAdjustmentsService(db)
    return service.update(id, data)

@router.delete("/revrec_adjustmentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_revrec_adjustments(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RevrecAdjustments (soft delete by default)."""
    service = RevrecAdjustmentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/revrec_adjustmentss/search", response_model=List[RevrecAdjustmentsResponse])
def search_revrec_adjustmentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RevrecAdjustments by name/code/description."""
    service = RevrecAdjustmentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/revrec_contractss", response_model=RevrecContractsListResponse)
def list_revrec_contractss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RevrecContracts records."""
    service = RevrecContractsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/revrec_contractss/{id}", response_model=RevrecContractsResponse)
def get_revrec_contracts(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RevrecContracts by ID."""
    service = RevrecContractsService(db)
    return service.get(id)

@router.post("/revrec_contractss", response_model=RevrecContractsResponse, status_code=status.HTTP_201_CREATED)
def create_revrec_contracts(
    data: RevrecContractsCreate,
    db: Session = Depends(get_db)
):
    """Create a new RevrecContracts."""
    service = RevrecContractsService(db)
    return service.create(data)

@router.put("/revrec_contractss/{id}", response_model=RevrecContractsResponse)
def update_revrec_contracts(
    id: UUID,
    data: RevrecContractsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RevrecContracts."""
    service = RevrecContractsService(db)
    return service.update(id, data)

@router.delete("/revrec_contractss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_revrec_contracts(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RevrecContracts (soft delete by default)."""
    service = RevrecContractsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/revrec_contractss/search", response_model=List[RevrecContractsResponse])
def search_revrec_contractss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RevrecContracts by name/code/description."""
    service = RevrecContractsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/revrec_performance_obligationss", response_model=RevrecPerformanceObligationsListResponse)
def list_revrec_performance_obligationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all RevrecPerformanceObligations records."""
    service = RevrecPerformanceObligationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/revrec_performance_obligationss/{id}", response_model=RevrecPerformanceObligationsResponse)
def get_revrec_performance_obligations(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get RevrecPerformanceObligations by ID."""
    service = RevrecPerformanceObligationsService(db)
    return service.get(id)

@router.post("/revrec_performance_obligationss", response_model=RevrecPerformanceObligationsResponse, status_code=status.HTTP_201_CREATED)
def create_revrec_performance_obligations(
    data: RevrecPerformanceObligationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new RevrecPerformanceObligations."""
    service = RevrecPerformanceObligationsService(db)
    return service.create(data)

@router.put("/revrec_performance_obligationss/{id}", response_model=RevrecPerformanceObligationsResponse)
def update_revrec_performance_obligations(
    id: UUID,
    data: RevrecPerformanceObligationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing RevrecPerformanceObligations."""
    service = RevrecPerformanceObligationsService(db)
    return service.update(id, data)

@router.delete("/revrec_performance_obligationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_revrec_performance_obligations(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a RevrecPerformanceObligations (soft delete by default)."""
    service = RevrecPerformanceObligationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/revrec_performance_obligationss/search", response_model=List[RevrecPerformanceObligationsResponse])
def search_revrec_performance_obligationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search RevrecPerformanceObligations by name/code/description."""
    service = RevrecPerformanceObligationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sec_access_logss", response_model=SecAccessLogsListResponse)
def list_sec_access_logss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SecAccessLogs records."""
    service = SecAccessLogsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sec_access_logss/{id}", response_model=SecAccessLogsResponse)
def get_sec_access_logs(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SecAccessLogs by ID."""
    service = SecAccessLogsService(db)
    return service.get(id)

@router.post("/sec_access_logss", response_model=SecAccessLogsResponse, status_code=status.HTTP_201_CREATED)
def create_sec_access_logs(
    data: SecAccessLogsCreate,
    db: Session = Depends(get_db)
):
    """Create a new SecAccessLogs."""
    service = SecAccessLogsService(db)
    return service.create(data)

@router.put("/sec_access_logss/{id}", response_model=SecAccessLogsResponse)
def update_sec_access_logs(
    id: UUID,
    data: SecAccessLogsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SecAccessLogs."""
    service = SecAccessLogsService(db)
    return service.update(id, data)

@router.delete("/sec_access_logss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sec_access_logs(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SecAccessLogs (soft delete by default)."""
    service = SecAccessLogsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sec_access_logss/search", response_model=List[SecAccessLogsResponse])
def search_sec_access_logss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SecAccessLogs by name/code/description."""
    service = SecAccessLogsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sec_certificatess", response_model=SecCertificatesListResponse)
def list_sec_certificatess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SecCertificates records."""
    service = SecCertificatesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sec_certificatess/{id}", response_model=SecCertificatesResponse)
def get_sec_certificates(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SecCertificates by ID."""
    service = SecCertificatesService(db)
    return service.get(id)

@router.post("/sec_certificatess", response_model=SecCertificatesResponse, status_code=status.HTTP_201_CREATED)
def create_sec_certificates(
    data: SecCertificatesCreate,
    db: Session = Depends(get_db)
):
    """Create a new SecCertificates."""
    service = SecCertificatesService(db)
    return service.create(data)

@router.put("/sec_certificatess/{id}", response_model=SecCertificatesResponse)
def update_sec_certificates(
    id: UUID,
    data: SecCertificatesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SecCertificates."""
    service = SecCertificatesService(db)
    return service.update(id, data)

@router.delete("/sec_certificatess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sec_certificates(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SecCertificates (soft delete by default)."""
    service = SecCertificatesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sec_certificatess/search", response_model=List[SecCertificatesResponse])
def search_sec_certificatess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SecCertificates by name/code/description."""
    service = SecCertificatesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sec_incidentss", response_model=SecIncidentsListResponse)
def list_sec_incidentss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SecIncidents records."""
    service = SecIncidentsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sec_incidentss/{id}", response_model=SecIncidentsResponse)
def get_sec_incidents(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SecIncidents by ID."""
    service = SecIncidentsService(db)
    return service.get(id)

@router.post("/sec_incidentss", response_model=SecIncidentsResponse, status_code=status.HTTP_201_CREATED)
def create_sec_incidents(
    data: SecIncidentsCreate,
    db: Session = Depends(get_db)
):
    """Create a new SecIncidents."""
    service = SecIncidentsService(db)
    return service.create(data)

@router.put("/sec_incidentss/{id}", response_model=SecIncidentsResponse)
def update_sec_incidents(
    id: UUID,
    data: SecIncidentsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SecIncidents."""
    service = SecIncidentsService(db)
    return service.update(id, data)

@router.delete("/sec_incidentss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sec_incidents(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SecIncidents (soft delete by default)."""
    service = SecIncidentsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sec_incidentss/search", response_model=List[SecIncidentsResponse])
def search_sec_incidentss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SecIncidents by name/code/description."""
    service = SecIncidentsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sec_policiess", response_model=SecPoliciesListResponse)
def list_sec_policiess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SecPolicies records."""
    service = SecPoliciesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sec_policiess/{id}", response_model=SecPoliciesResponse)
def get_sec_policies(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SecPolicies by ID."""
    service = SecPoliciesService(db)
    return service.get(id)

@router.post("/sec_policiess", response_model=SecPoliciesResponse, status_code=status.HTTP_201_CREATED)
def create_sec_policies(
    data: SecPoliciesCreate,
    db: Session = Depends(get_db)
):
    """Create a new SecPolicies."""
    service = SecPoliciesService(db)
    return service.create(data)

@router.put("/sec_policiess/{id}", response_model=SecPoliciesResponse)
def update_sec_policies(
    id: UUID,
    data: SecPoliciesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SecPolicies."""
    service = SecPoliciesService(db)
    return service.update(id, data)

@router.delete("/sec_policiess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sec_policies(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SecPolicies (soft delete by default)."""
    service = SecPoliciesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sec_policiess/search", response_model=List[SecPoliciesResponse])
def search_sec_policiess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SecPolicies by name/code/description."""
    service = SecPoliciesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sec_roles_exts", response_model=SecRolesExtListResponse)
def list_sec_roles_exts(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SecRolesExt records."""
    service = SecRolesExtService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sec_roles_exts/{id}", response_model=SecRolesExtResponse)
def get_sec_roles_ext(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SecRolesExt by ID."""
    service = SecRolesExtService(db)
    return service.get(id)

@router.post("/sec_roles_exts", response_model=SecRolesExtResponse, status_code=status.HTTP_201_CREATED)
def create_sec_roles_ext(
    data: SecRolesExtCreate,
    db: Session = Depends(get_db)
):
    """Create a new SecRolesExt."""
    service = SecRolesExtService(db)
    return service.create(data)

@router.put("/sec_roles_exts/{id}", response_model=SecRolesExtResponse)
def update_sec_roles_ext(
    id: UUID,
    data: SecRolesExtUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SecRolesExt."""
    service = SecRolesExtService(db)
    return service.update(id, data)

@router.delete("/sec_roles_exts/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sec_roles_ext(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SecRolesExt (soft delete by default)."""
    service = SecRolesExtService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sec_roles_exts/search", response_model=List[SecRolesExtResponse])
def search_sec_roles_exts(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SecRolesExt by name/code/description."""
    service = SecRolesExtService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sec_vulnerabilitiess", response_model=SecVulnerabilitiesListResponse)
def list_sec_vulnerabilitiess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SecVulnerabilities records."""
    service = SecVulnerabilitiesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sec_vulnerabilitiess/{id}", response_model=SecVulnerabilitiesResponse)
def get_sec_vulnerabilities(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SecVulnerabilities by ID."""
    service = SecVulnerabilitiesService(db)
    return service.get(id)

@router.post("/sec_vulnerabilitiess", response_model=SecVulnerabilitiesResponse, status_code=status.HTTP_201_CREATED)
def create_sec_vulnerabilities(
    data: SecVulnerabilitiesCreate,
    db: Session = Depends(get_db)
):
    """Create a new SecVulnerabilities."""
    service = SecVulnerabilitiesService(db)
    return service.create(data)

@router.put("/sec_vulnerabilitiess/{id}", response_model=SecVulnerabilitiesResponse)
def update_sec_vulnerabilities(
    id: UUID,
    data: SecVulnerabilitiesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SecVulnerabilities."""
    service = SecVulnerabilitiesService(db)
    return service.update(id, data)

@router.delete("/sec_vulnerabilitiess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sec_vulnerabilities(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SecVulnerabilities (soft delete by default)."""
    service = SecVulnerabilitiesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sec_vulnerabilitiess/search", response_model=List[SecVulnerabilitiesResponse])
def search_sec_vulnerabilitiess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SecVulnerabilities by name/code/description."""
    service = SecVulnerabilitiesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sub_invoices_scheduleds", response_model=SubInvoicesScheduledListResponse)
def list_sub_invoices_scheduleds(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SubInvoicesScheduled records."""
    service = SubInvoicesScheduledService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sub_invoices_scheduleds/{id}", response_model=SubInvoicesScheduledResponse)
def get_sub_invoices_scheduled(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SubInvoicesScheduled by ID."""
    service = SubInvoicesScheduledService(db)
    return service.get(id)

@router.post("/sub_invoices_scheduleds", response_model=SubInvoicesScheduledResponse, status_code=status.HTTP_201_CREATED)
def create_sub_invoices_scheduled(
    data: SubInvoicesScheduledCreate,
    db: Session = Depends(get_db)
):
    """Create a new SubInvoicesScheduled."""
    service = SubInvoicesScheduledService(db)
    return service.create(data)

@router.put("/sub_invoices_scheduleds/{id}", response_model=SubInvoicesScheduledResponse)
def update_sub_invoices_scheduled(
    id: UUID,
    data: SubInvoicesScheduledUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SubInvoicesScheduled."""
    service = SubInvoicesScheduledService(db)
    return service.update(id, data)

@router.delete("/sub_invoices_scheduleds/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sub_invoices_scheduled(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SubInvoicesScheduled (soft delete by default)."""
    service = SubInvoicesScheduledService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sub_invoices_scheduleds/search", response_model=List[SubInvoicesScheduledResponse])
def search_sub_invoices_scheduleds(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SubInvoicesScheduled by name/code/description."""
    service = SubInvoicesScheduledService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sub_planss", response_model=SubPlansListResponse)
def list_sub_planss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SubPlans records."""
    service = SubPlansService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sub_planss/{id}", response_model=SubPlansResponse)
def get_sub_plans(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SubPlans by ID."""
    service = SubPlansService(db)
    return service.get(id)

@router.post("/sub_planss", response_model=SubPlansResponse, status_code=status.HTTP_201_CREATED)
def create_sub_plans(
    data: SubPlansCreate,
    db: Session = Depends(get_db)
):
    """Create a new SubPlans."""
    service = SubPlansService(db)
    return service.create(data)

@router.put("/sub_planss/{id}", response_model=SubPlansResponse)
def update_sub_plans(
    id: UUID,
    data: SubPlansUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SubPlans."""
    service = SubPlansService(db)
    return service.update(id, data)

@router.delete("/sub_planss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sub_plans(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SubPlans (soft delete by default)."""
    service = SubPlansService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sub_planss/search", response_model=List[SubPlansResponse])
def search_sub_planss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SubPlans by name/code/description."""
    service = SubPlansService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sub_renewal_logs", response_model=SubRenewalLogListResponse)
def list_sub_renewal_logs(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SubRenewalLog records."""
    service = SubRenewalLogService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sub_renewal_logs/{id}", response_model=SubRenewalLogResponse)
def get_sub_renewal_log(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SubRenewalLog by ID."""
    service = SubRenewalLogService(db)
    return service.get(id)

@router.post("/sub_renewal_logs", response_model=SubRenewalLogResponse, status_code=status.HTTP_201_CREATED)
def create_sub_renewal_log(
    data: SubRenewalLogCreate,
    db: Session = Depends(get_db)
):
    """Create a new SubRenewalLog."""
    service = SubRenewalLogService(db)
    return service.create(data)

@router.put("/sub_renewal_logs/{id}", response_model=SubRenewalLogResponse)
def update_sub_renewal_log(
    id: UUID,
    data: SubRenewalLogUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SubRenewalLog."""
    service = SubRenewalLogService(db)
    return service.update(id, data)

@router.delete("/sub_renewal_logs/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sub_renewal_log(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SubRenewalLog (soft delete by default)."""
    service = SubRenewalLogService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sub_renewal_logs/search", response_model=List[SubRenewalLogResponse])
def search_sub_renewal_logs(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SubRenewalLog by name/code/description."""
    service = SubRenewalLogService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sub_subscriptionss", response_model=SubSubscriptionsListResponse)
def list_sub_subscriptionss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SubSubscriptions records."""
    service = SubSubscriptionsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sub_subscriptionss/{id}", response_model=SubSubscriptionsResponse)
def get_sub_subscriptions(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SubSubscriptions by ID."""
    service = SubSubscriptionsService(db)
    return service.get(id)

@router.post("/sub_subscriptionss", response_model=SubSubscriptionsResponse, status_code=status.HTTP_201_CREATED)
def create_sub_subscriptions(
    data: SubSubscriptionsCreate,
    db: Session = Depends(get_db)
):
    """Create a new SubSubscriptions."""
    service = SubSubscriptionsService(db)
    return service.create(data)

@router.put("/sub_subscriptionss/{id}", response_model=SubSubscriptionsResponse)
def update_sub_subscriptions(
    id: UUID,
    data: SubSubscriptionsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SubSubscriptions."""
    service = SubSubscriptionsService(db)
    return service.update(id, data)

@router.delete("/sub_subscriptionss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sub_subscriptions(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SubSubscriptions (soft delete by default)."""
    service = SubSubscriptionsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sub_subscriptionss/search", response_model=List[SubSubscriptionsResponse])
def search_sub_subscriptionss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SubSubscriptions by name/code/description."""
    service = SubSubscriptionsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/sub_usage_recordss", response_model=SubUsageRecordsListResponse)
def list_sub_usage_recordss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all SubUsageRecords records."""
    service = SubUsageRecordsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/sub_usage_recordss/{id}", response_model=SubUsageRecordsResponse)
def get_sub_usage_records(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get SubUsageRecords by ID."""
    service = SubUsageRecordsService(db)
    return service.get(id)

@router.post("/sub_usage_recordss", response_model=SubUsageRecordsResponse, status_code=status.HTTP_201_CREATED)
def create_sub_usage_records(
    data: SubUsageRecordsCreate,
    db: Session = Depends(get_db)
):
    """Create a new SubUsageRecords."""
    service = SubUsageRecordsService(db)
    return service.create(data)

@router.put("/sub_usage_recordss/{id}", response_model=SubUsageRecordsResponse)
def update_sub_usage_records(
    id: UUID,
    data: SubUsageRecordsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing SubUsageRecords."""
    service = SubUsageRecordsService(db)
    return service.update(id, data)

@router.delete("/sub_usage_recordss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_sub_usage_records(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a SubUsageRecords (soft delete by default)."""
    service = SubUsageRecordsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/sub_usage_recordss/search", response_model=List[SubUsageRecordsResponse])
def search_sub_usage_recordss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search SubUsageRecords by name/code/description."""
    service = SubUsageRecordsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_binss", response_model=WmsBinsListResponse)
def list_wms_binss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsBins records."""
    service = WmsBinsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_binss/{id}", response_model=WmsBinsResponse)
def get_wms_bins(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsBins by ID."""
    service = WmsBinsService(db)
    return service.get(id)

@router.post("/wms_binss", response_model=WmsBinsResponse, status_code=status.HTTP_201_CREATED)
def create_wms_bins(
    data: WmsBinsCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsBins."""
    service = WmsBinsService(db)
    return service.create(data)

@router.put("/wms_binss/{id}", response_model=WmsBinsResponse)
def update_wms_bins(
    id: UUID,
    data: WmsBinsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsBins."""
    service = WmsBinsService(db)
    return service.update(id, data)

@router.delete("/wms_binss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_bins(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsBins (soft delete by default)."""
    service = WmsBinsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_binss/search", response_model=List[WmsBinsResponse])
def search_wms_binss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsBins by name/code/description."""
    service = WmsBinsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_cycle_count_scheduless", response_model=WmsCycleCountSchedulesListResponse)
def list_wms_cycle_count_scheduless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsCycleCountSchedules records."""
    service = WmsCycleCountSchedulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_cycle_count_scheduless/{id}", response_model=WmsCycleCountSchedulesResponse)
def get_wms_cycle_count_schedules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsCycleCountSchedules by ID."""
    service = WmsCycleCountSchedulesService(db)
    return service.get(id)

@router.post("/wms_cycle_count_scheduless", response_model=WmsCycleCountSchedulesResponse, status_code=status.HTTP_201_CREATED)
def create_wms_cycle_count_schedules(
    data: WmsCycleCountSchedulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsCycleCountSchedules."""
    service = WmsCycleCountSchedulesService(db)
    return service.create(data)

@router.put("/wms_cycle_count_scheduless/{id}", response_model=WmsCycleCountSchedulesResponse)
def update_wms_cycle_count_schedules(
    id: UUID,
    data: WmsCycleCountSchedulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsCycleCountSchedules."""
    service = WmsCycleCountSchedulesService(db)
    return service.update(id, data)

@router.delete("/wms_cycle_count_scheduless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_cycle_count_schedules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsCycleCountSchedules (soft delete by default)."""
    service = WmsCycleCountSchedulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_cycle_count_scheduless/search", response_model=List[WmsCycleCountSchedulesResponse])
def search_wms_cycle_count_scheduless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsCycleCountSchedules by name/code/description."""
    service = WmsCycleCountSchedulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_inventory_movess", response_model=WmsInventoryMovesListResponse)
def list_wms_inventory_movess(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsInventoryMoves records."""
    service = WmsInventoryMovesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_inventory_movess/{id}", response_model=WmsInventoryMovesResponse)
def get_wms_inventory_moves(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsInventoryMoves by ID."""
    service = WmsInventoryMovesService(db)
    return service.get(id)

@router.post("/wms_inventory_movess", response_model=WmsInventoryMovesResponse, status_code=status.HTTP_201_CREATED)
def create_wms_inventory_moves(
    data: WmsInventoryMovesCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsInventoryMoves."""
    service = WmsInventoryMovesService(db)
    return service.create(data)

@router.put("/wms_inventory_movess/{id}", response_model=WmsInventoryMovesResponse)
def update_wms_inventory_moves(
    id: UUID,
    data: WmsInventoryMovesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsInventoryMoves."""
    service = WmsInventoryMovesService(db)
    return service.update(id, data)

@router.delete("/wms_inventory_movess/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_inventory_moves(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsInventoryMoves (soft delete by default)."""
    service = WmsInventoryMovesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_inventory_movess/search", response_model=List[WmsInventoryMovesResponse])
def search_wms_inventory_movess(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsInventoryMoves by name/code/description."""
    service = WmsInventoryMovesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_packing_stationss", response_model=WmsPackingStationsListResponse)
def list_wms_packing_stationss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsPackingStations records."""
    service = WmsPackingStationsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_packing_stationss/{id}", response_model=WmsPackingStationsResponse)
def get_wms_packing_stations(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsPackingStations by ID."""
    service = WmsPackingStationsService(db)
    return service.get(id)

@router.post("/wms_packing_stationss", response_model=WmsPackingStationsResponse, status_code=status.HTTP_201_CREATED)
def create_wms_packing_stations(
    data: WmsPackingStationsCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsPackingStations."""
    service = WmsPackingStationsService(db)
    return service.create(data)

@router.put("/wms_packing_stationss/{id}", response_model=WmsPackingStationsResponse)
def update_wms_packing_stations(
    id: UUID,
    data: WmsPackingStationsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsPackingStations."""
    service = WmsPackingStationsService(db)
    return service.update(id, data)

@router.delete("/wms_packing_stationss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_packing_stations(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsPackingStations (soft delete by default)."""
    service = WmsPackingStationsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_packing_stationss/search", response_model=List[WmsPackingStationsResponse])
def search_wms_packing_stationss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsPackingStations by name/code/description."""
    service = WmsPackingStationsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_receiving_scheduless", response_model=WmsReceivingSchedulesListResponse)
def list_wms_receiving_scheduless(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsReceivingSchedules records."""
    service = WmsReceivingSchedulesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_receiving_scheduless/{id}", response_model=WmsReceivingSchedulesResponse)
def get_wms_receiving_schedules(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsReceivingSchedules by ID."""
    service = WmsReceivingSchedulesService(db)
    return service.get(id)

@router.post("/wms_receiving_scheduless", response_model=WmsReceivingSchedulesResponse, status_code=status.HTTP_201_CREATED)
def create_wms_receiving_schedules(
    data: WmsReceivingSchedulesCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsReceivingSchedules."""
    service = WmsReceivingSchedulesService(db)
    return service.create(data)

@router.put("/wms_receiving_scheduless/{id}", response_model=WmsReceivingSchedulesResponse)
def update_wms_receiving_schedules(
    id: UUID,
    data: WmsReceivingSchedulesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsReceivingSchedules."""
    service = WmsReceivingSchedulesService(db)
    return service.update(id, data)

@router.delete("/wms_receiving_scheduless/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_receiving_schedules(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsReceivingSchedules (soft delete by default)."""
    service = WmsReceivingSchedulesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_receiving_scheduless/search", response_model=List[WmsReceivingSchedulesResponse])
def search_wms_receiving_scheduless(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsReceivingSchedules by name/code/description."""
    service = WmsReceivingSchedulesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_replenishment_taskss", response_model=WmsReplenishmentTasksListResponse)
def list_wms_replenishment_taskss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsReplenishmentTasks records."""
    service = WmsReplenishmentTasksService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_replenishment_taskss/{id}", response_model=WmsReplenishmentTasksResponse)
def get_wms_replenishment_tasks(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsReplenishmentTasks by ID."""
    service = WmsReplenishmentTasksService(db)
    return service.get(id)

@router.post("/wms_replenishment_taskss", response_model=WmsReplenishmentTasksResponse, status_code=status.HTTP_201_CREATED)
def create_wms_replenishment_tasks(
    data: WmsReplenishmentTasksCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsReplenishmentTasks."""
    service = WmsReplenishmentTasksService(db)
    return service.create(data)

@router.put("/wms_replenishment_taskss/{id}", response_model=WmsReplenishmentTasksResponse)
def update_wms_replenishment_tasks(
    id: UUID,
    data: WmsReplenishmentTasksUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsReplenishmentTasks."""
    service = WmsReplenishmentTasksService(db)
    return service.update(id, data)

@router.delete("/wms_replenishment_taskss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_replenishment_tasks(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsReplenishmentTasks (soft delete by default)."""
    service = WmsReplenishmentTasksService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_replenishment_taskss/search", response_model=List[WmsReplenishmentTasksResponse])
def search_wms_replenishment_taskss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsReplenishmentTasks by name/code/description."""
    service = WmsReplenishmentTasksService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_shipment_manifestss", response_model=WmsShipmentManifestsListResponse)
def list_wms_shipment_manifestss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsShipmentManifests records."""
    service = WmsShipmentManifestsService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_shipment_manifestss/{id}", response_model=WmsShipmentManifestsResponse)
def get_wms_shipment_manifests(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsShipmentManifests by ID."""
    service = WmsShipmentManifestsService(db)
    return service.get(id)

@router.post("/wms_shipment_manifestss", response_model=WmsShipmentManifestsResponse, status_code=status.HTTP_201_CREATED)
def create_wms_shipment_manifests(
    data: WmsShipmentManifestsCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsShipmentManifests."""
    service = WmsShipmentManifestsService(db)
    return service.create(data)

@router.put("/wms_shipment_manifestss/{id}", response_model=WmsShipmentManifestsResponse)
def update_wms_shipment_manifests(
    id: UUID,
    data: WmsShipmentManifestsUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsShipmentManifests."""
    service = WmsShipmentManifestsService(db)
    return service.update(id, data)

@router.delete("/wms_shipment_manifestss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_shipment_manifests(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsShipmentManifests (soft delete by default)."""
    service = WmsShipmentManifestsService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_shipment_manifestss/search", response_model=List[WmsShipmentManifestsResponse])
def search_wms_shipment_manifestss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsShipmentManifests by name/code/description."""
    service = WmsShipmentManifestsService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_wave_pickss", response_model=WmsWavePicksListResponse)
def list_wms_wave_pickss(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsWavePicks records."""
    service = WmsWavePicksService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_wave_pickss/{id}", response_model=WmsWavePicksResponse)
def get_wms_wave_picks(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsWavePicks by ID."""
    service = WmsWavePicksService(db)
    return service.get(id)

@router.post("/wms_wave_pickss", response_model=WmsWavePicksResponse, status_code=status.HTTP_201_CREATED)
def create_wms_wave_picks(
    data: WmsWavePicksCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsWavePicks."""
    service = WmsWavePicksService(db)
    return service.create(data)

@router.put("/wms_wave_pickss/{id}", response_model=WmsWavePicksResponse)
def update_wms_wave_picks(
    id: UUID,
    data: WmsWavePicksUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsWavePicks."""
    service = WmsWavePicksService(db)
    return service.update(id, data)

@router.delete("/wms_wave_pickss/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_wave_picks(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsWavePicks (soft delete by default)."""
    service = WmsWavePicksService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_wave_pickss/search", response_model=List[WmsWavePicksResponse])
def search_wms_wave_pickss(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsWavePicks by name/code/description."""
    service = WmsWavePicksService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/wms_zoness", response_model=WmsZonesListResponse)
def list_wms_zoness(
    company_id: Optional[UUID] = None,
    skip: int = Query(0, ge=0),
    limit: int = Query(100, ge=1, le=1000),
    db: Session = Depends(get_db)
):
    """List all WmsZones records."""
    service = WmsZonesService(db)
    return service.list(company_id=company_id, skip=skip, limit=limit)

@router.get("/wms_zoness/{id}", response_model=WmsZonesResponse)
def get_wms_zones(
    id: UUID,
    db: Session = Depends(get_db)
):
    """Get WmsZones by ID."""
    service = WmsZonesService(db)
    return service.get(id)

@router.post("/wms_zoness", response_model=WmsZonesResponse, status_code=status.HTTP_201_CREATED)
def create_wms_zones(
    data: WmsZonesCreate,
    db: Session = Depends(get_db)
):
    """Create a new WmsZones."""
    service = WmsZonesService(db)
    return service.create(data)

@router.put("/wms_zoness/{id}", response_model=WmsZonesResponse)
def update_wms_zones(
    id: UUID,
    data: WmsZonesUpdate,
    db: Session = Depends(get_db)
):
    """Update an existing WmsZones."""
    service = WmsZonesService(db)
    return service.update(id, data)

@router.delete("/wms_zoness/{id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_wms_zones(
    id: UUID,
    hard_delete: bool = Query(False, description="Permanently delete instead of soft delete"),
    db: Session = Depends(get_db)
):
    """Delete a WmsZones (soft delete by default)."""
    service = WmsZonesService(db)
    service.delete(id, hard_delete=hard_delete)
    return None

@router.get("/wms_zoness/search", response_model=List[WmsZonesResponse])
def search_wms_zoness(
    q: str = Query(..., min_length=1, description="Search query"),
    company_id: Optional[UUID] = None,
    limit: int = Query(20, ge=1, le=100),
    db: Session = Depends(get_db)
):
    """Search WmsZones by name/code/description."""
    service = WmsZonesService(db)
    return service.search(q, company_id=company_id, limit=limit)

@router.get("/dashboard/summary")
def get_new_p1_dashboard(
    company_id: UUID,
    db: Session = Depends(get_db)
):
    """Get New P1 dashboard summary."""
    return {
        "module": "new_p1",
        "company_id": str(company_id),
        "tables": 74,
        "metrics": {}
    }
