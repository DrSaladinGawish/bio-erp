"""
New P1 Module — CRUD Services
Auto-generated by erp_organ_generator.py on 2026-06-15T23:19:40.925009
Full CRUD with audit trail, soft delete, and business validations.
"""
from __future__ import annotations

from typing import List, Optional, Dict, Any
from uuid import UUID
from datetime import datetime
from decimal import Decimal

from sqlalchemy.orm import Session, joinedload
from sqlalchemy import and_, or_, desc, func
from fastapi import HTTPException, status
import uuid

from .new_p1_models import *
from .new_p1_schemas import *

class BiAuditLogService:
    """CRUD service for BiAuditLog."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiAuditLogCreate, created_by: Optional[UUID] = None) -> BiAuditLogResponse:
        """Create a new BiAuditLog record."""
        db_obj = BiAuditLog(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiAuditLogResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiAuditLogResponse]:
        """Get BiAuditLog by primary key."""
        db_obj = self.db.query(BiAuditLog).filter(BiAuditLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiAuditLog not found")
        return BiAuditLogResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiAuditLogListResponse:
        """List BiAuditLog records with pagination and filtering."""
        query = self.db.query(BiAuditLog)
        if company_id and hasattr(BiAuditLog, "company_id"):
            query = query.filter(BiAuditLog.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiAuditLog, key) and value is not None:
                    query = query.filter(getattr(BiAuditLog, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiAuditLog, order_by, BiAuditLog.created_at)))
        else:
            query = query.order_by(getattr(BiAuditLog, order_by, BiAuditLog.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiAuditLogListResponse(
            items=[BiAuditLogResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiAuditLogUpdate,
               updated_by: Optional[UUID] = None) -> BiAuditLogResponse:
        """Update an existing BiAuditLog record."""
        db_obj = self.db.query(BiAuditLog).filter(BiAuditLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiAuditLog not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiAuditLogResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiAuditLog record. Soft delete by default."""
        db_obj = self.db.query(BiAuditLog).filter(BiAuditLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiAuditLog not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiAuditLogResponse]:
        """Search BiAuditLog by name/code fields."""
        query = self.db.query(BiAuditLog)
        if company_id and hasattr(BiAuditLog, "company_id"):
            query = query.filter(BiAuditLog.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiAuditLog, f)]
        if search_fields:
            conditions = [getattr(BiAuditLog, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiAuditLogResponse.model_validate(i) for i in items]

class BiCacheService:
    """CRUD service for BiCache."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiCacheCreate, created_by: Optional[UUID] = None) -> BiCacheResponse:
        """Create a new BiCache record."""
        db_obj = BiCache(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiCacheResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiCacheResponse]:
        """Get BiCache by primary key."""
        db_obj = self.db.query(BiCache).filter(BiCache.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiCache not found")
        return BiCacheResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiCacheListResponse:
        """List BiCache records with pagination and filtering."""
        query = self.db.query(BiCache)
        if company_id and hasattr(BiCache, "company_id"):
            query = query.filter(BiCache.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiCache, key) and value is not None:
                    query = query.filter(getattr(BiCache, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiCache, order_by, BiCache.created_at)))
        else:
            query = query.order_by(getattr(BiCache, order_by, BiCache.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiCacheListResponse(
            items=[BiCacheResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiCacheUpdate,
               updated_by: Optional[UUID] = None) -> BiCacheResponse:
        """Update an existing BiCache record."""
        db_obj = self.db.query(BiCache).filter(BiCache.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiCache not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiCacheResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiCache record. Soft delete by default."""
        db_obj = self.db.query(BiCache).filter(BiCache.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiCache not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiCacheResponse]:
        """Search BiCache by name/code fields."""
        query = self.db.query(BiCache)
        if company_id and hasattr(BiCache, "company_id"):
            query = query.filter(BiCache.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiCache, f)]
        if search_fields:
            conditions = [getattr(BiCache, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiCacheResponse.model_validate(i) for i in items]

class BiDashboardsService:
    """CRUD service for BiDashboards."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiDashboardsCreate, created_by: Optional[UUID] = None) -> BiDashboardsResponse:
        """Create a new BiDashboards record."""
        db_obj = BiDashboards(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiDashboardsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiDashboardsResponse]:
        """Get BiDashboards by primary key."""
        db_obj = self.db.query(BiDashboards).filter(BiDashboards.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDashboards not found")
        return BiDashboardsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiDashboardsListResponse:
        """List BiDashboards records with pagination and filtering."""
        query = self.db.query(BiDashboards)
        if company_id and hasattr(BiDashboards, "company_id"):
            query = query.filter(BiDashboards.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiDashboards, key) and value is not None:
                    query = query.filter(getattr(BiDashboards, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiDashboards, order_by, BiDashboards.created_at)))
        else:
            query = query.order_by(getattr(BiDashboards, order_by, BiDashboards.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiDashboardsListResponse(
            items=[BiDashboardsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiDashboardsUpdate,
               updated_by: Optional[UUID] = None) -> BiDashboardsResponse:
        """Update an existing BiDashboards record."""
        db_obj = self.db.query(BiDashboards).filter(BiDashboards.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDashboards not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiDashboardsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiDashboards record. Soft delete by default."""
        db_obj = self.db.query(BiDashboards).filter(BiDashboards.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDashboards not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiDashboardsResponse]:
        """Search BiDashboards by name/code fields."""
        query = self.db.query(BiDashboards)
        if company_id and hasattr(BiDashboards, "company_id"):
            query = query.filter(BiDashboards.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiDashboards, f)]
        if search_fields:
            conditions = [getattr(BiDashboards, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiDashboardsResponse.model_validate(i) for i in items]

class BiDataSourcesService:
    """CRUD service for BiDataSources."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiDataSourcesCreate, created_by: Optional[UUID] = None) -> BiDataSourcesResponse:
        """Create a new BiDataSources record."""
        db_obj = BiDataSources(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiDataSourcesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiDataSourcesResponse]:
        """Get BiDataSources by primary key."""
        db_obj = self.db.query(BiDataSources).filter(BiDataSources.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDataSources not found")
        return BiDataSourcesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiDataSourcesListResponse:
        """List BiDataSources records with pagination and filtering."""
        query = self.db.query(BiDataSources)
        if company_id and hasattr(BiDataSources, "company_id"):
            query = query.filter(BiDataSources.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiDataSources, key) and value is not None:
                    query = query.filter(getattr(BiDataSources, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiDataSources, order_by, BiDataSources.created_at)))
        else:
            query = query.order_by(getattr(BiDataSources, order_by, BiDataSources.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiDataSourcesListResponse(
            items=[BiDataSourcesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiDataSourcesUpdate,
               updated_by: Optional[UUID] = None) -> BiDataSourcesResponse:
        """Update an existing BiDataSources record."""
        db_obj = self.db.query(BiDataSources).filter(BiDataSources.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDataSources not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiDataSourcesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiDataSources record. Soft delete by default."""
        db_obj = self.db.query(BiDataSources).filter(BiDataSources.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDataSources not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiDataSourcesResponse]:
        """Search BiDataSources by name/code fields."""
        query = self.db.query(BiDataSources)
        if company_id and hasattr(BiDataSources, "company_id"):
            query = query.filter(BiDataSources.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiDataSources, f)]
        if search_fields:
            conditions = [getattr(BiDataSources, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiDataSourcesResponse.model_validate(i) for i in items]

class BiDatasetsService:
    """CRUD service for BiDatasets."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiDatasetsCreate, created_by: Optional[UUID] = None) -> BiDatasetsResponse:
        """Create a new BiDatasets record."""
        db_obj = BiDatasets(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiDatasetsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiDatasetsResponse]:
        """Get BiDatasets by primary key."""
        db_obj = self.db.query(BiDatasets).filter(BiDatasets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDatasets not found")
        return BiDatasetsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiDatasetsListResponse:
        """List BiDatasets records with pagination and filtering."""
        query = self.db.query(BiDatasets)
        if company_id and hasattr(BiDatasets, "company_id"):
            query = query.filter(BiDatasets.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiDatasets, key) and value is not None:
                    query = query.filter(getattr(BiDatasets, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiDatasets, order_by, BiDatasets.created_at)))
        else:
            query = query.order_by(getattr(BiDatasets, order_by, BiDatasets.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiDatasetsListResponse(
            items=[BiDatasetsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiDatasetsUpdate,
               updated_by: Optional[UUID] = None) -> BiDatasetsResponse:
        """Update an existing BiDatasets record."""
        db_obj = self.db.query(BiDatasets).filter(BiDatasets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDatasets not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiDatasetsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiDatasets record. Soft delete by default."""
        db_obj = self.db.query(BiDatasets).filter(BiDatasets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiDatasets not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiDatasetsResponse]:
        """Search BiDatasets by name/code fields."""
        query = self.db.query(BiDatasets)
        if company_id and hasattr(BiDatasets, "company_id"):
            query = query.filter(BiDatasets.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiDatasets, f)]
        if search_fields:
            conditions = [getattr(BiDatasets, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiDatasetsResponse.model_validate(i) for i in items]

class BiReportsService:
    """CRUD service for BiReports."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiReportsCreate, created_by: Optional[UUID] = None) -> BiReportsResponse:
        """Create a new BiReports record."""
        db_obj = BiReports(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiReportsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiReportsResponse]:
        """Get BiReports by primary key."""
        db_obj = self.db.query(BiReports).filter(BiReports.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiReports not found")
        return BiReportsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiReportsListResponse:
        """List BiReports records with pagination and filtering."""
        query = self.db.query(BiReports)
        if company_id and hasattr(BiReports, "company_id"):
            query = query.filter(BiReports.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiReports, key) and value is not None:
                    query = query.filter(getattr(BiReports, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiReports, order_by, BiReports.created_at)))
        else:
            query = query.order_by(getattr(BiReports, order_by, BiReports.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiReportsListResponse(
            items=[BiReportsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiReportsUpdate,
               updated_by: Optional[UUID] = None) -> BiReportsResponse:
        """Update an existing BiReports record."""
        db_obj = self.db.query(BiReports).filter(BiReports.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiReports not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiReportsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiReports record. Soft delete by default."""
        db_obj = self.db.query(BiReports).filter(BiReports.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiReports not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiReportsResponse]:
        """Search BiReports by name/code fields."""
        query = self.db.query(BiReports)
        if company_id and hasattr(BiReports, "company_id"):
            query = query.filter(BiReports.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiReports, f)]
        if search_fields:
            conditions = [getattr(BiReports, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiReportsResponse.model_validate(i) for i in items]

class BiSchedulesService:
    """CRUD service for BiSchedules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiSchedulesCreate, created_by: Optional[UUID] = None) -> BiSchedulesResponse:
        """Create a new BiSchedules record."""
        db_obj = BiSchedules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiSchedulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiSchedulesResponse]:
        """Get BiSchedules by primary key."""
        db_obj = self.db.query(BiSchedules).filter(BiSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiSchedules not found")
        return BiSchedulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiSchedulesListResponse:
        """List BiSchedules records with pagination and filtering."""
        query = self.db.query(BiSchedules)
        if company_id and hasattr(BiSchedules, "company_id"):
            query = query.filter(BiSchedules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiSchedules, key) and value is not None:
                    query = query.filter(getattr(BiSchedules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiSchedules, order_by, BiSchedules.created_at)))
        else:
            query = query.order_by(getattr(BiSchedules, order_by, BiSchedules.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiSchedulesListResponse(
            items=[BiSchedulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiSchedulesUpdate,
               updated_by: Optional[UUID] = None) -> BiSchedulesResponse:
        """Update an existing BiSchedules record."""
        db_obj = self.db.query(BiSchedules).filter(BiSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiSchedules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiSchedulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiSchedules record. Soft delete by default."""
        db_obj = self.db.query(BiSchedules).filter(BiSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiSchedules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiSchedulesResponse]:
        """Search BiSchedules by name/code fields."""
        query = self.db.query(BiSchedules)
        if company_id and hasattr(BiSchedules, "company_id"):
            query = query.filter(BiSchedules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiSchedules, f)]
        if search_fields:
            conditions = [getattr(BiSchedules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiSchedulesResponse.model_validate(i) for i in items]

class BiUserFavoritesService:
    """CRUD service for BiUserFavorites."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: BiUserFavoritesCreate, created_by: Optional[UUID] = None) -> BiUserFavoritesResponse:
        """Create a new BiUserFavorites record."""
        db_obj = BiUserFavorites(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return BiUserFavoritesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[BiUserFavoritesResponse]:
        """Get BiUserFavorites by primary key."""
        db_obj = self.db.query(BiUserFavorites).filter(BiUserFavorites.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiUserFavorites not found")
        return BiUserFavoritesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> BiUserFavoritesListResponse:
        """List BiUserFavorites records with pagination and filtering."""
        query = self.db.query(BiUserFavorites)
        if company_id and hasattr(BiUserFavorites, "company_id"):
            query = query.filter(BiUserFavorites.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(BiUserFavorites, key) and value is not None:
                    query = query.filter(getattr(BiUserFavorites, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(BiUserFavorites, order_by, BiUserFavorites.created_at)))
        else:
            query = query.order_by(getattr(BiUserFavorites, order_by, BiUserFavorites.created_at))
        items = query.offset(skip).limit(limit).all()
        return BiUserFavoritesListResponse(
            items=[BiUserFavoritesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: BiUserFavoritesUpdate,
               updated_by: Optional[UUID] = None) -> BiUserFavoritesResponse:
        """Update an existing BiUserFavorites record."""
        db_obj = self.db.query(BiUserFavorites).filter(BiUserFavorites.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiUserFavorites not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return BiUserFavoritesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a BiUserFavorites record. Soft delete by default."""
        db_obj = self.db.query(BiUserFavorites).filter(BiUserFavorites.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="BiUserFavorites not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[BiUserFavoritesResponse]:
        """Search BiUserFavorites by name/code fields."""
        query = self.db.query(BiUserFavorites)
        if company_id and hasattr(BiUserFavorites, "company_id"):
            query = query.filter(BiUserFavorites.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(BiUserFavorites, f)]
        if search_fields:
            conditions = [getattr(BiUserFavorites, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [BiUserFavoritesResponse.model_validate(i) for i in items]

class ClmAmendmentsService:
    """CRUD service for ClmAmendments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ClmAmendmentsCreate, created_by: Optional[UUID] = None) -> ClmAmendmentsResponse:
        """Create a new ClmAmendments record."""
        db_obj = ClmAmendments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmAmendmentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ClmAmendmentsResponse]:
        """Get ClmAmendments by primary key."""
        db_obj = self.db.query(ClmAmendments).filter(ClmAmendments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmAmendments not found")
        return ClmAmendmentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ClmAmendmentsListResponse:
        """List ClmAmendments records with pagination and filtering."""
        query = self.db.query(ClmAmendments)
        if company_id and hasattr(ClmAmendments, "company_id"):
            query = query.filter(ClmAmendments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ClmAmendments, key) and value is not None:
                    query = query.filter(getattr(ClmAmendments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ClmAmendments, order_by, ClmAmendments.created_at)))
        else:
            query = query.order_by(getattr(ClmAmendments, order_by, ClmAmendments.created_at))
        items = query.offset(skip).limit(limit).all()
        return ClmAmendmentsListResponse(
            items=[ClmAmendmentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ClmAmendmentsUpdate,
               updated_by: Optional[UUID] = None) -> ClmAmendmentsResponse:
        """Update an existing ClmAmendments record."""
        db_obj = self.db.query(ClmAmendments).filter(ClmAmendments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmAmendments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmAmendmentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ClmAmendments record. Soft delete by default."""
        db_obj = self.db.query(ClmAmendments).filter(ClmAmendments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmAmendments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ClmAmendmentsResponse]:
        """Search ClmAmendments by name/code fields."""
        query = self.db.query(ClmAmendments)
        if company_id and hasattr(ClmAmendments, "company_id"):
            query = query.filter(ClmAmendments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ClmAmendments, f)]
        if search_fields:
            conditions = [getattr(ClmAmendments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ClmAmendmentsResponse.model_validate(i) for i in items]

class ClmClausesService:
    """CRUD service for ClmClauses."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ClmClausesCreate, created_by: Optional[UUID] = None) -> ClmClausesResponse:
        """Create a new ClmClauses record."""
        db_obj = ClmClauses(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmClausesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ClmClausesResponse]:
        """Get ClmClauses by primary key."""
        db_obj = self.db.query(ClmClauses).filter(ClmClauses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmClauses not found")
        return ClmClausesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ClmClausesListResponse:
        """List ClmClauses records with pagination and filtering."""
        query = self.db.query(ClmClauses)
        if company_id and hasattr(ClmClauses, "company_id"):
            query = query.filter(ClmClauses.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ClmClauses, key) and value is not None:
                    query = query.filter(getattr(ClmClauses, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ClmClauses, order_by, ClmClauses.created_at)))
        else:
            query = query.order_by(getattr(ClmClauses, order_by, ClmClauses.created_at))
        items = query.offset(skip).limit(limit).all()
        return ClmClausesListResponse(
            items=[ClmClausesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ClmClausesUpdate,
               updated_by: Optional[UUID] = None) -> ClmClausesResponse:
        """Update an existing ClmClauses record."""
        db_obj = self.db.query(ClmClauses).filter(ClmClauses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmClauses not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmClausesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ClmClauses record. Soft delete by default."""
        db_obj = self.db.query(ClmClauses).filter(ClmClauses.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmClauses not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ClmClausesResponse]:
        """Search ClmClauses by name/code fields."""
        query = self.db.query(ClmClauses)
        if company_id and hasattr(ClmClauses, "company_id"):
            query = query.filter(ClmClauses.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ClmClauses, f)]
        if search_fields:
            conditions = [getattr(ClmClauses, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ClmClausesResponse.model_validate(i) for i in items]

class ClmContractsService:
    """CRUD service for ClmContracts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ClmContractsCreate, created_by: Optional[UUID] = None) -> ClmContractsResponse:
        """Create a new ClmContracts record."""
        db_obj = ClmContracts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmContractsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ClmContractsResponse]:
        """Get ClmContracts by primary key."""
        db_obj = self.db.query(ClmContracts).filter(ClmContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmContracts not found")
        return ClmContractsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ClmContractsListResponse:
        """List ClmContracts records with pagination and filtering."""
        query = self.db.query(ClmContracts)
        if company_id and hasattr(ClmContracts, "company_id"):
            query = query.filter(ClmContracts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ClmContracts, key) and value is not None:
                    query = query.filter(getattr(ClmContracts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ClmContracts, order_by, ClmContracts.created_at)))
        else:
            query = query.order_by(getattr(ClmContracts, order_by, ClmContracts.created_at))
        items = query.offset(skip).limit(limit).all()
        return ClmContractsListResponse(
            items=[ClmContractsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ClmContractsUpdate,
               updated_by: Optional[UUID] = None) -> ClmContractsResponse:
        """Update an existing ClmContracts record."""
        db_obj = self.db.query(ClmContracts).filter(ClmContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmContracts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmContractsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ClmContracts record. Soft delete by default."""
        db_obj = self.db.query(ClmContracts).filter(ClmContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmContracts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ClmContractsResponse]:
        """Search ClmContracts by name/code fields."""
        query = self.db.query(ClmContracts)
        if company_id and hasattr(ClmContracts, "company_id"):
            query = query.filter(ClmContracts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ClmContracts, f)]
        if search_fields:
            conditions = [getattr(ClmContracts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ClmContractsResponse.model_validate(i) for i in items]

class ClmRenewalsService:
    """CRUD service for ClmRenewals."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: ClmRenewalsCreate, created_by: Optional[UUID] = None) -> ClmRenewalsResponse:
        """Create a new ClmRenewals record."""
        db_obj = ClmRenewals(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmRenewalsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[ClmRenewalsResponse]:
        """Get ClmRenewals by primary key."""
        db_obj = self.db.query(ClmRenewals).filter(ClmRenewals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmRenewals not found")
        return ClmRenewalsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> ClmRenewalsListResponse:
        """List ClmRenewals records with pagination and filtering."""
        query = self.db.query(ClmRenewals)
        if company_id and hasattr(ClmRenewals, "company_id"):
            query = query.filter(ClmRenewals.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(ClmRenewals, key) and value is not None:
                    query = query.filter(getattr(ClmRenewals, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(ClmRenewals, order_by, ClmRenewals.created_at)))
        else:
            query = query.order_by(getattr(ClmRenewals, order_by, ClmRenewals.created_at))
        items = query.offset(skip).limit(limit).all()
        return ClmRenewalsListResponse(
            items=[ClmRenewalsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: ClmRenewalsUpdate,
               updated_by: Optional[UUID] = None) -> ClmRenewalsResponse:
        """Update an existing ClmRenewals record."""
        db_obj = self.db.query(ClmRenewals).filter(ClmRenewals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmRenewals not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return ClmRenewalsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a ClmRenewals record. Soft delete by default."""
        db_obj = self.db.query(ClmRenewals).filter(ClmRenewals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="ClmRenewals not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[ClmRenewalsResponse]:
        """Search ClmRenewals by name/code fields."""
        query = self.db.query(ClmRenewals)
        if company_id and hasattr(ClmRenewals, "company_id"):
            query = query.filter(ClmRenewals.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(ClmRenewals, f)]
        if search_fields:
            conditions = [getattr(ClmRenewals, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [ClmRenewalsResponse.model_validate(i) for i in items]

class DocDocumentsService:
    """CRUD service for DocDocuments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DocDocumentsCreate, created_by: Optional[UUID] = None) -> DocDocumentsResponse:
        """Create a new DocDocuments record."""
        db_obj = DocDocuments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DocDocumentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DocDocumentsResponse]:
        """Get DocDocuments by primary key."""
        db_obj = self.db.query(DocDocuments).filter(DocDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocDocuments not found")
        return DocDocumentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DocDocumentsListResponse:
        """List DocDocuments records with pagination and filtering."""
        query = self.db.query(DocDocuments)
        if company_id and hasattr(DocDocuments, "company_id"):
            query = query.filter(DocDocuments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DocDocuments, key) and value is not None:
                    query = query.filter(getattr(DocDocuments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DocDocuments, order_by, DocDocuments.created_at)))
        else:
            query = query.order_by(getattr(DocDocuments, order_by, DocDocuments.created_at))
        items = query.offset(skip).limit(limit).all()
        return DocDocumentsListResponse(
            items=[DocDocumentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DocDocumentsUpdate,
               updated_by: Optional[UUID] = None) -> DocDocumentsResponse:
        """Update an existing DocDocuments record."""
        db_obj = self.db.query(DocDocuments).filter(DocDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocDocuments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DocDocumentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DocDocuments record. Soft delete by default."""
        db_obj = self.db.query(DocDocuments).filter(DocDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocDocuments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DocDocumentsResponse]:
        """Search DocDocuments by name/code fields."""
        query = self.db.query(DocDocuments)
        if company_id and hasattr(DocDocuments, "company_id"):
            query = query.filter(DocDocuments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DocDocuments, f)]
        if search_fields:
            conditions = [getattr(DocDocuments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DocDocumentsResponse.model_validate(i) for i in items]

class DocFoldersService:
    """CRUD service for DocFolders."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DocFoldersCreate, created_by: Optional[UUID] = None) -> DocFoldersResponse:
        """Create a new DocFolders record."""
        db_obj = DocFolders(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DocFoldersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DocFoldersResponse]:
        """Get DocFolders by primary key."""
        db_obj = self.db.query(DocFolders).filter(DocFolders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocFolders not found")
        return DocFoldersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DocFoldersListResponse:
        """List DocFolders records with pagination and filtering."""
        query = self.db.query(DocFolders)
        if company_id and hasattr(DocFolders, "company_id"):
            query = query.filter(DocFolders.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DocFolders, key) and value is not None:
                    query = query.filter(getattr(DocFolders, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DocFolders, order_by, DocFolders.created_at)))
        else:
            query = query.order_by(getattr(DocFolders, order_by, DocFolders.created_at))
        items = query.offset(skip).limit(limit).all()
        return DocFoldersListResponse(
            items=[DocFoldersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DocFoldersUpdate,
               updated_by: Optional[UUID] = None) -> DocFoldersResponse:
        """Update an existing DocFolders record."""
        db_obj = self.db.query(DocFolders).filter(DocFolders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocFolders not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DocFoldersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DocFolders record. Soft delete by default."""
        db_obj = self.db.query(DocFolders).filter(DocFolders.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocFolders not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DocFoldersResponse]:
        """Search DocFolders by name/code fields."""
        query = self.db.query(DocFolders)
        if company_id and hasattr(DocFolders, "company_id"):
            query = query.filter(DocFolders.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DocFolders, f)]
        if search_fields:
            conditions = [getattr(DocFolders, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DocFoldersResponse.model_validate(i) for i in items]

class DocOcrResultsService:
    """CRUD service for DocOcrResults."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DocOcrResultsCreate, created_by: Optional[UUID] = None) -> DocOcrResultsResponse:
        """Create a new DocOcrResults record."""
        db_obj = DocOcrResults(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DocOcrResultsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DocOcrResultsResponse]:
        """Get DocOcrResults by primary key."""
        db_obj = self.db.query(DocOcrResults).filter(DocOcrResults.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocOcrResults not found")
        return DocOcrResultsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DocOcrResultsListResponse:
        """List DocOcrResults records with pagination and filtering."""
        query = self.db.query(DocOcrResults)
        if company_id and hasattr(DocOcrResults, "company_id"):
            query = query.filter(DocOcrResults.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DocOcrResults, key) and value is not None:
                    query = query.filter(getattr(DocOcrResults, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DocOcrResults, order_by, DocOcrResults.created_at)))
        else:
            query = query.order_by(getattr(DocOcrResults, order_by, DocOcrResults.created_at))
        items = query.offset(skip).limit(limit).all()
        return DocOcrResultsListResponse(
            items=[DocOcrResultsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DocOcrResultsUpdate,
               updated_by: Optional[UUID] = None) -> DocOcrResultsResponse:
        """Update an existing DocOcrResults record."""
        db_obj = self.db.query(DocOcrResults).filter(DocOcrResults.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocOcrResults not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DocOcrResultsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DocOcrResults record. Soft delete by default."""
        db_obj = self.db.query(DocOcrResults).filter(DocOcrResults.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocOcrResults not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DocOcrResultsResponse]:
        """Search DocOcrResults by name/code fields."""
        query = self.db.query(DocOcrResults)
        if company_id and hasattr(DocOcrResults, "company_id"):
            query = query.filter(DocOcrResults.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DocOcrResults, f)]
        if search_fields:
            conditions = [getattr(DocOcrResults, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DocOcrResultsResponse.model_validate(i) for i in items]

class DocSharesService:
    """CRUD service for DocShares."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DocSharesCreate, created_by: Optional[UUID] = None) -> DocSharesResponse:
        """Create a new DocShares record."""
        db_obj = DocShares(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DocSharesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DocSharesResponse]:
        """Get DocShares by primary key."""
        db_obj = self.db.query(DocShares).filter(DocShares.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocShares not found")
        return DocSharesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DocSharesListResponse:
        """List DocShares records with pagination and filtering."""
        query = self.db.query(DocShares)
        if company_id and hasattr(DocShares, "company_id"):
            query = query.filter(DocShares.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DocShares, key) and value is not None:
                    query = query.filter(getattr(DocShares, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DocShares, order_by, DocShares.created_at)))
        else:
            query = query.order_by(getattr(DocShares, order_by, DocShares.created_at))
        items = query.offset(skip).limit(limit).all()
        return DocSharesListResponse(
            items=[DocSharesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DocSharesUpdate,
               updated_by: Optional[UUID] = None) -> DocSharesResponse:
        """Update an existing DocShares record."""
        db_obj = self.db.query(DocShares).filter(DocShares.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocShares not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DocSharesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DocShares record. Soft delete by default."""
        db_obj = self.db.query(DocShares).filter(DocShares.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocShares not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DocSharesResponse]:
        """Search DocShares by name/code fields."""
        query = self.db.query(DocShares)
        if company_id and hasattr(DocShares, "company_id"):
            query = query.filter(DocShares.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DocShares, f)]
        if search_fields:
            conditions = [getattr(DocShares, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DocSharesResponse.model_validate(i) for i in items]

class DocVersionsService:
    """CRUD service for DocVersions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DocVersionsCreate, created_by: Optional[UUID] = None) -> DocVersionsResponse:
        """Create a new DocVersions record."""
        db_obj = DocVersions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DocVersionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DocVersionsResponse]:
        """Get DocVersions by primary key."""
        db_obj = self.db.query(DocVersions).filter(DocVersions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocVersions not found")
        return DocVersionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DocVersionsListResponse:
        """List DocVersions records with pagination and filtering."""
        query = self.db.query(DocVersions)
        if company_id and hasattr(DocVersions, "company_id"):
            query = query.filter(DocVersions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DocVersions, key) and value is not None:
                    query = query.filter(getattr(DocVersions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DocVersions, order_by, DocVersions.created_at)))
        else:
            query = query.order_by(getattr(DocVersions, order_by, DocVersions.created_at))
        items = query.offset(skip).limit(limit).all()
        return DocVersionsListResponse(
            items=[DocVersionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DocVersionsUpdate,
               updated_by: Optional[UUID] = None) -> DocVersionsResponse:
        """Update an existing DocVersions record."""
        db_obj = self.db.query(DocVersions).filter(DocVersions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocVersions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DocVersionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DocVersions record. Soft delete by default."""
        db_obj = self.db.query(DocVersions).filter(DocVersions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DocVersions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DocVersionsResponse]:
        """Search DocVersions by name/code fields."""
        query = self.db.query(DocVersions)
        if company_id and hasattr(DocVersions, "company_id"):
            query = query.filter(DocVersions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DocVersions, f)]
        if search_fields:
            conditions = [getattr(DocVersions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DocVersionsResponse.model_validate(i) for i in items]

class DpConsensusService:
    """CRUD service for DpConsensus."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpConsensusCreate, created_by: Optional[UUID] = None) -> DpConsensusResponse:
        """Create a new DpConsensus record."""
        db_obj = DpConsensus(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpConsensusResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpConsensusResponse]:
        """Get DpConsensus by primary key."""
        db_obj = self.db.query(DpConsensus).filter(DpConsensus.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpConsensus not found")
        return DpConsensusResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpConsensusListResponse:
        """List DpConsensus records with pagination and filtering."""
        query = self.db.query(DpConsensus)
        if company_id and hasattr(DpConsensus, "company_id"):
            query = query.filter(DpConsensus.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpConsensus, key) and value is not None:
                    query = query.filter(getattr(DpConsensus, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpConsensus, order_by, DpConsensus.created_at)))
        else:
            query = query.order_by(getattr(DpConsensus, order_by, DpConsensus.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpConsensusListResponse(
            items=[DpConsensusResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpConsensusUpdate,
               updated_by: Optional[UUID] = None) -> DpConsensusResponse:
        """Update an existing DpConsensus record."""
        db_obj = self.db.query(DpConsensus).filter(DpConsensus.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpConsensus not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpConsensusResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpConsensus record. Soft delete by default."""
        db_obj = self.db.query(DpConsensus).filter(DpConsensus.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpConsensus not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpConsensusResponse]:
        """Search DpConsensus by name/code fields."""
        query = self.db.query(DpConsensus)
        if company_id and hasattr(DpConsensus, "company_id"):
            query = query.filter(DpConsensus.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpConsensus, f)]
        if search_fields:
            conditions = [getattr(DpConsensus, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpConsensusResponse.model_validate(i) for i in items]

class DpForecastsService:
    """CRUD service for DpForecasts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpForecastsCreate, created_by: Optional[UUID] = None) -> DpForecastsResponse:
        """Create a new DpForecasts record."""
        db_obj = DpForecasts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpForecastsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpForecastsResponse]:
        """Get DpForecasts by primary key."""
        db_obj = self.db.query(DpForecasts).filter(DpForecasts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpForecasts not found")
        return DpForecastsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpForecastsListResponse:
        """List DpForecasts records with pagination and filtering."""
        query = self.db.query(DpForecasts)
        if company_id and hasattr(DpForecasts, "company_id"):
            query = query.filter(DpForecasts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpForecasts, key) and value is not None:
                    query = query.filter(getattr(DpForecasts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpForecasts, order_by, DpForecasts.created_at)))
        else:
            query = query.order_by(getattr(DpForecasts, order_by, DpForecasts.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpForecastsListResponse(
            items=[DpForecastsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpForecastsUpdate,
               updated_by: Optional[UUID] = None) -> DpForecastsResponse:
        """Update an existing DpForecasts record."""
        db_obj = self.db.query(DpForecasts).filter(DpForecasts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpForecasts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpForecastsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpForecasts record. Soft delete by default."""
        db_obj = self.db.query(DpForecasts).filter(DpForecasts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpForecasts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpForecastsResponse]:
        """Search DpForecasts by name/code fields."""
        query = self.db.query(DpForecasts)
        if company_id and hasattr(DpForecasts, "company_id"):
            query = query.filter(DpForecasts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpForecasts, f)]
        if search_fields:
            conditions = [getattr(DpForecasts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpForecastsResponse.model_validate(i) for i in items]

class DpHistoricalDataService:
    """CRUD service for DpHistoricalData."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpHistoricalDataCreate, created_by: Optional[UUID] = None) -> DpHistoricalDataResponse:
        """Create a new DpHistoricalData record."""
        db_obj = DpHistoricalData(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpHistoricalDataResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpHistoricalDataResponse]:
        """Get DpHistoricalData by primary key."""
        db_obj = self.db.query(DpHistoricalData).filter(DpHistoricalData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpHistoricalData not found")
        return DpHistoricalDataResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpHistoricalDataListResponse:
        """List DpHistoricalData records with pagination and filtering."""
        query = self.db.query(DpHistoricalData)
        if company_id and hasattr(DpHistoricalData, "company_id"):
            query = query.filter(DpHistoricalData.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpHistoricalData, key) and value is not None:
                    query = query.filter(getattr(DpHistoricalData, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpHistoricalData, order_by, DpHistoricalData.created_at)))
        else:
            query = query.order_by(getattr(DpHistoricalData, order_by, DpHistoricalData.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpHistoricalDataListResponse(
            items=[DpHistoricalDataResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpHistoricalDataUpdate,
               updated_by: Optional[UUID] = None) -> DpHistoricalDataResponse:
        """Update an existing DpHistoricalData record."""
        db_obj = self.db.query(DpHistoricalData).filter(DpHistoricalData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpHistoricalData not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpHistoricalDataResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpHistoricalData record. Soft delete by default."""
        db_obj = self.db.query(DpHistoricalData).filter(DpHistoricalData.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpHistoricalData not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpHistoricalDataResponse]:
        """Search DpHistoricalData by name/code fields."""
        query = self.db.query(DpHistoricalData)
        if company_id and hasattr(DpHistoricalData, "company_id"):
            query = query.filter(DpHistoricalData.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpHistoricalData, f)]
        if search_fields:
            conditions = [getattr(DpHistoricalData, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpHistoricalDataResponse.model_validate(i) for i in items]

class DpPromotionsService:
    """CRUD service for DpPromotions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpPromotionsCreate, created_by: Optional[UUID] = None) -> DpPromotionsResponse:
        """Create a new DpPromotions record."""
        db_obj = DpPromotions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpPromotionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpPromotionsResponse]:
        """Get DpPromotions by primary key."""
        db_obj = self.db.query(DpPromotions).filter(DpPromotions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpPromotions not found")
        return DpPromotionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpPromotionsListResponse:
        """List DpPromotions records with pagination and filtering."""
        query = self.db.query(DpPromotions)
        if company_id and hasattr(DpPromotions, "company_id"):
            query = query.filter(DpPromotions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpPromotions, key) and value is not None:
                    query = query.filter(getattr(DpPromotions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpPromotions, order_by, DpPromotions.created_at)))
        else:
            query = query.order_by(getattr(DpPromotions, order_by, DpPromotions.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpPromotionsListResponse(
            items=[DpPromotionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpPromotionsUpdate,
               updated_by: Optional[UUID] = None) -> DpPromotionsResponse:
        """Update an existing DpPromotions record."""
        db_obj = self.db.query(DpPromotions).filter(DpPromotions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpPromotions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpPromotionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpPromotions record. Soft delete by default."""
        db_obj = self.db.query(DpPromotions).filter(DpPromotions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpPromotions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpPromotionsResponse]:
        """Search DpPromotions by name/code fields."""
        query = self.db.query(DpPromotions)
        if company_id and hasattr(DpPromotions, "company_id"):
            query = query.filter(DpPromotions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpPromotions, f)]
        if search_fields:
            conditions = [getattr(DpPromotions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpPromotionsResponse.model_validate(i) for i in items]

class DpSeasonalityService:
    """CRUD service for DpSeasonality."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpSeasonalityCreate, created_by: Optional[UUID] = None) -> DpSeasonalityResponse:
        """Create a new DpSeasonality record."""
        db_obj = DpSeasonality(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpSeasonalityResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpSeasonalityResponse]:
        """Get DpSeasonality by primary key."""
        db_obj = self.db.query(DpSeasonality).filter(DpSeasonality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpSeasonality not found")
        return DpSeasonalityResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpSeasonalityListResponse:
        """List DpSeasonality records with pagination and filtering."""
        query = self.db.query(DpSeasonality)
        if company_id and hasattr(DpSeasonality, "company_id"):
            query = query.filter(DpSeasonality.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpSeasonality, key) and value is not None:
                    query = query.filter(getattr(DpSeasonality, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpSeasonality, order_by, DpSeasonality.created_at)))
        else:
            query = query.order_by(getattr(DpSeasonality, order_by, DpSeasonality.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpSeasonalityListResponse(
            items=[DpSeasonalityResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpSeasonalityUpdate,
               updated_by: Optional[UUID] = None) -> DpSeasonalityResponse:
        """Update an existing DpSeasonality record."""
        db_obj = self.db.query(DpSeasonality).filter(DpSeasonality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpSeasonality not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpSeasonalityResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpSeasonality record. Soft delete by default."""
        db_obj = self.db.query(DpSeasonality).filter(DpSeasonality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpSeasonality not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpSeasonalityResponse]:
        """Search DpSeasonality by name/code fields."""
        query = self.db.query(DpSeasonality)
        if company_id and hasattr(DpSeasonality, "company_id"):
            query = query.filter(DpSeasonality.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpSeasonality, f)]
        if search_fields:
            conditions = [getattr(DpSeasonality, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpSeasonalityResponse.model_validate(i) for i in items]

class DpSimulationService:
    """CRUD service for DpSimulation."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpSimulationCreate, created_by: Optional[UUID] = None) -> DpSimulationResponse:
        """Create a new DpSimulation record."""
        db_obj = DpSimulation(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpSimulationResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpSimulationResponse]:
        """Get DpSimulation by primary key."""
        db_obj = self.db.query(DpSimulation).filter(DpSimulation.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpSimulation not found")
        return DpSimulationResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpSimulationListResponse:
        """List DpSimulation records with pagination and filtering."""
        query = self.db.query(DpSimulation)
        if company_id and hasattr(DpSimulation, "company_id"):
            query = query.filter(DpSimulation.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpSimulation, key) and value is not None:
                    query = query.filter(getattr(DpSimulation, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpSimulation, order_by, DpSimulation.created_at)))
        else:
            query = query.order_by(getattr(DpSimulation, order_by, DpSimulation.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpSimulationListResponse(
            items=[DpSimulationResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpSimulationUpdate,
               updated_by: Optional[UUID] = None) -> DpSimulationResponse:
        """Update an existing DpSimulation record."""
        db_obj = self.db.query(DpSimulation).filter(DpSimulation.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpSimulation not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpSimulationResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpSimulation record. Soft delete by default."""
        db_obj = self.db.query(DpSimulation).filter(DpSimulation.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpSimulation not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpSimulationResponse]:
        """Search DpSimulation by name/code fields."""
        query = self.db.query(DpSimulation)
        if company_id and hasattr(DpSimulation, "company_id"):
            query = query.filter(DpSimulation.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpSimulation, f)]
        if search_fields:
            conditions = [getattr(DpSimulation, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpSimulationResponse.model_validate(i) for i in items]

class DpStatisticalModelsService:
    """CRUD service for DpStatisticalModels."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: DpStatisticalModelsCreate, created_by: Optional[UUID] = None) -> DpStatisticalModelsResponse:
        """Create a new DpStatisticalModels record."""
        db_obj = DpStatisticalModels(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return DpStatisticalModelsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[DpStatisticalModelsResponse]:
        """Get DpStatisticalModels by primary key."""
        db_obj = self.db.query(DpStatisticalModels).filter(DpStatisticalModels.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpStatisticalModels not found")
        return DpStatisticalModelsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> DpStatisticalModelsListResponse:
        """List DpStatisticalModels records with pagination and filtering."""
        query = self.db.query(DpStatisticalModels)
        if company_id and hasattr(DpStatisticalModels, "company_id"):
            query = query.filter(DpStatisticalModels.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(DpStatisticalModels, key) and value is not None:
                    query = query.filter(getattr(DpStatisticalModels, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(DpStatisticalModels, order_by, DpStatisticalModels.created_at)))
        else:
            query = query.order_by(getattr(DpStatisticalModels, order_by, DpStatisticalModels.created_at))
        items = query.offset(skip).limit(limit).all()
        return DpStatisticalModelsListResponse(
            items=[DpStatisticalModelsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: DpStatisticalModelsUpdate,
               updated_by: Optional[UUID] = None) -> DpStatisticalModelsResponse:
        """Update an existing DpStatisticalModels record."""
        db_obj = self.db.query(DpStatisticalModels).filter(DpStatisticalModels.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpStatisticalModels not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return DpStatisticalModelsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a DpStatisticalModels record. Soft delete by default."""
        db_obj = self.db.query(DpStatisticalModels).filter(DpStatisticalModels.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="DpStatisticalModels not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[DpStatisticalModelsResponse]:
        """Search DpStatisticalModels by name/code fields."""
        query = self.db.query(DpStatisticalModels)
        if company_id and hasattr(DpStatisticalModels, "company_id"):
            query = query.filter(DpStatisticalModels.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(DpStatisticalModels, f)]
        if search_fields:
            conditions = [getattr(DpStatisticalModels, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [DpStatisticalModelsResponse.model_validate(i) for i in items]

class EdiMappingRulesService:
    """CRUD service for EdiMappingRules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EdiMappingRulesCreate, created_by: Optional[UUID] = None) -> EdiMappingRulesResponse:
        """Create a new EdiMappingRules record."""
        db_obj = EdiMappingRules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EdiMappingRulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EdiMappingRulesResponse]:
        """Get EdiMappingRules by primary key."""
        db_obj = self.db.query(EdiMappingRules).filter(EdiMappingRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiMappingRules not found")
        return EdiMappingRulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EdiMappingRulesListResponse:
        """List EdiMappingRules records with pagination and filtering."""
        query = self.db.query(EdiMappingRules)
        if company_id and hasattr(EdiMappingRules, "company_id"):
            query = query.filter(EdiMappingRules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EdiMappingRules, key) and value is not None:
                    query = query.filter(getattr(EdiMappingRules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EdiMappingRules, order_by, EdiMappingRules.created_at)))
        else:
            query = query.order_by(getattr(EdiMappingRules, order_by, EdiMappingRules.created_at))
        items = query.offset(skip).limit(limit).all()
        return EdiMappingRulesListResponse(
            items=[EdiMappingRulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EdiMappingRulesUpdate,
               updated_by: Optional[UUID] = None) -> EdiMappingRulesResponse:
        """Update an existing EdiMappingRules record."""
        db_obj = self.db.query(EdiMappingRules).filter(EdiMappingRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiMappingRules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EdiMappingRulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EdiMappingRules record. Soft delete by default."""
        db_obj = self.db.query(EdiMappingRules).filter(EdiMappingRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiMappingRules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EdiMappingRulesResponse]:
        """Search EdiMappingRules by name/code fields."""
        query = self.db.query(EdiMappingRules)
        if company_id and hasattr(EdiMappingRules, "company_id"):
            query = query.filter(EdiMappingRules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EdiMappingRules, f)]
        if search_fields:
            conditions = [getattr(EdiMappingRules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EdiMappingRulesResponse.model_validate(i) for i in items]

class EdiPartnersService:
    """CRUD service for EdiPartners."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EdiPartnersCreate, created_by: Optional[UUID] = None) -> EdiPartnersResponse:
        """Create a new EdiPartners record."""
        db_obj = EdiPartners(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EdiPartnersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EdiPartnersResponse]:
        """Get EdiPartners by primary key."""
        db_obj = self.db.query(EdiPartners).filter(EdiPartners.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiPartners not found")
        return EdiPartnersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EdiPartnersListResponse:
        """List EdiPartners records with pagination and filtering."""
        query = self.db.query(EdiPartners)
        if company_id and hasattr(EdiPartners, "company_id"):
            query = query.filter(EdiPartners.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EdiPartners, key) and value is not None:
                    query = query.filter(getattr(EdiPartners, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EdiPartners, order_by, EdiPartners.created_at)))
        else:
            query = query.order_by(getattr(EdiPartners, order_by, EdiPartners.created_at))
        items = query.offset(skip).limit(limit).all()
        return EdiPartnersListResponse(
            items=[EdiPartnersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EdiPartnersUpdate,
               updated_by: Optional[UUID] = None) -> EdiPartnersResponse:
        """Update an existing EdiPartners record."""
        db_obj = self.db.query(EdiPartners).filter(EdiPartners.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiPartners not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EdiPartnersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EdiPartners record. Soft delete by default."""
        db_obj = self.db.query(EdiPartners).filter(EdiPartners.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiPartners not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EdiPartnersResponse]:
        """Search EdiPartners by name/code fields."""
        query = self.db.query(EdiPartners)
        if company_id and hasattr(EdiPartners, "company_id"):
            query = query.filter(EdiPartners.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EdiPartners, f)]
        if search_fields:
            conditions = [getattr(EdiPartners, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EdiPartnersResponse.model_validate(i) for i in items]

class EdiTransactionsService:
    """CRUD service for EdiTransactions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EdiTransactionsCreate, created_by: Optional[UUID] = None) -> EdiTransactionsResponse:
        """Create a new EdiTransactions record."""
        db_obj = EdiTransactions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EdiTransactionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EdiTransactionsResponse]:
        """Get EdiTransactions by primary key."""
        db_obj = self.db.query(EdiTransactions).filter(EdiTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiTransactions not found")
        return EdiTransactionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EdiTransactionsListResponse:
        """List EdiTransactions records with pagination and filtering."""
        query = self.db.query(EdiTransactions)
        if company_id and hasattr(EdiTransactions, "company_id"):
            query = query.filter(EdiTransactions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EdiTransactions, key) and value is not None:
                    query = query.filter(getattr(EdiTransactions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EdiTransactions, order_by, EdiTransactions.created_at)))
        else:
            query = query.order_by(getattr(EdiTransactions, order_by, EdiTransactions.created_at))
        items = query.offset(skip).limit(limit).all()
        return EdiTransactionsListResponse(
            items=[EdiTransactionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EdiTransactionsUpdate,
               updated_by: Optional[UUID] = None) -> EdiTransactionsResponse:
        """Update an existing EdiTransactions record."""
        db_obj = self.db.query(EdiTransactions).filter(EdiTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiTransactions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EdiTransactionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EdiTransactions record. Soft delete by default."""
        db_obj = self.db.query(EdiTransactions).filter(EdiTransactions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EdiTransactions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EdiTransactionsResponse]:
        """Search EdiTransactions by name/code fields."""
        query = self.db.query(EdiTransactions)
        if company_id and hasattr(EdiTransactions, "company_id"):
            query = query.filter(EdiTransactions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EdiTransactions, f)]
        if search_fields:
            conditions = [getattr(EdiTransactions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EdiTransactionsResponse.model_validate(i) for i in items]

class EinvoiceDocumentsService:
    """CRUD service for EinvoiceDocuments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: EinvoiceDocumentsCreate, created_by: Optional[UUID] = None) -> EinvoiceDocumentsResponse:
        """Create a new EinvoiceDocuments record."""
        db_obj = EinvoiceDocuments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return EinvoiceDocumentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[EinvoiceDocumentsResponse]:
        """Get EinvoiceDocuments by primary key."""
        db_obj = self.db.query(EinvoiceDocuments).filter(EinvoiceDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EinvoiceDocuments not found")
        return EinvoiceDocumentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> EinvoiceDocumentsListResponse:
        """List EinvoiceDocuments records with pagination and filtering."""
        query = self.db.query(EinvoiceDocuments)
        if company_id and hasattr(EinvoiceDocuments, "company_id"):
            query = query.filter(EinvoiceDocuments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(EinvoiceDocuments, key) and value is not None:
                    query = query.filter(getattr(EinvoiceDocuments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(EinvoiceDocuments, order_by, EinvoiceDocuments.created_at)))
        else:
            query = query.order_by(getattr(EinvoiceDocuments, order_by, EinvoiceDocuments.created_at))
        items = query.offset(skip).limit(limit).all()
        return EinvoiceDocumentsListResponse(
            items=[EinvoiceDocumentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: EinvoiceDocumentsUpdate,
               updated_by: Optional[UUID] = None) -> EinvoiceDocumentsResponse:
        """Update an existing EinvoiceDocuments record."""
        db_obj = self.db.query(EinvoiceDocuments).filter(EinvoiceDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EinvoiceDocuments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return EinvoiceDocumentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a EinvoiceDocuments record. Soft delete by default."""
        db_obj = self.db.query(EinvoiceDocuments).filter(EinvoiceDocuments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="EinvoiceDocuments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[EinvoiceDocumentsResponse]:
        """Search EinvoiceDocuments by name/code fields."""
        query = self.db.query(EinvoiceDocuments)
        if company_id and hasattr(EinvoiceDocuments, "company_id"):
            query = query.filter(EinvoiceDocuments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(EinvoiceDocuments, f)]
        if search_fields:
            conditions = [getattr(EinvoiceDocuments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [EinvoiceDocumentsResponse.model_validate(i) for i in items]

class MdmDataQualityService:
    """CRUD service for MdmDataQuality."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MdmDataQualityCreate, created_by: Optional[UUID] = None) -> MdmDataQualityResponse:
        """Create a new MdmDataQuality record."""
        db_obj = MdmDataQuality(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmDataQualityResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MdmDataQualityResponse]:
        """Get MdmDataQuality by primary key."""
        db_obj = self.db.query(MdmDataQuality).filter(MdmDataQuality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmDataQuality not found")
        return MdmDataQualityResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MdmDataQualityListResponse:
        """List MdmDataQuality records with pagination and filtering."""
        query = self.db.query(MdmDataQuality)
        if company_id and hasattr(MdmDataQuality, "company_id"):
            query = query.filter(MdmDataQuality.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MdmDataQuality, key) and value is not None:
                    query = query.filter(getattr(MdmDataQuality, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MdmDataQuality, order_by, MdmDataQuality.created_at)))
        else:
            query = query.order_by(getattr(MdmDataQuality, order_by, MdmDataQuality.created_at))
        items = query.offset(skip).limit(limit).all()
        return MdmDataQualityListResponse(
            items=[MdmDataQualityResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MdmDataQualityUpdate,
               updated_by: Optional[UUID] = None) -> MdmDataQualityResponse:
        """Update an existing MdmDataQuality record."""
        db_obj = self.db.query(MdmDataQuality).filter(MdmDataQuality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmDataQuality not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmDataQualityResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MdmDataQuality record. Soft delete by default."""
        db_obj = self.db.query(MdmDataQuality).filter(MdmDataQuality.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmDataQuality not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MdmDataQualityResponse]:
        """Search MdmDataQuality by name/code fields."""
        query = self.db.query(MdmDataQuality)
        if company_id and hasattr(MdmDataQuality, "company_id"):
            query = query.filter(MdmDataQuality.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MdmDataQuality, f)]
        if search_fields:
            conditions = [getattr(MdmDataQuality, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MdmDataQualityResponse.model_validate(i) for i in items]

class MdmDataSourcesService:
    """CRUD service for MdmDataSources."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MdmDataSourcesCreate, created_by: Optional[UUID] = None) -> MdmDataSourcesResponse:
        """Create a new MdmDataSources record."""
        db_obj = MdmDataSources(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmDataSourcesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MdmDataSourcesResponse]:
        """Get MdmDataSources by primary key."""
        db_obj = self.db.query(MdmDataSources).filter(MdmDataSources.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmDataSources not found")
        return MdmDataSourcesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MdmDataSourcesListResponse:
        """List MdmDataSources records with pagination and filtering."""
        query = self.db.query(MdmDataSources)
        if company_id and hasattr(MdmDataSources, "company_id"):
            query = query.filter(MdmDataSources.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MdmDataSources, key) and value is not None:
                    query = query.filter(getattr(MdmDataSources, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MdmDataSources, order_by, MdmDataSources.created_at)))
        else:
            query = query.order_by(getattr(MdmDataSources, order_by, MdmDataSources.created_at))
        items = query.offset(skip).limit(limit).all()
        return MdmDataSourcesListResponse(
            items=[MdmDataSourcesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MdmDataSourcesUpdate,
               updated_by: Optional[UUID] = None) -> MdmDataSourcesResponse:
        """Update an existing MdmDataSources record."""
        db_obj = self.db.query(MdmDataSources).filter(MdmDataSources.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmDataSources not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmDataSourcesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MdmDataSources record. Soft delete by default."""
        db_obj = self.db.query(MdmDataSources).filter(MdmDataSources.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmDataSources not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MdmDataSourcesResponse]:
        """Search MdmDataSources by name/code fields."""
        query = self.db.query(MdmDataSources)
        if company_id and hasattr(MdmDataSources, "company_id"):
            query = query.filter(MdmDataSources.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MdmDataSources, f)]
        if search_fields:
            conditions = [getattr(MdmDataSources, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MdmDataSourcesResponse.model_validate(i) for i in items]

class MdmGoldenRecordsService:
    """CRUD service for MdmGoldenRecords."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MdmGoldenRecordsCreate, created_by: Optional[UUID] = None) -> MdmGoldenRecordsResponse:
        """Create a new MdmGoldenRecords record."""
        db_obj = MdmGoldenRecords(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmGoldenRecordsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MdmGoldenRecordsResponse]:
        """Get MdmGoldenRecords by primary key."""
        db_obj = self.db.query(MdmGoldenRecords).filter(MdmGoldenRecords.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmGoldenRecords not found")
        return MdmGoldenRecordsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MdmGoldenRecordsListResponse:
        """List MdmGoldenRecords records with pagination and filtering."""
        query = self.db.query(MdmGoldenRecords)
        if company_id and hasattr(MdmGoldenRecords, "company_id"):
            query = query.filter(MdmGoldenRecords.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MdmGoldenRecords, key) and value is not None:
                    query = query.filter(getattr(MdmGoldenRecords, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MdmGoldenRecords, order_by, MdmGoldenRecords.created_at)))
        else:
            query = query.order_by(getattr(MdmGoldenRecords, order_by, MdmGoldenRecords.created_at))
        items = query.offset(skip).limit(limit).all()
        return MdmGoldenRecordsListResponse(
            items=[MdmGoldenRecordsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MdmGoldenRecordsUpdate,
               updated_by: Optional[UUID] = None) -> MdmGoldenRecordsResponse:
        """Update an existing MdmGoldenRecords record."""
        db_obj = self.db.query(MdmGoldenRecords).filter(MdmGoldenRecords.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmGoldenRecords not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmGoldenRecordsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MdmGoldenRecords record. Soft delete by default."""
        db_obj = self.db.query(MdmGoldenRecords).filter(MdmGoldenRecords.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmGoldenRecords not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MdmGoldenRecordsResponse]:
        """Search MdmGoldenRecords by name/code fields."""
        query = self.db.query(MdmGoldenRecords)
        if company_id and hasattr(MdmGoldenRecords, "company_id"):
            query = query.filter(MdmGoldenRecords.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MdmGoldenRecords, f)]
        if search_fields:
            conditions = [getattr(MdmGoldenRecords, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MdmGoldenRecordsResponse.model_validate(i) for i in items]

class MdmMatchingRulesService:
    """CRUD service for MdmMatchingRules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MdmMatchingRulesCreate, created_by: Optional[UUID] = None) -> MdmMatchingRulesResponse:
        """Create a new MdmMatchingRules record."""
        db_obj = MdmMatchingRules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmMatchingRulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MdmMatchingRulesResponse]:
        """Get MdmMatchingRules by primary key."""
        db_obj = self.db.query(MdmMatchingRules).filter(MdmMatchingRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmMatchingRules not found")
        return MdmMatchingRulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MdmMatchingRulesListResponse:
        """List MdmMatchingRules records with pagination and filtering."""
        query = self.db.query(MdmMatchingRules)
        if company_id and hasattr(MdmMatchingRules, "company_id"):
            query = query.filter(MdmMatchingRules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MdmMatchingRules, key) and value is not None:
                    query = query.filter(getattr(MdmMatchingRules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MdmMatchingRules, order_by, MdmMatchingRules.created_at)))
        else:
            query = query.order_by(getattr(MdmMatchingRules, order_by, MdmMatchingRules.created_at))
        items = query.offset(skip).limit(limit).all()
        return MdmMatchingRulesListResponse(
            items=[MdmMatchingRulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MdmMatchingRulesUpdate,
               updated_by: Optional[UUID] = None) -> MdmMatchingRulesResponse:
        """Update an existing MdmMatchingRules record."""
        db_obj = self.db.query(MdmMatchingRules).filter(MdmMatchingRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmMatchingRules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmMatchingRulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MdmMatchingRules record. Soft delete by default."""
        db_obj = self.db.query(MdmMatchingRules).filter(MdmMatchingRules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmMatchingRules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MdmMatchingRulesResponse]:
        """Search MdmMatchingRules by name/code fields."""
        query = self.db.query(MdmMatchingRules)
        if company_id and hasattr(MdmMatchingRules, "company_id"):
            query = query.filter(MdmMatchingRules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MdmMatchingRules, f)]
        if search_fields:
            conditions = [getattr(MdmMatchingRules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MdmMatchingRulesResponse.model_validate(i) for i in items]

class MdmMergeLogService:
    """CRUD service for MdmMergeLog."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MdmMergeLogCreate, created_by: Optional[UUID] = None) -> MdmMergeLogResponse:
        """Create a new MdmMergeLog record."""
        db_obj = MdmMergeLog(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmMergeLogResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MdmMergeLogResponse]:
        """Get MdmMergeLog by primary key."""
        db_obj = self.db.query(MdmMergeLog).filter(MdmMergeLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmMergeLog not found")
        return MdmMergeLogResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MdmMergeLogListResponse:
        """List MdmMergeLog records with pagination and filtering."""
        query = self.db.query(MdmMergeLog)
        if company_id and hasattr(MdmMergeLog, "company_id"):
            query = query.filter(MdmMergeLog.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MdmMergeLog, key) and value is not None:
                    query = query.filter(getattr(MdmMergeLog, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MdmMergeLog, order_by, MdmMergeLog.created_at)))
        else:
            query = query.order_by(getattr(MdmMergeLog, order_by, MdmMergeLog.created_at))
        items = query.offset(skip).limit(limit).all()
        return MdmMergeLogListResponse(
            items=[MdmMergeLogResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MdmMergeLogUpdate,
               updated_by: Optional[UUID] = None) -> MdmMergeLogResponse:
        """Update an existing MdmMergeLog record."""
        db_obj = self.db.query(MdmMergeLog).filter(MdmMergeLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmMergeLog not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MdmMergeLogResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MdmMergeLog record. Soft delete by default."""
        db_obj = self.db.query(MdmMergeLog).filter(MdmMergeLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MdmMergeLog not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MdmMergeLogResponse]:
        """Search MdmMergeLog by name/code fields."""
        query = self.db.query(MdmMergeLog)
        if company_id and hasattr(MdmMergeLog, "company_id"):
            query = query.filter(MdmMergeLog.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MdmMergeLog, f)]
        if search_fields:
            conditions = [getattr(MdmMergeLog, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MdmMergeLogResponse.model_validate(i) for i in items]

class MobileAppsService:
    """CRUD service for MobileApps."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MobileAppsCreate, created_by: Optional[UUID] = None) -> MobileAppsResponse:
        """Create a new MobileApps record."""
        db_obj = MobileApps(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MobileAppsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MobileAppsResponse]:
        """Get MobileApps by primary key."""
        db_obj = self.db.query(MobileApps).filter(MobileApps.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileApps not found")
        return MobileAppsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MobileAppsListResponse:
        """List MobileApps records with pagination and filtering."""
        query = self.db.query(MobileApps)
        if company_id and hasattr(MobileApps, "company_id"):
            query = query.filter(MobileApps.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MobileApps, key) and value is not None:
                    query = query.filter(getattr(MobileApps, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MobileApps, order_by, MobileApps.created_at)))
        else:
            query = query.order_by(getattr(MobileApps, order_by, MobileApps.created_at))
        items = query.offset(skip).limit(limit).all()
        return MobileAppsListResponse(
            items=[MobileAppsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MobileAppsUpdate,
               updated_by: Optional[UUID] = None) -> MobileAppsResponse:
        """Update an existing MobileApps record."""
        db_obj = self.db.query(MobileApps).filter(MobileApps.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileApps not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MobileAppsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MobileApps record. Soft delete by default."""
        db_obj = self.db.query(MobileApps).filter(MobileApps.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileApps not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MobileAppsResponse]:
        """Search MobileApps by name/code fields."""
        query = self.db.query(MobileApps)
        if company_id and hasattr(MobileApps, "company_id"):
            query = query.filter(MobileApps.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MobileApps, f)]
        if search_fields:
            conditions = [getattr(MobileApps, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MobileAppsResponse.model_validate(i) for i in items]

class MobileDeviceRegistryService:
    """CRUD service for MobileDeviceRegistry."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MobileDeviceRegistryCreate, created_by: Optional[UUID] = None) -> MobileDeviceRegistryResponse:
        """Create a new MobileDeviceRegistry record."""
        db_obj = MobileDeviceRegistry(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MobileDeviceRegistryResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MobileDeviceRegistryResponse]:
        """Get MobileDeviceRegistry by primary key."""
        db_obj = self.db.query(MobileDeviceRegistry).filter(MobileDeviceRegistry.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileDeviceRegistry not found")
        return MobileDeviceRegistryResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MobileDeviceRegistryListResponse:
        """List MobileDeviceRegistry records with pagination and filtering."""
        query = self.db.query(MobileDeviceRegistry)
        if company_id and hasattr(MobileDeviceRegistry, "company_id"):
            query = query.filter(MobileDeviceRegistry.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MobileDeviceRegistry, key) and value is not None:
                    query = query.filter(getattr(MobileDeviceRegistry, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MobileDeviceRegistry, order_by, MobileDeviceRegistry.created_at)))
        else:
            query = query.order_by(getattr(MobileDeviceRegistry, order_by, MobileDeviceRegistry.created_at))
        items = query.offset(skip).limit(limit).all()
        return MobileDeviceRegistryListResponse(
            items=[MobileDeviceRegistryResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MobileDeviceRegistryUpdate,
               updated_by: Optional[UUID] = None) -> MobileDeviceRegistryResponse:
        """Update an existing MobileDeviceRegistry record."""
        db_obj = self.db.query(MobileDeviceRegistry).filter(MobileDeviceRegistry.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileDeviceRegistry not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MobileDeviceRegistryResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MobileDeviceRegistry record. Soft delete by default."""
        db_obj = self.db.query(MobileDeviceRegistry).filter(MobileDeviceRegistry.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileDeviceRegistry not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MobileDeviceRegistryResponse]:
        """Search MobileDeviceRegistry by name/code fields."""
        query = self.db.query(MobileDeviceRegistry)
        if company_id and hasattr(MobileDeviceRegistry, "company_id"):
            query = query.filter(MobileDeviceRegistry.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MobileDeviceRegistry, f)]
        if search_fields:
            conditions = [getattr(MobileDeviceRegistry, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MobileDeviceRegistryResponse.model_validate(i) for i in items]

class MobilePushNotificationsService:
    """CRUD service for MobilePushNotifications."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MobilePushNotificationsCreate, created_by: Optional[UUID] = None) -> MobilePushNotificationsResponse:
        """Create a new MobilePushNotifications record."""
        db_obj = MobilePushNotifications(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MobilePushNotificationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MobilePushNotificationsResponse]:
        """Get MobilePushNotifications by primary key."""
        db_obj = self.db.query(MobilePushNotifications).filter(MobilePushNotifications.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobilePushNotifications not found")
        return MobilePushNotificationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MobilePushNotificationsListResponse:
        """List MobilePushNotifications records with pagination and filtering."""
        query = self.db.query(MobilePushNotifications)
        if company_id and hasattr(MobilePushNotifications, "company_id"):
            query = query.filter(MobilePushNotifications.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MobilePushNotifications, key) and value is not None:
                    query = query.filter(getattr(MobilePushNotifications, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MobilePushNotifications, order_by, MobilePushNotifications.created_at)))
        else:
            query = query.order_by(getattr(MobilePushNotifications, order_by, MobilePushNotifications.created_at))
        items = query.offset(skip).limit(limit).all()
        return MobilePushNotificationsListResponse(
            items=[MobilePushNotificationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MobilePushNotificationsUpdate,
               updated_by: Optional[UUID] = None) -> MobilePushNotificationsResponse:
        """Update an existing MobilePushNotifications record."""
        db_obj = self.db.query(MobilePushNotifications).filter(MobilePushNotifications.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobilePushNotifications not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MobilePushNotificationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MobilePushNotifications record. Soft delete by default."""
        db_obj = self.db.query(MobilePushNotifications).filter(MobilePushNotifications.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobilePushNotifications not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MobilePushNotificationsResponse]:
        """Search MobilePushNotifications by name/code fields."""
        query = self.db.query(MobilePushNotifications)
        if company_id and hasattr(MobilePushNotifications, "company_id"):
            query = query.filter(MobilePushNotifications.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MobilePushNotifications, f)]
        if search_fields:
            conditions = [getattr(MobilePushNotifications, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MobilePushNotificationsResponse.model_validate(i) for i in items]

class MobileSyncQueuesService:
    """CRUD service for MobileSyncQueues."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: MobileSyncQueuesCreate, created_by: Optional[UUID] = None) -> MobileSyncQueuesResponse:
        """Create a new MobileSyncQueues record."""
        db_obj = MobileSyncQueues(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return MobileSyncQueuesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[MobileSyncQueuesResponse]:
        """Get MobileSyncQueues by primary key."""
        db_obj = self.db.query(MobileSyncQueues).filter(MobileSyncQueues.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileSyncQueues not found")
        return MobileSyncQueuesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> MobileSyncQueuesListResponse:
        """List MobileSyncQueues records with pagination and filtering."""
        query = self.db.query(MobileSyncQueues)
        if company_id and hasattr(MobileSyncQueues, "company_id"):
            query = query.filter(MobileSyncQueues.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(MobileSyncQueues, key) and value is not None:
                    query = query.filter(getattr(MobileSyncQueues, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(MobileSyncQueues, order_by, MobileSyncQueues.created_at)))
        else:
            query = query.order_by(getattr(MobileSyncQueues, order_by, MobileSyncQueues.created_at))
        items = query.offset(skip).limit(limit).all()
        return MobileSyncQueuesListResponse(
            items=[MobileSyncQueuesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: MobileSyncQueuesUpdate,
               updated_by: Optional[UUID] = None) -> MobileSyncQueuesResponse:
        """Update an existing MobileSyncQueues record."""
        db_obj = self.db.query(MobileSyncQueues).filter(MobileSyncQueues.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileSyncQueues not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return MobileSyncQueuesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a MobileSyncQueues record. Soft delete by default."""
        db_obj = self.db.query(MobileSyncQueues).filter(MobileSyncQueues.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="MobileSyncQueues not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[MobileSyncQueuesResponse]:
        """Search MobileSyncQueues by name/code fields."""
        query = self.db.query(MobileSyncQueues)
        if company_id and hasattr(MobileSyncQueues, "company_id"):
            query = query.filter(MobileSyncQueues.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(MobileSyncQueues, f)]
        if search_fields:
            conditions = [getattr(MobileSyncQueues, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [MobileSyncQueuesResponse.model_validate(i) for i in items]

class PmDevelopmentPlansService:
    """CRUD service for PmDevelopmentPlans."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PmDevelopmentPlansCreate, created_by: Optional[UUID] = None) -> PmDevelopmentPlansResponse:
        """Create a new PmDevelopmentPlans record."""
        db_obj = PmDevelopmentPlans(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PmDevelopmentPlansResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PmDevelopmentPlansResponse]:
        """Get PmDevelopmentPlans by primary key."""
        db_obj = self.db.query(PmDevelopmentPlans).filter(PmDevelopmentPlans.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmDevelopmentPlans not found")
        return PmDevelopmentPlansResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PmDevelopmentPlansListResponse:
        """List PmDevelopmentPlans records with pagination and filtering."""
        query = self.db.query(PmDevelopmentPlans)
        if company_id and hasattr(PmDevelopmentPlans, "company_id"):
            query = query.filter(PmDevelopmentPlans.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PmDevelopmentPlans, key) and value is not None:
                    query = query.filter(getattr(PmDevelopmentPlans, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PmDevelopmentPlans, order_by, PmDevelopmentPlans.created_at)))
        else:
            query = query.order_by(getattr(PmDevelopmentPlans, order_by, PmDevelopmentPlans.created_at))
        items = query.offset(skip).limit(limit).all()
        return PmDevelopmentPlansListResponse(
            items=[PmDevelopmentPlansResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PmDevelopmentPlansUpdate,
               updated_by: Optional[UUID] = None) -> PmDevelopmentPlansResponse:
        """Update an existing PmDevelopmentPlans record."""
        db_obj = self.db.query(PmDevelopmentPlans).filter(PmDevelopmentPlans.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmDevelopmentPlans not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PmDevelopmentPlansResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PmDevelopmentPlans record. Soft delete by default."""
        db_obj = self.db.query(PmDevelopmentPlans).filter(PmDevelopmentPlans.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmDevelopmentPlans not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PmDevelopmentPlansResponse]:
        """Search PmDevelopmentPlans by name/code fields."""
        query = self.db.query(PmDevelopmentPlans)
        if company_id and hasattr(PmDevelopmentPlans, "company_id"):
            query = query.filter(PmDevelopmentPlans.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PmDevelopmentPlans, f)]
        if search_fields:
            conditions = [getattr(PmDevelopmentPlans, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PmDevelopmentPlansResponse.model_validate(i) for i in items]

class PmGoalsService:
    """CRUD service for PmGoals."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PmGoalsCreate, created_by: Optional[UUID] = None) -> PmGoalsResponse:
        """Create a new PmGoals record."""
        db_obj = PmGoals(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PmGoalsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PmGoalsResponse]:
        """Get PmGoals by primary key."""
        db_obj = self.db.query(PmGoals).filter(PmGoals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmGoals not found")
        return PmGoalsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PmGoalsListResponse:
        """List PmGoals records with pagination and filtering."""
        query = self.db.query(PmGoals)
        if company_id and hasattr(PmGoals, "company_id"):
            query = query.filter(PmGoals.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PmGoals, key) and value is not None:
                    query = query.filter(getattr(PmGoals, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PmGoals, order_by, PmGoals.created_at)))
        else:
            query = query.order_by(getattr(PmGoals, order_by, PmGoals.created_at))
        items = query.offset(skip).limit(limit).all()
        return PmGoalsListResponse(
            items=[PmGoalsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PmGoalsUpdate,
               updated_by: Optional[UUID] = None) -> PmGoalsResponse:
        """Update an existing PmGoals record."""
        db_obj = self.db.query(PmGoals).filter(PmGoals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmGoals not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PmGoalsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PmGoals record. Soft delete by default."""
        db_obj = self.db.query(PmGoals).filter(PmGoals.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmGoals not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PmGoalsResponse]:
        """Search PmGoals by name/code fields."""
        query = self.db.query(PmGoals)
        if company_id and hasattr(PmGoals, "company_id"):
            query = query.filter(PmGoals.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PmGoals, f)]
        if search_fields:
            conditions = [getattr(PmGoals, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PmGoalsResponse.model_validate(i) for i in items]

class PmRatingsService:
    """CRUD service for PmRatings."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PmRatingsCreate, created_by: Optional[UUID] = None) -> PmRatingsResponse:
        """Create a new PmRatings record."""
        db_obj = PmRatings(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PmRatingsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PmRatingsResponse]:
        """Get PmRatings by primary key."""
        db_obj = self.db.query(PmRatings).filter(PmRatings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmRatings not found")
        return PmRatingsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PmRatingsListResponse:
        """List PmRatings records with pagination and filtering."""
        query = self.db.query(PmRatings)
        if company_id and hasattr(PmRatings, "company_id"):
            query = query.filter(PmRatings.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PmRatings, key) and value is not None:
                    query = query.filter(getattr(PmRatings, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PmRatings, order_by, PmRatings.created_at)))
        else:
            query = query.order_by(getattr(PmRatings, order_by, PmRatings.created_at))
        items = query.offset(skip).limit(limit).all()
        return PmRatingsListResponse(
            items=[PmRatingsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PmRatingsUpdate,
               updated_by: Optional[UUID] = None) -> PmRatingsResponse:
        """Update an existing PmRatings record."""
        db_obj = self.db.query(PmRatings).filter(PmRatings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmRatings not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PmRatingsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PmRatings record. Soft delete by default."""
        db_obj = self.db.query(PmRatings).filter(PmRatings.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmRatings not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PmRatingsResponse]:
        """Search PmRatings by name/code fields."""
        query = self.db.query(PmRatings)
        if company_id and hasattr(PmRatings, "company_id"):
            query = query.filter(PmRatings.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PmRatings, f)]
        if search_fields:
            conditions = [getattr(PmRatings, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PmRatingsResponse.model_validate(i) for i in items]

class PmReviewsService:
    """CRUD service for PmReviews."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PmReviewsCreate, created_by: Optional[UUID] = None) -> PmReviewsResponse:
        """Create a new PmReviews record."""
        db_obj = PmReviews(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PmReviewsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PmReviewsResponse]:
        """Get PmReviews by primary key."""
        db_obj = self.db.query(PmReviews).filter(PmReviews.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmReviews not found")
        return PmReviewsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PmReviewsListResponse:
        """List PmReviews records with pagination and filtering."""
        query = self.db.query(PmReviews)
        if company_id and hasattr(PmReviews, "company_id"):
            query = query.filter(PmReviews.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PmReviews, key) and value is not None:
                    query = query.filter(getattr(PmReviews, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PmReviews, order_by, PmReviews.created_at)))
        else:
            query = query.order_by(getattr(PmReviews, order_by, PmReviews.created_at))
        items = query.offset(skip).limit(limit).all()
        return PmReviewsListResponse(
            items=[PmReviewsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PmReviewsUpdate,
               updated_by: Optional[UUID] = None) -> PmReviewsResponse:
        """Update an existing PmReviews record."""
        db_obj = self.db.query(PmReviews).filter(PmReviews.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmReviews not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PmReviewsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PmReviews record. Soft delete by default."""
        db_obj = self.db.query(PmReviews).filter(PmReviews.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PmReviews not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PmReviewsResponse]:
        """Search PmReviews by name/code fields."""
        query = self.db.query(PmReviews)
        if company_id and hasattr(PmReviews, "company_id"):
            query = query.filter(PmReviews.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PmReviews, f)]
        if search_fields:
            conditions = [getattr(PmReviews, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PmReviewsResponse.model_validate(i) for i in items]

class PortalCustomersService:
    """CRUD service for PortalCustomers."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PortalCustomersCreate, created_by: Optional[UUID] = None) -> PortalCustomersResponse:
        """Create a new PortalCustomers record."""
        db_obj = PortalCustomers(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalCustomersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PortalCustomersResponse]:
        """Get PortalCustomers by primary key."""
        db_obj = self.db.query(PortalCustomers).filter(PortalCustomers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalCustomers not found")
        return PortalCustomersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PortalCustomersListResponse:
        """List PortalCustomers records with pagination and filtering."""
        query = self.db.query(PortalCustomers)
        if company_id and hasattr(PortalCustomers, "company_id"):
            query = query.filter(PortalCustomers.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PortalCustomers, key) and value is not None:
                    query = query.filter(getattr(PortalCustomers, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PortalCustomers, order_by, PortalCustomers.created_at)))
        else:
            query = query.order_by(getattr(PortalCustomers, order_by, PortalCustomers.created_at))
        items = query.offset(skip).limit(limit).all()
        return PortalCustomersListResponse(
            items=[PortalCustomersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PortalCustomersUpdate,
               updated_by: Optional[UUID] = None) -> PortalCustomersResponse:
        """Update an existing PortalCustomers record."""
        db_obj = self.db.query(PortalCustomers).filter(PortalCustomers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalCustomers not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalCustomersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PortalCustomers record. Soft delete by default."""
        db_obj = self.db.query(PortalCustomers).filter(PortalCustomers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalCustomers not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PortalCustomersResponse]:
        """Search PortalCustomers by name/code fields."""
        query = self.db.query(PortalCustomers)
        if company_id and hasattr(PortalCustomers, "company_id"):
            query = query.filter(PortalCustomers.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PortalCustomers, f)]
        if search_fields:
            conditions = [getattr(PortalCustomers, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PortalCustomersResponse.model_validate(i) for i in items]

class PortalKnowledgeBaseService:
    """CRUD service for PortalKnowledgeBase."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PortalKnowledgeBaseCreate, created_by: Optional[UUID] = None) -> PortalKnowledgeBaseResponse:
        """Create a new PortalKnowledgeBase record."""
        db_obj = PortalKnowledgeBase(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalKnowledgeBaseResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PortalKnowledgeBaseResponse]:
        """Get PortalKnowledgeBase by primary key."""
        db_obj = self.db.query(PortalKnowledgeBase).filter(PortalKnowledgeBase.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalKnowledgeBase not found")
        return PortalKnowledgeBaseResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PortalKnowledgeBaseListResponse:
        """List PortalKnowledgeBase records with pagination and filtering."""
        query = self.db.query(PortalKnowledgeBase)
        if company_id and hasattr(PortalKnowledgeBase, "company_id"):
            query = query.filter(PortalKnowledgeBase.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PortalKnowledgeBase, key) and value is not None:
                    query = query.filter(getattr(PortalKnowledgeBase, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PortalKnowledgeBase, order_by, PortalKnowledgeBase.created_at)))
        else:
            query = query.order_by(getattr(PortalKnowledgeBase, order_by, PortalKnowledgeBase.created_at))
        items = query.offset(skip).limit(limit).all()
        return PortalKnowledgeBaseListResponse(
            items=[PortalKnowledgeBaseResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PortalKnowledgeBaseUpdate,
               updated_by: Optional[UUID] = None) -> PortalKnowledgeBaseResponse:
        """Update an existing PortalKnowledgeBase record."""
        db_obj = self.db.query(PortalKnowledgeBase).filter(PortalKnowledgeBase.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalKnowledgeBase not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalKnowledgeBaseResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PortalKnowledgeBase record. Soft delete by default."""
        db_obj = self.db.query(PortalKnowledgeBase).filter(PortalKnowledgeBase.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalKnowledgeBase not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PortalKnowledgeBaseResponse]:
        """Search PortalKnowledgeBase by name/code fields."""
        query = self.db.query(PortalKnowledgeBase)
        if company_id and hasattr(PortalKnowledgeBase, "company_id"):
            query = query.filter(PortalKnowledgeBase.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PortalKnowledgeBase, f)]
        if search_fields:
            conditions = [getattr(PortalKnowledgeBase, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PortalKnowledgeBaseResponse.model_validate(i) for i in items]

class PortalRfqsService:
    """CRUD service for PortalRfqs."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PortalRfqsCreate, created_by: Optional[UUID] = None) -> PortalRfqsResponse:
        """Create a new PortalRfqs record."""
        db_obj = PortalRfqs(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalRfqsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PortalRfqsResponse]:
        """Get PortalRfqs by primary key."""
        db_obj = self.db.query(PortalRfqs).filter(PortalRfqs.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalRfqs not found")
        return PortalRfqsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PortalRfqsListResponse:
        """List PortalRfqs records with pagination and filtering."""
        query = self.db.query(PortalRfqs)
        if company_id and hasattr(PortalRfqs, "company_id"):
            query = query.filter(PortalRfqs.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PortalRfqs, key) and value is not None:
                    query = query.filter(getattr(PortalRfqs, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PortalRfqs, order_by, PortalRfqs.created_at)))
        else:
            query = query.order_by(getattr(PortalRfqs, order_by, PortalRfqs.created_at))
        items = query.offset(skip).limit(limit).all()
        return PortalRfqsListResponse(
            items=[PortalRfqsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PortalRfqsUpdate,
               updated_by: Optional[UUID] = None) -> PortalRfqsResponse:
        """Update an existing PortalRfqs record."""
        db_obj = self.db.query(PortalRfqs).filter(PortalRfqs.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalRfqs not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalRfqsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PortalRfqs record. Soft delete by default."""
        db_obj = self.db.query(PortalRfqs).filter(PortalRfqs.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalRfqs not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PortalRfqsResponse]:
        """Search PortalRfqs by name/code fields."""
        query = self.db.query(PortalRfqs)
        if company_id and hasattr(PortalRfqs, "company_id"):
            query = query.filter(PortalRfqs.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PortalRfqs, f)]
        if search_fields:
            conditions = [getattr(PortalRfqs, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PortalRfqsResponse.model_validate(i) for i in items]

class PortalTicketsService:
    """CRUD service for PortalTickets."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PortalTicketsCreate, created_by: Optional[UUID] = None) -> PortalTicketsResponse:
        """Create a new PortalTickets record."""
        db_obj = PortalTickets(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalTicketsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PortalTicketsResponse]:
        """Get PortalTickets by primary key."""
        db_obj = self.db.query(PortalTickets).filter(PortalTickets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalTickets not found")
        return PortalTicketsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PortalTicketsListResponse:
        """List PortalTickets records with pagination and filtering."""
        query = self.db.query(PortalTickets)
        if company_id and hasattr(PortalTickets, "company_id"):
            query = query.filter(PortalTickets.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PortalTickets, key) and value is not None:
                    query = query.filter(getattr(PortalTickets, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PortalTickets, order_by, PortalTickets.created_at)))
        else:
            query = query.order_by(getattr(PortalTickets, order_by, PortalTickets.created_at))
        items = query.offset(skip).limit(limit).all()
        return PortalTicketsListResponse(
            items=[PortalTicketsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PortalTicketsUpdate,
               updated_by: Optional[UUID] = None) -> PortalTicketsResponse:
        """Update an existing PortalTickets record."""
        db_obj = self.db.query(PortalTickets).filter(PortalTickets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalTickets not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalTicketsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PortalTickets record. Soft delete by default."""
        db_obj = self.db.query(PortalTickets).filter(PortalTickets.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalTickets not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PortalTicketsResponse]:
        """Search PortalTickets by name/code fields."""
        query = self.db.query(PortalTickets)
        if company_id and hasattr(PortalTickets, "company_id"):
            query = query.filter(PortalTickets.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PortalTickets, f)]
        if search_fields:
            conditions = [getattr(PortalTickets, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PortalTicketsResponse.model_validate(i) for i in items]

class PortalVendorEvaluationsService:
    """CRUD service for PortalVendorEvaluations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PortalVendorEvaluationsCreate, created_by: Optional[UUID] = None) -> PortalVendorEvaluationsResponse:
        """Create a new PortalVendorEvaluations record."""
        db_obj = PortalVendorEvaluations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalVendorEvaluationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PortalVendorEvaluationsResponse]:
        """Get PortalVendorEvaluations by primary key."""
        db_obj = self.db.query(PortalVendorEvaluations).filter(PortalVendorEvaluations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalVendorEvaluations not found")
        return PortalVendorEvaluationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PortalVendorEvaluationsListResponse:
        """List PortalVendorEvaluations records with pagination and filtering."""
        query = self.db.query(PortalVendorEvaluations)
        if company_id and hasattr(PortalVendorEvaluations, "company_id"):
            query = query.filter(PortalVendorEvaluations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PortalVendorEvaluations, key) and value is not None:
                    query = query.filter(getattr(PortalVendorEvaluations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PortalVendorEvaluations, order_by, PortalVendorEvaluations.created_at)))
        else:
            query = query.order_by(getattr(PortalVendorEvaluations, order_by, PortalVendorEvaluations.created_at))
        items = query.offset(skip).limit(limit).all()
        return PortalVendorEvaluationsListResponse(
            items=[PortalVendorEvaluationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PortalVendorEvaluationsUpdate,
               updated_by: Optional[UUID] = None) -> PortalVendorEvaluationsResponse:
        """Update an existing PortalVendorEvaluations record."""
        db_obj = self.db.query(PortalVendorEvaluations).filter(PortalVendorEvaluations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalVendorEvaluations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalVendorEvaluationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PortalVendorEvaluations record. Soft delete by default."""
        db_obj = self.db.query(PortalVendorEvaluations).filter(PortalVendorEvaluations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalVendorEvaluations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PortalVendorEvaluationsResponse]:
        """Search PortalVendorEvaluations by name/code fields."""
        query = self.db.query(PortalVendorEvaluations)
        if company_id and hasattr(PortalVendorEvaluations, "company_id"):
            query = query.filter(PortalVendorEvaluations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PortalVendorEvaluations, f)]
        if search_fields:
            conditions = [getattr(PortalVendorEvaluations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PortalVendorEvaluationsResponse.model_validate(i) for i in items]

class PortalVendorsService:
    """CRUD service for PortalVendors."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: PortalVendorsCreate, created_by: Optional[UUID] = None) -> PortalVendorsResponse:
        """Create a new PortalVendors record."""
        db_obj = PortalVendors(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalVendorsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[PortalVendorsResponse]:
        """Get PortalVendors by primary key."""
        db_obj = self.db.query(PortalVendors).filter(PortalVendors.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalVendors not found")
        return PortalVendorsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> PortalVendorsListResponse:
        """List PortalVendors records with pagination and filtering."""
        query = self.db.query(PortalVendors)
        if company_id and hasattr(PortalVendors, "company_id"):
            query = query.filter(PortalVendors.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(PortalVendors, key) and value is not None:
                    query = query.filter(getattr(PortalVendors, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(PortalVendors, order_by, PortalVendors.created_at)))
        else:
            query = query.order_by(getattr(PortalVendors, order_by, PortalVendors.created_at))
        items = query.offset(skip).limit(limit).all()
        return PortalVendorsListResponse(
            items=[PortalVendorsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: PortalVendorsUpdate,
               updated_by: Optional[UUID] = None) -> PortalVendorsResponse:
        """Update an existing PortalVendors record."""
        db_obj = self.db.query(PortalVendors).filter(PortalVendors.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalVendors not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return PortalVendorsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a PortalVendors record. Soft delete by default."""
        db_obj = self.db.query(PortalVendors).filter(PortalVendors.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="PortalVendors not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[PortalVendorsResponse]:
        """Search PortalVendors by name/code fields."""
        query = self.db.query(PortalVendors)
        if company_id and hasattr(PortalVendors, "company_id"):
            query = query.filter(PortalVendors.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(PortalVendors, f)]
        if search_fields:
            conditions = [getattr(PortalVendors, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [PortalVendorsResponse.model_validate(i) for i in items]

class RecCandidatesService:
    """CRUD service for RecCandidates."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RecCandidatesCreate, created_by: Optional[UUID] = None) -> RecCandidatesResponse:
        """Create a new RecCandidates record."""
        db_obj = RecCandidates(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RecCandidatesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RecCandidatesResponse]:
        """Get RecCandidates by primary key."""
        db_obj = self.db.query(RecCandidates).filter(RecCandidates.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecCandidates not found")
        return RecCandidatesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RecCandidatesListResponse:
        """List RecCandidates records with pagination and filtering."""
        query = self.db.query(RecCandidates)
        if company_id and hasattr(RecCandidates, "company_id"):
            query = query.filter(RecCandidates.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RecCandidates, key) and value is not None:
                    query = query.filter(getattr(RecCandidates, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RecCandidates, order_by, RecCandidates.created_at)))
        else:
            query = query.order_by(getattr(RecCandidates, order_by, RecCandidates.created_at))
        items = query.offset(skip).limit(limit).all()
        return RecCandidatesListResponse(
            items=[RecCandidatesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RecCandidatesUpdate,
               updated_by: Optional[UUID] = None) -> RecCandidatesResponse:
        """Update an existing RecCandidates record."""
        db_obj = self.db.query(RecCandidates).filter(RecCandidates.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecCandidates not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RecCandidatesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RecCandidates record. Soft delete by default."""
        db_obj = self.db.query(RecCandidates).filter(RecCandidates.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecCandidates not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RecCandidatesResponse]:
        """Search RecCandidates by name/code fields."""
        query = self.db.query(RecCandidates)
        if company_id and hasattr(RecCandidates, "company_id"):
            query = query.filter(RecCandidates.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RecCandidates, f)]
        if search_fields:
            conditions = [getattr(RecCandidates, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RecCandidatesResponse.model_validate(i) for i in items]

class RecInterviewsService:
    """CRUD service for RecInterviews."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RecInterviewsCreate, created_by: Optional[UUID] = None) -> RecInterviewsResponse:
        """Create a new RecInterviews record."""
        db_obj = RecInterviews(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RecInterviewsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RecInterviewsResponse]:
        """Get RecInterviews by primary key."""
        db_obj = self.db.query(RecInterviews).filter(RecInterviews.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecInterviews not found")
        return RecInterviewsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RecInterviewsListResponse:
        """List RecInterviews records with pagination and filtering."""
        query = self.db.query(RecInterviews)
        if company_id and hasattr(RecInterviews, "company_id"):
            query = query.filter(RecInterviews.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RecInterviews, key) and value is not None:
                    query = query.filter(getattr(RecInterviews, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RecInterviews, order_by, RecInterviews.created_at)))
        else:
            query = query.order_by(getattr(RecInterviews, order_by, RecInterviews.created_at))
        items = query.offset(skip).limit(limit).all()
        return RecInterviewsListResponse(
            items=[RecInterviewsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RecInterviewsUpdate,
               updated_by: Optional[UUID] = None) -> RecInterviewsResponse:
        """Update an existing RecInterviews record."""
        db_obj = self.db.query(RecInterviews).filter(RecInterviews.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecInterviews not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RecInterviewsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RecInterviews record. Soft delete by default."""
        db_obj = self.db.query(RecInterviews).filter(RecInterviews.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecInterviews not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RecInterviewsResponse]:
        """Search RecInterviews by name/code fields."""
        query = self.db.query(RecInterviews)
        if company_id and hasattr(RecInterviews, "company_id"):
            query = query.filter(RecInterviews.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RecInterviews, f)]
        if search_fields:
            conditions = [getattr(RecInterviews, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RecInterviewsResponse.model_validate(i) for i in items]

class RecJobRequisitionsService:
    """CRUD service for RecJobRequisitions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RecJobRequisitionsCreate, created_by: Optional[UUID] = None) -> RecJobRequisitionsResponse:
        """Create a new RecJobRequisitions record."""
        db_obj = RecJobRequisitions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RecJobRequisitionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RecJobRequisitionsResponse]:
        """Get RecJobRequisitions by primary key."""
        db_obj = self.db.query(RecJobRequisitions).filter(RecJobRequisitions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecJobRequisitions not found")
        return RecJobRequisitionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RecJobRequisitionsListResponse:
        """List RecJobRequisitions records with pagination and filtering."""
        query = self.db.query(RecJobRequisitions)
        if company_id and hasattr(RecJobRequisitions, "company_id"):
            query = query.filter(RecJobRequisitions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RecJobRequisitions, key) and value is not None:
                    query = query.filter(getattr(RecJobRequisitions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RecJobRequisitions, order_by, RecJobRequisitions.created_at)))
        else:
            query = query.order_by(getattr(RecJobRequisitions, order_by, RecJobRequisitions.created_at))
        items = query.offset(skip).limit(limit).all()
        return RecJobRequisitionsListResponse(
            items=[RecJobRequisitionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RecJobRequisitionsUpdate,
               updated_by: Optional[UUID] = None) -> RecJobRequisitionsResponse:
        """Update an existing RecJobRequisitions record."""
        db_obj = self.db.query(RecJobRequisitions).filter(RecJobRequisitions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecJobRequisitions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RecJobRequisitionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RecJobRequisitions record. Soft delete by default."""
        db_obj = self.db.query(RecJobRequisitions).filter(RecJobRequisitions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecJobRequisitions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RecJobRequisitionsResponse]:
        """Search RecJobRequisitions by name/code fields."""
        query = self.db.query(RecJobRequisitions)
        if company_id and hasattr(RecJobRequisitions, "company_id"):
            query = query.filter(RecJobRequisitions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RecJobRequisitions, f)]
        if search_fields:
            conditions = [getattr(RecJobRequisitions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RecJobRequisitionsResponse.model_validate(i) for i in items]

class RecOffersService:
    """CRUD service for RecOffers."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RecOffersCreate, created_by: Optional[UUID] = None) -> RecOffersResponse:
        """Create a new RecOffers record."""
        db_obj = RecOffers(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RecOffersResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RecOffersResponse]:
        """Get RecOffers by primary key."""
        db_obj = self.db.query(RecOffers).filter(RecOffers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecOffers not found")
        return RecOffersResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RecOffersListResponse:
        """List RecOffers records with pagination and filtering."""
        query = self.db.query(RecOffers)
        if company_id and hasattr(RecOffers, "company_id"):
            query = query.filter(RecOffers.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RecOffers, key) and value is not None:
                    query = query.filter(getattr(RecOffers, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RecOffers, order_by, RecOffers.created_at)))
        else:
            query = query.order_by(getattr(RecOffers, order_by, RecOffers.created_at))
        items = query.offset(skip).limit(limit).all()
        return RecOffersListResponse(
            items=[RecOffersResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RecOffersUpdate,
               updated_by: Optional[UUID] = None) -> RecOffersResponse:
        """Update an existing RecOffers record."""
        db_obj = self.db.query(RecOffers).filter(RecOffers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecOffers not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RecOffersResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RecOffers record. Soft delete by default."""
        db_obj = self.db.query(RecOffers).filter(RecOffers.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RecOffers not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RecOffersResponse]:
        """Search RecOffers by name/code fields."""
        query = self.db.query(RecOffers)
        if company_id and hasattr(RecOffers, "company_id"):
            query = query.filter(RecOffers.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RecOffers, f)]
        if search_fields:
            conditions = [getattr(RecOffers, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RecOffersResponse.model_validate(i) for i in items]

class RevrecAdjustmentsService:
    """CRUD service for RevrecAdjustments."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RevrecAdjustmentsCreate, created_by: Optional[UUID] = None) -> RevrecAdjustmentsResponse:
        """Create a new RevrecAdjustments record."""
        db_obj = RevrecAdjustments(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RevrecAdjustmentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RevrecAdjustmentsResponse]:
        """Get RevrecAdjustments by primary key."""
        db_obj = self.db.query(RevrecAdjustments).filter(RevrecAdjustments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecAdjustments not found")
        return RevrecAdjustmentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RevrecAdjustmentsListResponse:
        """List RevrecAdjustments records with pagination and filtering."""
        query = self.db.query(RevrecAdjustments)
        if company_id and hasattr(RevrecAdjustments, "company_id"):
            query = query.filter(RevrecAdjustments.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RevrecAdjustments, key) and value is not None:
                    query = query.filter(getattr(RevrecAdjustments, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RevrecAdjustments, order_by, RevrecAdjustments.created_at)))
        else:
            query = query.order_by(getattr(RevrecAdjustments, order_by, RevrecAdjustments.created_at))
        items = query.offset(skip).limit(limit).all()
        return RevrecAdjustmentsListResponse(
            items=[RevrecAdjustmentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RevrecAdjustmentsUpdate,
               updated_by: Optional[UUID] = None) -> RevrecAdjustmentsResponse:
        """Update an existing RevrecAdjustments record."""
        db_obj = self.db.query(RevrecAdjustments).filter(RevrecAdjustments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecAdjustments not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RevrecAdjustmentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RevrecAdjustments record. Soft delete by default."""
        db_obj = self.db.query(RevrecAdjustments).filter(RevrecAdjustments.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecAdjustments not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RevrecAdjustmentsResponse]:
        """Search RevrecAdjustments by name/code fields."""
        query = self.db.query(RevrecAdjustments)
        if company_id and hasattr(RevrecAdjustments, "company_id"):
            query = query.filter(RevrecAdjustments.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RevrecAdjustments, f)]
        if search_fields:
            conditions = [getattr(RevrecAdjustments, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RevrecAdjustmentsResponse.model_validate(i) for i in items]

class RevrecContractsService:
    """CRUD service for RevrecContracts."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RevrecContractsCreate, created_by: Optional[UUID] = None) -> RevrecContractsResponse:
        """Create a new RevrecContracts record."""
        db_obj = RevrecContracts(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RevrecContractsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RevrecContractsResponse]:
        """Get RevrecContracts by primary key."""
        db_obj = self.db.query(RevrecContracts).filter(RevrecContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecContracts not found")
        return RevrecContractsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RevrecContractsListResponse:
        """List RevrecContracts records with pagination and filtering."""
        query = self.db.query(RevrecContracts)
        if company_id and hasattr(RevrecContracts, "company_id"):
            query = query.filter(RevrecContracts.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RevrecContracts, key) and value is not None:
                    query = query.filter(getattr(RevrecContracts, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RevrecContracts, order_by, RevrecContracts.created_at)))
        else:
            query = query.order_by(getattr(RevrecContracts, order_by, RevrecContracts.created_at))
        items = query.offset(skip).limit(limit).all()
        return RevrecContractsListResponse(
            items=[RevrecContractsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RevrecContractsUpdate,
               updated_by: Optional[UUID] = None) -> RevrecContractsResponse:
        """Update an existing RevrecContracts record."""
        db_obj = self.db.query(RevrecContracts).filter(RevrecContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecContracts not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RevrecContractsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RevrecContracts record. Soft delete by default."""
        db_obj = self.db.query(RevrecContracts).filter(RevrecContracts.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecContracts not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RevrecContractsResponse]:
        """Search RevrecContracts by name/code fields."""
        query = self.db.query(RevrecContracts)
        if company_id and hasattr(RevrecContracts, "company_id"):
            query = query.filter(RevrecContracts.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RevrecContracts, f)]
        if search_fields:
            conditions = [getattr(RevrecContracts, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RevrecContractsResponse.model_validate(i) for i in items]

class RevrecPerformanceObligationsService:
    """CRUD service for RevrecPerformanceObligations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: RevrecPerformanceObligationsCreate, created_by: Optional[UUID] = None) -> RevrecPerformanceObligationsResponse:
        """Create a new RevrecPerformanceObligations record."""
        db_obj = RevrecPerformanceObligations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return RevrecPerformanceObligationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[RevrecPerformanceObligationsResponse]:
        """Get RevrecPerformanceObligations by primary key."""
        db_obj = self.db.query(RevrecPerformanceObligations).filter(RevrecPerformanceObligations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecPerformanceObligations not found")
        return RevrecPerformanceObligationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> RevrecPerformanceObligationsListResponse:
        """List RevrecPerformanceObligations records with pagination and filtering."""
        query = self.db.query(RevrecPerformanceObligations)
        if company_id and hasattr(RevrecPerformanceObligations, "company_id"):
            query = query.filter(RevrecPerformanceObligations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(RevrecPerformanceObligations, key) and value is not None:
                    query = query.filter(getattr(RevrecPerformanceObligations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(RevrecPerformanceObligations, order_by, RevrecPerformanceObligations.created_at)))
        else:
            query = query.order_by(getattr(RevrecPerformanceObligations, order_by, RevrecPerformanceObligations.created_at))
        items = query.offset(skip).limit(limit).all()
        return RevrecPerformanceObligationsListResponse(
            items=[RevrecPerformanceObligationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: RevrecPerformanceObligationsUpdate,
               updated_by: Optional[UUID] = None) -> RevrecPerformanceObligationsResponse:
        """Update an existing RevrecPerformanceObligations record."""
        db_obj = self.db.query(RevrecPerformanceObligations).filter(RevrecPerformanceObligations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecPerformanceObligations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return RevrecPerformanceObligationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a RevrecPerformanceObligations record. Soft delete by default."""
        db_obj = self.db.query(RevrecPerformanceObligations).filter(RevrecPerformanceObligations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="RevrecPerformanceObligations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[RevrecPerformanceObligationsResponse]:
        """Search RevrecPerformanceObligations by name/code fields."""
        query = self.db.query(RevrecPerformanceObligations)
        if company_id and hasattr(RevrecPerformanceObligations, "company_id"):
            query = query.filter(RevrecPerformanceObligations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(RevrecPerformanceObligations, f)]
        if search_fields:
            conditions = [getattr(RevrecPerformanceObligations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [RevrecPerformanceObligationsResponse.model_validate(i) for i in items]

class SecAccessLogsService:
    """CRUD service for SecAccessLogs."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SecAccessLogsCreate, created_by: Optional[UUID] = None) -> SecAccessLogsResponse:
        """Create a new SecAccessLogs record."""
        db_obj = SecAccessLogs(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SecAccessLogsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SecAccessLogsResponse]:
        """Get SecAccessLogs by primary key."""
        db_obj = self.db.query(SecAccessLogs).filter(SecAccessLogs.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecAccessLogs not found")
        return SecAccessLogsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SecAccessLogsListResponse:
        """List SecAccessLogs records with pagination and filtering."""
        query = self.db.query(SecAccessLogs)
        if company_id and hasattr(SecAccessLogs, "company_id"):
            query = query.filter(SecAccessLogs.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SecAccessLogs, key) and value is not None:
                    query = query.filter(getattr(SecAccessLogs, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SecAccessLogs, order_by, SecAccessLogs.created_at)))
        else:
            query = query.order_by(getattr(SecAccessLogs, order_by, SecAccessLogs.created_at))
        items = query.offset(skip).limit(limit).all()
        return SecAccessLogsListResponse(
            items=[SecAccessLogsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SecAccessLogsUpdate,
               updated_by: Optional[UUID] = None) -> SecAccessLogsResponse:
        """Update an existing SecAccessLogs record."""
        db_obj = self.db.query(SecAccessLogs).filter(SecAccessLogs.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecAccessLogs not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SecAccessLogsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SecAccessLogs record. Soft delete by default."""
        db_obj = self.db.query(SecAccessLogs).filter(SecAccessLogs.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecAccessLogs not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SecAccessLogsResponse]:
        """Search SecAccessLogs by name/code fields."""
        query = self.db.query(SecAccessLogs)
        if company_id and hasattr(SecAccessLogs, "company_id"):
            query = query.filter(SecAccessLogs.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SecAccessLogs, f)]
        if search_fields:
            conditions = [getattr(SecAccessLogs, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SecAccessLogsResponse.model_validate(i) for i in items]

class SecCertificatesService:
    """CRUD service for SecCertificates."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SecCertificatesCreate, created_by: Optional[UUID] = None) -> SecCertificatesResponse:
        """Create a new SecCertificates record."""
        db_obj = SecCertificates(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SecCertificatesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SecCertificatesResponse]:
        """Get SecCertificates by primary key."""
        db_obj = self.db.query(SecCertificates).filter(SecCertificates.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecCertificates not found")
        return SecCertificatesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SecCertificatesListResponse:
        """List SecCertificates records with pagination and filtering."""
        query = self.db.query(SecCertificates)
        if company_id and hasattr(SecCertificates, "company_id"):
            query = query.filter(SecCertificates.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SecCertificates, key) and value is not None:
                    query = query.filter(getattr(SecCertificates, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SecCertificates, order_by, SecCertificates.created_at)))
        else:
            query = query.order_by(getattr(SecCertificates, order_by, SecCertificates.created_at))
        items = query.offset(skip).limit(limit).all()
        return SecCertificatesListResponse(
            items=[SecCertificatesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SecCertificatesUpdate,
               updated_by: Optional[UUID] = None) -> SecCertificatesResponse:
        """Update an existing SecCertificates record."""
        db_obj = self.db.query(SecCertificates).filter(SecCertificates.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecCertificates not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SecCertificatesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SecCertificates record. Soft delete by default."""
        db_obj = self.db.query(SecCertificates).filter(SecCertificates.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecCertificates not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SecCertificatesResponse]:
        """Search SecCertificates by name/code fields."""
        query = self.db.query(SecCertificates)
        if company_id and hasattr(SecCertificates, "company_id"):
            query = query.filter(SecCertificates.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SecCertificates, f)]
        if search_fields:
            conditions = [getattr(SecCertificates, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SecCertificatesResponse.model_validate(i) for i in items]

class SecIncidentsService:
    """CRUD service for SecIncidents."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SecIncidentsCreate, created_by: Optional[UUID] = None) -> SecIncidentsResponse:
        """Create a new SecIncidents record."""
        db_obj = SecIncidents(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SecIncidentsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SecIncidentsResponse]:
        """Get SecIncidents by primary key."""
        db_obj = self.db.query(SecIncidents).filter(SecIncidents.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecIncidents not found")
        return SecIncidentsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SecIncidentsListResponse:
        """List SecIncidents records with pagination and filtering."""
        query = self.db.query(SecIncidents)
        if company_id and hasattr(SecIncidents, "company_id"):
            query = query.filter(SecIncidents.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SecIncidents, key) and value is not None:
                    query = query.filter(getattr(SecIncidents, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SecIncidents, order_by, SecIncidents.created_at)))
        else:
            query = query.order_by(getattr(SecIncidents, order_by, SecIncidents.created_at))
        items = query.offset(skip).limit(limit).all()
        return SecIncidentsListResponse(
            items=[SecIncidentsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SecIncidentsUpdate,
               updated_by: Optional[UUID] = None) -> SecIncidentsResponse:
        """Update an existing SecIncidents record."""
        db_obj = self.db.query(SecIncidents).filter(SecIncidents.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecIncidents not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SecIncidentsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SecIncidents record. Soft delete by default."""
        db_obj = self.db.query(SecIncidents).filter(SecIncidents.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecIncidents not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SecIncidentsResponse]:
        """Search SecIncidents by name/code fields."""
        query = self.db.query(SecIncidents)
        if company_id and hasattr(SecIncidents, "company_id"):
            query = query.filter(SecIncidents.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SecIncidents, f)]
        if search_fields:
            conditions = [getattr(SecIncidents, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SecIncidentsResponse.model_validate(i) for i in items]

class SecPoliciesService:
    """CRUD service for SecPolicies."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SecPoliciesCreate, created_by: Optional[UUID] = None) -> SecPoliciesResponse:
        """Create a new SecPolicies record."""
        db_obj = SecPolicies(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SecPoliciesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SecPoliciesResponse]:
        """Get SecPolicies by primary key."""
        db_obj = self.db.query(SecPolicies).filter(SecPolicies.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecPolicies not found")
        return SecPoliciesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SecPoliciesListResponse:
        """List SecPolicies records with pagination and filtering."""
        query = self.db.query(SecPolicies)
        if company_id and hasattr(SecPolicies, "company_id"):
            query = query.filter(SecPolicies.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SecPolicies, key) and value is not None:
                    query = query.filter(getattr(SecPolicies, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SecPolicies, order_by, SecPolicies.created_at)))
        else:
            query = query.order_by(getattr(SecPolicies, order_by, SecPolicies.created_at))
        items = query.offset(skip).limit(limit).all()
        return SecPoliciesListResponse(
            items=[SecPoliciesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SecPoliciesUpdate,
               updated_by: Optional[UUID] = None) -> SecPoliciesResponse:
        """Update an existing SecPolicies record."""
        db_obj = self.db.query(SecPolicies).filter(SecPolicies.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecPolicies not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SecPoliciesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SecPolicies record. Soft delete by default."""
        db_obj = self.db.query(SecPolicies).filter(SecPolicies.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecPolicies not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SecPoliciesResponse]:
        """Search SecPolicies by name/code fields."""
        query = self.db.query(SecPolicies)
        if company_id and hasattr(SecPolicies, "company_id"):
            query = query.filter(SecPolicies.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SecPolicies, f)]
        if search_fields:
            conditions = [getattr(SecPolicies, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SecPoliciesResponse.model_validate(i) for i in items]

class SecRolesExtService:
    """CRUD service for SecRolesExt."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SecRolesExtCreate, created_by: Optional[UUID] = None) -> SecRolesExtResponse:
        """Create a new SecRolesExt record."""
        db_obj = SecRolesExt(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SecRolesExtResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SecRolesExtResponse]:
        """Get SecRolesExt by primary key."""
        db_obj = self.db.query(SecRolesExt).filter(SecRolesExt.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecRolesExt not found")
        return SecRolesExtResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SecRolesExtListResponse:
        """List SecRolesExt records with pagination and filtering."""
        query = self.db.query(SecRolesExt)
        if company_id and hasattr(SecRolesExt, "company_id"):
            query = query.filter(SecRolesExt.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SecRolesExt, key) and value is not None:
                    query = query.filter(getattr(SecRolesExt, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SecRolesExt, order_by, SecRolesExt.created_at)))
        else:
            query = query.order_by(getattr(SecRolesExt, order_by, SecRolesExt.created_at))
        items = query.offset(skip).limit(limit).all()
        return SecRolesExtListResponse(
            items=[SecRolesExtResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SecRolesExtUpdate,
               updated_by: Optional[UUID] = None) -> SecRolesExtResponse:
        """Update an existing SecRolesExt record."""
        db_obj = self.db.query(SecRolesExt).filter(SecRolesExt.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecRolesExt not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SecRolesExtResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SecRolesExt record. Soft delete by default."""
        db_obj = self.db.query(SecRolesExt).filter(SecRolesExt.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecRolesExt not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SecRolesExtResponse]:
        """Search SecRolesExt by name/code fields."""
        query = self.db.query(SecRolesExt)
        if company_id and hasattr(SecRolesExt, "company_id"):
            query = query.filter(SecRolesExt.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SecRolesExt, f)]
        if search_fields:
            conditions = [getattr(SecRolesExt, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SecRolesExtResponse.model_validate(i) for i in items]

class SecVulnerabilitiesService:
    """CRUD service for SecVulnerabilities."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SecVulnerabilitiesCreate, created_by: Optional[UUID] = None) -> SecVulnerabilitiesResponse:
        """Create a new SecVulnerabilities record."""
        db_obj = SecVulnerabilities(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SecVulnerabilitiesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SecVulnerabilitiesResponse]:
        """Get SecVulnerabilities by primary key."""
        db_obj = self.db.query(SecVulnerabilities).filter(SecVulnerabilities.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecVulnerabilities not found")
        return SecVulnerabilitiesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SecVulnerabilitiesListResponse:
        """List SecVulnerabilities records with pagination and filtering."""
        query = self.db.query(SecVulnerabilities)
        if company_id and hasattr(SecVulnerabilities, "company_id"):
            query = query.filter(SecVulnerabilities.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SecVulnerabilities, key) and value is not None:
                    query = query.filter(getattr(SecVulnerabilities, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SecVulnerabilities, order_by, SecVulnerabilities.created_at)))
        else:
            query = query.order_by(getattr(SecVulnerabilities, order_by, SecVulnerabilities.created_at))
        items = query.offset(skip).limit(limit).all()
        return SecVulnerabilitiesListResponse(
            items=[SecVulnerabilitiesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SecVulnerabilitiesUpdate,
               updated_by: Optional[UUID] = None) -> SecVulnerabilitiesResponse:
        """Update an existing SecVulnerabilities record."""
        db_obj = self.db.query(SecVulnerabilities).filter(SecVulnerabilities.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecVulnerabilities not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SecVulnerabilitiesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SecVulnerabilities record. Soft delete by default."""
        db_obj = self.db.query(SecVulnerabilities).filter(SecVulnerabilities.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SecVulnerabilities not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SecVulnerabilitiesResponse]:
        """Search SecVulnerabilities by name/code fields."""
        query = self.db.query(SecVulnerabilities)
        if company_id and hasattr(SecVulnerabilities, "company_id"):
            query = query.filter(SecVulnerabilities.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SecVulnerabilities, f)]
        if search_fields:
            conditions = [getattr(SecVulnerabilities, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SecVulnerabilitiesResponse.model_validate(i) for i in items]

class SubInvoicesScheduledService:
    """CRUD service for SubInvoicesScheduled."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SubInvoicesScheduledCreate, created_by: Optional[UUID] = None) -> SubInvoicesScheduledResponse:
        """Create a new SubInvoicesScheduled record."""
        db_obj = SubInvoicesScheduled(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SubInvoicesScheduledResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SubInvoicesScheduledResponse]:
        """Get SubInvoicesScheduled by primary key."""
        db_obj = self.db.query(SubInvoicesScheduled).filter(SubInvoicesScheduled.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubInvoicesScheduled not found")
        return SubInvoicesScheduledResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SubInvoicesScheduledListResponse:
        """List SubInvoicesScheduled records with pagination and filtering."""
        query = self.db.query(SubInvoicesScheduled)
        if company_id and hasattr(SubInvoicesScheduled, "company_id"):
            query = query.filter(SubInvoicesScheduled.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SubInvoicesScheduled, key) and value is not None:
                    query = query.filter(getattr(SubInvoicesScheduled, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SubInvoicesScheduled, order_by, SubInvoicesScheduled.created_at)))
        else:
            query = query.order_by(getattr(SubInvoicesScheduled, order_by, SubInvoicesScheduled.created_at))
        items = query.offset(skip).limit(limit).all()
        return SubInvoicesScheduledListResponse(
            items=[SubInvoicesScheduledResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SubInvoicesScheduledUpdate,
               updated_by: Optional[UUID] = None) -> SubInvoicesScheduledResponse:
        """Update an existing SubInvoicesScheduled record."""
        db_obj = self.db.query(SubInvoicesScheduled).filter(SubInvoicesScheduled.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubInvoicesScheduled not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SubInvoicesScheduledResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SubInvoicesScheduled record. Soft delete by default."""
        db_obj = self.db.query(SubInvoicesScheduled).filter(SubInvoicesScheduled.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubInvoicesScheduled not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SubInvoicesScheduledResponse]:
        """Search SubInvoicesScheduled by name/code fields."""
        query = self.db.query(SubInvoicesScheduled)
        if company_id and hasattr(SubInvoicesScheduled, "company_id"):
            query = query.filter(SubInvoicesScheduled.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SubInvoicesScheduled, f)]
        if search_fields:
            conditions = [getattr(SubInvoicesScheduled, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SubInvoicesScheduledResponse.model_validate(i) for i in items]

class SubPlansService:
    """CRUD service for SubPlans."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SubPlansCreate, created_by: Optional[UUID] = None) -> SubPlansResponse:
        """Create a new SubPlans record."""
        db_obj = SubPlans(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SubPlansResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SubPlansResponse]:
        """Get SubPlans by primary key."""
        db_obj = self.db.query(SubPlans).filter(SubPlans.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubPlans not found")
        return SubPlansResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SubPlansListResponse:
        """List SubPlans records with pagination and filtering."""
        query = self.db.query(SubPlans)
        if company_id and hasattr(SubPlans, "company_id"):
            query = query.filter(SubPlans.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SubPlans, key) and value is not None:
                    query = query.filter(getattr(SubPlans, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SubPlans, order_by, SubPlans.created_at)))
        else:
            query = query.order_by(getattr(SubPlans, order_by, SubPlans.created_at))
        items = query.offset(skip).limit(limit).all()
        return SubPlansListResponse(
            items=[SubPlansResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SubPlansUpdate,
               updated_by: Optional[UUID] = None) -> SubPlansResponse:
        """Update an existing SubPlans record."""
        db_obj = self.db.query(SubPlans).filter(SubPlans.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubPlans not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SubPlansResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SubPlans record. Soft delete by default."""
        db_obj = self.db.query(SubPlans).filter(SubPlans.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubPlans not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SubPlansResponse]:
        """Search SubPlans by name/code fields."""
        query = self.db.query(SubPlans)
        if company_id and hasattr(SubPlans, "company_id"):
            query = query.filter(SubPlans.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SubPlans, f)]
        if search_fields:
            conditions = [getattr(SubPlans, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SubPlansResponse.model_validate(i) for i in items]

class SubRenewalLogService:
    """CRUD service for SubRenewalLog."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SubRenewalLogCreate, created_by: Optional[UUID] = None) -> SubRenewalLogResponse:
        """Create a new SubRenewalLog record."""
        db_obj = SubRenewalLog(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SubRenewalLogResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SubRenewalLogResponse]:
        """Get SubRenewalLog by primary key."""
        db_obj = self.db.query(SubRenewalLog).filter(SubRenewalLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubRenewalLog not found")
        return SubRenewalLogResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SubRenewalLogListResponse:
        """List SubRenewalLog records with pagination and filtering."""
        query = self.db.query(SubRenewalLog)
        if company_id and hasattr(SubRenewalLog, "company_id"):
            query = query.filter(SubRenewalLog.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SubRenewalLog, key) and value is not None:
                    query = query.filter(getattr(SubRenewalLog, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SubRenewalLog, order_by, SubRenewalLog.created_at)))
        else:
            query = query.order_by(getattr(SubRenewalLog, order_by, SubRenewalLog.created_at))
        items = query.offset(skip).limit(limit).all()
        return SubRenewalLogListResponse(
            items=[SubRenewalLogResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SubRenewalLogUpdate,
               updated_by: Optional[UUID] = None) -> SubRenewalLogResponse:
        """Update an existing SubRenewalLog record."""
        db_obj = self.db.query(SubRenewalLog).filter(SubRenewalLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubRenewalLog not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SubRenewalLogResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SubRenewalLog record. Soft delete by default."""
        db_obj = self.db.query(SubRenewalLog).filter(SubRenewalLog.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubRenewalLog not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SubRenewalLogResponse]:
        """Search SubRenewalLog by name/code fields."""
        query = self.db.query(SubRenewalLog)
        if company_id and hasattr(SubRenewalLog, "company_id"):
            query = query.filter(SubRenewalLog.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SubRenewalLog, f)]
        if search_fields:
            conditions = [getattr(SubRenewalLog, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SubRenewalLogResponse.model_validate(i) for i in items]

class SubSubscriptionsService:
    """CRUD service for SubSubscriptions."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SubSubscriptionsCreate, created_by: Optional[UUID] = None) -> SubSubscriptionsResponse:
        """Create a new SubSubscriptions record."""
        db_obj = SubSubscriptions(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SubSubscriptionsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SubSubscriptionsResponse]:
        """Get SubSubscriptions by primary key."""
        db_obj = self.db.query(SubSubscriptions).filter(SubSubscriptions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubSubscriptions not found")
        return SubSubscriptionsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SubSubscriptionsListResponse:
        """List SubSubscriptions records with pagination and filtering."""
        query = self.db.query(SubSubscriptions)
        if company_id and hasattr(SubSubscriptions, "company_id"):
            query = query.filter(SubSubscriptions.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SubSubscriptions, key) and value is not None:
                    query = query.filter(getattr(SubSubscriptions, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SubSubscriptions, order_by, SubSubscriptions.created_at)))
        else:
            query = query.order_by(getattr(SubSubscriptions, order_by, SubSubscriptions.created_at))
        items = query.offset(skip).limit(limit).all()
        return SubSubscriptionsListResponse(
            items=[SubSubscriptionsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SubSubscriptionsUpdate,
               updated_by: Optional[UUID] = None) -> SubSubscriptionsResponse:
        """Update an existing SubSubscriptions record."""
        db_obj = self.db.query(SubSubscriptions).filter(SubSubscriptions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubSubscriptions not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SubSubscriptionsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SubSubscriptions record. Soft delete by default."""
        db_obj = self.db.query(SubSubscriptions).filter(SubSubscriptions.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubSubscriptions not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SubSubscriptionsResponse]:
        """Search SubSubscriptions by name/code fields."""
        query = self.db.query(SubSubscriptions)
        if company_id and hasattr(SubSubscriptions, "company_id"):
            query = query.filter(SubSubscriptions.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SubSubscriptions, f)]
        if search_fields:
            conditions = [getattr(SubSubscriptions, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SubSubscriptionsResponse.model_validate(i) for i in items]

class SubUsageRecordsService:
    """CRUD service for SubUsageRecords."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: SubUsageRecordsCreate, created_by: Optional[UUID] = None) -> SubUsageRecordsResponse:
        """Create a new SubUsageRecords record."""
        db_obj = SubUsageRecords(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return SubUsageRecordsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[SubUsageRecordsResponse]:
        """Get SubUsageRecords by primary key."""
        db_obj = self.db.query(SubUsageRecords).filter(SubUsageRecords.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubUsageRecords not found")
        return SubUsageRecordsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> SubUsageRecordsListResponse:
        """List SubUsageRecords records with pagination and filtering."""
        query = self.db.query(SubUsageRecords)
        if company_id and hasattr(SubUsageRecords, "company_id"):
            query = query.filter(SubUsageRecords.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(SubUsageRecords, key) and value is not None:
                    query = query.filter(getattr(SubUsageRecords, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(SubUsageRecords, order_by, SubUsageRecords.created_at)))
        else:
            query = query.order_by(getattr(SubUsageRecords, order_by, SubUsageRecords.created_at))
        items = query.offset(skip).limit(limit).all()
        return SubUsageRecordsListResponse(
            items=[SubUsageRecordsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: SubUsageRecordsUpdate,
               updated_by: Optional[UUID] = None) -> SubUsageRecordsResponse:
        """Update an existing SubUsageRecords record."""
        db_obj = self.db.query(SubUsageRecords).filter(SubUsageRecords.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubUsageRecords not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return SubUsageRecordsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a SubUsageRecords record. Soft delete by default."""
        db_obj = self.db.query(SubUsageRecords).filter(SubUsageRecords.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="SubUsageRecords not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[SubUsageRecordsResponse]:
        """Search SubUsageRecords by name/code fields."""
        query = self.db.query(SubUsageRecords)
        if company_id and hasattr(SubUsageRecords, "company_id"):
            query = query.filter(SubUsageRecords.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(SubUsageRecords, f)]
        if search_fields:
            conditions = [getattr(SubUsageRecords, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [SubUsageRecordsResponse.model_validate(i) for i in items]

class WmsBinsService:
    """CRUD service for WmsBins."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsBinsCreate, created_by: Optional[UUID] = None) -> WmsBinsResponse:
        """Create a new WmsBins record."""
        db_obj = WmsBins(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsBinsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsBinsResponse]:
        """Get WmsBins by primary key."""
        db_obj = self.db.query(WmsBins).filter(WmsBins.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsBins not found")
        return WmsBinsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsBinsListResponse:
        """List WmsBins records with pagination and filtering."""
        query = self.db.query(WmsBins)
        if company_id and hasattr(WmsBins, "company_id"):
            query = query.filter(WmsBins.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsBins, key) and value is not None:
                    query = query.filter(getattr(WmsBins, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsBins, order_by, WmsBins.created_at)))
        else:
            query = query.order_by(getattr(WmsBins, order_by, WmsBins.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsBinsListResponse(
            items=[WmsBinsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsBinsUpdate,
               updated_by: Optional[UUID] = None) -> WmsBinsResponse:
        """Update an existing WmsBins record."""
        db_obj = self.db.query(WmsBins).filter(WmsBins.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsBins not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsBinsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsBins record. Soft delete by default."""
        db_obj = self.db.query(WmsBins).filter(WmsBins.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsBins not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsBinsResponse]:
        """Search WmsBins by name/code fields."""
        query = self.db.query(WmsBins)
        if company_id and hasattr(WmsBins, "company_id"):
            query = query.filter(WmsBins.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsBins, f)]
        if search_fields:
            conditions = [getattr(WmsBins, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsBinsResponse.model_validate(i) for i in items]

class WmsCycleCountSchedulesService:
    """CRUD service for WmsCycleCountSchedules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsCycleCountSchedulesCreate, created_by: Optional[UUID] = None) -> WmsCycleCountSchedulesResponse:
        """Create a new WmsCycleCountSchedules record."""
        db_obj = WmsCycleCountSchedules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsCycleCountSchedulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsCycleCountSchedulesResponse]:
        """Get WmsCycleCountSchedules by primary key."""
        db_obj = self.db.query(WmsCycleCountSchedules).filter(WmsCycleCountSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsCycleCountSchedules not found")
        return WmsCycleCountSchedulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsCycleCountSchedulesListResponse:
        """List WmsCycleCountSchedules records with pagination and filtering."""
        query = self.db.query(WmsCycleCountSchedules)
        if company_id and hasattr(WmsCycleCountSchedules, "company_id"):
            query = query.filter(WmsCycleCountSchedules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsCycleCountSchedules, key) and value is not None:
                    query = query.filter(getattr(WmsCycleCountSchedules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsCycleCountSchedules, order_by, WmsCycleCountSchedules.created_at)))
        else:
            query = query.order_by(getattr(WmsCycleCountSchedules, order_by, WmsCycleCountSchedules.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsCycleCountSchedulesListResponse(
            items=[WmsCycleCountSchedulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsCycleCountSchedulesUpdate,
               updated_by: Optional[UUID] = None) -> WmsCycleCountSchedulesResponse:
        """Update an existing WmsCycleCountSchedules record."""
        db_obj = self.db.query(WmsCycleCountSchedules).filter(WmsCycleCountSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsCycleCountSchedules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsCycleCountSchedulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsCycleCountSchedules record. Soft delete by default."""
        db_obj = self.db.query(WmsCycleCountSchedules).filter(WmsCycleCountSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsCycleCountSchedules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsCycleCountSchedulesResponse]:
        """Search WmsCycleCountSchedules by name/code fields."""
        query = self.db.query(WmsCycleCountSchedules)
        if company_id and hasattr(WmsCycleCountSchedules, "company_id"):
            query = query.filter(WmsCycleCountSchedules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsCycleCountSchedules, f)]
        if search_fields:
            conditions = [getattr(WmsCycleCountSchedules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsCycleCountSchedulesResponse.model_validate(i) for i in items]

class WmsInventoryMovesService:
    """CRUD service for WmsInventoryMoves."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsInventoryMovesCreate, created_by: Optional[UUID] = None) -> WmsInventoryMovesResponse:
        """Create a new WmsInventoryMoves record."""
        db_obj = WmsInventoryMoves(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsInventoryMovesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsInventoryMovesResponse]:
        """Get WmsInventoryMoves by primary key."""
        db_obj = self.db.query(WmsInventoryMoves).filter(WmsInventoryMoves.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsInventoryMoves not found")
        return WmsInventoryMovesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsInventoryMovesListResponse:
        """List WmsInventoryMoves records with pagination and filtering."""
        query = self.db.query(WmsInventoryMoves)
        if company_id and hasattr(WmsInventoryMoves, "company_id"):
            query = query.filter(WmsInventoryMoves.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsInventoryMoves, key) and value is not None:
                    query = query.filter(getattr(WmsInventoryMoves, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsInventoryMoves, order_by, WmsInventoryMoves.created_at)))
        else:
            query = query.order_by(getattr(WmsInventoryMoves, order_by, WmsInventoryMoves.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsInventoryMovesListResponse(
            items=[WmsInventoryMovesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsInventoryMovesUpdate,
               updated_by: Optional[UUID] = None) -> WmsInventoryMovesResponse:
        """Update an existing WmsInventoryMoves record."""
        db_obj = self.db.query(WmsInventoryMoves).filter(WmsInventoryMoves.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsInventoryMoves not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsInventoryMovesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsInventoryMoves record. Soft delete by default."""
        db_obj = self.db.query(WmsInventoryMoves).filter(WmsInventoryMoves.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsInventoryMoves not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsInventoryMovesResponse]:
        """Search WmsInventoryMoves by name/code fields."""
        query = self.db.query(WmsInventoryMoves)
        if company_id and hasattr(WmsInventoryMoves, "company_id"):
            query = query.filter(WmsInventoryMoves.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsInventoryMoves, f)]
        if search_fields:
            conditions = [getattr(WmsInventoryMoves, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsInventoryMovesResponse.model_validate(i) for i in items]

class WmsPackingStationsService:
    """CRUD service for WmsPackingStations."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsPackingStationsCreate, created_by: Optional[UUID] = None) -> WmsPackingStationsResponse:
        """Create a new WmsPackingStations record."""
        db_obj = WmsPackingStations(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsPackingStationsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsPackingStationsResponse]:
        """Get WmsPackingStations by primary key."""
        db_obj = self.db.query(WmsPackingStations).filter(WmsPackingStations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsPackingStations not found")
        return WmsPackingStationsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsPackingStationsListResponse:
        """List WmsPackingStations records with pagination and filtering."""
        query = self.db.query(WmsPackingStations)
        if company_id and hasattr(WmsPackingStations, "company_id"):
            query = query.filter(WmsPackingStations.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsPackingStations, key) and value is not None:
                    query = query.filter(getattr(WmsPackingStations, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsPackingStations, order_by, WmsPackingStations.created_at)))
        else:
            query = query.order_by(getattr(WmsPackingStations, order_by, WmsPackingStations.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsPackingStationsListResponse(
            items=[WmsPackingStationsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsPackingStationsUpdate,
               updated_by: Optional[UUID] = None) -> WmsPackingStationsResponse:
        """Update an existing WmsPackingStations record."""
        db_obj = self.db.query(WmsPackingStations).filter(WmsPackingStations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsPackingStations not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsPackingStationsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsPackingStations record. Soft delete by default."""
        db_obj = self.db.query(WmsPackingStations).filter(WmsPackingStations.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsPackingStations not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsPackingStationsResponse]:
        """Search WmsPackingStations by name/code fields."""
        query = self.db.query(WmsPackingStations)
        if company_id and hasattr(WmsPackingStations, "company_id"):
            query = query.filter(WmsPackingStations.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsPackingStations, f)]
        if search_fields:
            conditions = [getattr(WmsPackingStations, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsPackingStationsResponse.model_validate(i) for i in items]

class WmsReceivingSchedulesService:
    """CRUD service for WmsReceivingSchedules."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsReceivingSchedulesCreate, created_by: Optional[UUID] = None) -> WmsReceivingSchedulesResponse:
        """Create a new WmsReceivingSchedules record."""
        db_obj = WmsReceivingSchedules(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsReceivingSchedulesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsReceivingSchedulesResponse]:
        """Get WmsReceivingSchedules by primary key."""
        db_obj = self.db.query(WmsReceivingSchedules).filter(WmsReceivingSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsReceivingSchedules not found")
        return WmsReceivingSchedulesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsReceivingSchedulesListResponse:
        """List WmsReceivingSchedules records with pagination and filtering."""
        query = self.db.query(WmsReceivingSchedules)
        if company_id and hasattr(WmsReceivingSchedules, "company_id"):
            query = query.filter(WmsReceivingSchedules.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsReceivingSchedules, key) and value is not None:
                    query = query.filter(getattr(WmsReceivingSchedules, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsReceivingSchedules, order_by, WmsReceivingSchedules.created_at)))
        else:
            query = query.order_by(getattr(WmsReceivingSchedules, order_by, WmsReceivingSchedules.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsReceivingSchedulesListResponse(
            items=[WmsReceivingSchedulesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsReceivingSchedulesUpdate,
               updated_by: Optional[UUID] = None) -> WmsReceivingSchedulesResponse:
        """Update an existing WmsReceivingSchedules record."""
        db_obj = self.db.query(WmsReceivingSchedules).filter(WmsReceivingSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsReceivingSchedules not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsReceivingSchedulesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsReceivingSchedules record. Soft delete by default."""
        db_obj = self.db.query(WmsReceivingSchedules).filter(WmsReceivingSchedules.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsReceivingSchedules not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsReceivingSchedulesResponse]:
        """Search WmsReceivingSchedules by name/code fields."""
        query = self.db.query(WmsReceivingSchedules)
        if company_id and hasattr(WmsReceivingSchedules, "company_id"):
            query = query.filter(WmsReceivingSchedules.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsReceivingSchedules, f)]
        if search_fields:
            conditions = [getattr(WmsReceivingSchedules, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsReceivingSchedulesResponse.model_validate(i) for i in items]

class WmsReplenishmentTasksService:
    """CRUD service for WmsReplenishmentTasks."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsReplenishmentTasksCreate, created_by: Optional[UUID] = None) -> WmsReplenishmentTasksResponse:
        """Create a new WmsReplenishmentTasks record."""
        db_obj = WmsReplenishmentTasks(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsReplenishmentTasksResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsReplenishmentTasksResponse]:
        """Get WmsReplenishmentTasks by primary key."""
        db_obj = self.db.query(WmsReplenishmentTasks).filter(WmsReplenishmentTasks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsReplenishmentTasks not found")
        return WmsReplenishmentTasksResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsReplenishmentTasksListResponse:
        """List WmsReplenishmentTasks records with pagination and filtering."""
        query = self.db.query(WmsReplenishmentTasks)
        if company_id and hasattr(WmsReplenishmentTasks, "company_id"):
            query = query.filter(WmsReplenishmentTasks.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsReplenishmentTasks, key) and value is not None:
                    query = query.filter(getattr(WmsReplenishmentTasks, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsReplenishmentTasks, order_by, WmsReplenishmentTasks.created_at)))
        else:
            query = query.order_by(getattr(WmsReplenishmentTasks, order_by, WmsReplenishmentTasks.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsReplenishmentTasksListResponse(
            items=[WmsReplenishmentTasksResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsReplenishmentTasksUpdate,
               updated_by: Optional[UUID] = None) -> WmsReplenishmentTasksResponse:
        """Update an existing WmsReplenishmentTasks record."""
        db_obj = self.db.query(WmsReplenishmentTasks).filter(WmsReplenishmentTasks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsReplenishmentTasks not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsReplenishmentTasksResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsReplenishmentTasks record. Soft delete by default."""
        db_obj = self.db.query(WmsReplenishmentTasks).filter(WmsReplenishmentTasks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsReplenishmentTasks not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsReplenishmentTasksResponse]:
        """Search WmsReplenishmentTasks by name/code fields."""
        query = self.db.query(WmsReplenishmentTasks)
        if company_id and hasattr(WmsReplenishmentTasks, "company_id"):
            query = query.filter(WmsReplenishmentTasks.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsReplenishmentTasks, f)]
        if search_fields:
            conditions = [getattr(WmsReplenishmentTasks, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsReplenishmentTasksResponse.model_validate(i) for i in items]

class WmsShipmentManifestsService:
    """CRUD service for WmsShipmentManifests."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsShipmentManifestsCreate, created_by: Optional[UUID] = None) -> WmsShipmentManifestsResponse:
        """Create a new WmsShipmentManifests record."""
        db_obj = WmsShipmentManifests(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsShipmentManifestsResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsShipmentManifestsResponse]:
        """Get WmsShipmentManifests by primary key."""
        db_obj = self.db.query(WmsShipmentManifests).filter(WmsShipmentManifests.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsShipmentManifests not found")
        return WmsShipmentManifestsResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsShipmentManifestsListResponse:
        """List WmsShipmentManifests records with pagination and filtering."""
        query = self.db.query(WmsShipmentManifests)
        if company_id and hasattr(WmsShipmentManifests, "company_id"):
            query = query.filter(WmsShipmentManifests.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsShipmentManifests, key) and value is not None:
                    query = query.filter(getattr(WmsShipmentManifests, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsShipmentManifests, order_by, WmsShipmentManifests.created_at)))
        else:
            query = query.order_by(getattr(WmsShipmentManifests, order_by, WmsShipmentManifests.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsShipmentManifestsListResponse(
            items=[WmsShipmentManifestsResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsShipmentManifestsUpdate,
               updated_by: Optional[UUID] = None) -> WmsShipmentManifestsResponse:
        """Update an existing WmsShipmentManifests record."""
        db_obj = self.db.query(WmsShipmentManifests).filter(WmsShipmentManifests.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsShipmentManifests not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsShipmentManifestsResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsShipmentManifests record. Soft delete by default."""
        db_obj = self.db.query(WmsShipmentManifests).filter(WmsShipmentManifests.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsShipmentManifests not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsShipmentManifestsResponse]:
        """Search WmsShipmentManifests by name/code fields."""
        query = self.db.query(WmsShipmentManifests)
        if company_id and hasattr(WmsShipmentManifests, "company_id"):
            query = query.filter(WmsShipmentManifests.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsShipmentManifests, f)]
        if search_fields:
            conditions = [getattr(WmsShipmentManifests, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsShipmentManifestsResponse.model_validate(i) for i in items]

class WmsWavePicksService:
    """CRUD service for WmsWavePicks."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsWavePicksCreate, created_by: Optional[UUID] = None) -> WmsWavePicksResponse:
        """Create a new WmsWavePicks record."""
        db_obj = WmsWavePicks(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsWavePicksResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsWavePicksResponse]:
        """Get WmsWavePicks by primary key."""
        db_obj = self.db.query(WmsWavePicks).filter(WmsWavePicks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsWavePicks not found")
        return WmsWavePicksResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsWavePicksListResponse:
        """List WmsWavePicks records with pagination and filtering."""
        query = self.db.query(WmsWavePicks)
        if company_id and hasattr(WmsWavePicks, "company_id"):
            query = query.filter(WmsWavePicks.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsWavePicks, key) and value is not None:
                    query = query.filter(getattr(WmsWavePicks, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsWavePicks, order_by, WmsWavePicks.created_at)))
        else:
            query = query.order_by(getattr(WmsWavePicks, order_by, WmsWavePicks.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsWavePicksListResponse(
            items=[WmsWavePicksResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsWavePicksUpdate,
               updated_by: Optional[UUID] = None) -> WmsWavePicksResponse:
        """Update an existing WmsWavePicks record."""
        db_obj = self.db.query(WmsWavePicks).filter(WmsWavePicks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsWavePicks not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsWavePicksResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsWavePicks record. Soft delete by default."""
        db_obj = self.db.query(WmsWavePicks).filter(WmsWavePicks.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsWavePicks not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsWavePicksResponse]:
        """Search WmsWavePicks by name/code fields."""
        query = self.db.query(WmsWavePicks)
        if company_id and hasattr(WmsWavePicks, "company_id"):
            query = query.filter(WmsWavePicks.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsWavePicks, f)]
        if search_fields:
            conditions = [getattr(WmsWavePicks, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsWavePicksResponse.model_validate(i) for i in items]

class WmsZonesService:
    """CRUD service for WmsZones."""

    def __init__(self, db: Session):
        self.db = db

    def create(self, data: WmsZonesCreate, created_by: Optional[UUID] = None) -> WmsZonesResponse:
        """Create a new WmsZones record."""
        db_obj = WmsZones(**data.model_dump(exclude_unset=True))
        if hasattr(db_obj, "id") and getattr(db_obj, "id") is None:
            setattr(db_obj, "id", uuid.uuid4())
        if created_by and hasattr(db_obj, "created_by"):
            db_obj.created_by = created_by
        self.db.add(db_obj)
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsZonesResponse.model_validate(db_obj)

    def get(self, id: UUID) -> Optional[WmsZonesResponse]:
        """Get WmsZones by primary key."""
        db_obj = self.db.query(WmsZones).filter(WmsZones.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsZones not found")
        return WmsZonesResponse.model_validate(db_obj)

    def list(self, company_id: Optional[UUID] = None, skip: int = 0, limit: int = 100,
             order_by: str = "created_at", order_desc: bool = True,
             filters: Optional[Dict[str, Any]] = None) -> WmsZonesListResponse:
        """List WmsZones records with pagination and filtering."""
        query = self.db.query(WmsZones)
        if company_id and hasattr(WmsZones, "company_id"):
            query = query.filter(WmsZones.company_id == company_id)
        if filters:
            for key, value in filters.items():
                if hasattr(WmsZones, key) and value is not None:
                    query = query.filter(getattr(WmsZones, key) == value)
        total = query.count()
        if order_desc:
            query = query.order_by(desc(getattr(WmsZones, order_by, WmsZones.created_at)))
        else:
            query = query.order_by(getattr(WmsZones, order_by, WmsZones.created_at))
        items = query.offset(skip).limit(limit).all()
        return WmsZonesListResponse(
            items=[WmsZonesResponse.model_validate(i) for i in items],
            total=total, skip=skip, limit=limit)

    def update(self, id: UUID, data: WmsZonesUpdate,
               updated_by: Optional[UUID] = None) -> WmsZonesResponse:
        """Update an existing WmsZones record."""
        db_obj = self.db.query(WmsZones).filter(WmsZones.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsZones not found")
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            if hasattr(db_obj, field) and value is not None:
                setattr(db_obj, field, value)
        if updated_by and hasattr(db_obj, "updated_by"):
            db_obj.updated_by = updated_by
        if hasattr(db_obj, "updated_at"):
            db_obj.updated_at = datetime.utcnow()
        self.db.commit()
        self.db.refresh(db_obj)
        return WmsZonesResponse.model_validate(db_obj)

    def delete(self, id: UUID, hard_delete: bool = False) -> bool:
        """Delete a WmsZones record. Soft delete by default."""
        db_obj = self.db.query(WmsZones).filter(WmsZones.id == id).first()
        if not db_obj:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="WmsZones not found")
        if not hard_delete and hasattr(db_obj, "is_active"):
            db_obj.is_active = False
            self.db.commit()
            return True
        self.db.delete(db_obj)
        self.db.commit()
        return True

    def search(self, query_str: str, company_id: Optional[UUID] = None,
               limit: int = 20) -> List[WmsZonesResponse]:
        """Search WmsZones by name/code fields."""
        query = self.db.query(WmsZones)
        if company_id and hasattr(WmsZones, "company_id"):
            query = query.filter(WmsZones.company_id == company_id)
        search_fields = [f for f in ["name", "code", "description", "email", "phone"]
                         if hasattr(WmsZones, f)]
        if search_fields:
            conditions = [getattr(WmsZones, f).ilike("%" + query_str + "%") for f in search_fields]
            query = query.filter(or_(*conditions))
        items = query.limit(limit).all()
        return [WmsZonesResponse.model_validate(i) for i in items]
